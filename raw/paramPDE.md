> 2023-05-05 从 compTree.md 独立
* `paramPDE%` PDE 对于一类参数 $\lambda$ 快速求解；{n3bg3e}
	* 这里主要讨论针对不含时 PDE 类问题设计的方法；一般的函数间映射建立方式见 `NO%`
		* 含时方程，若((o3de31))时空等价处理/((n8jh0x))时间视为参数，则也算当前框架
		* 含时方程，但只要找稳态解（时间趋于无穷）则仍属当前框架；{n8jh17}
			* ((n8jh0b))FNO-DEQ 认为该类稳态解可视为非线性算子不动点，据此采用 DEQ 架构；考虑了 Darcy 流、稳态不可压 NS
			* 时间步长作 NO 输入((n8jh0x))时，令其趋于无穷可使含时架构可用不含时方程数据训练，见((_o6cj7g))Poseidon；{o6cj7w}
			* 相关：RANS 也是含时方程获得不含时的解？
			* 其他稳态相关：{n8jh6y}
				* 暂稳态：((n8jh6v))从动力学数据中识别，((n23m8q))双尺度动力学-快尺度达到稳态
				* 动力学稳定性((p2se58))，稳态附近性质 or 演化后可收敛至稳态
				* 动力学周期轨作为广义“稳态”（狭义的只是动力学不动点）；{n8jh7e}
					* Kalman vortex street，圆柱绕流
					* ((n8jh74))Helmholtz 方程传统求解器，先算含时方程稳态、再对其解做时域 FT
		* 一般含时方程见 ((o5sm3k))singleDyn，((n3bh2y))fastPhyDyn
		* 不含时方程，若某个空间维度特殊处理成像时间维度，也算含时框架（化学、生物数据分析里有一些）
	* 需求场景；{o2dg2r}
		* `DL-ROM-2103.06183`“n26e9k”敏感性分析、UQ、多尺度方法典型任务 等涉及 multi-query；{n26f0d}
			* 其中 UQ 记录于((n26f0c))
		* 实时求解
		* 反问题、反向设计
		* ((_o2dg2c))设计、控制、优化、UQ，以及((o2dg2p))工业界许多具体例子
	* 参数 $\lambda$ 形式：
		* 低维，若干实数参数；eg. Meta-MgNet（$A$ 可写为卷积，因为其内部涉及的参数不随空间变化）
		* 函数或场，包括边界条件等
		* 区域形状等不易给出参数化的表达：使用 meta-learning 方式（hypernet 不行但是 MAML，AD 还可以）
		* 含时方程有做法将时间视为参数，或者（短时问题）的初值视为参数；更多见 ((n3bh2y))fastPhyDyn
		* 混合（反问题、反向设计常见），例如机翼流场计算，包括机翼形状（给出流场区域形状）、工况（攻角、流速）
		* 术语混淆问题：为避免与 NN 参数混淆，有工作将 NN 参数称为 NN weights，如 Cape-2304.14118
	* 加速方式-纯 AI 方法：
		* 对 PINN 等求解单个方程的做法使用 meta-learning；{n5bm09}
			* hypernet：PI-DeepONet、`PINO-2111.03794`（弱意义上的 hypernet，元测试允许微调），`2003.12159`（文中只考虑 $\lambda$ 为初值）、`HyperPINN-2111.01008`, `1901.06314`
				* `2110.13361`（hypernet 预测初值、对超网络用传统逼近器而非 NN、有监督训练）
				* 该做法对区域形状的泛化能力可能有限
				* 一般 NO 均可，例如((n42e9a))图片转 INR，用 ViT 接收图片、输出 INR 网络参数
			* 其他：
				* `metaPINN-2107.05544`（学 loss）{p1mf6b}
				* NRPINN-2107.10991 用 Reptile，`Meta-PDE-2211.01604` 用 MAML,LEAP
				* `2110.13361` 从 hypernet 预测所得初始参数开始训练，实验将 MAML 作为基线；AD；`1910.09098` BO 选超参
				* `2303.07127` 学优化器（基于 Adam，实验确实有提升）
			* 相关：`paramPDE%`“ROM”靠降维加速求解，这点类似 AD 元测试，但降维模型的寻找方式（元训练）目前没有用 PDE loss 的，故不归到元学习框架下
			* 好处包括 `PINO-2111.03794`“不需要将初边值传播到区域内部”（若较多可单开笔记）
			* 元学习中 task 采样考虑不同 PDE 的难度，((_o6nf4e))找最优训练采样概率以极小化验证任务 loss，为((n32e3d))双层优化问题，通过找解析解简化求解；{o6nf4q}
				* 相关：((n8jk45))元学习-基于优化-求导阶数
		* "learning operators" `NO%` 表达解算子: 
			* 常见做法：给定 grid $V$（另有基底表达的版本），NN 预测解（对于含时问题为预测更新），映射为 $a|_V\mapsto u|_V$ 或含时的 $(a,u)|V\mapsto u|V$
				* 含时 eg. Unsupervised-DL-fluid（U-Net, loss 无监督，类似 PINN）
			* 可以在没有方程形式时使用纯观测数据（尽管其目的不是加速）
			* 通常没有理论保证预测阶段能达到任意给定精度（尽管有方程形式可以知道当前误差多大）；
				* 但训练数据够多时算子逼近性质有相关定理
			* 如果训练良好，方程线性，算子输入包含右端项，((n3fe1c))则不断将残差作为右端项输入仍能达到指定精度（即利用训出的算子人为构造迭代格式）
	* AI 增强传统算法((ncmk3t))，包括加速、提高精度等
		* （针对椭圆方程）((n3fe2j))学 Green 函数，可用 PINN
		* （已弃用旧内容）
			* （现见下方 ROM）类似 POD，找出问题降阶方式，只需求解降阶的问题
				* 降阶问题写为代数方程解，或 PINN loss（包括含时问题单步时间推进 Euler 格式等给出的 loss）；线性降阶情形后者有显式解而成为前者
				* （非线性）降阶映射，预测时据 PINN loss 优化解码器输入的隐向量
					* 训练若用 PINN loss 则就是 AD，按上方元学习做法处理
					* 另可用有监督 loss 训，还可进一步用 AE loss `1909.09754`
			* 用网格生成办法（见下方）；可以继续用传统数值格式，或者额外结合 ML 改进版的解算子（mesh-topology 可变的那类）
	* 可能性：NN 输入除 $\lambda$ 外还有某种参考解 $\tilde u$；可利用传统算法结果
		* 同方程同参数，粗网格近似解；可由传统算法给出
		* 同方程同参数，欠迭代近似解：CFDNet-2005.04485 `2022-07-22`(CSImeet2) 用传统数值格式最初几步迭代（称为「预热」）的结果
		* 同方程、不同但相关的参数的解
			* 预计算几个典型参数的结果，ML 输入最相近参数对应的解
			* 反问题、反向设计常见：对{混合结构参数} $\lambda=(\lambda_1,\lambda_2)$，参考解可用参数 $(\lambda_1,\lambda_2')$，其中 $\lambda_2'$ 只取典型值（可仅一个）
				* 反问题、反向设计场景，$\lambda_1$ 待设计，目标函数涉及对 $\lambda_2$ 取期望/极小/极大，从而主要计算量来自算不同 $\lambda_2$ 的解：`invDP%`“额外非设计参数”
				* 如机翼流场计算，对新机翼先取定一个工况做传统模拟，再 ML 泛化至其他工况 `2022-05-13`(CSImeet2)
				* 相当于结合传统模拟、做部分泛化
		* 不同但相关的方程的解（如降维）：`2022-07-22`(CSImeet2) 3D 飞机或许可先算 2D 翼形流场（传统方法或 NO），作为 3D 流场预测 NO 的输入
		* 注：若 $\tilde u$ 与真解接近，NN 可只预测残差
	* 网格泛化（默认指输入 $\lambda$ 的网格）
		* 固定网格：`OneShotNO-2104.05512`，PDE-Net（`2021-12-31`(CSImeet2) 学的离散格式是针对特定分辨率的）
		* lattice/grid-size 可变：Meta-MgNet（局部卷积）
		* mesh-topology 可变：线性算子情形可逼近 Green's func
		* mesh-free：PINN 使用 meta-learning（如果问题给定的 $a$ 自带 mesh 则可能需要额外处理），以及 PINN 学 Green's func 情形
		* （以上主要考虑 $\lambda$ 为函数、场的情形）
		* 相关：若 $\lambda$ 仅有局部变化，可考虑只建立局部 NO；`2022-07-22`(CSImeet2) 3D 飞机仅翼形变化，$\lambda$ 为形状，仅局部调整
	* 基于特定变换构造算子（((n7n96y))NO-基于特定变换构造算子 已有的这里不重复）
		* 学 Green 函数；{n3fe2j}
			* `GF-Net-2105.11045`
			* 方程预处理—非线性方程先变为线性 `DeepGreen-2101.07206`（降维表达后 Green 函数成为线性变换）{n3g88l}
				* 相关框架：((n3g88p))PDE-线性vs非线性
			* ansatz—用边界元 ansatz 表达 Green 函数 `BI-GreenNet-2204.13247`（减掉全空间 Green 函数从而只表达无奇点部分）
			* ansatz—奇点处理，((_q4pg34))分解为 解析自由空间分量+学习修正分量
			* 误差处理—考虑到 NN 拟合误差，`2023-03-15`(AISCmeet2)“n3fe45”发现该误差主要在 Green 函数（作为积分算子）的小特征值，对应微分算子的大特征值；{n3fe5l}
				* 导致的用法：((n3fe85))将学出的 Green 函数用于残差迭代格式时，与传统迭代法交替使用，优势互补
			* 用于求解—考虑到适用的方程通常线性，可进一步使用针对线性方程的((n3fe1c))残差迭代格式，而不限于仅通过单次调用求解；{n3g886}
				* NN 拟合的 Green 函数通常有误差，导致表达的解算子不准，但如果用作迭代格式（而非单步求解），仍可使解达到机器精度
				* 进一步考虑到((n3fe5l))该误差主要出现在微分算子的大特征值，迭代在这部分较慢，而这刚好是传统迭代算法快的范围，可考虑交替使用两种迭代格式，其优势互补；{n3fe85}
					* `2023-03-15`(AISCmeet2)“n3fe61”实际采用的做法是动态判断当前残差大小，据此决定调用哪个，而非预先设定的交替调用法则；{n3fe74}
					* 相关框架：((n3fe73))学迭代算子-不同迭代格式混用
			* ansatz—一般的非线性积分算子，积分 kernel 依赖于被积函数；{n86k1x}
				* 一系列 NO 构造基于此类积分算子的复合，如李宗宜系列（GKN，FNO）
				* ((n86j9u))基于积分算子构造的 NO 与离散化无关，换离散格点相当于换积分格点
				* 相关：((n7ul4f))Boltzmann 方程积分项（碰撞项）
		* 学 DtN+NtD map：线性椭圆方程的全空间基本解已知，((_q2oa5d))NO 学二映射即可，数据可人造
		* 若 PDE 形式允许，可只预测部分区域，通过已知表达式推得全区域场
			* 如 `MaxwellNet-2107.06164` 只预测透镜所在矩形，后方的电磁场分布用已知表达式的 Green 函数推出（为均匀介质，故 Helmholtz 方程对应 Green 函数已知）
		* （主要含时问题）PDE-Net 卷积获得一系列偏导，channel-wise 全连接得到方程的时间更新量
			* （含时问题主要在下方 ((o5sm3k))singleDyn；PDE-Net 见 `2021-12-31`(CSImeet2)）
			* 有可解释性，可当黑箱用于预测，也可拟合数据后根据卷积核得到方程形式
			* 卷积是空间局部、频域全局变换，或许可结合空间全局、频域局部的 FNO `2022-07-22`(CSImeet2)
		* 若 PDE 已知、有低精度求解器，`PEDS-2111.05841` 将其作为 NO 的一部分，只学低精度解与高精度解的残差；{ncbn67}
			* 文中称数据需求降低百倍，相关：((ncbm9z))NO-数据生成昂贵的解决
			* 相似的做法((ncbn0w))多个网络逐步修正解、提高预测解保真度
			* 相关：((ncbn0o))NO 利用多保真数据，((n3bh2y))针对含时问题加速传统算法
			* 相关：这算 numeric in NN，另见((nclm36))NN in numeric，((nca99w))phy in NN
		* PDE 部分项用人工设计格式、剩余项黑箱学出，((_ocoe49))所用数据的离散化误差可能误导其训练
	* 用 NO 处理含时含参 PDE 的方式：{o1lg2x}
		* 预测时间推进 $(\lambda,u_t)\mapsto\dot u$ 或 $u_{t+1}-u_t$，((o5sm3k))singleDyn 中许多方法如此
			* 时间更新算子通常只需要局部依赖性（除非长时），不需一般 NO 的 UAP，如 PDE-Net
		* 时间视为参数的一部分 $(\lambda,t)\mapsto u_t$（副本((n8jh0z))动力系统时间分解）；{n8jh0x}
			* 如 `DL-ROM-2001.04001`，`NIF-2204.03216`
			* `CTFNO-2302.00854` 空间有网格（FNO）时间无网格²，$t$ 输入超网络、生成 FNO 参数的 modulation；{n8ja9w}
				* 副本：((n8jb05))modulation
				* ²相关：((n8jb0f))场表征-混合表征-不同分量不同表征，((n8jh0d))FNO-与其他架构联用
			* FourierMIONet-2303.04778（by 陆路）fig4 空间 FNO 时间 DeepONet，DeepONet 的输出作为 FNO 输入
			* 时间推进步长在网络架构中引入方式
				* 以 adaLN 形式调制 SWinV2 架构 NO 前传((_o6ci14))，训练时输入、输入时间步位置均从数据中随机采样
				* 可学类算子分裂，((_q7am1m))输出分裂策略的网络 输入包含预计演化总时长
			* 使 NO 获得其他用途：解不含时方程，((o6cj7w))令 $t\to\infty$ 即可
		* 不区分时空维度 $\lambda\mapsto u|[0,T]$，可预测时长 $T$ 取决于数据集；{o3de31}
			* ((_o3df15))3D U-Net，其中时间占一维
			* 若要时间外推：`POD-LSTM-ROM-2201.10215` 区分时间内插、时间外推两个结构，内插为 NO 且形式上不区分时空维度（实现上 LSTM 区分），外推为普通动力系统演化
			* 相关：((o3de3t))动力系统-时间块内不区分时空，即 $(u|t,\lambda)\mapsto u|[t,t+1]$
		* 含参动力学可((o1lh1z))转化为有记忆不含参动力学，进而用有记忆 NO 处理
			* 属于((o1lh4p))NO-输入形式-间接输入-迭代历史作为间接输入方式
		* 相关：((o1lg3c))NO 可处理的问题，((n8jh17))含时方程求稳态解 可用一般 NO 处理
	* {学求解器迭代算子}；{n2rm3m}
		* 基于传统迭代求解格式构造算子，学习高效迭代方式
			* 基于多重网格
				* Meta-MgNet MG 迭代算子由 NN 根据 PDE 参数生成
				* `2008.11520` MG 提升算子、时间演化算子
				* 注：多重网格格式与 ((n5da0x))NO-多尺度架构 有关；{n13a5b}
			* 预测 Krylov 子空间
				* 该方式与上面的“NN 直接表达解算子”在做法上类似，只是会同时生成多个向量
				* `s41598-019-56212-5` 迭代所用 Krylov 子空间由 CNN 根据微分算子生成
				* NeurKItt((_p2ie4r))NO 输出 Krylov 子空间的 QR 分解，训练用投影 loss
				* 预测生成 Krylov 子空间的 preconditioner，NPO((_p2ee56))基于 Transformer 架构
					* 作者说现有其他文章是 NN 生成 Krylov 子空间、preconditioner 用传统方法，这篇反过来
						* 二者结合的做法需探索，“关键要找到这两步之间可以联合优化的点”
			* 传统牛顿迭代换为 NN((_oa4m2y))，降低初值敏感性；需保证为收缩映射
			* 非线性 PDE Newton 迭代((_p2ee8o))NINO
			* 纠正传统算法迭代步骤((_pcij8i))
			* 由于基于传统算法，只能处理方程已知情形，无法使用纯观测数据，但可无监督
			* 预测阶段可以指定精度，训练不好只会延长计算时间；如果迭代算子涉及的网络利用了残差 $r=f-Au$ 的信息，有希望加快收敛
				* 类比：如果将元学习用于 PINN $u(x;\theta)$ 做法，元学习方式为预测其初始化参数 $\theta_0=NN(A,f)$，则“（$NN$）训练不好只会延长计算（$u(x)$ 的 fine-tune 训练）时间”同样成立；
				* 不过这里基于传统方法的做法不会影响到收敛的结果，而这样的 meta-PINN 可能遇到非随机初始化参数效果不如随机初始的问题，即使 $NN$ 训练得看起来不错；迁移学习可能常见？暂时没有看相关研究，也许有避免方式，比如足够宽前提下试图让 $\theta_0$ 与随机初始差别不大，仍然位于 NTK regime
			* 基于传统格式迭代与黑箱映射的联系：`2010.15761` 许多传统格式可以统一用有记忆的迭代算子表示，若记忆历史梯度可表达拟牛顿法，若记忆历史残差可表达 Krylov 子空间方法；黑箱迭代算子或许可自适应换用多种格式
		* 由 NO 等黑箱网络执行迭代求解步骤
			* 希望行为近似（耗时的）传统迭代步骤，用传统求解器迭代轨迹、有监督训练
				* 即：已有传统迭代格式，只是单步迭代计算比较慢，现在用一个 surrogate 代替原来的精确格式来执行迭代
			* 按 RL 训练，见((o1lg15))迭代算子训练 loss
			* 按 NO 训练、按迭代求解器使用：针对线性方程 $Lu=f$，NO 训练所得的 $f\mapsto u$ 可用于迭代求解，不断将残差作为算子输入，理论上可任意提高精度；{n3fe1c}
				* 若 $L=L_a$ 也含参，$(a,f)\mapsto u$ 算子也可不断用于处理残差；
					* 例子如((_paia6k))HyPINO
				* 另可不显式引入 $a$，而用含记忆的 $f\mapsto u$ 算子((o1lg1g))：由于 $a$ 影响残差计算方式、进而影响残差 $f_k$ 历史值，可认为 $a$ 隐式编码于记忆中
				* 部分线性方程有对应的 Green 函数，可((n3g886))用近似版本的 Green 函数给出该近似算子
				* 相关框架：((n3g88p))PDE-线性vs非线性
			* ((_n31e4p))认为让 NN 负责传统求解器做不好的部分即可，没必要整个迭代求解器让 NN 负责
				* 不过若考虑算法本身易用性（而不限于性能、可解释性等），或许纯黑箱形式更便于相关应用领域（非计算数学专业）的人自行实现、改进等
				* 是否可能考虑同时给两个版本，论文中给一些 ablation？也可拆两篇论文（行文组织也可用面向不同读者群体的写法）
			* 相关：((o1lg3c))NO-可处理的问题类型
		* 混合迭代算子，两种不同类型的迭代交替使用；{n3fe73}
			* 相关：((n3ri89))仿TRIZ原理-迭代-单一格式迭代改交替格式迭代
			* 这里主要讨论不含时 PDE 的迭代求解格式，另有((n3sf6k))含时 PDE 的交替格式时间推进
			* 交替依据除了固定顺序之外，还可考虑依据两种迭代器的不同特性（如分别擅长处理高频、低频分量），每步动态决定调用哪一个，例如((n3fe74))
			* 考虑传统迭代算法不擅长处理低频分量，可引入一个学出的迭代器（可处理高频）来与之配合
				* 注：传统靠多重网络来增强低频处理能力
				* 相关：((n2pg3v))F-principle
				* 使用黑箱 NO：`HINTS-2208.13273` 若干步 Jacobi 迭代后加一步 DeepONet 迭代，包括 MG 内的迭代
				* 方程本身存在 Green 函数时，可考虑((n3fe85))用学出的 Green 函数作用于残差，由 NN 拟合结果的性质，它可高效地处理低频分量
			* ((_n2o88z))Krylov 迭代与 U-Net 交替，保留前者可用于正则化、防止解发散
			* `Rizutti2019LearnedIS` Helmholtz 方程，若干步 Krylov 子空间迭代中加 U-Net 迭代，每步迭代所用 U-Net 不同；（固定总迭代步数后已与 NO 接近）
		* 迭代步在局部执行，并结合全局通信；包括区域分解
			* `OneShotNO-2104.05512` 计算/实验资源只够生成单样本时，据此推测类似 Jacobi 不动点迭代算子；所有局部组合起来得更新所有点的算子，此时类似 FPN 版解算子
			* `2011.00568` 区域分解后交替进行每区域近似求解、区域间通信，区域近似解用已有数据的 kNN 插值获得（原文针对刚性的摄动问题；不涉及 NN）
			* NEST((_q7vn5b))学出局部解算器、Schwarz 全局装配
		* loss 有单步 loss，RL loss（考虑长期收益；分终态 loss 和多步平均 loss）{o1lg15}
			* 似乎主要是无监督 PDE loss（如 `2010.15761`，Meta-MgNet），较少用有监督 loss
			* `2010.15761` 所有步平均 loss，用 RL 中 replay buffer、截断时间反传技巧使反传只涉及 10 步迭代
				* 其用 RL 的另一方面原因：((o1lg1g))属于有记忆迭代算子，故用有状态记忆的 RL（单纯多步 loss 不要求 RL 有记忆）
			* `Rizutti2019LearnedIS` 固定总迭代步数的算子（已接近 NO），可用有监督或 PDE loss
			* 相关：((o1lg12))动力学-多步 loss，((o1lj8r))NO-训练-已知 PDE 情形
		* 迭代步数，固定步数 vs 设定停机准则，注意((n3hf35))二者训练 loss 有所区别
		* 与时间推进的迭代算子的关系？
			* ((n3bh2y))fastPhyDyn 含时方程（形式也完全已知）的迭代算子有类似之处，不过通常自由度比这里小（迭代要符合物理，不是只作为优化手段）
			* 与元学习下 L2O 的关系？
			* 一般讨论或可放在((n5bl92))映射形式-迭代
		* 相关：((n5bl92))迭代算法描述的映射，((nclm36))NN 增强传统数值求解器
	* {ROM}，找出问题降阶方式，将原系统（包括离散后的代数系统）限制到 trial manifold 上再求解；{n8jb8q}
		* 注：求解单个不含时方程、计算含时方程时间离散后的单步时间推进 类似，多数方法同时适用
			* 仅针对含时方程的做法记录于 ((ncmk5e))fastPhyDyn-ROM，其中将 $t$ 也视为解 snapshot 的参数，即所用 snapshot 为 $\{u(-,t)|t,\lambda\}$ 而非 $\{u(-,-)|\lambda\}$
		* 传统线性方法 `Quarteroni2016book-RBM`，包括 POD 等；{o1mj3j}
			* 原 PDE 限制在离散网格成为高维代数系统，建立基底后进一步限制到基底系数域为低维代数系统，只需求解该系统
			* Kolmogorov n-width；{o1mj39}
				* 针对特殊方程的 width 衰减阶估计：((o1mj3b))对流方程
				* 注：与 width 相关内容目前主要在独立文件内
				* 熵数 entropy number；{o42l65}
					* 多面体熵数估计 可利用 点集凸包可由 k 点平均近似，见 SRS((o5ul30))；来源 HDP 教材；{o5ul7b}
					* GRF 及其有限截断版本的熵数估计((o1mh2l))
					* 相关：((n6sf8q))VQ-VAE 类似于惩罚隐向量分布的熵数
					* 相关：((_pc6g5q))多尺度 VQ-VAE 类似小波，对图像尺度残差的假设分别对应熵数、Kolmogorov n-width
			* 相关：((o1mj3g))场的数值表征-双向映射-线性基底
		* 线性降阶，降阶方式可根据 PDE 参数动态生成，如 `2105.14633`（虽含时但基底未区分时空）
			* 若降阶降至一维则几乎成为 NO
			* 对线性方程，若对残差反复迭代（且残差作为降阶方式生成依据）则类似 Meta-MgNet 子空间校正格式
				* 还需要结果允许表为多层残差的求和；$u$ 用离散/基底法表达时还算方便，用 NN 参化则麻烦
			* 含时方程可能性：基底只表达空间域，根据当前时间步场生成下一步降阶依据
		* NN 非线性 ROM，用隐向量到空间场的解码器
			* 解码器用空间网格离散：`1812.08373`，`1909.09754`
			* 解码器用空间参化，无网格（如 INR）：`CROM-2206.02607`
			* 备用：((_pbtk1a))未确认的相关工作
		* 解码器训练方式：AE，AD
			* AE（训练完成后可丢掉编码器）：`1812.08373`，`1909.09754`，`CROM-2206.02607`
			* AD：暂无（可能只有我们的 MAD 能算）；若进一步它用 PDE loss 训练则算上方元学习加速法
		* 对特定 $\lambda$ 求解对应隐向量 $z$：
			* 构造迭代格式，许多做法应可通过仿照含时问题时间迭代获得 ((ncmk5e))fastPhyDyn-ROM
			* 直接求解优化问题
			* NN 直接给出映射 $\lambda\mapsto z$：算((n7n96y))NO-基于特定变换构造算子-有限维中介
		* 另：若解码器用空间参化、靠优化/迭代找 $z$，常需自行选空间散点来求值以给出优化问题；散点规模可以很小；{n8je04}
			* `CROM-2206.02607` 散点规模只需使非线性最小二乘良态（方程个数多于自变量个数）即可
			* `CROM-2206.02607` 实验表明均匀散点不高效；其前序工作用随机散点
			* `CROM-2206.02607` 用贪心算法生成散点集（offline）：可在当前散点集上用 ROM 生成解，并与数值解（细网格）比较，从误差较大的位置挑选对解精度提升最大的点加入散点集，如此反复至达到给定精度要求
			* 另：优化问题内常涉及空间导数，((n79i86))数值微分有可能好于 BP
			* 不局限于 ROM 的情形见 ((n8jb6f))coordLoss-INR-空间采样点选取
		* 相关：ROM 解精度可能低于 full-order model，属于 ((ncbn2v))多保真数据的可能产生方式
		* 适用范围：((_pbtk1g))多尺度系统中有困难，据此有“ROM 专用闭合模型”
		* 相关框架：一般约简策略((ncmk3t))，还包括约简物理、约简网格等，ROM 则是约简（给定网格离散下的）代数系统
		* 真实应用场景，((_o2dg2j))ROM 有收益（同时考虑离线成本）的许多工业界具体场景例子；{o2dg2p}
			* 相关：((o2dg2r))参化 PDE-需求场景
* `NO%` 描述函数间的映射 $a\mapsto u$；{n3qi2x}
	* 注：架构设计问题见 NOarch.md
	* 形式相关：
		* ((n32e9r))场的数值表征 函数的有限维表达
		* `unifyMethod1:`(metaL)“映射形式”；NO 方法主要是直接映射
		* ((n32f33))NN架构设计 针对问题性质改善网络架构设计（可能会把一些东西迁移过来）
		* NOasMetaL PDE 解算子方法视作元学习（hypernet）的可行性；{n35e9n}
			* 解算子法若用真解训练，则一般不算元学习
				* 目前见过的只有 PI-DeepONet，`PINO-2111.03794` 算（后者算弱意义 hypernet）
				* 若训练数据和输出的 $u$ 为空间离散，没有 regular task
				* 若 $u$ 按 ((n32e9r))场的数值表征 空间基底展开或参数化（如 DeepONet），则
					* 元训练时 regular task 为对离散数据点（空间有限差分离散的数值真解）进行函数拟合，依据为 $\{(x_i,u_i)\}$
					* 推断时的任务则是解方程而没有数据供拟合，依据为 $\lambda$
					* 因此用元学习描述这样的设定很奇怪
			* 解算子无论是否按元学习理解，形式都接近，这得益于元学习视作特殊有监督学习的观点
			* 仍有可能按 `unifyMethod2:`(metaL)“额外学了模型” 的方式理解为元学习；{n35e9p}
				* 以下按其中的记号，但针对 PDE 任务形式 $[\lambda,\bar u;/]$ 替代其中统一的 $[D^s,\bar h]$
				* 常规任务仍理解为解优化问题（PINN loss 或 DRM loss 等），在有限维网格上，或者 DeepONet 那样解一个完整函数
					* 暂未仔细思考 mesh-topology 可变的情形（GKN 等）
				* 外层 loss 的构造方式不用普通任务的 loss，而是生成普通任务的完整参考解 $u^*$
					* 这里由于问题特殊，生成方式可使用 PDE 求解器（一般元学习任务可能需要通过常规训练+验证集调参的方式生成）
				* $u=g(\lambda)$ 的元模型形式使用 hypernet，也不涉及普通任务的 loss
				* 从而尽管有普通任务，但并没有真正用它的具体形式（即未使用其 loss）
			* 一般的元学习加速 PINN 的工作汇总于 compTree
	* NO 的能力
		* 可处理的问题，不限于 PDE 正问题；{o1lg3c}
			* PDE QoI：只用于提供参数到可观测量的 surrogate，信息少于完整 PDE 的解
			* 解含时含参 PDE((o1lg2x))，有多种方式
			* 解反问题((o1hn0d))
			* 参化 PDE 迭代求解器((n2rm3m))
			* 机理未知的自然系统((p3rb0r))
		* universal approximation 理论性质（UAP）：{n7na92}
			* `FNOvsDpONet-2111.05512` 提到了 DeepONet, FNO 的，((_n7na9k))均为 $O(1/m)$ 误差所需网络尺寸 $O(m^3\ln m)$
			* CNN 的可见 `2207.01546`（其引言引的其他文献也可参考）
			* `MIONet-2202.06137` 输入含多个函数分量时的 UAP 定理
			* 相关：((ncqf18))NO优先拟合低频的先验
		* OoD 迁移适配，实证评估为主；{q7oh0e}
			* 相关：对象（NO）一般化见((q7kh6a))OoD-影响程度评估；不针对 NO 的指标也在那里记录
			* 类似框架((pbf88t))NO-引入辅助任务-预训后微调
				* 区别：这里从特定手段（已训网络）展开多目的（适配新映射），那里是特定目的（目标映射确定）展开多手段
				* （旧）场景 这里是特定已训网络适配多下游场景，预训阶段不预知下游场景，那里是 据特定已知目标场景设计预训策略
			* 适配依据：微调，零样本（靠网络先验），二级结合（近似分布数据微调后零样本迁移到目标分布）
				* 微调 可迁移至不同映射关系情形，包括方程类型，或解不同任务（如正问题模型迁移至反问题）
				* 微调 涉及 early-stopping，时机选择可((q7pa4e))依据物理诊断
				* 先微调后泛化 二级迁移，如 Walrus 先((_q7of4e))微调迁移至 RTI DNS（新 IC 跨大 regime），后 zero-shot 迁移到实测 IC（包括((q7oc3b))同 regime、((q7oa3n))跨小 regime）
			* 注：含时方程步进也在此讨论，因适配依据、OoD 维度有耦合，维度据是否含时分开讨论会导致不便
			* 迁移轴—参数分布（含 IC），进一步考虑是否跨物理 regime
				* （含时类）子轴—演化速度 vs IC复杂度，((_q7kl1g))前者 OoD 影响远大于后者；{q7og7b}
				* 子轴—IC 分布（regime 不变）零样本，((_q7oa3m))Walrus 迁移基本成功；{q7oc3b}
				* 跨 regime 零样本，((_q7oc3a))Walrus 定性正确、偏差指向训练分布（具体子轴为 IC sim2real）{q7oa3n}
			* 迁移轴—保真度：多保真训练相关条目，sim2real 也算
			* 迁移轴—维数：((q3kk8u))HalfWalrus
			* 迁移轴—方程类型：映射已变（不属常规 OoD），必须微调；预训练视为辅助任务，见((q3aa55))辅助任务-近似or类似方程，辅助任务使用方式为 单任务预训后迁移学习或((pbf88t))多任务预训后微调
	* 数据问题
		* 已有数据集；{n8i96v}
			* 正问题 `[PDEBench-2210.07182]`（另可为项目贡献自己的数据集、基准模型），((_o39c1i))PDEArena
				* 另有((n8i96u))多物理，((n8i975))低分辨率动力学，((_q82g08))多保真数据（粗细网格配对数据）
				* ((_q1ng5m))RealPDEBench 结合真实观测的数据集
				* 更多见((n8i979))正问题-典型方程，如 NS 方程的
				* 特殊“数据集”，((_ocll5f))APEBench 提供 46 方程的快速实时求解器，训练时动态造数据，声称无需事先生成、下载；{ocll5x}
					* 动态造数据的案例还包括((_q7if8g))Tadpole 伪谱法，用队列、缓存机制替换旧数据
			* 反问题的已有 ((n93l24))OpenFWI；((_q7kf6n))PDEInvBench 据单个完整解恢复参数（标量为主）
			* 优化设计：拓扑优化((_q46f6y))OpenTO，多目标优化((_q4ta9t))BikeBench
			* 控制问题((_o6lg7o))PDE Control Gym
			* 特征值问题
				* （知乎摘录）量子多体物理（“用function regression求某些有结构的矩阵的特征值”）varbench
			* 非结构网格相关数据集，((_p2of0x))相关综述
			* 另见((n8i97i))一般数据集（不限于科学计算）
		* 针对已知形式的 PDE 求解情形（可介入数据生成过程）{o1ol2l}
			* 训练集所用 $a$ 分布；{n4fe9o}
				* ((_n4fe89))认为若 $u$ 光滑则 $a$ 适用 GRF，$u$ 有激波则 $a$ 适用间断初边值
				* 使相应的 $u$ 具有关键 pattern 即可，此时已可有较强的 OoD 能力；{n4jg4g}
					* 之前似乎有工作的 $a$ 为区域形状，对半平面、尖角区域训，可泛化至星形障碍物等复杂形状
					* ((_n4fe8m))$u$ 有激波，用少量间断 $a$（IC,BC）训，可泛化至有更多间断的 $a$
					* 非科研相关：((n4jg55))考虑不同程度的 OoD，泛化能力或来自 通过适当训练集选取，降低 OoD 程度
				* GRF、均匀采样等不符合实际场景（PDECO invD 情形）((p8g99m))
				* IC 多样性比（标量为主的）参数覆盖范围 更重要((q7kg20))（尽管是反问题设定）
				* 相关：((n4fe9r))ML-数据-数据生成
				* 若用于 invDP 等，((p8g918))只需在最优 $a$ 附近好用
			* {数据生成昂贵}时可考虑（除无监督、半监督训练外）：{ncbm9z}
				* 降低求解精度，方式多样：{ncbn2v}
					* 一般可参考 ((ncmk3t))传统格式局限性的因果链，降求解成本的方法都能用
					* 低精算法：((n8jb8q))ROM
					* 粗网格：离散网格分辨率不同（包括时间步长）{n5da1a}
						* 相关：((n5da0x))NO-场的多尺度结构，((n5d99m))场，((o8fe66))NO架构-分辨率泛化
						* 使用：((q6jg5c))NO-训练-多分辨率数据配比
					* （不含时）提前终止迭代：求解器迭代次数
					* 改用近似 PDE((q3aa55))，只要它更容易求解
					* 用途—直接用于训练；有效性：((pcj876))NO 有归纳偏置，预测精度高于训练数据
					* 用途—多保真训练((ncbn0o))
					* 用途—数据模型飞轮，((n95b99))低精度数据训出的 NO 加速高精度数据生成；不是事先准备好多保真数据
				* 设法加速数据生成
					* 单方程求解加速方法见((pcij4m))；此处仅针对数据集准备的加速，涉及多参数样例的求解
					* ROM：`DL-ROM-2111.12511` 造高保真小数据集，据此算 POD 降阶基底、造大数据集，再训 NO
						* 考虑约简模型可能精度不如原模型，可视为((ncbn0o))多保真数据
					* 数据模型飞轮：`2022-07-29`(CSImeet2) 粗网格生成数据，训粗网格 NO，预测结果插值可为细网格传统算法快速生成好初值，细网格生成数据、训 NO，再用来加速更细网格数据生成；{n95b99}
						* 湍流等问题简单插值可能不是细网格的好初值，此时不排除可用 ML 学插值
						* 相关：((ncbn0o))NO 利用多保真数据，((n95b8v))data engine
				* 若可控制采何样本（实验、数值），可((n81f1z))选高效采样点，包括事先选、active-learning
				* 不使用纯黑箱架构；{nckf86}
					* 考虑((nclm36))改进传统求解器，或((ncbn67))整合低精度求解器、只学其与高精度解误差
					* 相关：((n3bh2y))针对含时问题加速传统算法，((nca99w))NN 架构设计-引入先验
				* 自监督预训练((n8mn6x))，提高数据利用率
				* 相关：非 PDE 的一般框架 ((n32b4n))数据量不足的解决，包括 active-learning 等
			* LeCun 自监督所用数据增强((_n8ve3d))，涉及 crop、对解 Lie 变换（详细描述了 Burgers，KdV，KS，不可压 NS 方程的 Lie 对称群生成元）{n8ve45}
				* Lie 变换—副本((n8ve48))等变性-对称群类型-据方程形式
				* Lie 变换—部分来自((q81971))定义域对称群作用的 pull back
				* 相关：((n31n1z))数据增强，((ncln3x))网络引入物理先验-靠数据间接引入
				* 基于 PDE 尺度缩放等变性，((_ocka29))尺度因子作为 NO 输入，crop+改尺度因子 可生成新数据样本（其实是 self-consistency loss）；用于 Scale-informed NO
				* 多维联训时低维嵌入高维方式 数据增强，((_q1nb7i))2D 随机朝向嵌入 3D
				* 时空 crop 方式依据物理系统特点((p8nj9k))，时空同时变、仅变时间、在同一边界附近 crop
			* 可利用的传统求解器汇总见((o1ol2j))
			* 数据生成用 PDE 形式而非求解器：随机生成 $u$、代入方程算源项 $f$，得数据对 $(f,u)$；{pcja5j}
				* 约定—方法称为 MMS((_paia5k))，可追溯至 1995 年文献
					* ((_o1lk2r))ICLR2024 重新发明该方法
				* 局限—分布差异：((_o1s95f))鲁剑锋之前工作已说明该做法 $f$ 分布与真实不同，这会导致问题
				* 局限—函数空间错位：((_o1s95n))对 Darcy flow 等方程会出问题，随机生成的 $u$ 可能不对应任何一个 $f$ 的解；{o1sa11}
					* 若用 PDE 残差 loss，微分计算基于 BP 情形也会出问题((n29l0z))，用有限差分（见((n79i86))FD 而非 BP）似乎不会，至少鲁剑锋的实验结果看起来没问题
				* 比较—((_o1s95r))鲁剑锋认为该 $\|u-N(L(u))\|$ 做法不如用 PDE loss $\|f-L(N(f))\|$；{o1s961}
					* 相关：((n8jk1b))NO-训练-PDE loss，((o3da4d))PINN-残差范数
				* 改进—学 u 分布：随机生成 u 所用分布不用预设随机场，而是学出，从而接近真实分布
					* 其学习可用 PDE loss 或小样本（相对）有监督
					* 用贝叶斯 PINN 学出 u 分布((_pa8823))
				* 改进—联合修正：（可能性）这样生成的 (f,u) 仅作初始猜测，用类传统算法修正使成为实际解，f,u 都可变
				* 改进—摄动造 u：((_q13f5j))u 不是完全随机生成，而是基于传统求解器已解得的 u 组合、扰动
				* 用法—数据联用：与常规方法生成的数据配合，((ncbn0o))按多保真数据使用
				* 相关—正反向数据生成、同时使用，不限于 PDE 求解：{oaie9v}
					* ((_oaie9j))ODE 与其 Lyapunov 函数
					* 印象中用来求不定积分的论文也用到
					* 相关：使用场景包括((n8v95j))符号任务，((o1lj8k))反问题-直接 NO 求解
			* 数据生成用全空间基本解((_q2oa7j))，主要针对基本解已知的线性椭圆方程
		* 多保真数据的使用((ncbn0o))
		* 数据规模影响大于网络规模((_n4fc0d))
		* 有噪声数据，((_n7na6i))认为 FNO 对噪声的 robustness 不足；{n7nf7k}
		* 利用多保真数据可见 ((ncbm9z))paramPDE-数据生成昂贵
		* `INDEED-2304.07993` 用超网络，直接输入数据集得一 NO（INR 形式），regular task 可为正问题、反问题、时序预测等；{n4km5c}
			* 相关框架((nbim8p))不限于单个参化 PDE 的 NO
			* 与传统小样本学习的超网络的一处不同：((_o3nb6o))单向注意力 Transformer 输入所有 算子输入-输出 数据对，对每一个输出位置都求 MSE loss，从而单步训练同时涉及了 超网络输入数据量在一定范围 的情形（而非针对特定输入数据量训一步）
			* 后续工作 `ICON-2401.07364` 数据集由 ODE 推广到 1D 守恒律 PDE，包括正反问题
				* 时序推理方法，((_o1lb53))few-shot 可以是当前初值的历史时间步，也可为其他初值（单个或多个均可）的时序数据、要求预测新初值；{o1lj2o}
					* 副本：((o1lj2z))动力学-有状态记忆-来自动力学形式可变
					* Zebra((_oakf6q))类似，有多种上下文指定方式
						* Zebra 与 ICON 的区别 1. 序列格式，Zebra 各时间步 token 直接依次排列，用特殊 token 分隔；ICON 全部组织为输入—输出样本对形式，设计了特殊的注意力掩码
						* 区别 2. 单时间步表示方式，Zebra 用了 VQVAE
			* 后续工作 `ICON-LM-2308.05061` 引入文本，包括自然语言、LaTeX，用于澄清后面所给的输入数据的含义；{nbih07}
				* 相关框架((nbig72))方程形式作为网络输入
			* 后续工作 VICON((_ocoa1m)) 针对 2D 流体，预设了分量全集
			* 后续工作 GenICON((_pakg2y))为生成模型，提供 UQ
			* ((_o6mb5w))也声称用了 in-context learning，其架构基于 ViT
			* 架构变种—示例样本输入基于 DeepSet 超网络而非注意力上下文((_q1qf13))
			* 输入形式变种—((q6rk68))上下文建模 ICM，网络输入不直接对应目标映射（本构关系）的示例数据（输入输出对），而是系统满足的离散方程，((q6rk2p))相当于隐式指定目标映射
			* OoD 推理：增加上下文长度可达，前提为预训练任务足够多样；((_obgf7g))来自其他组的理论与实验研究
			* OoD 推理：((_q7ph0y))输入输出过一系列自适应变换，导出映射落入模型可靠范围内；{q7pk2a}
			* 同时处理正反问题的另一做法：((o6uf1s))拟合 $(a,u)$ 联合分布、推理时条件生成
			* 相关：NO 额外输入单个（而非一批）参考样本((o72k8l))，可提高对 a OoD 泛化能力
	* 引入辅助任务（常相对低成本）
		* 场景—数据不足¹，辅助任务则不受限（数据充足or无需数据）
			* ¹包括：观测数据采集昂贵、（已知方程形式）仿真数据生成昂贵
		* 用法，与原任务协同方式：多保真，残差学习，迁移学习（预训练-微调），多任务学习
			* 多保真（单任务）：与原映射不显式区分；前提：辅助任务映射关系差异很小，如 低保真解、近似方程解
				* loss 用 telescopic sum：eg. ((_p6d97s))基于分辨率的多保真，低分辨率部分残差用较多样本算 empirical mean，高分辨率部分用较少样本；{p6d97z}
			* 残差预测（1.5 任务），辅助任务解 修正得原任务解，网络架构多保真；{q8272d}
				* 即：NO 输出 $[u_1+]g(a[,u_1])$，其中 $u_1=f(a)$ 为低保真辅助任务解
					* 少数情形 $g$ 输入可不包含 $a$，如((n8i94h))超分辨率情形（辅助任务为粗网格上求解）；不过似乎输入 $a$ 总没坏处
				* 前提：二任务输入输出空间相同，且映射关系差异不大
				* 低保真求解 $f$ 可基于传统算法（低保真故低成本）or 黑箱网络（用低保真数据训）
				* 修正网络 $g$ 训练：可用((q81m9u))半监督学习-引入辅助网络造伪标签用于互监督
				* `2204.06684` multifidelity DeepONet，高精网络输入包括低精预测结果、输出对低精预测的修正；{ncbn0u}
				* `MLevelML-1909.09448` 启发（尽管原文考虑低维问题）：网格有多种分辨率，每网络学相邻分辨率数值解改变，有理论泛化误差定理；{ncbn0w}
					* 有相似性的做法：((ncbn67))低精度解直接由低精度传统求解器产生，NO 修正为高精度解
			* 输入增强（双任务）：辅助任务网络输出（or 中间层特征）作为 NO 引入额外输入
				* 训练阶段：一阶段只学辅助任务，二阶段训原任务 NO，可同步微调辅助任务网络
				* 残差预测 作为特例，NO 输出为 两个阶段网络输出之和
				* 他人已训的辅助任务 网络直接复用
					* eg. 变区域 NO，辅助任务网络取为 混元 3D VAE((_q3ab4v))，尽管效果不好
			* 迁移学习（双任务），预训练((n37n1v)) 后微调
				* eg. `MLFT-2102.07169` 单网络先在粗分辨率大量数据训练，后逐步用细分辨率数据 fine-tune
				* eg. 2304.06972 大量低精度数据预训练后 少量高精度数据微调，用 FNO
				* 微调阶段可引入额外参数，此时不要求二任务输入输出空间相同
					* eg. ((_q39b23))引入任务专用头
				* 表现可能不如二任务同时学，如((q3kk8u))高维 NO 用低维方程解数据辅助情形
				* 迁移能力讨论见((q7oh0e))NO-OoD迁移
			* 同时学（多任务学习 MTL），可引入不止一个辅助任务
				* 方式—任务类型作网络输入：输入输出空间相同，映射关系有差异
				* 方式—网络使用不同输出头：输入空间相同
			* 多任务预训练，再微调迁移到原映射，如((_o6mb52))Poseidon；{pbf88t}
				* 所属框架：((nbim8p))PDE基模-隐式输入-微调过程，即解特定方程总需微调
				* 微调阶段参数分三类((_o6mb54))，大量慢更新参数、部分普通更新参数、少量的随机初始化快更新参数
				* 类似的多方程预训练策略也用于((_pbf88x))P3D
				* 预训练改基于单时间步 AE（无时间推进）：((_q7if15))Tadpole，为 P3D 后续工作
				* 预训练不总有益：37.5% PDE基模架构-PDE pair 预训练负迁移((_q7kh68))；{q7kj7k}
				* 基础模型迁移到下游任务 案例整理
					* ((_q39b0i))MORPH（自回归）用于惯性约束聚变 估计系统设计参数（正问题预训、反问题下游）
					* ((_q3kl07))Poseidon 迁移到 3D 火星大气预报任务
				* 一般迁移能力讨论见((q7oh0e))NO-OoD迁移-能力评估
			* 多手段联用：如((_q3kh0i))待解反问题，同时用 1 任务—多样方程正问题+协同—微调迁移（即 基础模型），2 任务—对应方程正问题+协同—MTL
		* 分布简化：映射关系相同，输入输出分布不同
			* 随机选 u 算 RHS((pcja5j))，需 PDE 形式完全已知
			* 输入简化：原本关注的输入范围难造数据¹，辅助任务换用好造数据的范围
				* ¹包括 模拟数据计算成本高（PDE 完全已知）、实验观测数据相关场景昂贵
				* eg. 降雷诺数、减障碍物数 生成大量数据((_q1og80))；其参数作为网络输入，属于多任务学习
				* 注意区分：PDE 换参数范围 可能有两种情形，1. 该参数作为 NO 输入的一部分，算输入简化；2. 仅视为背景方程的一部分，算近似方程
					* eg. INS 方程 变粘性系数，若 NO 输入只有初值 算近似方程，若同时有初值、粘性系数 算输入分布简化
			* 同方程低维版本，相当于输入某维度取 const repeat；{q3kj0o}
				* 低维数据处理（输入输出形式调整）
					* tensor shape：const repeat 到相同 shape；保留 shape
					* 额外轴朝向：固定一个额外轴；随机选轴（如 Walrus）
				* 架构适配（仅 tensor shape 保留情形，否则平凡）
					* 同架构，需((q1nb9f))架构支持不同维数
					* 高维有额外参数，如((q3ki9c))2D SWin 微调到 3D 时引入 z-轴向注意力
				* 训练协同：微调（分阶段），多任务同时训；((q3kk8u))后者通常更好
		* 低保真解：输入输出空间相同，映射关系有差异；{ncbn0o}
			* 来源—传统求解器降低求解精度((ncbn2v))（PDE 形式完全已知情形）
			* 相关来源—用 PDE 残差 loss 而非有监督数据，((o1lb8e))半监督训练（PDE 形式完全已知情形）
			* 来源—观测数据，用多种成本精度不同的配置、观测手段（不假定 PDE 已知）
			* 比例：((_q17g2g))实验研究，高保真数据占比对精度影响
			* 比例—降分辨率类低保真：((q6jg5c))NO-训练-多分辨率数据配比
			* 用法—也视为新任务：预测目标取为 传统算法数值解，而非真实解
				* 多任务设定下 网络额外输入可能选取：求解算法类型（离散）；（控制保真度的）算法超参数（连续），如 ROM 维数、网格分辨率、迭代算法终止的 tolerance
				* 相较一般“低保真数据”特殊性：本身可作为良定义任务的输入输出；其他 ML 领域低保真数据可能是噪声大、错误样本多等
			* 相关：((n31m8z))一般 ML 中多保真数据的来源、使用
			* 相关：算法而非数据的多保真，((pcig07))数值格式（可 AI 增强）引入多保真版本混合
		* 近似or类似方程：预测近似 PDE 的解，其求解成本较低、可用数据量大；{q3aa55}
			* 场景—原方程部分未知（只有观测数据），已知部分机理对应的方程可造数值解，将其作为辅助任务
				* 见 ((p3rb3u))观测数据+近似方程仿真数据，针对 PDE 部分已知情形
			* 场景—原方程已知、但求解困难，近似方程数值解相对便宜
			* 近似方程((ncmk54))：所用方程不同，精确vs近似方程
			* 类似方程：物理性质相似，生成成本更低，通过迁移学习、多任务学习等辅助原方程预测
				* ((_paf927))多相流从单相流迁移，含时（瞬态）从稳态迁移
				* 含时（瞬态）从稳态迁移，((q5ca2q))稳态解用于自监督预训练
			* 相关：同资源，使用方式（构建辅助任务）换为引入数值格式（不再纯黑箱架构），见((n3sg1r))动力学-方程部分已知
		* 多样方程，即((nbim8p))基础模型
			* 方程类型间迁移能力有限((_q3uf9f))，预训练需确保方程多样性
		* 不同性质的有监督任务，有标签 但不是 PDE 的解，并且容易生成
			* 前传 解反问题((o1hn0d))：如((_o73k0h))输入物理场预测 PDE 系数
			* （对偶情形）待解反问题，引入正问题作辅助任务；{q3kh4l}
				* 如((_q39b26))保留正问题 loss 防退化，即 协同方式为 多任务学习
			* 数值求导：((_q3af8x))输入物理场输出其各时空导数
			* 几何特征提取：变区域 NO((_q3af9x))预测 SDF 或矢量距离
				* 预测 occupancy field：((_q5cg5n))计算域形状用内部点云描述、作网络输入，输出按 INR、在计算域外 query point（如机翼内部）输出 0
			* 几何边界粘附：针对变区域散点 NO，((_q3ag09))GeoPT 预测常速粘附输运下的各粒子轨迹
		* 自监督((n8mn5a)) 类 proxy task（此处只记录仅适用于 PDE 的）；{o6mb23}
			* 相关：((n8ve45))NO-数据问题-PDE 数据增强
	* 训练问题
		* 针对已知形式的 PDE 求解情形（可无监督、半监督）{o1lj8r}
			* 纯 PDE loss：`PI-DeepONet-2103.10974`（$u$ NN 参化，无网格），`PINO-2111.03794`（$u$ Fourier 基底表达），`MaxwellNet-2107.06164`（$u$ 为 CNN 输出，有限差分给出 PDE loss）{n8jk1b}
				* 可能用到：((n2o879))数值微分，((n8jk0h))随训练进行不断增加网格分辨率
				* 可参考((n3bg3i))PDE loss（不是只有 PINN loss；可参考其中((p34c00))PINN loss 不正确情形）
				* 对 PDE 残差建议施加 $H^{-1}$ 范数((_p34a8n))，类似传统方法后验误差估计；{p34b9e}
				* NeuFENet-2110.01601（似未发表）似乎是用了 U-Net + Ritz loss
				* 鲁剑锋较早的文章已使用 PDE loss 训 NO((o1s961))，残差用于训网络好过用于人造数据
			* 半监督，相较纯有监督、纯 PDE loss 均有优势：{o1lb8e}
				* 即使有监督数据充足，引入额外 PDE loss 也可能提高表现；{n4fe4x}
					* 如((n4fe4b))提高不连续解的预测效果，((n4fe6l))避免初值解在长时间后被抹平、表现不出激波
				* 分阶段训练，((_p34c08))预训练阶段纯有监督、仅几个 epoch，微调纯 PDE residual 无监督
				* MOD-Net（粗网格生成部分数据，相比纯 PDE loss 能起到 ((n41g1e))正则化 效果）{n41g1a}
				* meta-learning 系列的部分（DeepONet，我的 AD 做法，`AD3losses:` 3 种 loss 同时包含有/无监督版本）
				* 扩散生成模型建立 $(a,u)$ 联合分布，((_o6uf4l))推理（扩散生成）过程额外引入 PDE loss 可提高生成质量、预测精度
				* 相关：((o1lb81))coordLoss-多项 loss
			* 相关：((ncbn0o))用多保真数据训练
			* 相关：除 PDE 正问题求解外，另有((o1lg15))作求解器迭代算子，((o1lj8k))反问题解算子 的训练问题
		* loss 设置见 ((n3pj3j))coordLoss，包括 PDE-Net 加 noise
			* FNO 训练 loss 有时用 $H^1$((o2ga6g))，包括李宗宜的工作
		* 混合精度训练；{o2ga98}
			* ((o2el0j))ML-低浮点精度-DeepONet
			* ((_o44e60))NO 混合精度训练的逼近误差理论估计
			* precision schedule，((_o2ga9c))FNO 早期半精度、中期仅 FFT 全精度、后期全部全精度；{o2gb03}
				* 相关：((o2gb08))学习率 schedule
		* 相关：((ncqf18))NO 优先拟合低频的先验
	* 其他
		* operator learning 在统计中有古老的名字：functional data analysis（from lyp）
			* 统计角度解读的相关文章((_p5ge5d))
		* NO 精度超越其训练数据，((_pcj86r))基于归纳偏置消除低质量数据中数值伪影；{pcj876}
			* 相关：((p1mf8o))AISC 适用范围，((ncbn2v))NO-低精度数据-来源
		* 对 NO 用 ensemble 做 UQ((o6ml3l))
		* NO 形式改为 $(a,a')\mapsto u-u'$ 预测与参考解残差((o72k8l))；{o72l9g}
		* OoD 情形推理，((o6mm5b))结合先验物理约束，从带 UQ 的 NO 中挑最符合约束的解
		* （2021-07-31 李宗宜线上报告）
			* 数据规模对精度的影响大于网络规模；{_n4fc0d}
				* 2021-10-29 CSI讨论提到数据达到一定规模后 NO 精度不再提高，分辨率等的误差无法消除？
			* 对于塑性材料模拟问题等，可以材料划分小格，每个运用 GKN
			* Markov Neural Operator 可以用于预测混沌系统长时间统计行为（在无法预测精确位置的情况下）
			* FNO 适合拟合输出在连续函数空间内的算子，来源于 FT 适合处理连续函数；{_n4fb9t}
		* 关于解算子，2021-09-29 组会li+1学长给的报告有用：`reviewNO-2108.08481` 形式上统一了现有 NO 版本（其版本汇总可参考；版本间差异在于构造积分算子的方式），并且有理论逼近性质证明
		* [关于 Neural Operator 的文献列表](https://zhuanlan.zhihu.com/p/361050232)
* AI 增强传统算法；{ncmk3t}
	* 展开范式：从传统算法局限出发（现有系统改进问题），因果链分析展开，AI 作为具体缺点的解决方案
	* 要素：物理方程（包括 BC），数值表征，求解格式
	* 考量：误差，成本
		* 二者原处于矛盾权衡状态；记录解决方案时仅按其直接解决的缺点，如在某预设的误差阈值下，某方案允许用粗网格、从而降低成本，它直接解决的缺点是误差
		* 降低误差 也不总表现为最终成本降低：可能是在缓解数值格式的根本缺陷，无法通过增大网格密度修复
			* 例如当时 ENO、WENO（虽然不涉及 NN）缓解之前格式的振荡问题？
	* 方程误差
		* 方程本身不完全准确？包括 方程形式、系数与系数场、BC（及人为约束到有限区间）、初值
	* 方程成本：通过((ncmj8o))模型约简
	* 表征（空间离散）误差；可参考((n32e9r))场的数值表征
		* 网格分辨率不足（主要均匀网格离散类）
			+ 结合先验（数据中习得的），((_pcij8b))用超分辨率网络
		* mesh 划分不合理（主要非均匀离散，FEM、FVM）包括疏密分配、形状；{n7pf4y}
			* （评）生成 mesh 可提升解精度；原则上也有提升求解速度的可能性，目前不太涉及
			+ 基于优化，移动调整节点位置（固定总数）
				* 支撑—可微性：`HiDeNN`(AISC2) 用 PINN 表达 FEM 函数空间，从而节点位置可优化
				* loss—依据固定细网格参考解：细网格固定（包括方程定义域），优化粗网格格点位置使得：任意参数下，粗网格上的数值解对细网格上预测解最为 informative, eg. `2007.04439`
			+ 基于预测，各局部网格目标形态
				* 预测依据：PDE 参数（静态），当前解（数值迭代or时间推进，动态）
				* 预测对象：面积，（有向）边长
				* eg. MeshingNet，依据 PDE 参数，对象为网格面积，每次只预测一个区域
				* eg. `MeshGraphNets-2010.03409` 依据时间推进当前解，对象为有向边长（用 sizing field）；解表达基于 GNN 而非 FEM
					* （评）网格性态应该需要有向，有时候解具有各向异性特点，各方向需要的网格密度不一样
			* 训练依据—若可定义目标函数，则 mesh 优化可转化为 shape optimization，按 inverse design 处理
				* eg. `MeshSDF` 对 mesh 顶点的梯度反传为对形状的梯度
				* PDE 求解精度可作为目标函数的一种？
			* 训练依据—（可能性）RL，可 value-based
				* RL policy-gradient，GAN 可能需要透过求解器求导？`2021-10-20b`(AISCmeet)
				* `2021-12-22`(AISCmeet) 提到 RL 调整网格，相应的动作、状态、reward
				* 相关综述，((_p2of0c))RL 生成or优化网格
			* 可参考综述((_q5be37))AI 辅助网格生成
			* 未处理：`2020-12-16`(AISCmeet)
			* 相关：((n8jb6c))coordLoss-调采样点密度，((o4dk76))网格划分（一般，不限于 ML 方法）
			* 必要性讨论在 `whyAISC%` 涉及
		* 基底选取不合理（mesh 单元内，主要 FEM）
			+ FEM 基底函数可学；{pcij6c}
				* FEM 中各块基函数学出（而非预设多项式），((_p73f2i))DD-FEM
					* FEM 仅关键区域换为 NO，原本就粗网格的部分仍继续用原 FEM 基底，((_p8ff3n))NOEM
		* ROM 降维导致信息损失（全局）
			+ 用非线性 ROM
		+ Mori-Zwanzig 整合历史信息，((p8tk9n))NO 引入历史记忆项
	* 表征成本：
		* 表征维度高
			* 网格密
				* 相关：((_q5be3b))预处理简化计算区域，去除小孔、倒角、压花文字等小特征（不影响物理），避免引入计算开销
			* 单位网格阶数高（FEM 多项式阶数）
			+ 降维 ROM：对方程（包括对空间域网格、基底离散所得的代数方程）降阶建模 ROM；{ncmk5e}
				* 主要见((n8jb8q))paramPDE-ROM，包括空间处理方式；这里只放时间离散后单步推进的针对性做法、技巧
				* 线性降阶，可能性：基底只表达空间域，NN 根据当前时间步的场生成下一步降阶的 trial manifold
					* 不区分时空的降阶已在 `2105.14633` 考虑
				* NN 非线性 ROM，求解隐向量时间更新：{n2h902}
					* 相关框架：((n2h89x))优化隐向量，((n8jb0f))场数值表征方式-时空分量不同
					* 在空间域算时间更新，再投影得 $z$ 更新；涉及场表征方式转换¹，参化 + 离散；{oa4m7t}
						* ¹具体地，考虑((n8jb0f))场的数值表征（仅空间），为了对参化表征算时间更新（即隐向量更新），先转换为离散表征，对离散表征算时间更新，再转换回参化表征的更新
						* 另可解读为 先（在原离散空间¹）时间离散，再降阶建模
							* ¹这里将离散表征结果视为原空间，引入 ROM 是为了减少离散表征的维数
						* 空间域算时间更新方式：传统时间积分器（RK 等）演化得 $\hat u_{t+1}=\Phi(u(z_t),\tau=1)$
						* 投影方式 1：优化 $z_{t+1}$ 使 $u(z_{t+1})\approx\hat u_{t+1}$；{n8je1c}
							* 如((p99f5e))CROM 作为非线性最小二乘问题、有较好初值而用 Gauss-Newton + 回溯线搜索 优化器而非 SGD 类；{n2ha62}
								* 优化初值用 $z_t$ 即可，解突变的极端情况用 NO $z_\theta(\hat u_{t+1})$（可沿用训练时获得的 encoder 部分）
							* 可能性：((n8je1m))PDE INR 无监督训练，用传统迭代算法给出更新方向，而非用 PINN loss
						* 投影方式线性化版本：优化 $z_{t+1}$ 使 $\nabla_zu\cdot\Delta z\approx\hat u_{t+1}-u(z_t)$
							* 如 `CROM-2206.02607` secC.2，线性最小二乘问题可直接精确解代数方程；用到的 Jacobian 矩阵靠前传而非更慢的 BP 计算；{n2ha6f}
					* 直接在隐空间算时间导数、做时间积分，找 $\dot z$ 使 $\nabla_zu\cdot\dot z\approx f(u(z_t))$（相当于极小化 PDE loss）
						* 算是 先降阶建模，再（在隐空间）时间离散
						* 如 `1812.08373`，`1909.09754`
						* 相关：隐空间时间导数由 NN（而非动力学方程形式）给出((n2h92i))
					* 也可 NN 直接给出隐空间动力学((n2h965))，包括离散时间推进、连续时间推进等各种做法
				* 时间推进保持物理守恒律：`1909.09754`，包括线性降阶（其引文）与非线性降阶
				* 相关：低维状态空间（空间域无需降维）、对时域函数找降维基底做法 HiPPO（见 `HiPPO-LSSL-S4`），涉及动态基底（基底针对已知历史 $[0,t]$），可导出该动态基底下系数满足的 ODE
		* 网格划分成本：AI 生成网格？
	* 格式误差
		* 时间类
			* 单步推进不准
				* 时间步长大
				* 时间推进格式精度低（包括 阶数、显式隐式）
				* 方程刚性（包括 ODE）
					+ 预条件处理
			* 多步数值不稳定，CFL 条件（仅 PDE）
				* 时间步长大
				* 空间网格小
				* 信息传播速度大
				+ 设计新数值格式，综合多点信息？
			* 多步误差累积
				* 单步迭代有误差，尤其敏感、易被放大的分量、模式
					+ 多步 loss 联合训练
				* 先前误差保留，未被修正
					+ NN 修正；需考虑离线习得方式
						+ 学去噪
						+ push-forward trick/replay buffer
				* 迭代步数多
					+ 增大时间步长
						+ 时间打包，一次预测一段而非一步
						+ 时间多尺度
			* 结果时间分辨率不足 or 目标时间步位置受限
				* 注：问题需求不同，前者需完整时间轨迹，后者只需要特定时间位置的解
				* 时间步长大
				+ 时间多尺度代理模型：黑箱预测长时间演化，再传统模拟补充短时预测；{ncmk5k}
					* ((n7mj2c))不同时间尺度推进的多个 NN，短时间步可直接 RK 解 ODE
			+ NO 黑箱时间推进，可用大时间步长；{ncmk39}
				* eg. `2022-08-19`(lectures) 许志钦化学反应从机理计算，靠时间演化算子增大时间步长
		* 空间类-数值格式误差（离散导致，局部）
			* mesh 划分不合理；见 mesh 生成与调整
			* WoS 类随机算法((q7ia3q)) 来自 Monte Carlo 采样的随机误差
			+ 可学数值格式
				* 对象：
					* （FD 类）微分算子离散化方式，包括 局部（如 PDE-Net）、全局 的格式
						* PDE-Net 约束必要性：原版有理论导出的系数 moment 约束，`2022-02-11`(CSImeet2) 若只关心预测精度、长时稳定性可去掉约束
					* （FVM 类）通量函数，如 `[Dong]-RL-WENO-1905.11079` 学 WENO 系数
				* 可参考综述((_o96a65))粗分辨率下精确模拟，包括学离散化方案、通量、封闭建模、简化建模
				* 动态系数格式，学出系数生成法则，如 `[Dong]-RL-WENO-1905.11079` 类似 WENO 动态生成卷积核
			+ 谱方法离散
			+ 后处理，修正预测结果；数值格式独立运行
				* 粗网格上 NN 恢复细尺度物理过程；{ncmj8c}
					* 相关：网格-((p8tl0t))离散误差、((n2pf83))多尺度网格，((ncmk39))简化空间网格（用更大时间步）
					* `MNO-2207.11417` 粗网格（文中称为“大尺度解”）上直接调用传统算法产生的误差项用 NO 刻画，相当于给出了某种模型封闭；{p99f6l}
					* `PPNN-2205.03990` 用粗网格（允许大时间步）FD 计算，NO 学残差，若有未知项也能学出
					* LaPON-2505.12360 做法似乎类似
					* 用 NN 恢复被遗漏的细网格物理过程，((_ncmj7l))包括对流、边界层等，天气预报模型 NeuralGCM 中用到
					* 注：若用观测数据，可同步补全未知的方程项
		+ 精度补偿方式
			* 后处理，修正预测结果；数值格式独立运行；{q7ak67}
				* 超分辨率
				* 误差预测；相关，对象（数值格式预测结果）一般化为现有映射输出 见((n7d90o))拟合残差
				* 结果投影（类似去噪）
				* 对 SDE 等随机方法所得有噪声解去噪((_q7ia49))；相关：((q7ia3q))传统算法-WoS
				* 相关：此处假定数值解提供大部分信息；若数值解信息量相对少（如网格很稀疏/方程不完整等），主要求解难度由后续网络承担，应解读为((q7aj9t))NN输入增强-基于资源-参考解
			* 修改数值格式，方程保留原形式
			+ 改方程形式，抵消数值格式误差((pcif73))
	* 格式成本
		* 单次调用成本-单次求解复杂度高（同维度）
			+ NN 拟合原格式，可多保真
				* FVM 通量：高阶迎风格式通量精确计算复杂，用 NN 拟合((_p49f1g))，包括双保真 NN 版本
				* 矩阵求逆：((_oa4m2x))牛顿迭代格式中 Jacobi 矩阵求逆换为 NN
		* 迭代调用次数（非含时；时间隐式推进格式内的迭代也算）
			* 初值质量
				+ 低保真数值解((ncbn2v)) 作为初值
					+ 包括 若有近似方程、近似参数的解已知，可作为初值
					* 似乎多重网格就是这个原理？粗网格解作为细网格解的好初值
				+ NN 预测初值；{pcij4m}
					* NO 预测近似解，基于良好初值可用简单优化算法，如((p99f3m))
					* NO 输入增强—传统格式最初迭代步：CFDNet-2005.04485 `2022-07-22`(CSImeet2) 输入除翼形 $\lambda$ 外还有传统格式最初几步迭代（称为「预热」）的结果 $\bar u$
			* 收敛速度（迭代步质量）
				* （传统线性）高频收敛速度、低频收敛速度
					+ pre-conditioning；可参考综述((_pcij7r))
					+ 多重网格（加速低频收敛），Meta-MgNet
				+ NN 预测 or 修正迭代步，((n2rm3m))学求解器的迭代算子
		* 迭代调用串行性（非含时）
		* 时间推进迭代次数
			* 时间步长小
				* （旧）注：本身为主动选择节点；鉴于其可能导致的缺点类型较多，逐个单独展开因果链太复杂，故以下对“无法使用较大时间步长”分析原因，实际上是“用小时间步长”的目的因
			* 目标时间范围大
		* 时间推进迭代串行性
			+ 长时间步模型预测各段初值：
			+ 预测-校正格式，后续时间步预计算，之后再（根据前序时间的完整计算结果）微调；{n5ae5u}
				* 注：作为传统算法可能叫 parareal
				* `PPINN-1909.10145` 无预先训好的黑箱、只用方程，近似方程（如忽略小尺度项）演化长时间步、精确方程演化短时间步，鉴于精确方程初值不准，引入预测-校正迭代格式
					* 相关：((ncmk54))近似方程 以降低数值成本
					* 相关：只有精确方程的情形，((n8qm3f))PINN 每次推进一小段时间，效果明显好于直接预测整段时间
				* 可能性：考虑到部分优化算法（包括 NN 训练）可解读为 ODE，不排除该做法可用于加速优化算法？
				* 校正相关：((nckb3u))修正插值结果
		+ 改解近似方程((ncmk54))，降低数值求解难度，甚至可有解析解
	* 多保真版本结合使用；{pcig07}
		* 多保真对象类型：方程，数值格式，网格划分
			* 前项不同 可导致 后项也不同；如简化版方程可用更大时间步长，更简单的算法，或甚至有解析解
			* 方程多保真如 PPINN
			* 多保真相关：((ncbn0o))NO 数据生成
		* 用途：
			* （含时）多时间区间并行计算：PPINN 预测-校正格式((n5ae5u))，每次迭代先估所有区间初值（低精度、低成本，串行），再独立算各个区间（高精度、高成本，并行）
			* 按状态复杂度切换，平缓区域用低精度，复杂区域用高精度
				* 依据—空间，对象—网格：FEM 网格自适应加密or升高阶数
				* 依据—时间，对象—方程：时间双尺度动力学
	* 方程改写、形式调整
		* 改写后形式区分：有完整表达式、纯理论推出 vs 含 NN 表达的项、需训练
			* 后者相关：((pcfj5p))方程项由 NN 表示，此处（约简情形）依据为底层原理从头算模拟数据，另有依据观测数据的
		* 模型约简，针对方程成本；{ncmj8o}
			* （旧）简化物理：给定物理模型（PDE）已有 reduced 方案，其中需要给出的本构关系（或模型封闭）通过学习给出
				* 与 ((o5sm3k))singleDyn, `ML2phy%`(MR), `phyGuidedDyn:`(MR) 的关系见 ((nckl0m))phy+MLpersp-数据、模型中机理占比
				* `phyGuidedDyn:`(MR) 的做法事实上也可用，分开记录的原因与方法的能力范围无关
				* 相关：系统部分已知（如知内部方程不知边界条件）时学出未知部分也算学本构方程，但不叫降阶
			* 仅时间离散？
			* 无视时间：RANS 含时变稳态，通过系综平均
				* RANS 使用 $u\mapsto\tau$ nonlocal 映射 `2021-08-08`(lectures), 湍流封闭 `2105.08633`
				* 无需解析导数（ensemble Kalman 估计）的 `2202.05122`
				> 回忆 RANS 是做了系综平均，包括时间平均，故方程不含时
				* 可参考 [GitHub肖恒RANS-PIML主页](https://github.com/xiaoh/turbulence-modeling-PIML)，包括 UQ；若停更，可考虑看用户的最新仓库
			* 空间维数部分消除
				* 时间双尺度动力学？((n23m8q))快尺度动力学的极限情形写为慢尺度动力学
				* Born-Oppenheimer 近似，电子波函数自由度积分消除
					* DeepPot；大模型相关 `DPA-1-2208.08236`
				* （含时）转为部分可观测的有记忆动力学
					* ODE 降维后构建封闭模型的理论合理性：降维后的微分-积分方程与原问题等价 ((n35e9h))Mori-Zwanzig，用 RNN 等近似积分项即为合理的近似封闭模型
			* 变换后维数部分消除：li+1学长做的流体 Boltzmann 方程 moment closure
			* 空间尺寸缩减，限制到子计算区域，((p8jg5m))设定数值 BC
			* 常涉及模型封闭、本构方程，见((pcfj5p))方程项用 NN 表示
		* 近似方程，针对数值成本，甚至可能有解析解；{ncmk54}
			* 忽略因素：多尺度方程忽略细尺度；((_q2ka3d))仅保留主导动力学的项
			* 去耦合：多相流用单相流近似
			* 添加新项：加粘性使良态
			* 扰动化归已知：参化动力学参数微扰（原参数有解析解，或已训 NO）
			* 极限化归：时间双尺度动力学，((n23m8q))快尺度动力学的极限情形写为慢尺度动力学
			* 相关：这里用于特定系统预测；另可用于((ncbn2v))NO 数据生成-多保真，分布（而非具体解）接近、有迁移性即可
		* 改方程形式，针对数值精度，抵消格式误差，通常稍增加方程复杂性；{pcif73}
			* POD 降阶后模型不稳定，学出余项（作为模型封闭）使之稳定 `RL_MR4PDE`
			* 可学反应项((_pcil3e))，表现好于修正数值格式所得解；即修正项引入位置在数值格式内而非外
			* 相关：方程形式调整 还用于((ncmj8o))模型约简、降低求解成本
		* 相关：((n41f93))PDE 形式等价改写
