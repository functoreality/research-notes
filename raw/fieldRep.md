> 2026-08-16 从 numeric.md 独立
* 场的数值表征，或函数的有限维表征，离散、基底与参数化；{n32e9r}
* 适用范围—图像（属框架((n3bg7w))CV），((_n3gm4s))认为图像也应按场（而非像素集合）理解，离散化只是用计算机中处理时的手段；{n3gm56}
* 函数空间 $F$ 参数空间 $P$；离散为 $F\to P$ 映射，参数化为映射 $P\to F$
* 散点离散 $F\to P$（用例包括((n8o92z))有限差分），不适合 PINN loss
	* 反向映射 $P\to F$：无 canonical 选取，各种插值都行
	* 均匀网格
		* 交错网格：流体、电磁里有 MAC grid，可表达规则网格的向量场，不同分量在不同格点记录取值；{n7je9s}
			* 该做法适用于一般 mesh 表征((n7je9x))
		* 有限的分辨率会在频域带来 alias 效应((_n3gm4j))；{n3h98i}
			* 一个后果：考虑对场进行变换的 NO（CNN 中的层也算），((n3h98j))alias 会破坏网络的等变性
			* 参化表征网络的训练也需考虑 alias，((o6ea4b))INR 拟合高频函数所需采样点更多
			* ((o8fe6k))NO 输入层先上采样，以避免 aliasing
			* ViT NO 输入层 patchify 降分辨率引起 alias，((q1mc9j))事先随机短距平移以使网络能应对
			* 传统谱方法需要 dealias，结合 NO 的方法不需要((p34e4l))
		* 资源：patch，一般见((q68m4l))区域的子区域结构
	* 形变均匀网格（四边形、六面体）{q8173v}
		* 非等距，如 谱方法非周期 BC 所用正交多项式的离散点（Dedalus 等）
		* 变形网格，如 2D 机翼 C 形网格，有时还有 O 形；原方程变换到形变域上可视为满足新方程
			* 衍生方案((n32f0k))多块变形网格拼成复杂区域
			* 含时形变（网格点位置不断移动）如 ((q4qm3q))动态离散，用于流固耦合等
			* 目的包括((q8173w))一般域上场表征-用均匀 grid-域转矩形-域变换
	* 多块均匀网格拼成复杂区域，来自 ((n32d90))PDE区域分解-复杂区域划分为简单形状组合；{n32f0k}
		* 该策略的一般讨论见((n7ne10))，以下主要讨论场表征方式
		* 网格比散点、graph 顶点规整，由于问题区域形状复杂，无法做到单个 grid 那样规整
		* 拼成大规则网格：不同块 grid 可重组得一整个大 grid，多出来的无意义部分置零（mask-out）{p3v96m}
			* 以整个作为 CNN/ViT 输入/输出
			* 另可根据区域划分方式（先验知识），在大 grid 中引入边界连续性等约束 `2022-08-12`(CSImeet2)
			* 若块可分为多组、组内尺寸一致，可考虑每组组合为一个 grid（无需 mask）、分别 CNN `2022-11-25`(CSImeet3)，CNN 之间通信可 concat 或类似((p3v96z))ViG
			* 拼接操作还用于((p3v979))triplane-INR，各维网格量不同时，三平面沿空间拼为大 grid
		* 处理的网络架构另可参考((p3v96z))VIG，不同块 grid 为 GNN 不同顶点，无需拼接；和纯序列 Transformer 可能差别不大
		* 对 `NO%` 有用（规整网格 CNN/ViT 精度高于 GNN 等）
		* 有已知位置边界层等也可使用多块规则网格（无论区域复杂与否）`2022-07-22`(CSImeet2)
		* 多块均匀网格 另一用途：简单区域、但希望采样点非均匀分布，如((n7je8l))NUNO
		* 相关：((o4dk76))网格划分
	* mesh；表达的向量场可以离散为：每边赋予一个数值，而非每顶点赋予一个向量；{n7je9x}
		* 做法类似计算微分几何，方便梯度、散度定义（标量场仍每顶点赋值），作中介定义标量场的 Laplacian，结合 2-单纯形定义旋度，可结合 GNN `DPGN-1902.02950`(x)
		* 相关：((n7je9s))均匀交错网格 涉及类似做法
		* 相关：GNN 中顶点和边都可有 feature 但不像这里有表达函数的具体含义
	* 有任意性的散点
		* 随机撒点、graph 顶点等
		* 散点位置随时间改变：粒子方法表达含时函数，算位置非固定的散点
		* 散点离散下数值求导方式见((n2o879))
	* 插值：不同离散方案转换，如不规则点到规则点；{n7jf0i}
		* 若函数有间断可考虑各向异性插值，类似 WENO `2022-04-22`(CSImeet2)
		* 任意散点插值到分块均匀散点，从而（一定程度上）保持散点空间分布，如((n7je8l))NUNO
		* 为减小插值误差，((_nckb3i))NeuralGCM 用可学网络修正插值结果；{nckb3u}
			* 相关：“修正”还见于((n5ae5u))预测-校正格式
		* 多项式插值((o1ol3w))
	* 多尺度与网格加密；{n2pf83}
		* 此处仅讨论全局加密；局部加密见((q1ij5t))
		* 维度 2：加密部分覆盖原网格（这部分场只用细网格表示），不覆盖（对不同尺度网格表达的场求和）
		* 全局（非覆盖式）加密的做法((n2pf3f))，可结合 hash 等减小 $P$ 维数
		* 降低离散化误差 有网格加密以外的方式，见((p8tl0t))离散误差
		* 相关：多重网格((n2pf9o)) 用于有限差分时，是逐步降低网格密度（全局网格），有覆盖式（不同层网格可表示同一个场）、非覆盖式（粗网格只记录残差）
		* 相关：((o8fe66))NO-网格分辨率可变，((o4dk76))网格划分
* 有限体积法 FVM((n8o931)) 直接给出的是 $F\to P$ 单向映射
* 线性基底组装 $P\to F$；{o1mj3g}
	* 反向映射 $F\to P$：常依赖于函数空间内积结构（$L^2$ 为主）
		* FVM 原本直接给出的就是 $F\to P$，只是可解读为 取定分片常数基底后向其 $L^2$ 投影
		* 非 canonical 存在案例：小波 frame（不是小波基底）只是左逆不是右逆
		* 若基于散点离散后的 $F$ 求反向映射，警惕((o1ol3w))龙格现象
	* 基底类型（局部）：FEM（常局部多项式），spline，小波；FVM 也算（分片常数）
		* 高阶 B-样条((_n79j2w))用 Cox-de Boor 递推式构造，作为某种单位分解；有针对特定 BC 处理方案¹；{n79j3a}
			* ¹相关：((n79j3d))PINN-BC处理
		* FEM 可学变种((pcij6c))
	* 基底类型（全局）：Fourier、正交多项式基底，靠 FFT 对应
		* 基底慎选：龙格现象，据散点（如全局均匀网格）离散值 求基底（如高阶多项式）系数时；{o1ol3w}
			* 相关：((n7jf0i))插值
			* 用非均匀网格似可缓解，如谱方法正交多项式零点（边界附近加密），Gauss quadrature；{o1ol4p}
				* 相关：((n8jb3v))谱方法
				* 例：Dedalus 谱方法软件包处理非周期边界时的 Chebyshev 基底
				* Dedalus 文档还提到该非均匀网格的额外潜在好处：处理((o1ol51))边界层
		* 非传统基底，如 ELM（仅最后一层可学的 NN），见 `假设空间参化方式汇总`“基底表达”
		* 线性 ROM((o1mj3j))
	* 基底类型（混合）：窗口 Fourier、正交多项式 所用基底
		* 局部多项式基底，如 `POUnet-2101.11256` 结合区域分解后视为 FEM 的推广
	* TT（tensor train）属于衍生辅助技巧
	* 有时可理解为在变换域的离散化，例如在 Fourier 域打网格
	* 离散化若指定标准插值格式，则成为基底表达，如 FFT；{n32f00}
	* 逼近误差度量：解集的((o1mj39))Kolmogorov n-width
	* 相关：同手段（线性基底）的对象（场）换为现成场表征，见((o1mj3j))线性ROM
	* 支撑—网格划分（而非针对给定网格讨论用它表征的不同函数）{o4dk76}
		* 相关：((n32f0k))多块规则网格拼成复杂区域，((n2pf83))多尺度与网格加密，((n7pf4y))AI 生成网格
		* 数值软件-网格生成工具，针对 OpenFOAM 的见((o4dk7z))
		* AI 生成网格见((ncmk3t))AI增强传统算法-表征成本-网格划分成本
* 非线性参化 $P\to F$，如 ((o58a0h))INR，RBF
	* 反向映射 $F\to P$：未必良定义（非线性参化常非单射），且求值费劲（解优化问题）
		* AE 情形有预设的该映射
	* RBF，场表征为多基底求和，但关于各元素中心位置非线性；{q8g80j}
		* 相关：手段（多元素联用）所属大类((n8qk4m))ensemble-目的-表征数学对象-场
			* 类似物((p9pk6y))3DGS，((oc9a3v))GB-INR 自适应格点变种
	* AE、AD；{n38n4h}
		* `DINo-2209.14855`“n2h91n”AE 比 AD 更容易欠拟合，且灵活性也稍差；{n38928}
			* 相关框架：((n38921))欠拟合
		* 相关框架：((n38n48))Transformer 的编解码器，((n31g86))隐向量
	* `假设空间参化方式汇总`
	* 逼近误差度量：解集的 manifold/Lipschitz width，参见((o1mj39))Kolmogorov n-width
	* 反向映射求解可结合元学习等加速，INR 与 PINN 都有相关做法
* 散点（网格）离散、参化的复合，所谓 grid-based INR，((_n2pe9t))InstantNGP；{n31e8h}
	* 形式解读：函数按复合分解 $f=f_2\circ f_1$，$f_1:\R^d\to\R^n$ 靠散点离散结果插值给出，$f_2$ 为参化 NN
		* 散点离散部分 $F_1=C(D,\R^n)\to P_1=C(\{x_i\},\R^n)$，后者为离散空间
		* 若 $f_1=id$ 则退化为纯参化表征（注意此时是线性函数，散点离散再插值后可完全恢复原来的函数）
		* 若 $f_1$ 中有 $d$ 个分量的输出取定为 id，则可从另一个角度理解，视为((n31e95))在纯参化表征中引入了区域分解的情形
		* $f_1$ 视为((n3t97n))高频位置编码，只是其中涉及的系数（网格点取值）可学
		* $f_1$ 视为特殊的((nbih2c))交叉注意力，输入 x 为 Q，格点 x 为 K，其上隐向量为 V；{p5kf7e}
			* 可比较基于常规交叉注意力 Transformer 的 INR-hypernet 结构，KV 为 INR 的 condition（超网络输入）
			* QK 维数、含义：一般注意力中为高维 embedding（先对原坐标编码升维），此处为原始低维坐标（有明确物理含义）
				* attn 初始设计时即针对语言这种本来就抽象的结构，原生针对高维，无网格等特殊数据结构
			* K 选取：一般可学or前序网络生成，此处为固定网格点（以低维为前提，否则难打网格）
			* V token 间关系：一般情形无结构，按独立 tokens 处理；此处 V 在格点上取值，可用 CNN 等有结构网络生成
			* 注意力稀疏性：一般需全局注意力，计算量随 KV token 量线性增长；此处仅激活邻域若干 K，计算量 O(1)
			* MLP 各层使用的 Q：
				* 普通 GB-INR：Q=x 仅在输入层使用一次
				* modulation GB-INR((p44f3l))：Q=x 在每层都用一次，各层用的相同
				* Transformer-INR：Q 每层都用，且每层用的不同，由网络上一层的输出决定
					* Q 可更新 前提为 Q 无明确物理含义（为 embedding）
	* 参数量分布：多数信息在 $f_1$ 中，$f_2$ 参数量可较小((_nb1m9m))
		* 可降训推成本：((_obnk05))主要训练推理成本在 MLP，网格参数多但不意味着计算成本增加
		* 离散部分 feature 可视化，((_p4re3l))同对象的不同通道；同时可视化相应小波系数；{p4rf1y}
			* 原文针对 3D 物体、网格部分用了三平面，仅对平面 feature 用小波；相关策略见((p4re9y))
			* 相关：((p4re9u))多分辨率网格的可视化，可自动看到类似小波的多尺度结构
	* 离散部分参数量压缩，尤其使用全局细网格时（此时 $\dim P_1\gg 1$）{oa3b7z}
		* 问题分析：点集存储量 = (总点量 × 点数压缩率) × (单点维度 × 维度压缩率)
			* 总格点量 = (长度 × 点密度)^空间维数
				* 类似划分还见于((o8h98r))NO-交互范围引入空间限制
			* 相关，作为压缩数据量的一般原则：整块数据先赋予集合结构，再用这一要素拆分来压缩；{oa3c1p}
				* ((n5bf17))图像划分 patch/slice manifold
				* 超网络输出降维((n8ch1z))，通过对主网络参数集合分组
				* NN 处理高维输入输出((n4ce5l))
		* 改用粗网格（针对点密度），可多组
		* 点压缩率：限制格点 feature 可能性总数，feature 跨格点共享机制
			* 可学 codebook((_oc2e4v))，集合映射关系动态学出；{p6ub5j}
				* 相关：((p6ub5m))VQ-用于压缩-codebook
			* hash 压缩（集合映射关系预先取定）：((_n2pf99))InstantNGP
				* 前提：目标场稀疏；目标场光滑时或许也可以？
				* 由于并列的多层网格引入了一定的冗余，这里可不处理 hash 碰撞问题
			* feature 信息量压缩，SHACIRA((_ocge97))用解码器压缩为整数值向量，且维数降低
		* 点压缩率：多点 feature 打包，信息统一压缩（解压后各点 feature 仍可不同）
			* 注意“压缩”含义，针对待表征对象而非现成数据；相关讨论见((p3va9b))空间分辨率压缩
			* ACORN((ocaf6y))：打包方式为对网格划 patch，再压缩方式类似 ((n5bf17))patch-manifold；{p59e68}
				* 整体相当于((o33a2v))进行了两次空间分辨率压缩：第一次将连续信号用 grid-based INR 压缩为 grid features，第二次用 CNN 解码器将各 grid patch 压缩为隐向量
		* 空间降维（针对空间维数），用若干 2D 网格表示 3D：((_oa2a9p))EG3D（triplane-NeRF）在 Oxy,Oyz,Ozx 平面分别建立网格，用三平面网格点的组合表示原 3D 空间中一点；{oc2a2n}
			* 元素语义：各平面点对应原空间的 ((n5bf17))坐标slice
			* 效果：((_oa3f1f))用于 NeRF 紧凑、表达力强，适用于复杂形状纹理的对象
			* 前提推测：待表示对象有某种稀疏性，允许“三视图”，如单物体 NeRF
				* 若减弱为局部三视图：3D 划分网格块，每小块内用这种三平面表示
				* 三视图不足，需补充额外信息：{p4l99n}
					* 需求场景：((_p4l99q))地图类场景（而非物体）表征，主要特征沿地面延伸，竖直方向信息（高度等）附着，纯竖直视图出现前后遮挡、无法恢复完整场景信息
					* 方式，额外信息类型：((p4ge7e))3D 网格带压缩，((p4l95u))原始空间坐标
			* 只用单平面（而非三平面）：((_p4la1f))NeRF 描述地图而非物体，主要信息沿地面方向
			* 用于 4D，投影到 6 个 2D 网格：((_oc2e6e))HexPlane，((_oc2e6d))K-Planes
			* 平面投影换为低秩分解，((_oc2e6y))TensoRF 三类 2D 网格均与额外 1D 网格张量积
				* 此外每类均用多组求和（非张量积情形求和无意义）
			* 降维表示用于下游任务，尤其 2D 网格比 3D 易处理情形
				* 3D 生成：((_oc2a3a))扩散模型生成三平面隐向量，((_p3va5f))DiT 生成 HexPlane；{oc2a7u}
					* 用 GAN：((_oa2b0i))EG3D 用 StyleGAN 生成三平面表示
						* GAN 判别器也可 2D，((p4re7g))渲染得图像后再输入网络
				* 3D 场隐编码为三平面 feature，三平面 INR 解优化问题：((_p53b5a))TripNet
				* 数据压缩：((_oc2e3u))3D 对象用 TensoRF 表示，2D 部分小波压缩，相较 3D 小波更好算；{p4re9y}
					* TriNeRFLet 用三平面表示，其小波系数可视化见((p4rf1y))
					* 多尺度引入方式，细网格上小波 vs 网格构造即按多分辨率 concat，((p4re9u))后者可自动学出类似小波的特征
				* 可能需((oc2a3v))对隐向量正则化
				* 三组 2D 网格合并处理，多种方式
					* 多平面独立处理；可分别打 patch 后用 ViT，CNN 不再适用；{p8jh4p}
						* 如((_p4pa3q))LRM，ViT 输入位置编码用可学的（而非根据 xyz 手动指定）
						* 相关：多对象分别 tokenize 还包括((p8jh4r))不同物理量独立
					* 按通道拼为统一 2D 网格：若各维网格量相同、均为 N，各平面可作为 NxN img 的不同通道组
						* 如((_p4rf1d))EG3D，((_p4pa3g))Title，((_p53b4v))TripNet 输入输出三平面均用
					* 按空间拼为统一 2D 大网格：适用于各维网格量不同情形；会引入多余部分；{p3v979}
						* 如((_p3sh9d))多余部分置零（尽管其原空间使用 grid 而非无网格表征）
						* 好处：拼接边缘为共享维度，保留各平面结构化信息
						* 拼得大 grid 做法一般讨论见((p3v96m))场数值表征-多块规则网格拼成复杂区域-拼接
				* 2D 网格处理所用架构
					* ViT，可逐点作为 token；不打 patch 是由于不同于纯网格表示：1. 分辨率相对低（因有后续参化表征），2. 通道已经较多
						* 也可用很小的 patch，如((_p4pa5s))LRM 分辨率 2x2、通道 80 → 1024
					* CNN，包括 U-Net（用于((_p53b54))TripNet）
				* 2D 网格处理引入额外信息；{p4pb42}
					* 在网络旁路而非输入((p4pb1p))，ViT 交叉注意力、一般网络（ViT/CNN/FNO）modulation 等
						* 原因为 额外信息形式不同于 2D 网格信息，如 2D 网格合并处理方式特殊、不同于原模态等
						* 如((_p4pb72))单图像转 NeRF 任务
			* 多平面 feature 组合方式，不只有 concat：{p4re9i}
				* 求和：((_p4re8t))EG3D；
				* 逐元素乘积：((_p4rf0j))DynamicCity（尽管不是 INR）
				* （可能性）concat 基础上再 concat 逐元素乘积
				* ((_p5bh36))concat 效果好于求和
				* 相关：((n2pf3f))不同分辨率网格 feature 组合方式
			* 相关，((p3vc15))三平面用于客观数据降维表征（不同于这里改架构后就不再存在完整 3D 对象）
		* 单点维度，靠多尺度、结合粗网格
			* 信号有全局、局部成分，全用细网格算全按局部表征，导致信息冗余，需较大单点维度
			* 若引入粗网格 负责全局成分表征，细网格部分表征压力降低、所需单点维度降低；而粗网格的额外参数量相对不多
		* 压缩需求来自超系统情形：宏观区域分解，每块用 grid-based INR，逐块存 grid feature 量太大；{ocae93}
			* 各区域格点 feature 由超网络输出，超网络输入为宏观区域的坐标等信息，见((_ocae80))ACORN；{ocaf6y}
		* 整合方法
			* ((_p4f99q))3D grid（hash 压缩）、2D 高分辨 grid concat，针对无界场景表征；{p4ge7e}
				* 动机：后者为降存储量，前者为((p4l99n))场景特点导致 2D grid 有信息缺失、需补信息
				* 3D grid 信息总量控制
					* 必要性：完整的高分辨率 3D grid 参数量过大
					* 可行性：仅需补充 2D grid 缺失的那部分信息，信息量
					* 方式：降低分辨率 or 高分辨率结合 hash 压缩；原文实验结果为后者更好
				* 3D grid 也可直接换为原始坐标位置编码((p4l95u))
		* 想法：长度一般给定，变不了；不过既然空间维数能变，总长度能否用特殊方式变了？
		* 类似场景((n4dl73))NO-高维高分辨率场压缩（区别在于其网格来自原生物理场离散，而此处网格为人造网络架构的一部分）
	* 离散部分插值方式：$f_1$ 所用插值方式取定，从而这部分有双向映射；{p4dc1z}
		* 可用最简单的线性插值，而由于后续参化部分的灵活性，表达的场也不限于分片线性
		* 原文是双线性插值((_nb1m8t))，并提到用更高阶（更光滑）的插值格式反而降低精度
		* HiNOTE 将 $2^d$ 个小方形的相对面积作为插值系数((_o8fe5a))；（> 应该和 d-线性插值 结果一致）
		* 可学版本，((oc9a3v))基于 网格插值过程的 RBF 解读，插值方式相当于 RBF 核函数，允许自适应学出
		* 可学版本、NN 参化，((_oc9a4d))用 MFN 变种，首层输入 x、中间层输入格点隐向量值
		* 基于 GKN 的可学插值，((_p6af1z))信息来源为 r 半径内所有格点（而非仅所在格子的顶点），系数依赖于相对位置¹、依赖方式可学
			* ¹可能性：系数同时依赖于 grid feature？表达能力是否有明显变化，且能补偿网络复杂度提升？
		* 多格点 feature concat 而非插值((p4dc21))
		* 相关框架：((q8r98i))元素分类处理-解包操作
	* 多尺度表征：主要靠网格离散部分体现((_n2pe8m))；{n2pf3f}
		* 使用从疏到密的多套网格；不同尺度信息整合，一般直接 concat
			* 若离散、参化部分不是复合而是复杂方式混合，((o6fn5a))不同尺度信息可在不同位置引入
			* 相关：((p4re9i))三平面网格特征组合方式
		* 局部自适应加密
		* 各尺度 feature 维数差异，细尺度网格减少通道数？
		* 相关：((n2pe88))参化下的多尺度表征，((n2pf83))散点离散的多尺度表征（局部或全局加密）
		* ((_nb1n3v))各层网格 feature 可进行可视化，多尺度结构与小波系数有相似性；{p4re9u}
			* 相关：((p4rf1y))单层网格 ansatz 情形的可视化
			* 相关：((p4re9y))对单套细网格显式地算小波分解，可用于数据压缩等
	* 离散部分网格变种，不用普通方形网格
		* ((_oc9a1z))用单纯形（三角），以减小单点前传涉及的网格点个数，高维效果更明显
		* ((_oc9a2s))自适应格点位置，参与优化更新；插值方式可能没做稀疏化
			* Point-NeRF-2201.08845 同样自适应
			* ((_p4dc1q))ARC 自适应格点视为表征原对象的点云，可用作后续分类网络输入
		* ((_oc9a3n))自适应格点解读，可类比 ((q8g80j))RBF，据此设计格点位置、插值方式均可学的网络；{oc9a3v}
			* 相关：手段（多可变格点联用）所属大类((n8qk4m))ensemble-目的-表征数学对象-场
		* ((_oc9a3c))自适应、固定格点 二者加权组合
	* 变种，离散、参化不直接复合，而采用更复杂的信息混合；{p44f3l}
		* 离散插值结果 作为参化部分（MLP）modulation 而非输入，每层均整合一次散点离散结果
			* 又见((n3vf61))INR-坐标反复输入，((p48e5p))INR-区域分解-仅独立隐向量-按modulation输入，((p44d8l))INR-区域分解-switcher位置-modulation
			* CAM((_p42a2z))x 本身也直接作为 MLP 输入
			* NFFB((_o6fn53))浅层 modulation 用粗网格离散、深层用细网格；{o6fn5a}
				* 又见((o6g03a))INR-多尺度-不同层输入不同scale
				* 相关，浅层低频深层高频还用于((p5ja23))MLP 参数层面的区域分解
		* 离散插值结果 给出 MLP 参数而非输入((ocge5x))
		* 离散插值结果、原始坐标（可带位置编码）concat 作为 MLP 输入；{p4l95u}
			* 属于((n3t97n))INR-坐标高频编码-混合方案（Fourier + grid）
			* 如((_p4l95n))用于 NeRF；其中针对输入 x,d 输出 σ,c 设定将 MLP 分前后部分
			* 动机，引入原始坐标输入的好处
				* 纯网格难表示 voxel 内小尺度信息（高频细节）((_p4le2p))，又不值得全局加密网格
				* （若离散部分用了 triplane）((p4l99n))场景特点导致 2D grid 有信息缺失、需补信息
				* （若用于 NeRF）纯网格局部过拟合，出现噪声伪影；((_p4le3u))坐标输入约束空间连续性，并利用不同位置语义相似性降低过拟合；{p4le5m}
					* 仅对 grid feature 引入正则化 loss 也可缓解，loss 形式可为((_p4le5e))grid concat 坐标输入另一 MLP 拟合相同目标
					* 知识蒸馏也可，((_p56h3v))先训普通 NeRF（全局 INR），再用带网格区域分解的 INR 拟合其输出；{p56h48}
						* 副本：((p56h4e))不同算法、架构的数据利用率不同
						* 相关：((n7pe4y))teacher-student，((n31m9t))用辅助网络抑制过拟合
					* 相关：((p4ga25))图像渲染-ansatz
			* 训练时约束二类输入功能、强化角色分工，方式
				* 对 grid feature 预训练，((_p4le63))用无坐标输入的另一 MLP 拟合场景
				* 对 grid feature 额外正则化 loss，((_p4le93))还是直接输入另一 MLP 独立拟合场景
	* 引入外部条件方式，((n3vg75))modulation 等；{oa2b1i}
		* 对 $f_2$ 引入((p4pb1p))旁路输入，((_o6sl6f))CViT 用 Transformer 交叉注意力整合 NO 输入信息
		* 生成 $f_1$ 网格表示：3D 网格直接处理架构不成熟，可((oc2a7u))依据外部条件生成三平面 feature
	* 相关理论，((_o6g04s))训练动态的 GTK（grid tangent kernel）分析，仿照 ((n3ff9q))NTK；{o6g04z}
	* 用于 NeRF 时可能局部过拟合，处理方式见((p4le5m))
	* 相关：这里中间层的各网格点均有隐向量，若视其整体为大隐向量，则算((n4ta57))结构化隐向量
* 注：术语使用差异，有限维表征、离散、基底 在 DeepONet 中分别叫 离散、evaluation、projection
* 无界区域场 的数值表征方案；{p4fa30}
	* 空间截断：关注范围 ((q8gb5i))缩减到有界子区域内，忽略子区域外的物理场
		* 对 ((p4ga25))NeRF渲染，截断丢失的远景需用其他方式补回，远处视线处理方式；{p4ga32}
			* 场景设定：相机位置有界、视线范围无界
			* 区分近景、远景、无穷远天空((_p4ga1c))，用不同 ansatz，天空建模只输入视线方向、输出颜色
			* 远处包围面 ansatz 保存远处信息，对特定视线考察与这些面的交点位置信息
				* 包围面形状
					* 球面（NeRF++ 用的是这个么？）；((_p4nf8i))SRF 在球面上用经纬网格
					* 方框，与坐标轴平行，((_p4ga3q))StreetSurf 将近景范围外框按比例膨胀得到
					* 多方向平面组合，((_p4ga45))MMPI 单视线可与多个不同朝向的平面相交，此时靠插值连续过渡
				* 多重包围面，视线与一系列面均有交点，积分时整合所有交点信息
					* ((_p4fe6x))MPI 多平面似乎等距
					* 远处面间距离更远，((_p4ga4u))使 1/r 线性减少
					* ((_p4nf8m))近处 r 线性增长（等距远离），远处 1/r 线性减少
		* 对物理场，难点在动力学演化描述，涉及((p8jg4b))截断域数值边界条件
		* 相关：((nahe5m))边界条件，((n7n92y))物理场延拓（作为截断的反面）
	* 基于空间变换：使用((n7ne5l))参考域，先变换到有界区域，只需表征有界区域的场
		* ((_p4fa0p))常用于 NeRF
		* （PDE 情形）变换构造方式 可结合解的预期收敛阶（原点、无穷远），((_p9ua13))用于 PINN
	* 原生无界表征
		* 基于 INR，参考((n79j3d))PINN-BC-无穷区域，loss 用 VMC
		* 基于基底：仿谱方法 ansatz，选空间基底 $\phi_i(x)$，含时方程用 NN 表达各系数，坐标采样自无界分布，如 `s-PINN-2202.02710`
	* 相关：((p9ua7b))INR-区域形状复杂
* 混合表征策略
	* 函数各（自变量）分量使用不同策略，例如时间用离散、空间用基底表达；{n8jb0f}
		* 时间参化、空间离散：((n8ja9w))CTFNO
		* 时间离散、空间参化：ESNN？
			* ((oa4m7t))时间更新涉及空间表征方式切换，空间先转换为离散表征、对该表征时间更新、再转换回参化表征
			* 特殊的时间离散：((_oa4m3o))PINN 原论文离散时间版本，整个时间范围用一步高阶 IRK，能获得的中间时间步位置由 IRK 决定
				* 训练 loss：IRK 所得方程组的残差
	* 宏观微观用不同策略表征：{n76e2e}
		* INR 区域分解-手动划分区域-网格划分((n2pk9m)) 可理解为区域内（微观）参化、区域间（宏观）离散
			* 区域间信息交换可利用其网格结构，类似 AFNO（尽管它区域内是离散而非参化）
		* 宏观无网格、微观有网格，区域内离散、区域间参化，((_oam95z))patch decoder 输入 patch 位置、输出整个 patch 像素取值；{oam965}
			* 副本：((oam96b))patch
			* 多尺度 patch 机制，((_ocke7q))多个 INR 分别生成不同尺度 patch，结果求和得最终输出场
		* ((_n76e1u))视频中时空区域分解，区域内（微观）离散表征，区域间（宏观）空间用参化、时间用离散表征
	* 多 ansatz 直接求和，提供不同先验
	* 二次表征提取：基础表达之上再降维（((n8jb8q))ROM），如 POD（线性基底组装）、AE（非线性参化）
	* 可能性：均匀网格+INR 同时引入、共同参与学习（二者提供的先验可互补？）
	* 可能性：用 AE 将离散、参化表征对齐（INR 作为解码器）
* 各表征策略特点
	* 作为网络输入：INR 不能直接输入，一般需先离散（均匀网格/散点）
	* 作为网络输出：均可
	* 引入的先验不同：有网格包括 FNO、DIP 等，无网格的有什么先验？
	* 作为输出时，INR 可泛化到任意形状
	* （待定）散点格式的输入：[x,u], INR 格式的输出：$x\mapsto u$
		* 作为输入时，不同 x 位置的 u 需要考虑其相互作用；作为输出时，无需考虑
* 离散化误差；{p8tl0t}
	* 表示信息损失，如湍流缺失小尺度信息
		* 引入先验，通过 NO 架构设计，((_p8ff8j))PhysiX 引入细化模块
		* 靠历史信息补全，((p8tk9n))自回归 NO 补充历史记忆项
		* 小尺度信息专门编码，((_p9hb7x))引入微尺度编码器 编码粗分辨率信息的残差
	* 数值计算误差，如导数 FD 近似截断误差
		* 谱方法导数计算精度高于 FD 等，收敛阶更高
		* 针对传统数值格式，((ncbn67))靠 NN 恢复出在粗网格被忽略的解性态
	* 局部自适应加密；{q1ij5t}
		* 抽象化功能描述：空间中分布的信息，使用离散元素集合近似表示，单元素可表示信息容量大致固定
			* 元素含义：网格，mesh，散点粒子
			* 单元素内信息表示方式：抽象隐向量（tokenize），INR，体素 01 值，顶点函数值/积分平均/多项式基底系数
			* 相关：((n8jb6c))coordLoss-调采样点密度，要点不在表示信息
		* 使用场景：物理场离散表征；((p3va9b))ViT tokenize；INR 区域分解；体素网格
		* 加密条件
			* 原信息复杂度：区块内物理场方差((_q1ij4n))，用于 ViT 自适应 patch 加密
			* 解 PDE 情形后验误差估计
			* 据场景手工设计，如((_q4pg04))NS 方程 4 物理量联合判断
			* 网络预测是否加密；((n7pf4y))用于 mesh 划分，通过移动元素位置or预测局部所需密度
			* 含时场景兼顾未来演化：((_q4pf9m))含时 PDE 做单步外推，与当前活跃区域取并集进行加密
		* 加细后老元素是否保留：粗细共存or细替换粗；{n2pg2o}
		* 典型用例
			* CV 里传统 SDF 的“八岔树”
			* FDTD 误差大的位置（覆盖替换式）加密网格，并在计算时减小这部分的时间步长
			* ((n2pf3y))INR 局部非覆盖式加密，有点像小波
		* 相关：((nckf50))多尺度
* 区域，作为物理场的定义域；{q8fa35}
	* 形状刻画((n3gd80))，可用作网络输入
	* 转换—空间坐标变换、得参考域，场的自变量不用其原始值；{q8169o}
		* 输入坐标范围标准化，((q81b66))用于 INR、FEM 等
		* 区域形状复杂（形状固定），将其规则化（以简化数值表征方式）：((n7ne5l))变换到标准参考域
		* 区域形状可变（拓扑固定）((q81a31))
		* pull-back 给出函数空间变换，要求区域固定
			* 刻画函数空间对称结构((q81971))
			* 用于构造 NO 层((q4rg7w))
			* 原动力学复杂，简化描述：((q4rb00))连续介质物理-Lagrange描述（其坐标变换常据物理意义直接设定，非学出）
			* 原解流形复杂：((_q81b46))人工设计区域变换 以加速 Kolmogorov n-width 衰减
		* 共轭作用于现成函数变换（如 NO 层）导出新的函数变换：在坐标变换后新域上执行现成函数变换，结果逆坐标变换回到原域；新域与原域可同可不同
			* eg. ((q81b7f))针对 1D 函数（来自 2D 逐轴处理），现成函数变换为全注意力，修改位置编码所用坐标值 相当于在变换后新域上执行普通全注意力；{q81c0f}
	* 转换—延拓得((n7n92y))master domain
	* 转换—((q8gb5i))限制到子区域
	* 资源—子区域结构；{q68m4l}
		* 具体表现，取决于区域离散方案
			* 未离散（理论对象）：((n5bf17))patch manifold
			* 均匀网格离散：patch
			* 散点离散：((n98h1e))点云局部聚合
			* 隐空间多元素（未必严格对应原始物理区域）：((o33a2v))在隐空间打 patch
		* 属性—子区域位置，可选子区域全集，离散（位置全集预设）、连续；{q8fb7i}
			* 分阶段，连续训离散推：((_p59b7r))COIN++ step1 训练 patch manifold 的参化映射时 patch 连续移动，step2 对各 patch 找其对应参数时 patch 离散移动
			* 连续取值 场景：CNN((n37m6h)) 各深层神经元的感受野对应的原图区域
				* 严谨说法：CNN 视为某连续空间 NO（连续卷积、池化）的网格离散，其离散前版本对应连续移动
			* 连续取值 解读：组成((n5bf17))patch manifold
				* patch 全量编码（表征提取），方式包括((n8hj6h))CNN卷积
					* 退化为 区域分解（patch 切分）情形，可通过在卷积中设定 stride 达到
			* 离散取值 各位置边界：默认无重叠，有时有重叠
				* 考虑场景包括：((q8fb7o))区域分解-多区域输出协同
		* 属性—子区域大小，包括多尺度结构
			* patch size 可变，见((oapa1r))空间分辨率压缩-可变压缩率
		* 取单个子区域；{q8gb5i}
			* 用途—作输入
				* 用途—代表完整对象，原始场（包括图片）只使用一小块
					* 用途—降本，((n4sj7z))GAN判别器降计算量
			* 用途—作输出
				* 用途—扩充单点信息：INR 的微观有网格变种，((oam965))输出为 patch 而非单点值；{oam96b}
			* 用途—同时作输入输出，在子区域上构建算子
				* 支撑—输入输出子域可不一致
					* 输入放大：输入范围适当 pad 以大于输出，如((_q7jh9b))
				* 用途—问题限制到重要子域，忽略大多数次要位置
					* （改写已有算子）如((p8jg4b))PDE区域限制，物理域大计算域小，包括无穷区域到有穷
					* （构建新算子）如((q7jh93))boosting-仅修关键局部残差
			* 相关：反向操作，限制到子区域的反面是((n7n92y))延拓到 master domain
		* 区域分解，多块小区域拼成原始区域，位置全集预设；{q8fb7o}
			* 相关，该策略的对象（空间子域）一般化为元素子集，见((p2qf2a))集合元素划分后处理
			* 用途—问题描述
				* 区域简化：复杂区域划分为简单形状组合，每块内可用（变形）均匀网格；{n32d90}
					* 注：强行划分特别多则成为 FEM 网格剖分，每块单个三角形、四边形网格；这里是块数少，每块内部有均匀网格
					* 例子：2D 非连通机翼（前缘、尾缘为独立小块），3D 飞机形状；另外 2D 普通机翼也有 CH 网格（C 型网格+尾端小矩形网格块组合）`2022-07-29`(CSImeet2)
					* 退化情形：环形区域沿线剪开为矩形区域，或机翼 C 型网格（小区别：后者剪开线与内环接上，为矩形同一边，前者则为矩形邻边）
					* 下游使用：((n32f0k))作为场的数值表征方式（特殊的离散）{n7ne10}
						* 再下游：((n7n994))NO架构设计-多块规则网格拼成
							* 相关：同挑战（复杂定义域）的其他处理方案见((n7na1l))
				* 机理划分：多物理情形划分不同物理主导的区域((q4rd9n))
				* 系数场划分：系数场有大突变，可划为两块区域分别处理 `2022-10-05`(AISCmeet2)
			* 用途—场对象
				* ansatz 增强：把空间不同区域当成不同 task 以降低函数表达难度
					* 包括空间异质性（有已知位置边界层等）
					* 如 ((n2pj5z))INR-区域分解-目的，((p2qf2a))NO-自适应区域分解
				* 物理场（包括图像）tokenize，组成 patch 集合，场编码为((oaqb09))有结构隐向量-特征图
					* 见((p3va9b))NO-输入-空间分辨率压缩，次要的((n4rm8d))tokenize，包括((n37m6i))ViT
						* patch size 可变，见((oapa1r))空间分辨率压缩-压缩率可变
				* PDE 区域分解((n32d8v))
			* 用途—计算过程
				* 算子构造：解算子 ansatz 取为 各子域解算子匹配((q8g71l))，OneShotNO
				* 格式划分：各块用不同算法格式（如 NN、传统算法协同）；以下区分依据适用性差异来源；{q8ef29}
					* 反问题有无观测：((q8ef3p))有观测区域 PINN、无观测区域传统谱元法
					* 几何复杂度→ 网格密度：((q8ef30))关键区域基底由 NO 生成，粗网格区域用传统 FEM 基底
					* 相关框架((q8f79a))AI协同传统算法-分区域
				* 先验引入：体现（较弱的）空间平移不变性
					* 可用于 ((n2pe8f))coordMLP-区域分解
					* ViT patch-size 减小后泛化表现好，尤其 PDE NO 类任务
				* 算力分配：各块分配给不同硬件，并行加速计算，包括((n32d8v))传统方法、((n7nd86))PINN
			* 支撑—多区域输出协同；有时需考虑((q8fb7i))各子域是否有重叠
				* 靠 loss—((n2pj84))INR-区域分解-硬分解
				* 靠 loss—ViT feature-map unpatchify，((_q2kf0k))训练时额外引入梯度连续性 loss
				* 靠 ansatz：((n2pj84))INR-区域分解-软分解，((p8ag76))集合元素划分后处理-划分边界-软
				* 靠((q8gg4x))找算法不动点，包括迭代修复：((n32d8v))PDE-传统格式-区域分解
* NN 表达函数空间中分布/刻画随机函数 的方式汇总；{n32f2t}
	* 相关：((n8ql5a))概率分布及表征（一般）
	* 刻画函数空间分布的用途：((o1ok77))UQ（包括 active-learning），元学习，生成模型，及其他；{o6mm5j}
		* 注意前两个的分布来自先验，表达的是信息不足时的情况；生成模型的分布来自数据拟合
			* 元学习的先验通过元训练给出，UQ 先验人为设置（NN 特定参数初始化方式表达的先验也算）
			* 另有 UQ 用于物理预测，表达客观随机性，此时不再是主观随机性
		* UQ 和元学习需要能拟合特定样本 $f$（表达数据给出的后验分布），生成模型只需拟合样本的分布，不涉及后验
			* 从而元学习 loss 涉及单样本表达误差，生成模型 loss 用判别器给出，只考虑分布的距离
		* 注意许多 UQ 只需度量单点预测值的不确定性，未必是作为一个函数的整体随机性！
		* 分布刻画的额外用途：结合其他先验标准提高解的质量，如((o6mm5b))从 PDE 预测解分布中找出符合物理约束（守恒律等）的解
		* 以下默认只处理 UQ，除非特别注明（后期各方法变多后可改成各用途分别汇总的方式）
	* GP 高斯过程；元学习的 NP（Neural Process），CNP 等算是变种，UQ、元学习均可
	* BNN（Bayes NN）`[BNN综述文章介绍]`
	* 网络前传引入随机性-推理阶段引入((p97e7d))，其中做法通常只要求有随机性，不要求能表示特定分布
		* MC-Dropout（在 `DEBOSH-2109.13337` 引用），似可看作每个权重为独立两点分布的 BNN；{n37g7p}
			* 相关框架：((n37g82))dropout
		* NO 网络末层引入扩散((q2oa0o)) 以刻画 UQ
	* Deep Ensembles 训多独立模型，计算代价稍高，见((n8qk3w))bagging
	* DeepSDF 原版 AD（主要用于元学习）
		* 刻画 $z,s(x)$ 联合分布，给定待拟合函数后 MAP 找特定的 $z$（非元学习任务不需反向寻找 $z$，不必按照联合分布解读）
		* 这种框架下概率描述不是必须的，按集合（流形）解读也差不多
		* 如果 MAP 改成对 $z$ 后验采样则概率描述确实有意义
	* 场用隐向量表征((ncnc0b))，之后只需刻画（有限维）隐向量分布
		* Google WeatherNext 2 的((_pbuf81))函数生成模型，32 维隐向量调制模型的所有层
	* 表达的随机场可用于((n32b3o))生成模型，生成用 INR 表达的数据；{n3vg4l}
		* 如 `funcGAN-2102.04776`，((_n3vg4b))
		* 相关：((n5bk3f))在隐空间训生成模型
	* 可能性：修改后的 VAE 模型，随机性通过隐向量 $z$ 引入，编码器用 `NO%` 建立
	* 与之相关，但简单许多的一种情形：只要求函数（映射）的输出由确定性改为随机，考虑的随机性在输出空间 $Y$ 而非函数空间 $(X\to Y)$；{n3bg65}
		* 二情形区别：随机映射可考虑不同 X 下 Y 的联合分布（即 (Y(X₁),Y(X₂)) 分布），及 (∇_X)Y 分布；这里的单点输出随机性不考虑 X 的变化
		* 别名：((_n3c91l))将此称为“概率 kernel”
		* 方法—输出分布参数：输出分布属于特定分布族，其参数由网络额外输出，用传统确定性输出网络即可
			* 完整 categorical 分布：构建确定性映射 $X\to P(Y)$，后者为有限维、连续的空间；{n3bg9o}
				* 适用条件：通常要求 $Y$ 为有限集、规模较小
				* 例如图像分类
				* NLP 中的文本生成((n3bg95))，每次选用哪个词是这类随机生成任务
				* 网络架构—输出层需((q6o732))归一化权重，主要是 softmax
				* 可使用 cross-entropy loss；{n3bh0m}
					* 训练当心：输出权重高度集中于某元素时，反传计算可能出现((q6kh08))softmax collapse，源于 fp32 尾数不足
					* 相关：((n3bh0y))loss 形式
				* 相关：((o42l4r))离散分布vs连续分布
			* 正态分布方差：连续回归问题，结果取（一元）高斯分布，只需额外预测方差；{padg5u}
				* 可用于((padg5p))loss-加权-弱化难数据
		* 方法—生成模型((n32b3o))：复杂 $Y$ 适用；注意((n5bh10))随机映射可等价改写为条件生成模型
		* 方法—提供随机映射（即上方的一般框架），只是不考虑不同 X 下联合分布
		* 用途—构建 ensemble((n8qk4m))
		* 相关：((n3c975))随机性的作用，((n3gd6q))映射形式（不限于 NN 前传）
		* 与确定性输出函数的比较：{n3c93q}
			* ((n3c93p))有人将这作为 ChatGPT 类工具与传统搜索引擎等工具的重要区别
			* ((_n3c92h))可能有随机偏差、不准确结果
			* ((_n3c920))有时能帮助处理 嘈杂、或格式不好的输入；{n3c97c}
			* ((n37n39))通过加随机性提高 NN 稳定性，主要是在网络架构（前传）中就加入了随机性；不过可能需区分训练的前传、测试的前传
			* 避免模糊：确定性输出可能导致模糊结果，随机输出可以生成多种清晰的结果((_o5af87))
				* 扩散生成 NO 精度高于确定性输出版本((_p9da3m))
			* 可结合其他先验标准提高解质量，如((o6mm5b))从 PDE 预测解分布中找符合物理约束（守恒律等）的解
				* 一般框架((p97e9u))ensemble-用于束搜索
			* 非科研相关：((n3c93j))随机性与确定性
* 除表征方式外，与“场”相关的更多话题：{n5d99m}
	* ((n4ta57))结构化隐向量，((n3pj3j))场数据的 loss 设置，((n5da0x))NO-场的多尺度结构，((n5da1a))分辨率，((nafg7e))场数据（有量级差异）归一化，((n2o879))求空间导数
* 数学对象的可计算表征，表征方式多样性，不限于场；{n8re4f}
	* 可包括：((n8re3h))曲面与形状，((n8ql5a))概率分布
	* 相关：((n3gd6q))映射的可计算 ansatz，未必表达为 NN 直接前传
	* 相关：((nas97x))非科研-逻辑对象的多种等价表达形式
