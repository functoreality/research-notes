> 2026-05-01 从 NN.md 独立
* 生成模型；{n32b3o}
* 相关话题：((o2dg1a))BIP-与生成模型比较，((n32e95))梯度计算 对随机采样求导，((n3bg65))随机输出的网络
	* `DeepM&Mnet-2009.12935` 反问题中方程给出的约束用类似 cycleGAN 的方式刻画，域迁移器换为 NO
* 待生成对象
	* 图像、形状等可用 INR 表示，此时可考虑((n3vg4l))随机场-用于生成模型
	* 除了连续对象生成，还可考虑 离散序列生成，如 ((n38n48))Transformer 解码器；{n6te4o}
		* 图片作为离散序列、自回归生成((n6te49))，使每 patch 对应离散取值的隐向量，从而生成问题转化为下一个 patch 隐向量预测问题，如 VQ-VAE、VQ-GAN
		* 自回归vs扩散((o42g70))
	* 在隐空间学生成模型；{n5bk3f}
		* `LDM-2112.10752` AE 后用 diffusion model 学隐向量的分布（`2022-11-18`(CSImeet3) 推荐）{n5bk3h}
			* 其中用到了((n5bk3r))有结构隐向量
		* ((n61965))CoDi 的图文视音生成均用 LDM
		* ((oc2a7u))基于三平面 NeRF，扩散模型生成三平面表示
		* 用于保证原空间始终良态，如((q4ni2m))隐空间扩散生成、原空间始终可算 PDE loss
		* 可能性：场用隐向量表征((ncnb9m)) 后，通过生成隐向量来生成 ((n32f2t))随机场
		* 隐向量若有结构，生成效果可能更好；见((n4ta57))有结构隐向量
		* 相关：((n31g86))隐向量-含义
* 基本生成模型，先区分生成（采样、推理）过程，再区分训练依据（保证生成分布匹配数据分布）
* 单步生成 ϵ ↦ y 网络
	* 训练依据—数据最大似然
		* VAE：似然 ELBO 放缩，涉及引入编码器
			* 注：若最大似然问题换用平均场变分 EM 方式求解，则不再引入编码器；见 SRS((q67m72))
			* `2022-11-18`(CSImeet3) 导师认为理论好但效果不行；有时更适合做 AE 而非生成
			* 只用 MSE loss 导致生成结果模糊化，可额外结合 GAN loss 等
			* 相关：`VAD-1903.00840` 也是用 ELBO 的生成模型，考虑每条数据都有若干维度不可观测
	* 训练依据—极小化分布差异；需引入 loss 以衡量该差异，GAN 等
		* 注：此处框架按手段链分析的形式化解读版见(('q68a7n))
		* 使用场景：分布衡量能力 用途（不限于生成），作额外 loss 提升待训网络输出质量
			* 质量标准为((n4sj30))图像视觉真实度，特例为((nckf3f))边缘清晰化
				* 待训网络如((_n4sj88))VAE，((_q64m8c))NO
			* 待训网络表达特定映射，((n2bf31))（除有监督 loss 外）质量标准为 其输出与输入对应程度
		* 衡量标准：判别函数，预期性质为 判断样本为 生成or真数据，即 GAN 判别器；{n4sj8e}
			* 质料实现（输出计算过程）：常用 CNN 等网络
				* 相关框架((p98k7x))辅助网络给出 loss
			* 动力实现（判别能力来源）：按普通判别器训练
				* 相关：生成、判别网络联训，属于((n2bg0w))对抗训练（不限于生成模型）
				* loss 使用，((_n4sn61))VG-GAN 的对抗 loss 用的是 hinge loss
			* 额外性质—WGAN 限制关于输入的梯度，以增强对抗训练稳定性；{p2se3g}
				* NO 中也可用 1-Lip 对抗网络来((p2se3f))近似估计最优传输 Wasserstein 距离
				* `[DL可解释性综述2001.02522]` 提到 WGAN 与最优传输联系表明生成器、判别器可闭式解相互表示、无需竞争训练
			* 性质变种—RaGAN((_q64m81))评估相对（而非绝对）逼真度，从而生成质量提高后，判别器所给的梯度仍有高信息量
			* 输入形态—图像，均匀网格物理场情形
				* 继承其((q68m4l))patch 结构，可((n4sj7z))只取小 patch 用于判别，降低计算存储需求
			* 使用案例如 PINN 变种 `1812.03511`（初边值等有噪声需要 UQ 的情形）
		* GAN 算法特点，`2022-11-18`(CSImeet3) 导师认为 GAN 目前理论无保证，不过效果好、速度快、实现方便
		* GAN 训练技巧见 `zhP603615843`，如：规范化输入，改损失函数，训练生成器时翻转标签，采样 z 用高斯而非均匀分布，batch 训练时不同时出现真、假样本，……
	* 训练依据—强行随机匹配 ϵ, y
		* `GLO-1707.05776` 随机为每样本 $x_i$ 生成隐向量 $z_i$，只训解码器 $\theta$ 使之能对应，无编码器
			* 包括通过优化隐向量进行条件生成
	* 训练依据—拟合已有的 ϵ ↦ y；常通过蒸馏 多步生成-状态更新-ODE 类生成模型；{q51d9j}
		* 优势—引入额外 loss，如((q52b57))对生成结果施加额外约束，对原有的多步生成模型不方便施加，但对蒸馏后的模型容易施加
		* 可扩大学习范围，输入、输出 均可包括一般时间步
		* consistency models：输入一般化 xₜ ↦ y，训练 loss：1. t,s 输入网络后输出结果一致，2. 输入干净解（t=1）输出自身
		* MeanFlow：输入输出均一般化 xₜ ↦ xₛ（NN 输出其平均速度），训练 loss 类似 PINN
		* 额外学任意二时间步间的平均速度，用类似 PINN loss 的方式训
	* 训练依据—联合，Stable Diffusion 所用 VAE 除标准 MSE loss（来自数据最大似然），还有 perceptual、patch GAN loss（来自极小化分布差异）
	* 后训练依据—((_q54a5i))FD-loss 匹配数据集统计量
		* 基于的模型 可原生按单步训练，或多步扩散模型按单步推理来用
* 多步生成—状态更新：所有中间状态位于相同空间，生成过程需逐步调整在该空间中的位置，时间可连续
	* 初态类型：逐点噪声（需去噪），常数场（需去模糊），另一域的数据（OT/Schrödinger 桥连接）
		* 一般情形：中间状态来自((q8ag8a))输入数据-破坏，初态对应完全破坏、不包含任何能分辨出特定样本的信息的情形，具体分布根据所用破坏方式确定
		* 常数初态 随机性需由推理过程引入，故推理过程不能用确定性方式
	* 采样推理路径设计（及其学习过程）：确定性版本（ODE 及其离散化），带随机性（SDE 及其离散化）
		* 路径可行性要求：初末态分布正确，即 x₀ 符合数据分布，x₁ 符合人造分布
		* 可学路径构造—构造逐样本轨迹 xₜ|y 作为中介对象，用于传递可行性约束
			* 要素—构造逐样本轨迹 xₜ|y（正向过程），满足初末态分布约束
				* 约束满足容易，x₀ = y 为原始干净数据即可
				* 构造方式：扩散模型对每个数据样本 y 演化 SDE，流匹配对每对 (y,ε) 用确定性路径连接（路径形态包括 扩散、OT）
					* 一般方案：中间状态基于((q8ag8a))输入数据-破坏，包括 加噪、模糊、mask 等；{q8am5h}
			* 要素—构造采样轨迹路径 xₜ（反向过程），使其边缘分布 pₜ(xₜ) 匹配逐样本轨迹
			* 采样轨迹构造—score matching（正向 SDE 情形），反向采样用 SDE/ODE；{q8ah6i}
				* 目标性质分析：正向轨迹边缘分布满足 ∂ₜp = -∇·(fp) + ½Δp = -∇·((f - ½sₜ)p)
				* 目标性质（边缘分布匹配）的充分条件：ODE 速度场等于 f - ½sₜ，SDE 类似
				* 实现要素：训 NN 表征 score sₜ(x)
				* 目标性质分析：sₜ(x) = 𝔼sₜ(x|y)，而 sₜ(x|y) ∝ 噪声 ε（见 SRS((p3jg30))）
				* 可行手段：NN 训练以 ε 为 label 用 MSE loss（因 MSE 极小在期望处取得）
				* 反向 SDE 中 drift 去噪 noise 重加噪，后者属框架((q8ah60))多步生成-基于修复-每步重破坏
			* 采样轨迹构造—流匹配（正向 ODE 情形），反向采样用 ODE（SDE 的暂时没见过）
				* 目标的等价表述：造速度场 vₜ(x)，满足 ∂ₜp = -∇·(vp)
				* 目标性质分析：正向轨迹边缘分布满足 pₜ(x) = 𝔼_y pₜ(x|y)
					* ∂ₜp = 𝔼_y∂ₜpₜ(x|y) =𝔼_y-∇·(vₜ(x|y)pₜ(x|y)) = -∇·((𝔼_{y|xₜ=x}vₜ(x|y))pₜ(x))
				* 目标性质（边缘分布匹配）的充分条件：vₜ(x) = 𝔼vₜ(x|y)
				* 训练方式类似 score matching
			* 注：以上框架按手段链分析解读的版本见(('q53a94))；{q53a95}
			* 训练—t 采样用正态而非均匀分布((_p84e49))，强化中间值
			* 训练—引入额外表征对齐 loss 引导，((_oafe7g))DiT 中间层过 MLP 后与预训练图像编码器输出算 loss；{oafe7r}
				* 相关：((oafe86))数据表征-利用方式
		* 理想路径刻画—轨迹损失；可视为约束优化问题的解，极小化轨迹损失，约束为初末态分布
			* 轨迹损失度量，连续时间 OT 为平均动能，Schrödinger bridge 为与标准布朗运动的 KL 距离；见 SRS((q5199z))
		* 可行路径改进—ODE 轨迹降低弯曲幅度：rectified flow 蒸馏得线性轨迹
		* 理论解读—序列决策，((_q4og30))视为 MDP、可用 RL 方式微调
	* 采样推理执行（已学出预设路径、完成训练之后）((q8bc2u))
	* （以下列举见过的具体用例，基本都基于((q8ah60))样本破坏后多步重建）
	* 基于去噪，DDPM/neural SDE/Langevin 动力学/diffusion model/流匹配；{n37n5n}
		* 相关框架：((n37n5d))CV 大模型
		* 理论原理相关—DSM（denoising score matching）可用于数据似然估计((n4sl24))，与生成任务对偶
		* 去噪网络((n99a2u))
		* （旧）理论与推导
			* SDE 生成模型公式推导 `2022-05-11`(dbGrpMeet2)，反向的 SDE 通过后验分布给出
			* neural SDE 可参考 `[NeuralDE综述-2202.02435]`，包括 BP 方式
				* ((n35f03))invCtrlRL 我的不完善的框架
			* `2022-11-18`(CSImeet3) 导师认为 diffusion model 有理论保证（但目前 condition 生成的做法没有），速度目前比 GAN 慢很多，性能现在不错；易用性可能不如 GAN
			* `2022-11-30`(dbGrpMeet2) 比较完整的介绍，包括离散时间的 DDPM、连续时间 SDE、ODE（速度快质量低）、条件生成模型（利用额外训练的分类器网络）
			* `GenModSDE-2011.13456`
			* 理念，对简单（正态）分布逐步完善、获得复杂分布，((_n79l0x))认为与模拟退火算法有共通之处
			* Flow Matching ((_oa5g93))直接学习样本从初始噪声状态向目标数据分布转化的过程，相比扩散据说效果好成本低，Meta Movie Gen 用到
		* 图片生成、处理
			* 低质量图像转高质量：`2022-05-11`(dbGrpMeet2) DDPM 图像生成，先 VAE 生成低质量图像作为初值，前向 SDE 一步加噪声，再反向多步去噪得高质量生成结果
			* 作为 NO：`2021-06-02`(dbGrpMeet) 讨论了可能性：用 SDE flow 建立 CT 与重建图像 pair 之类的映射
				* 似乎 `2111.08005` 就干的这件事？
			* inpainting，方法不唯一；{q51c03}
				* 基于 mask：((_n4sn4o))仅在 mask 外区域加噪声，masked diffusion
				* 按一般反问题处理（与去模糊等一致）：((q4oh5c))终态 loss 惩罚 mask 内与已知值差异，用其梯度引导采样去噪过程
				* （非扩散生成）多尺度生成模型 ((q51c09))各尺度只采样 对待重建位置有影响 的表征分量
			* 可不直接生成图片，而是生成图片的隐向量，如((n5bk3h))Stable Diffusion
		* 非图片的其他领域的应用；{n61a0f}
			* 分子结构预测：`2023-02-10`(lectures)“n2ak4w”，原子位置随机初始化；我的理解是利用 NN 梯度信息而比传统模拟退火更高效；{n2ak58}
				* 副本：((n2ak5q))invDPs
				* `GeoDiff-2203.02923`（可能就是这篇文章）从分子图预测分子构型时用了 diffusion model
					* 引入额外要求：生成过程关于平移旋转等变，通过((_n2me7o))引入特定机制达成（看原文似乎就是普通的 GNN）；{n2me84}
						* 副本：((n2me8g))网络架构设计-等变性
				* ((_n79l98))DiG 进一步预测平衡态下分子结构分布（而非给出单一分子结构）
				* ((_o5af7x))AlphaFold3 预测复杂分子构型，“小噪声去噪重点在于理解局部立体化学，而大噪声去噪则关注系统的大尺度结构”
					* 网络分工，((_q6tg14))大主干一次性预测整体结构，扩散只负责采样细节；其功能不同于图像扩散
				* 相关：((n79m1b))一般意义上的 graph 任务
			* 点云的条件生成((n5be19))
			* INR 参数的生成((n5bg9t))，包括条件生成（文本转 NeRF）
				* 此时可考虑数据集生成方式，INR 参数如何获得：{n5bk0b}
					* 靠直接优化：((_n5bk15))HyperDiffusion （迁移学习加速）
					* 编码器生成：((n5bg3k))Shap-E
				* 初始噪声对应的 INR 无意义，((_n5bk1z))有展示去噪过程 INR 变化
			* ((_n6198e))文本 LDM，去噪网络为 1D 卷积
				* 相关：((_o42g6t))OpenAI Andrej Karpathy 认为生成模型 NLP 适合自回归、CV 适合扩散模型；{o42g70}
					* 离散vs连续：这种差异也许来自 离散生成模型 vs 连续生成模型 的区别？{oapf91}
						* 见((o42l4r))离散分布vs连续分布
						* 自回归更方便用离散分类模型 的观点还见于((_nc6n5g))
						* 图像 tokenizer 区分连续vs离散版本((o8ue5k))
							* ((n6sf8q))VQ-VAE、VQ-GAN 图像生成模型是先为图像 patch 建立离散词典，从而可用自回归模型生成各 patch
						* 离散数据难训练扩散模型、GAN 判别器，((_o4he7h))原因为离散值梯度反传困难
					* 串行vs并行：自回归为串行，扩散为并行；{o4ha7u}
						* 相关：((n7bg8b))并行（一般意义）
						* ((o4ha86))视觉生成 VAR 模型基于超分辨率，尺度内并行生成、尺度间串行生成
					* 自回归范式相关：这里视为生成方法、与扩散模型比较，还可((n8mn8t))视为预训练方法、与完形填空比较
					* 自回归灵活性，可再补充后训练，SFT、RLHF 等
						* ((_oaqf5y))Emu3 视觉生成用 DPO 对齐人类偏好
					* CV 自回归生成见((n6te49))；文本扩散生成上方已讨论
				* SEDD 文本扩散模型（直接在原空间扩散）((_o4he6y))，拟合 score function 的离散版本，采样类似 ((n8ql6d))MCMC；{o4he7a}
				* 文本考虑整句离散概率分布，建立扩散模型，(('p42k2d))D3PM
				* 微观扩散、宏观自回归，(('p42k2k))LLaDA 用到
			* 隐向量的生成：((n5bk3h))LDM
			* 多模态联合生成：((n61965))CoDi 为使生成的各模态（图文视音）匹配，各模态反扩散过程中引入信息互换（而非独立进行）
			* PDE 求解((o3cn5s))，扩散模型表达 PDE 解、参数、控制项的联合分布
				* 结合目标函数 Langevin 采样后可求解控制问题
				* 额外还用了另一扩散模型表达控制项分布，将生成结果推离该分布（以使生成结果质量高于数据）
		* 引入 condition 方式见((n3q94a))
	* 基于 ((q8ac0z))mask 重建，通过调 re-mask 比例构造中间态，GRN
	* 基于电动力学 Poisson flow，平面上据图像形状排布电荷，沿电场线推至无穷远半球后电荷均匀分布，据此给出分布间的对应 `PFGM-2209.11178`
		* PFGM++ 引入参数 D（对应物理空间维度），较小时模型鲁棒性强、较大时模型训练所需数据少，((_na5j5q))D 趋于无穷后成为扩散模型
	* 正则化流（normalizing flow；似乎不同于普通连续时间生成，框架划分待定）{n4sk8g}
		* `2020-07-21`(AISCmeet) 似乎讨论了一篇正则化流的综述
			* [我之前自找的介绍](https://zhuanlan.zhihu.com/p/59615785)
		* 连续时间的版本可用 NODE 表达 `[NeuralDE综述-2202.02435]`
		* 符号化表达的版本见 `2021-03-24`(AISCmeet)，即强制学出的模型可解释
		> 可用于解 ((o2dg1b))BIP
		* 正则化流刻画数据分布，不仅可用于生成模型，还可用于((n4sk8c))似然估计
* 多步生成—信息补全：随生成进行，结果占用的存储量不断增大；需区分 不断填充完整的 是什么域
	* 空间域：如特征图像素or patch 自回归；普通自回归语言模型 也算；{q85k5k}
		* 语言模型 一次性预测多 token，可提升训练稳定性等
		* 推理加速，语言模型 投机解码
		* 补全次序：最常见的是 NLP 自然词序，图像 patch 拉平后顺序；（可能性）图像 patch 中心向外螺旋扩张
		* 相关：这里每次生成单 token，另有每次生成全部 token 又动态撤回的((q8ac0z))GRN
	* 尺度域：VAR 等 next-scale；{n8i94k}
		* 要素：多尺度表征方式（包括编解码）x ↔ (zₖ)，增量尺度信息条件概率 p(zₖ|z₁,..,zₖ₋₁)
		* WC-RG-2207.04941：表征 zₖ 为小波系数（预设不可学），条件概率 ansatz p(zₖ|xₖ) ∝ exp(-Hₖ(zₖ|xₖ))，物理问题中能量模型 Hₖ 用特定 ansatz
		* `RG-Flow-2010.00029`
			* 表征：编解码可逆双路网络（类似正则化流生成），错位编码两层后 降采样，未被采样的部分信息作为当前层 zₖ
			* 条件概率：p(zₖ) 逐点独立（无条件，预设不可学），用 Laplace 分布以提高稀疏性（进而提高特征可解释性）
		* `2303.02984` 基于小波分解，根据现有低频图像生成高频小波系数，用于超分辨率等
		* Imagen((_n4sn4y))基于超分辨率
		* VAR((_o4ha7y))残差，随机性引入方式为 输出层离散采样 VQ-VAE token；{o4ha86}
			* 超分辨率过程中利用历史所有的 token map，而不仅是当前分辨率的((_o4ha8l))
			* 同尺度各 token 独立采样；同尺度 token 并行生成、尺度间串行生成；相关：((o4ha7u))串行vs并行
			* 表征：VQ-VAE¹ 离散 token，编解码均 coarse-to-fine，编码逐步减去残差，解码累积残差
				* ¹后续工作有换为 LFQ、BSQ 等预设 codebook，生成过程变为 逐分量 binary 生成
				* 分辨率非 2 的幂次，原文 1, 2, 3, 4, 5, 6, 8, 10, 13, 16
				* 完整解码结果 简单修复：CNN 后处理
				* 训练 loss：重建，隐空间量化损失，视觉感知（如 LPIPS），判别（StyleGAN 判别器）
			* 条件概率 即自回归 Transformer；先前尺度信息 z₁,..,zₖ₋₁ 条件引入方式：同时有 1. 注意力上文，2. Transformer 当前位置输入 xₖ（由先前尺度结果重建）
			* class label c 条件引入：同时有 1. Transformer 首 token 位置输入，2. Transformer AdaLN
			* 推理过程稳定性，粗尺度的噪声在细尺度无法修复
				* 通过 训练时人工引入噪声（模型自己学会修正）((q51c1a))
		* 下游任务—inpainting((q51c03))：采样范围限制，仅生成 影响待补全位置 的 zₖ 分量；{q51c09}
		* 上采样的网络架构，((_o8fe3x))结合卷积、Fourier 模块，分别捕捉局部、全局特征
			* 并建议 NO 带上采样时，上采样放网络输入位置，而非输出；{o8fe4l}
				* 相关：((n8hj6b))NO架构-输入层-patchify，((o8fe66))NO-网格泛化-grid分辨率可变
				* ((_o8fe6f))NO 输入层上采样可避免 ((n3h98i))aliasing；{o8fe6k}
		* 一般超分辨率，不限于生成用途
			* 用途—恢复重建，((n8i94h))粗网格观测or数值解恢复细尺度物理场
			* 用途—((p3va9b))空间分辨率压缩，对高分辨率待表征对象，允许先仅输出低分辨率版本
			* 方法—视为条件生成，如(('patm85))MovieGen 用流匹配条件生成，条件引入方式为低分辨图片简单上采样后与噪声 concat
				* 注意目的手段反向，VAR 类以生成为目的、超分辨率为手段，这里超分辨率为目的、条件生成为手段
	* 训练策略，通常 极大似然，因概率密度 tractable
		* 自回归误差累积问题，通过((q51c1a))训练时加随机噪声 等手段缓解
* 多步生成 采样推理执行（已学出预设路径、完成训练之后）{q8bc2u}
	* 过程 确定vs随机，前者意味着所有随机性都在网络输入的随机种子
		* （扩散）用确定性采样 eg.((_n5bg1u))Shap-E 用 Heun sampler
		* （扩散）随机性优势：2024-03-06 导师提到鄂维南说过 ODE 相比 SDE 少了 stochastic cancellation，因此即使刻画的分布一致，效果也不如 SDE
	* 非连续路径式采样—IR 采样（迭代步不对应 reverse ODE/SDE）：((_q4ib3e))从 tₙ 完全去噪得干净图像、重新加噪到 tₙ₋₁，类似地到 tₙ₋₂，循环直到完全去噪；{q4oc1s}
		* ！注意其采样分布不准((_q8an59))
		* 重加噪所用噪声：原文(a)每步重采样；另可(b)所有步用同一噪声，(c)定期（每隔几步）重采样
			* ECI 论文同时提及三种
		* 用途—约束生成((q4oc24))ECI，为保证生成结果满足约束，中途的干净图像补充投影修正操作
		* 方法所属框架((q8ah60))多步生成-基于修复-每步重破坏
	* 采样加速
		* 状态更新类-步长选择，大步长可降本
			* 支持大步长：改进 ODE 正则性，如 rectified flow
			* 后期增大：ODE 推进步长先小后大(('pau92k))（流匹配）
			* 简单样本增大，需评估当前样本难度（可能需((q8bc4b))终态指标预判）；{q8bc53}
				* 评估指标—((_q8bc3p))完整图片分布平均熵
			* 相关：数值迭代格式-自适应步长
		* 蒸馏单次生成模型((q51d9j))，成为单步生成
		* （信息补全类）投机解码，一次生成多个，再评估是否接受；语言模型常用
			* 预测方式：轻量级小模型，原模型引入轻量级预测机制（如额外预测头）
			* 重要性采样 保持生成结果的分布与原始模型一致
	* 约束生成/额外引导—DOIT 单步去噪调整，((q4oh3d))（为使生成结果概率按 R(x₀) 加权）独立多次标准去噪迭代得多候选 x₀，组合其迭代轨迹得当前迭代步
		* 迭代步总数 O(MN²)，明显多于标准去噪迭代步 N（M 为每次采样的候选样本数）
	* 约束生成/额外引导—一般见((q4of8s))引入数据分布以外的信息引导生成结果
* 多步生成 终态指标预判，据中间态 $x_t$ 预判终态指标 $R(x_0)$；{q8bc4b}
	* 指标类型：((q4of8s))约束满足程度，样本复杂度（用于((q8bc53))采样-步长选择）
	* 主要挑战—中间态 xₜ 病态，不像终态 x₀ 可直接代入指标
		* 病态表现—（状态更新类）已破坏状态，例如 未完全去噪、多尺度生成时缺细尺度
		* 病态表现—（信息补全类）中间状态不完整
	* 中间态指标 $R(x_t)$
		* 散点观测 等简单 loss 可直接用于中间状态，如 ((p4jh6v))S3GM、((o6uf1s))DiffusionPDE
		* PDE 残差支持—指标换形态：残差改弱形式，解微分次数少，有噪声时 数值微分（FD 等）更稳定
			* eg. 2508.09156 R(a,u) = ⊕ᵢ₌₁ᴺ|⟨Lᵃu,ψᵢ⟩|²
		* PDE 残差支持—改良中间态：去噪版本光滑化改造，常涉及((q8ba1m))破坏在变换后空间进行
			* 隐空间扩散((n5bk3f))，保证原空间光滑性；{q4ni2m}
				* eg. FunDiff-2506.07902 decoder 用 INR，PDE 残差可按 PINN 自动微分计算
			* 频域扩散，通过频域系数衰减速度 保证原空间光滑性（函数空间 Hᵏ FT 所得空间已知）
				* eg. PISD-2602.09708 频域引入 mask 保证高频系数为 0，从而原空间光滑
		* PDE 残差支持—亦可参考((n23j97))数值微分-预处理，包括改离散、噪声应对等（系统识别场景也需对带噪数据算 PDE 残差）
	* 终态预测—（状态更新类）重建网络单步估计，去噪情形对应后验均值
		* 解读：由训练目标，去噪网络预测的无噪声图像 应等于 𝔼[x₀|xₜ]
		* 常改参化：去噪网络输出 变为去噪结果 x₀（而非提取的噪声 ε），便于直接计算
		* Jensen's gap：惩罚的是 R(𝔼[x₀|xₜ])，而非真正想要的 𝔼[R(x₀)|xₜ]
			* 2409.00730 讨论了 R 线性、多重线性、凸性等特殊情形
			* 验证：PIDDM-2505.22391 fig2d 训练引入该惩罚后，原始的扩散模型训练 loss 降不下去
			* 应对—惩罚加权，gap 大情形减少惩罚系数
				* PIDM-2403.14404（mean estimate 变种）t 大→ Var(x₀|xₜ) 大→ Jensen's gap 大→ 惩罚项不准→ 降低权重
				* 效果验证，PIDM-2403.14404 fig2 训练引入该额外惩罚后，原扩散 loss 大小几乎不受影响（我认为与 PIDDM-2505.22391 fig2d 形成对比）
	* 终态预测—实际后验采样，执行涉及多步迭代
		* 迭代步数/步长
			* 对不同 t 用相同步数，以便于 batch-training
				* 导致大 t 采样精度低（因步长过大），R(x₀) 估计不准
					* PIDM-2403.14404（sample estimate 变种）对大 t 降低惩罚权重
			* 渐进式训练((n7d931))，随训练进行增大步数 eg. ((_q4nh96))PBFM；{q4nh9h}
		* PIRF((_q4og1o))迭代生成视为 MDP、模型按 RL 微调，R(x₀) 视为稀疏 reward
* 生成过程 额外引导（包括施加约束），引入数据分布以外的信息引导生成结果；{q4of8s}
	* 引导类型 例：反问题观测（y=H(x)+n），PDE（((n3bg3i))PINN loss），已知性质，图像质量
		* PDE loss 特殊性，((_q4og14))认为只涉及局部，故用于微调时只微调网络末层（认为预测网络浅层控制全局形态，深层控制局部）
		* 已知性质 eg. 2409.00730 旋转平移不变、能量动量守恒
	* （以下讨论各引导方式）
	* 训练内化，通过 loss 额外惩罚项
		* loss 约束引入额外 loss 项，成为((n9hg32))多目标优化；如((q4of81))PBFM 流匹配+PDE loss 二项梯度方向进行特殊组合
		* 多步生成情形常需((q8bc4b))终态指标预判
	* 训练变种—模型蒸馏时内化约束，原教师模型为无约束生成；{q52b57}
		* 学生模型—可单步生成（即使教师多步），eg. PIDDM-2505.22391 学生模型 ϵ ↦ x₀
			* 蒸馏依据 见((q51d9j))单步生成模型-来自蒸馏，包括 MeanFlow 等高效策略
		* 约束引入—loss 额外惩罚项
		* 约束引入—（可能性）样本筛选，教师模型生成结果集合中 选出好的用来蒸馏
		* 约束引入—（可能性）样本修订，教师模型生成结果微调使之满足约束，才用于训学生模型
	* 推理过程调制（多步生成情形 各步单独调制）
		* 多步生成情形常需((q8bc4b))终态指标预判
		* 惩罚项梯度
			* 理论解读—Langevin 动力学，梯度引导采样更新
		* 多合理方向整合，基于((p97e9u))ensemble-筛选（那里有的不在此处讨论）；{q4oh3d}
			* 元素形态—((_q8ba79))从 xₜ rollout 到 x₀ 的完整轨迹
			* 元素形态—((_q8bb2g))单步 look-ahead 后单步终点预测
			* 元素被利用部分—((_q4oh28))单步 t→ t' 更新量（整合得最终当前 t→ t' 步更新量）
	* 重建态修订（多步生成&硬约束）：ECI 框架，多轮破坏重建采样¹ 中对临时的干净解投影；{q4oc24}
		* ¹目前主要为((q4oc1s))IR 采样
		* 具体描述：每步去噪不用 ODE 推进，而是 常速完全去噪得干净解（Extrapolate）、无噪声解沿约束投影（Correction）、插值重新加回噪声（Interpolate）
		* 文献起源—ECI-2412.01786 原文应为独立工作而非续 IR：未引，公开时间近，作者无重叠，IR 用 SDE 本文流匹配
		* ！采样分布不准（继承了 IR 的问题）
		* 可选额外步骤—终态 loss 微调：重加回噪声后，((_q4ob7n))PBFM 预测其终态 loss，用于微调修正当前有噪声中间态，并惩罚更新幅度避免过大
		* 投影算法—线性，原始 ECI 框架使用，但主要仅适用于线性约束（IC/BC/守恒律等）
		* 投影算法—Gauss-Newton，见((_q4ob8c))PBFM
		* 投影算法—近端优化，ProFlow-2601.20227 使用
		* PBFM tbl1 还提到 FFM，ProbConserv
		* 投影对象 通常仅为 x₀，涉及 去噪、投影、重加噪 迭代
			* ((_q4ib3e))IR（iterative refinement）eqn(6) 似讨论了多轮重加噪后 网络预测的 x0 和 最终真正采样所得的 x0 差异不断减小的过程？
	* 推理种子调优 min_ϵ R(x₀)
		* 解读，有说法将网络输入 ε 视为某种 prompt，该做法“noise prompting”类似提示词工程
		* 前提—随机性来源集中，如流匹配
		* 前提—推理过程易 BP，如单步生成
		* 好于直接优化 x₀：避免结果远离（生成模型刻画的）概率分布
			* 分布正则化 min R(x) - λ log p(x) 也不可行：生成模型的 p(x) intractable（只能采样，不能算似然）
		* D-Flow-2402.14017 需穿过 reverse ODE 求解的 rollout 迭代过程 BP
			* 原文用于图像反问题（去模糊、inpainting）{q4oh5c}
		* 结合蒸馏：PIDDM-2505.22391 alg2 只需优化学生模型 ϵ ↦ x₀ 的输入，不再穿过多步采样
	* 多结果择优（仅推理），((p97e9u))ensemble 生成-结果挑最好；多步情形无需 ∇R(x₀)；{q51g0s}
		* ensemble 规模：多尺度生成类 ((_q51c2o))随生成进行 规模递减，因早期信息量少、单样本成本低
		* 筛选方式—早期关注多样性，((_q51c2c))多尺度生成类 早期已有足够结构信息
		* 策略属于框架((n8qk2t))优化算法-ensemble
		* 特例：((o6mm5b))生成模型形如 NO 附带 UQ，从中筛满足约束的
	* 改架构—网络额外输入约束残差，作为生成所用 condition，为不可靠约束
		* eg. PG-Diffusion-2211.14680（待确认）PDE 残差作为 condition
* 部分生成模型网络架构比较（loss 形式等暂不比较；大多都有反向的对偶网络结构）
	* 随机性引入：CNN（包括 GAN，VAE）仅初始隐向量随机
		* `GenModSDE-2011.13456` 初始全噪声、中途去噪也引入随机性
		* `RG-Flow-2010.00029` 随机基本在 FP 中引入，初始随机性小
	* FP 过程：CNN 为特征重组、反卷积，中间结果无直接意义
		* `GenModSDE-2011.13456` 去噪，`RG-Flow-2010.00029` 提分辨率（类似小波重建）
	* 噪声先验：通常高斯（包括 SDE 布朗运动），`RG-Flow-2010.00029` Laplace 先验以增强可解释性
* 条件生成模型；{n4te8y}
	* CGAN，生成器和判别器输入都包括条件 $y$
		* 判别器可以输入 $(y,x)$ 看二者是否匹配，也可以普通 surrogate/分类器输入 $x$ 预测 $y$ 然后和原有输入 $y$ 对比
	* CVAE，多个版本（这里只给出部分）
		1. $\max p(x\mid y)$，模型生成方式为 $p(x\mid y,z)$，$y,z$ 独立，辅助 encoder $q(z\mid x,y)$
			* 衍生版本 A-CVAE "Generative Deep Learning Model for Inverse Design of Integrated Nanophotonic Devices", 另外使用一个 adversarial 网络确保 $z$ 里不包含 condition $y$ 的信息，相应 loss
		1. 概率图 $y\to z\to x$，例如 `acsphotonics.0c01481`
			* encoder 给出的是 $y$ 而不是 $x$ 的表达，导致可能无法利用训练数据（训练数据可能只有后续 surrogate/discriminator 使用），从而生成结果的多样性可能不足
			* 如果选用无限表达能力的函数空间，loss 的最优点仍然依赖于 $p(y)$ 选取（因为 $p(z)$ 要尽量接近标准正态），而上一个版本则不依赖
			* 视为 $z=\mu(y)+\sigma(y)\xi$ 则成为 CGAN 的生成器部分（输入 $(y,\xi)$）；后续的判别器同 CGAN 部分
			* $z\to x\to y$（接上 surrogate）整体可以视为一个 VAE（针对 $y$ encode）的 decoder 部分，只是其后半部分不能在这里训练；
		1. （勉强也算一个）VAE 部分之外加入 surrogate $z\to c$，训练时与 VAE 联合训练，生成时给定 $c$ 先优化算法找到 $z$ 再输入 decoder
	* MVAE 处理数据有缺失项的情形（见 2021spring 我在组会讲的）
	* 扩散模型的条件生成；{n3q94a}
		* 目前的记录包括((n2ak58))从分子图预测空间构型
		* 注意力：stable diffusion 非条件生成使用 U-Net，条件生成则在其中每层（ResNet block）后加入注意力层，此时引入语义 condition；{n3ri6n}
			* 相关：((n3ri89))仿TRIZ原理-迭代-单一格式迭代改交替格式迭代
			* ((_n4sn3g))条件为 CLIP 对齐的图文编码，因此可用图片的 condition 训练、用文本的 condition 推断
		* 同模态 concat：stable diffusion 用的另一种策略((_n4sn37))，来自前序工作；可以与注意力 condition 同时用
			* 另一例子为 MovieGen 逐帧超分辨率模型(('patm5q))，低分辨率图像双线性插值后与目标高分辨图像的随机噪声 concat
		* 训练时即引入生成条件，如 DiT
		* 条件作为 loss 提供梯度，生成过程类似 Langevin 采样；条件不输入网络，仅干预生成过程；{o6uf35}
			* classifier guidance 属于此类
			* ((o6uf1s))PDE $(a,u)$ 联合分布生成时提供 observation、PDE residual 两种 loss
	* ViT 的条件生成((o3lk9o))
	* VAR 类的条件生成
		* 原始 VAR：条件为 class label，引入方式为 网络输入 [BOS] + AdaLN
		* Infinity：条件为 text，引入方式 网络输入 [BOS]（带约简） + 交叉注意力
		* InfinityStar：条件含 text,img,video，引入方式 自注意力上文
	* condition 的强化：((_n4sn4g))同时训无条件、有条件生成模型，推断时超松弛、远离无条件生成的结果；{n4tj4z}
		* 相关：((n9me8w))使用另一已训好网络辅助推断
	* 可基于已训好的生成模型构建条件生成模型（或引入新种类的条件），一般框架((n4te79))，例子有 ControlNet
	* 根据单图片生成 3D 表示（包括背面），((_p19f3y))SPAR3D 稀疏点云、精细网格 二阶段生成，网格阶段正面依据图像重建、背面依据点云
	* 若部分数据无 condition 信息，可能训练方式；{n4tj2d}
		* 可见((n4tj2p))数据信息缺失
		* ((_n4tj3t))ControlNet 在文生图遇到这种情况
		* 多保真可能性，无条件网络用大量数据训、有条件的用少量数据？
			* 或可结合((n4tj4z))condition 强化 做法
	* 与 随机映射 等价性：本需刻画((n3bg65))随机映射 $c\mapsto y$，用 ((n5bg8b))映射分析-输入转参数 成为条件生成问题 $?\xmapsto{c}y$；{n5bh10}
		* 如 ((n5bg9t))Shap-E 文本转 INR 参数
	* 应用相关：`2022-05-18`(AISCmeet2) deep Kalman filter 条件生成模型根据历史控制、当前观测推断当前状态
	* 生成所用的 condition 的产生：((_nc9k6e))视频生成时将 Blender 物理仿真结果作为条件，仿真脚本可用 GPT4 等自动生成
* 时序数据的生成模型：`2022-11-09`(AISCmeet2) DVAE（dynamical VAE）类似 HMM（隐 Markov 模型）的建模方式；如果背后物理机制已知，可进一步结合 EKF（Extended Kalman filter）设计正态方差等的更新方式
* 生成函数空间分布（例如以 NN 输入位置输出 RGB 值表达的无像素图像）见 ((n32f2t))NN表达函数空间中分布
* 若考虑对数据分布建模，从该分布采样¹是生成问题；另可考虑似然估计（likelihood estimate）问题；{n4sk8c}
	* 另见((n8jb6f))采样作为决策动作（非来自给定分布）
	* 一般意义上的概率、分布：(a) 一般概率测度，(b) 连续分布 $p(x)$，(c) 散点分布 $\oplus \delta_{x_i}$；{n8ql5a}
		* 其他分布相关：((n32e7d))分布迁移，((n32f2t))函数空间中的分布、随机函数，((nasa2w))非科研-逻辑对象等价表达方式-概率分布（PDF、CDF、特征函数）
		* (b) 在这里指对任意 $x$ 可求出 $p(x)$ 的情形（至少求出 $Cp(x)$）
		* 对于 (a) 在计算机中的不同表达方式，需区分 a→ b（似然估计）、a→ c（采样）二者计算复杂度；SMCM 课程中对正则化流((n4sk8g)) 讨论了该区分
		* b→ c 从给定概率密度（可能差常数）中采样，MCMC 等；{n8ql6d}
			* SVGD((n8qk7i))的情形稍有不同，需预先指定样本总数，同时更新所有样本点
			* ((o3cn54))给定概率似然函数 $E$ + 额外目标函数 $G$，Langevin 采样（利用 $\nabla G$）
			* 针对文本的扩散生成模型((o4he7a))，使用类似 MCMC 方式采样
			* “采样”相关：((n8jb6f))coordLoss-空间采样点
		* 生成模型推断为 a→ c，生成模型训练为 c→ a（且假定这里 a ansatz 方便推断）
		* 用 c 作为近似表达 a 的 ansatz，见((n8ql7o))ensemble-用于刻画分布
		* (c) 另有加权版本，加权散点分布
			* 相关：((n8qk4m))ensemble（一般）
			* 不是所有数据都在欧氏空间，某些分布的选项集合本来就离散，如整数取值、分类问题（包括 NLP 词分布）{o42l4r}
				* 可能离散分布适合自回归、连续分布适合扩散模型((oapf91))
				* 相关，离散值生成：((n3bg9o))NN 生成 categorical 分布
				* 离散来自人为设定而非天然：((n6sf8q))离散取值隐向量，((o8ue5k))图像 tokenizer 连续vs离散版本
		* 相关：((n8re4f))数学对象表征方式多样性
		* 概率分布的距离度量方式；{o1se5r}
			* KL 散度及其对称化版本
			* 算统计量的距离，如（正态假设下）均值、协方差的距离求和
				* 可先做变换（如((oafe86))特征提取器）再算统计量，如图像生成的 FID；{q54a7v}
					* 变换选取—FID 默认用 Inception Net-V3 全连接前的 2048 维向量作图片 feature
					* FID 高到一定程度后不再有参考价值(('o4i99n))，需由人来评判图像质量；((_q54a5t))正式实验论据
					* 变换选取—综合用多种变换，((_q54a6z))FDrᵏ 多种表征空间计算归一化 FD 比值并取平均
					* 相关，先特征提取再算分布距离 也用于((p2bf1r))半监督学习-通过算条件分布距离
			* ((_o1se5l))涉及 Wasserstein，MMD，KSD
			* ((_p15a5h))离散分布用 OT（最优传输），连续的可用 MMD
			* 条件分布((_p2be6s))CEOD 距离
			* 其他数据形式的距离度量方式；{o4gh0d}
				* 符号表达式的距离度量；{o4gg9v}
					* ((_o4gg9g))PROSE 不直接比较符号形式，先判断是否为合法表达式，若合法则二表达式均视为函数、在函数空间算距离；{o4gh20}
					* 相关：((n3bh3b))系统识别
				* 函数之间的距离，((o4gh1a))函数空间：
			* 用途—分布对齐，((p15a5m))引入新模态时，训编码器使其嵌入分布与原模态的对齐
			* 用途—半监督学习，((p2bf0c))惩罚（已知、未知数据）二条件分布距离
			* 用途—采数据时选代表样本，((q82a2s))所采样本分布 接近目标分布
			* 相关，metric 其他含义：{o1se62}
				* ((n9hf83))衡量模型表现的指标
		* 概率分布间的变换：((_o1se6r))d 维空间的二分布间变换可用 $\nabla g$ 近似，$g$ 为 ReLU 激活 NN
	* 负似然函数用作优化中惩罚项：{o3dj27}
		* ((n4sl10))为下游少数据任务提供正则化项，((o3cn5s))PDECO-表达 PDE 约束（约束用惩罚项近似）
		* 正似然函数作为惩罚项，((o3dj4o))使生成结果推离数据分布、以使生成结果质量高于训练集
	* NN 直接表达 $-\log p$，((_n4sk8x))训练使用类似 DSM（denoising score matching）的 loss；{n4sl24}
		* 注意普通 DSM 是用 NN 表达 score $\nabla_x\log p$，与这里稍有区别
		* 其实这里还考虑了噪声强度，刻画的是加不同噪声强度后的似然大小
		* 相关：((n37n5n))扩散生成模型
