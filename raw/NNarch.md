> 2024-04-17 从 NN.md 独立
* NN 架构设计，网络架构设计技巧的汇总（不排除用于元学习可能性）：{n32f33}
> 部分内容可能需迁移至 `PINN%`,`NO%`(compTree)，尤其是网络输入为空间坐标、场的情况；
> 可按目标细分，如 OoD 泛化能力的针对性技巧
* 相关：输入为坐标的函数见 `coordMLP%`；对已有网络架构解读的见 ((n32b5g))NN架构解读；((n37g4c))NN 初始化，((n3bh0y))loss 选取
* 不同输出下网络设计方式；{n3qi2r}
	* 关于不同输入形式的讨论目前见((n3qg83))NO-不同输入形式，包括((n5bl4q))对不同输入、输出形式通用的架构
	* 场、函数，见((n3qi2x))NO神经算子；均匀网格形式另见 CNN，ViT
	* 序列：RNN，Transformer 解码器
		* 生成置换（permutation）：`2022-04-27`(AISCmeet2) AE-LSTM 或 RL；{n8ve1a}
			* 生成置换应该算特殊 seq2seq 问题；见((n8v96k))序列数据的含义
	* 集合；{n8re18}
		* 点云生成，Point-E((_n5be04))用扩散模型，去噪网络架构基于 Transformer；{n5be19}
			* 相关：((n3qn83))集合作为网络输入，((n32f77))点云作为网络输入
	* 区域形状：自由边界 PDE 需预测区域形变方式，((_q5tg2v))FBNO 将((n7n97v))参考域到输出域变换 作为网络输出；{q5tg3e}
	* 另一网络参数：见 hypernet
	* 高维输出时需考虑参数共享，用另一种方式（序列、集合等）解读输出；{n4dl7e}
		* 注意到序列、集合解读算向量按维度拆分，这可再视为按加法拆分、support 不交的情形，可进一步改写为 support 部分相交情形
			* 两种路径得到的算法相似：一种是按维度拆分（分开生成）、改观点为加法拆分、扩大 support，另一种是按加法拆分（线性基底）、缩小 support；{n4ce3h}
			* ((_n4c950))support 选取利用了输出对象（权重矩阵）的结构
			* 可能性：support 选取利用 hash，类似 NeRF_5s
		* 离线生成若干线性基底、在线生成各基底的系数，即输出通过线性/仿射变换给出；{n4ue64}
			* 生成所需参数量包含两部分：线性基底的存储；系数由 NN 动态生成，该部分 NN 的参数
				* 从而主要适用于 维数不算太高 情形
			* 如 INR modulation，超网络只（用这种方式）生成 bias，应可同理用于 CNN 等
			* `HyperFNO-NIPS22` 如此生成 FNO 各层二矩阵，由于分别对应空间域、频域而用不同线性依赖方式，均低秩
			* 对输出先分组，每组内用这种策略降维；如((_n42e8y))输出为另一网络权重矩阵，按列分组
			* 基底数多时，总参数量大；此时可考虑对基底使用 finite support 以减少参数量
				* 这种做法有另一种解读方式，((n4ce3h))从向量按维度拆分来解读
				* 各 support 通常可有一些交集；较激进的情形也可相互不交（相当于进一步对输出分量拆分，每分量用单一基底生成；会有 mode collapse）
			* 各基底系数也未必用 NN 生成，如 `2110.13361` 用高斯过程、样条等
				* 结合之前说的线性基底后，整体 ansatz 为多元高斯过程、多元样条，可理解为本身允许高维输出的非 NN 映射 ansatz；{n4uf0u}
		* 输出视为序列，各元素顺序生成；可用 Transformer，RNN 等；{n8vg6a}
			* 用 ODE，其不同步的状态作为不同的输出，如((n35f0h))利用 CDE 构建 hypernet；{n8vg6b}
			* 相关：((n8v96k))序列数据的不同含义
		* 视为集合、graph，同步生成；可用 Transformer，GNN 等；另外 CNN 等规则数据也算同步生成
		* 解读为映射，$x_i$ 换 $x(i),x(z_i)$，各元素可分开、独立生成；如图像像素太多，改用 INR 描述
			* 注：之前我将这作为 仿TRIZ原理-合并
		* 层次生成，先生成 $x_{i:}$（其实是生成它所需的信息），再生成 $x_{ij}$；两步可用不同方式；{n4ce5l}
			* 视为数据总量压缩策略，((oa3c1p))整块数据先赋予集合结构，再依据要素压缩
				* i 层次对应集合结构；相比目标结果 $x_{ij}$，先生成 $x_{i:}$ 是提高了单点维数压缩率
			* 如((n42e9a))局部由线性基底生成，基底的系数在上一层面视为集合、用 Transformer 生成
			* 大致原则：低维输出可简单 NN，稍高维可线性基底，再高维通常要拆为集合、序列、$x(z_i)$ 等
			* 高维输入也可层次处理，$x_{ij}$ 先变换为 $x_{i:}$ 再后续输入
				* 对输入而非输出情形，上面除了“解读为映射”外其他做法均直接可用
				* 如 ViT 先对 patch（低维）过 NN 得 token，再将不同 patch 视为 set（带位置编码）过 Transformer
				* 分子动力学也有粗粒化等做法；同理可用于点云、graph
		* 分组生成((n8ch1z))
		* 相关场景：((n4dl73))NO-高维或高分辨率场，((n42f33))hypernet-生成主网络隐层参数（不过该类型输出的冗余度稍高，可只生成若干分量，其他分量取定）
			* 另：NeRF_5s 的 hash 技巧也算一种降维？或许是对参数降维，但用于输出降维亦可
	* 输出值跨量级；{p9ua2i}
		* 不同输入下输出量级不同：((_p9ua2d))PINN 网络末层用指数激活
		* 多输出分量量级不同：（根据物理背景）INR 输出有尺度不同的多个分量
			* 如 Boltzmann 方程平衡态项+非平衡态项，量级相差 Kn 可能 1e-2 `2022-06-24`(CSImeet2)
	* 相关：输出的含义，考虑((n9999m))网络(输入,输出)≠ 数据(样本,标签)
* 架构设计体现人对任务、场景的理解；{n4tc09}
	* 相关：((nca97g))可解释性，((n3ag7c))数据不足解决-引入先验知识
	* 引入（显式/硬）先验知识：对称性，等变性，物理知识；{nca99w}
		* 针对 INR 的见((n3pa7n))
		* 对称性（不变性），如 DeepSets
			* 注：置换对称性架构汇总目前还放在 metaL/hypernet 下
		* 等变性 equivariance；{n3h983}
			* 对应的群作用：平移，旋转；群包括连续版本与离散版本
				* 据场景选对称群，如 ((n8ve45))PDE 数据增强据特定方程形式选 Lie 群作用；{n8ve48}
				* 函数空间群作用 由定义域作用的 pull back 给出；{q81971}
					* 依赖要素—定义域作用涉及((q8169o))空间坐标变换
						* 若不要求可逆，成为函数空间变换，可((q4rg7w))用于构造 NO 层
					* 刻画的下游性质—算子等变性，相应架构设计见((n23h00))
					* 利用—((n8ve45))PDE 数据增强
			* （严格）等变性架构设计
				* 区分架构所表达的映射类型
					* 输入输出—散点数据，输入输出为相同散点集上的数据；分子领域常涉及
					* 输入输出—物理场，算子等变性，其中((q81971))函数空间对称性来自定义域对称性；{n23h00}
						* 对 PDO 已有相关构造方式的研究((n23g87))，包括系数自适应依赖于空间位置的情形
						* 若用任意散点离散（见((n32e9r))场的数值表征），则转化为 散点输入输出 情形
					* 映射功能—生成模型（去噪等），保证所生成的分布对称
				* 对称群—置换 permute，实现可基于((n93k53))多元素规约，使 i 更新量由 {(i,j)} 对所有 j 规约给出；{q6o70c}
					* 用途：((n2gf92))动力学-多体系统ODE，((n3qn83))网络输入集合-中间层-更新元素表示
					* 相关：同样的信息交互目的下，若取消置换等变约束、加计算量约束，可见((p8ag76))集合元素划分后处理
					* UAP((n7na7l))，Transformer((n37n4q))（无 attn-mask、无位置编码）满足，见 1912.10077；{q8dl37}
					* 涉及二阶信息的映射 置换等变性叙述，如((q8dl4c))图映射（GNN 表达），其输入涉及额外二阶信息（邻接矩阵）
				* 对称群—网格旋转，一套卷积核旋转多个角度同时作用于输入，((_p78a72))可用于交错网格情形
				* 对称群—2D 方形旋转反射群 D4，((q7vg9v))用于生成 FNO 频域核
				* 对称群—3D 平移旋转 SE(3)，分子空间构型预测需要；{n2me8g}
					* （散点输入）((n2me84))扩散生成模型中形如 GNN 的网络自动满足
					* （散点输入）可构造商空间((_q6k87j))，在商空间上构建扩散模型等
					* Transformer（ViT）也可引入旋转等群等变性质((n3ha70))
					* （场输入）`INSP-Net-2210.08772` 作用于 INR 的 NO，表达为可学微分算子作用，自动平移不变（等变？）、设计微分算子 ansatz 可旋转不变
						* 基于（物理空间）PDE 的网络架构设计应都能以类似方式保证平移、旋转不变
					* 重要性与贡献大小
						* AlphaFold2 消融中((_q6tf9e))贡献 marginal
						* ((_o5af8b))AlphaFold3 发现可省略架构中平移、旋转不变性的处理
						* 直观解释：((_q6tf9i))平移旋转对称性不强，远弱于粒子、残基等的置换不变性
				* 保证方式—各种群（旋转、置换等）下等变映射有特定形式 `2022-03-30`(AISCmeet2)，可在架构设计中体现；{n23g9c}
					* 架构设计理论框架，((_n7mj31))用 steerable kernel 给出等变网络的一般形式，包括标量、向量的变换
				* 保证方式—基于频域，((n7jm27))G-FNO 架构涉及 在频域的 activation 值用其群变换结果增强；{n7jm2c}
				* 保证方式—基于商空间，((_q6k85z))用于扩散生成模型时 普通等变模型仅保证学出的分布对称，商空间版本才减轻学习量
				* `PDE-G-CNN-2001.09046`
				* 其他涉及等变性参考材料：`2022-08-03`(AISCmeet2) 群等变 CNN，steerable G-conv，参考链接
			* 代码实现：`2022-11-25`(CSImeet3) 提到实现时有 [e3nn](https://e3nn.org/) 库，利用群的不可约表示
			* 靠合成数据提供新 loss，惩罚模型非等变的程度((ncln3x))；另可数据增强
			* 等变性的破坏：((_n3h986))可被((n3h98i))alias 现象（由于网格分辨率有限而产生）破坏；{n3h98j}
				* CNN 架构中破坏等变性的关键因素：((_n3h98y))下采样，((_n3h994))非线性激活函数（非光滑激活函数尤其明显）；均涉及 alias；{ncqf1p}
					* 激活函数引入高频信息，((ncqf1j))FNO 认为这缓解了截断高频 mode 的精度损失
					* 浮点半精度对高频成分的精度损失更大，如((_o2gb2v))FNO；{o2gb35}
						* 相关：((n7sa8l))半精度
					* 相关：((ncqf18))NO-频率问题，((ncqf28))激活函数
				* ViT、MLPmixer：主要在最开始的 patch embedding 破坏等变性((_n3ha62))，总体而言（经过训练后）破坏等变性幅度不大，甚至可好于 CNN；{n3ha7e}
					* 相关：((o8gg6g))MLPmixer
			* 近似等变性，及其度量方式
				* 度量等变性破坏程度：((_n3ha5a))使用 Lie 导数
					* 一个性质：((_n3ha5f))复合函数的各成分可分开考虑，从而可对 NN 各层独立分析
				* 靠训练而非架构：((_n3ha6a))增加模型大小、数据集大小、改训练方法等 减少等变误差效果更显著；{n3ha7h}
		* 守恒律（质量、动量等）
			* 结合交错网格，((_p78a5h))NO 输出层特殊设计可保证流体质量、动量守恒
			* 针对线性、二次守恒律，((_p7ba5u))FNO 输出层引入校正操作保证严格满足守恒律
				* 有可学参数：满足守恒律的输出有多种，可学参数使网络能从中选择更接近真解的那种
			* 相关：((o5pg31))ML2phy-数据中提取守恒律
		* `ML2phy%` 对数据拟合后的函数进一步做符号回归也可能提高 OoD 性能，如 `2006.11287`
		* 相关：((n3ag7c))引入硬先验（一般，不限于通过 NN 架构引入）
		* 相关：这算 phy in NN，另见((nckl0b))NN in phy，((nckf92))二者关系讨论
	* 引入软先验，常可降低数据需求；{nclk76}
		* 特定对象—物理场输入输出：((nclm6b))NO 架构，传统数值格式作为特殊的机理，numeric in NN
		* 在数据量较小的情况下，Hinton 的胶囊网络优于 CNN、又优于 ViT，而数据量大的情况反之？{n3ag7v}
			* 相关：((n37m6d))CNN vs ViT
		* AlphaFold2 Evoformer((_q6tf91))利用蛋白质演化特点，较基于通用 CNN 的 AF1 ((_q6tg0m))数据效率提百倍
		* 相关：((nclk75))“软”先验（一般意义）
	* 根据人的理解调整架构，如拆成多个部分的拼接，可引入相应中间 loss（`2021-08-13` CSI讨论）
	* 引入人为设置的变换，FT 等（在 PDE 求解 NO 法中较多）
		* `2022-06-10`(CSImeet2) 先提取小波系数再输入 U-Net 做分割能提效果
	* distribution shift（OoD 为单数据，这里为批量数据）有相应研究，包括 distribution align
	* `2021-08-03`(lectures) 用于处理无标签数据的可用先验：结构（NN 惩罚项），领域知识，不变性（如对比学习），多模态先验（如图文信息）
	* NN 输入为场时，((n4tc04))尽量先将数据变换到规则 grid
	* 关于是否端到端：{n4tc10}
		* `2021-02-26`(CSImeet) 有人工干预环节时才分各环节，机器能全权负责决策后还是应端到端
			* 现在许多图像处理的文章处理去雨滴、去模糊、去噪等问题，声称可以用于无人驾驶，但是这样还不如两者同时训练，即带着雨滴做决策
			* 传统先求解 PDE 而不是端到端，是因为涉及许多人工干预环节；
			* 医疗影像也同理，现在需要生成图像是因为需要医生决策，但不应该假设所有数据都是人能够理解的形式，机器决策后应该直接端到端
			* 传统数值算法现在退化为为 ML 模型生成数据的工具；这部分也可以用 ML 提高速度精度
		* `2021-08-13`(CSImeet) 有时可根据领域知识拆为多部分拼接，不是简单地端到端
			* 每个部分单独设计架构、中间连接部分引入额外 loss；猜测这样会好训练一些
			* 中间层引入对应 loss 之后，才方便说这几个模块分别在干什么事情，否则别人也不信
			* （SyQi）无监督 PINN 做法在简单情形还能解决，像复杂的湍流这些就未必效果好；而有监督学习是更为成熟的做法
	* 可解释性是否必要：针对科学应用中模型可解释性，`2022-09-16`(CSImeet2) 导师：先问为什么需要一个解释（用户只关心好用）；能通过分析所得 ML 模型找到一些线索，但须与专业背景人士（如纯数、理论物理）合作
	* 反过来的场景：NN 架构解读，理解出现在架构设计之后；{n32b5g}
		* 相关：((n32f33))关心架构设计；与 FEM 等比较的见 `coordMLP%`“逼近能力”
			* 优化过程另有 ODE 解读优化算法、NTK 等等，暂未整理统一框架
		* ResNet¹ 按 ODE 解读，另可参考 ((n35f03))invCtrlRL, `[NeuralDE综述-2202.02435]`；{n8kh2l}
			* ¹消歧义：指一种 ((n4tb9n))MLP 变种；作为一种 CNN 架构的 ResNet 见((n37m6h))CNN
				* “残差”其他含义：((n3bg3i))PDE residual；已有映射输出上再叠加额外网络输出的((n7d90o))boosting
			* 相关架构设计的见 ((n32f3o))DE启发的NN架构设计
			* `2023-01-11`(lectures)张伟伟 提到：ResNet=前向 Euler，PolyNet=后向 Euler，FractalNet=RK，RevNet=（未记录）
			* 残差连接作用：((n8kh29))深层 MLP 不可能好训（无法兼顾梯度、增量爆炸问题），引入残差连接后可以
			* ODE 所用时间步数动态增加（翻倍），对应的架构((n37g6a))ResNet 动态新增残差层
			* stochastic depth，相当于针对残差连接引入的 dropout；{n93a03}
				* 某些层以一定概率去掉；或者说只保留残差连接，忽略与该层网络参数相关的部分
				* 原文关闭概率 $p$ 不是常数，而是随深度线性增长，使输入层 0、输出层 0.5
					* 理由：浅层网络特征提取重要，应降低关闭概率
				* 使用的例子，((_n9me53))改进 FNO 效果，且使 loss landscape 拓扑结构简化；{n9me5e}
					* 相关：((n8qm1p))改进 loss landscape 的技巧
				* 相关：((n37g82))dropout，((n37n39))网络前传引入随机性
			* 残差层引入 LayerNorm 有 preLN、postLN 两种方式((nbqg84))；{nbqh3f}
		* CNN、U-Net 等按多重网格解读，如 MgNet；{ocna3l}
			* MGKN((ocna3c))可能实际上相当于将 U-Net 表达成了 传统快速多极分解+V-cycle 近似 的形式
			* 相关：((n5da0x))NO 设计时就基于空间多尺度
		* ViT 的解释见((n7na8n))，例如((n7na8o))单样本前传的极限行为是 diffusion
		* 理论逼近性质，UAP；{n7na7l}
			* 二要素：目标函数空间，假设空间（网络 ansatz）
			* 逼近性能度量：离散情形有 Rademacher 复杂度，连续情形可用函数空间范数
			* 所用函数空间，鄂维南老师工作涉及的 Barron space, compositional function space
			* 误差趋于 0 时的深度宽度增长阶，((o4tn1k))OptDNN 架构只需常数（有限）宽度
				* 刘子鸣 KAN 也有相应 UAP((_o5a94p))，收敛阶与普通 MLP 有所不同
			* ((n7na92))NO 的 UAP
			* 等变映射 可被 Transformer 一致逼近((q8dl37))，((q8dl4c))等变图映射 有 Keriven–Peyré GNN
			* 相关：((n4kl23))有效模型复杂度，将训练过程也纳入逼近能力考量
		* 除架构还考虑学得的参数，解释训好的 NN 的工作机制等；{q6sm7m}
			* 可视化；{ncab93}
				* 绘制 ((_ncab7z))activation heatmap；{ncab8i}
					* 相关：((ncab8m))NN 激活值
					* 另可考虑特定神经元激活值与输入数据的关系，((_ncab9d))观察输入的哪些部分影响当前神经元激活情况；{ncac08}
						* 或许包括((ncac02))CNN 的 DFF
				* （不限于工作机理解释的）一般可视化：((n37n3t))loss landscape，((ncac0i))梯度图
				* ((_ncn95g))训练前、训练中、训练后与推理阶段 需要的可视化方案有区别
				* attention map，为((n7pe3p))attention score 计算结果的可视化
					* attention sink；{q5ra8m}
						* 研究话题：领域内((_q5rb8p))早期基本利用，中期机制理解，后期策略性消除
						* 位置：((_q5rb6w))LLM 常 [BOS]，偶尔 强分隔符、换行、弱语义 token；ViT 背景 patch；扩散语言模型 moving sinks
						* 危害
							* ((_q5rb3y))低数值精度适配、训练不稳定、被攻击隐患、注意力错配（导致幻觉）
							* ((_q5rb7t))也有观点认为 sink 作为归纳偏置有益
							* 上下文外推适配差((_q5rf1t))，因 YaRN 等外推方案影响注意力计算、破坏模型已学出的 attn sink
						* 解决方式
							* 去除归一化要求：((n7pe44))softmax 换 softmax1
							* 输出加门控：((_q5ra8x))Qwen gated attn
							* 人为引入冗余 token，((_q5rb63))ViT 常用，LLM 相对不适用；{q5rb6k}
								* 相关：((n4t96h))特殊 token
							* ((_q5rb8a))可学 attn bias、训练过程干预
					* 解读谨慎：注意力强未必表明有信息流动，前提为((q5rb2r))attn head 真正激活
					* 规律不明显时可能考虑((_q5rb5i))消除 attn sink，可净化 attn map
			* 中间层激活值 ((oaff40))视为隐表征，提取后用于可视化or质量评估
				* 质量评估方案—((q6sm7b))考察其与（已知）任务关键/非关键特征的相关性大小，如((_q6sm8w))
				* 用于机制解释，解释中间层表征怎样支持任务相关物理
					* 分离混合激活：((_q7qj9l))用 SAE 等稀疏分解，再逐特征检验；{q7qj9m}
					* 相关性只筛候选((_q7qk1c))，不能据此直接命名物理机制
					* 检验跨条件稳定性：((_q7qk1d))改变参数、时间等条件后，候选特征的空间模式是否仍一致
				* 用于预测or干预：((p79l07))推理模型提取思考进度，通过末隐层线性探头
			* 用途—后续架构调整：案例((_q6tg08))AlphaFold2 发现后面层重点在优化几何构型 而非利用演化信息，故 AF3 大幅缩减层数
		* ((_o84f7p))认为重要的不是 NN 参数、结构，而是“等效交互”，不同结构网络殊途同归
		* DL 中的可解释性（不限于 NN 架构）：{nca97g}
			* RL 中((_ncaa5n))MBRL 可解释性好于 MFRL
			* 通过 DL 提取对已有数据的解释，见((n3bh3b))ML2phy
			* 注：freeLit1.md 内部分文献笔记与该主题相关、未整理
* 基于已有（已训练）网络构建新网络；{n4te7l}
	* 相关：((n2ek9h))与辅助网络联合训练
	* 功能上与原有网络的区别
		* 输入的 domain shift，专业化，面向下游任务；目前主要见((n4t94a))微调
		* 输入形式改变，引入 condition：如((n4tj11))ControlNet；{n4te79}
			* 一种重要场景：已训（非条件）生成模型、用它构建((n4te8y))条件生成模型，或引入新种类的条件
			* 技术手段可能包括：((n4tf6o))hypernet-训完主网络后构建超网络
	* 新网络构建方式：
		* 相关：这里指架构、参数层面使用已有网络；在训练层面使用已有网络的包括((n4sj3s))辅助网络提供 loss
		* 只做参数微调：见((n4t94a))微调
		* 引入从头训练的新网络
		* 引入已有网络的 trainable copy，如((n4tj0u))ControlNet
		* 缝合怪：集成，多已有网络联合使用
			* 注意区分网络架构设计中的 多架构联用，这里联用的二网络均已完成训练
	* 已有网络参数不更新，等到新增的参数完成训练后，可启动联合微调
		* 例如((_n4tj1l))ControlNet 用到
	* 相关：推断时引入另一个已有（已训练）网络作为辅助；{n9me8w}
		* 图像生成模型，((n4tj4z))条件生成时同时计算非条件生成结果，并试图远离该结果，以强化生成时的 condition；被称为 CFG（classifier free guidance）；{n9nb1u}
			* 相关：((o31e9h))分类
		* 文本生成的((_n9me9f))对比解码，使强模型生成结果远离弱小的“业余”模型；{n9nb1z}
			* ((_n9nb2d))CM3Leon 提出改进 CD-K，考虑最大的 k 个（未确认细节）；实验结果好于 CFG
		* 相关：((n4sj63))仿TRIZ原理-对比
	* 相关：不排除可用 映射分析 解读，已尝试((n4tj11))用于分析 ControlNet
* 动态性，dynamic NN 对不同数据/图像空间区域/时间序列不同时段 采用不同网络架构/参数：{n2pk00}
	* 作用包括：提高 OoD 性能（而无需先验知识）
	* 综述 `dynamicNNreview-2102.04906`
	* 若只对不同数据改参数，可视为单任务分解为多任务的 hypernet `1TaskHypernet:`
		* 有时实现为：网络的额外输入维度动态生成，这可等价于网络部分参数动态生成
		* 不同数据用不同架构的特殊情形：((n7bg7n))Transformer 解码器生成加速，引入草案模型快速生成多 token，结果让主模型判断概率大小、选择是否接受；{n7bg90}
	* `2022-02-11`(CSImeet2)“DDF-2104.14107”CNN 卷积核依赖于图片内容、像素位置
		* 减少 hypernet 参数量：卷积核张量分解（空间、通道）
		* 且空间卷积核写为二核逐点乘，二核分别由空间、通道的 dynamic filter 给出
		* 文中认为是某种注意力机制
	* 参数与时段有关：
		* `[NeuralDE综述-2202.02435]`“hypernet”neuralODE 控制变量来自另一 neuralODE 
			* 这里控制变量不依赖于数据；可能性：其初值据数据动态生成 $\alpha_0=\alpha_\theta(x)$
	* attention 注意力机制；{n3gm7a}
		* 相关：((n37n4q))Transformer，((n7pe3p))Transformer 注意力
		* 学出的 GNN 注意力提供对数据的解释((ncaa6c))，帮助科学研究
	* 不局限于推断的动态性：训练时动态新增参数的网络，涉及新参数初始化；{n37g5t}
		* `1710.10348` ResNet 动态新增残差层（开始只训浅层网络以降成本），用 ODE 视角使每层复制一份、步长减半；{n37g6a}
			* 又见((n8kh2l))ResNet 与 ODE 解读，((n32f3o))DE启发的NN架构设计
		* 逐渐在残差网络后追加新层((o2gh56))，针对 PINN
		* `2022-11-28`(lectures) ANE，ReLU 网络视为 FEM moving mesh，根据误差分布动态新增神经元，初始化使成为要加的 FEM 基底；{n37g53}
		* 相关框架：((n37g4c))NN 初始化，((ocaf68))INR 区域分解-动态新增区域-参数初始化
* 引入随机性（前传），有时可提高稳定性；{n37n39}
	* 激活值：前传时对网络 activation 加噪声；{n9183f}
		* dropout((n37g82))，对 activation 加（随机置零形式的）噪声，甚至在推断时加噪声也能提高
		* ((n9174x))batch norm 也对 activation 引入随机性
		* （一般意义上的）activation 相关：{ncab8m}
			* 作为隐式特征提取((oaff40))，网络中间层 activation 自动形成数据的特征表示
			* ((ncab8i))可视化 activation heatmap
			* ((_o9rk8b))类脑激活架构（非常规 NN），完成不同任务时激活的神经元不同（训练时即如此），类似人脑读/写/思考/运动时调用不同脑区，可节省计算量；{o9rk8p}
				* 属于特殊的((n2pj82))MoE 做法，不同激活神经元集合可解释为不同专家
				* 比较：若激活神经元选取随机（而非受网络控制），则类似((n37g82))dropout
			* normalization 正则化；{n9gg5k}
				* 统计范围：batch, instance, layer
					* 分别对应 BND 维度；相关：并行训练((n3gm7v))激活值类策略也对应 BND
					* BatchNorm 见((n9174b))，因组 batch 方式随机而有特殊性
					* NO 架构中 LN、IN 含义可解释：GalerkinTf 线性注意力中 layernorm 对应逐点 feature 归一化，效果稍好的是((_n9g94q))OFormer instance norm（对应基函数归一化）{n9g951}
				* 统计量：均值+标准差，RMSNorm 仅标准差
					* RMSNorm，不再（像普通 LayerNorm）先平移至均值 0，仅保留其后的向量长度归一化操作；SRS 中位置((nbqa1w))；{nbqg3u}
						* LLaMA2 中用到
					* 仅均值中心化：部分场景除以标准差操作会降精度，如((_q82812))超分辨率 GNN
					* 实现细节—防除零：LayerNorm 中（防止除以 0 的）超参数 epsilon 要设 1e-5（当时最新版 PyTorch 默认值），(('nbfg0u))换成 1e-6 都会导致训崩
					* 输出再调整统计量：固定（均值 0 标准差 1），逐分量可学（默认情形），外部输入调制
						* 外部调制版本((o3ll25))adaLN，scale 和 shift 根据外部输入的 condition 决定
						* 补回最初正则化消除的统计量信息，((nafg7s))NO 输出层依据最初输入层调整
					* 相关：((nafg7s))NO 网络整体保持输入统计量，通过去除 norm 层或输出层补回
				* 作用位置（残差块中）：pre, post, res-post（SWinV2），sandwich
					* pre-LN vs. post-LN，后者((_nbqf5i))残差连接被抑制、或致梯度消失、从而不易训练，前者((_nbqf6v))表达能力相对受限、效果稍差；{nbqg84}
						* 副本：((nbqh3f))残差层
					* sandwich norm，Grok-1、Switti 用到
					* DeepNorm 改进 post-LN，强化¹残差连接；SRS 中位置((nbqf20))；{nbqf90}
						* 提出于((nbqf8s))DeepNet，其中 Transformer 深度达 1000 层
						* ¹涉及超参数 $\alpha$，选取方式：((_nbqg2o))与权重初始化方式、优化器种类联合选取；{nbqg30}
							* 副本：((nbqg38))参数初始化
				* 场景—注意力 QKNorm，Stable Diffusion 3 涉及
				* 场景—INR 超网络可引入 LayerNorm：((_nb2l12))超网络中引入 layernorm 对性能提升重要（原文针对图片、音频、视频压缩）{nb2l1e}
					* 副本：((nb2l1f))hypernet，((nb2l28))modulation
				* 相关：这里是网络前传激活值的正则化，另有针对数据的((nafg7x))数据预处理-正则化
				* 适用范围，部分场景 norm 会丢信息
					* 物理场尺度信息：NO 若希望尺度信息跨层传播，输入输出不归一化、中间层不 LayerNorm；如((n9gg54))Galerkin Transformer
					* 物理场全局均值信息对部分任务重要，((_q82805))中间层减均值会降精度
			* 大幅激活值 及其抑制（不利于((n7sa8l))低数值精度-指数位不足）{q6j88g}
				* 产生机制—((_q6j881))权重特定取值，训练导致，时机为 MLP 二投影主奇异方向+输入共线
				* 抑制方案—((_q6j887))TWEO 惩罚激活值 lp 范数
					* 相关：((n4sj9i))正则化项-作用于激活值
				* 抑制方案—预处理，激活值数值范围规约：pre-activation 将数值限制到有限区间((o2ga7u))，用于网络特殊模块（FNO FFT 层前）
				* 相关：对象（激活值）若换为网络权重，见((q6nn0s))大幅权重及其抑制
	* 中间层：ResNet 前传随机关闭某些层的((n93a03))随机深度技巧
	* 输入：引入随机的((q8ag8a))输入破坏
	* 用途—训练，前传引入随机性的目的
		* 训练正则化：((n4sk0m))噪声和 loss 正则化项有部分等价性
			* [QiSun]-1812.00174 加噪声可改进 loss landscape 帮助训练；{n37n3x}
				* 相关框架：((n37n3t))loss landscape
		* ((n1bl9k))动力学预测提高稳定性
	* 用途—推理；{p97e7d}
		* 用于构建((n32f2t))随机映射，可进一步用于构建((n3c93q))随机输出函数
			* 进一步：多次独立推理构成((n8qk4m))ensemble，结果求平均可用于((p8nf0w))提高预测稳定性
		* 训推都加：NeuralSDE-1906.02355，NeuralODE 训练推断都加噪声，对输入扰动、对抗攻击稳定性更强
	* 注：这似乎是 NN 训练、loss 的问题，不完全适合这里 架构设计 的框架，不过暂时放这；{n1bl8p}
		* 另可见 ((n1bl8t))adversarial 用途汇总，((n37g9c))训练、优化过程中加噪声方式
	* 相关：提高稳定性可能需以降低精度为代价
		* `2023-01-11`(lectures)张俊杰 仿照量子力学不确定原理 给出一种证明，原 loss 作为坐标表象，动量（loss 对输入的梯度）对应 attack；{n1bm1q}
* 内存瓶颈解决方案：{n3gm7j}
	* ((n32f5j))DEQ=FPN，`[NeuralDE综述-2202.02435]` 内存复杂度均与深度无关，前者 BP 时间也深度无关
	* 深网络前传时存 milestone 层激活值只是时间换空间，且内存复杂度仍高
	* ((n2jj40))可用较省内存的优化器，如 Lion，尤其对大模型
	* 相关框架：((n3gm7v))并行计算（本身也有分担存储任务的考量），((n7pe48))模型压缩，((n7sa8l))半精度
		* memory 的另一含义：((n41958))算法设计中的记忆
* MLP 的变种；{n4tb9n}
	* 注意((n3gd6q))映射未必直接表达为 NN 前传形式（理论上或实现上）
	* INR((o58a0h))
	* 引入残差连接，ResNet((n8kh2l))
	* DEQ=FPN：NN 隐式层，称为 deep equilibrium model 或 fixed point network；{n32f5j}
		* 属于((n2h88d))以算法形式描述的映射
		* 不动点形式表达的隐层；前传靠迭代，反传靠隐函数求导；内存复杂度、BP 时间复杂度均与前传迭代次数无关（优于普通网络）
			* 相关：((n3gm7j))内存瓶颈
		* 最早提出为 DEQ-1909.01377，形如 $z=f_\theta(z,x)$，$z$ 为隐层 activation
		* 叠两个这种不动点层无意义：原文指出这可视为单个合并了的层（复合后的映射）的不动点
		* 若 $f$ 本身来自梯度 $f=z-\nabla_zg(z,x)$，即标量函数 $g$ 的极小点形式，则可参考元学习做法 ((n8jk45))OB-diffOrd，包括避免二阶导和求逆的 `2104.01677`
			* 虽然该形式也可是极大、鞍点，但鉴于 $z$ 靠迭代求出，故不考虑这些不稳定平衡点
		* `(Implicit)2` 认为 CV 对象用函数表征时尤其适用隐式层，因为 full-batch 为所有空间格点、规模小，可存所有 $x$（在上一次 $\theta$ 更新前）得出的不动点 $z$，作为下次 FP 的迭代初值
		* `FNO-DEQ` 认为含时 PDE 稳态解可表达为算子不动点，据此设计 NO ansatz、用 FNO 表达该迭代算子；{n8jh0b}
			* 另见((n8jh0d))FNO-与其他架构联用，((n8jh17))含时 PDE 稳态解
		* 相关：`2022-12-25`(lectures) 动物视觉神经网络有循环结构，难图片识别时间更长
			* （评）生物与人工 NN 区别还包括训练机制，是否 BP
		* 暂无法用于 PINN、元学习，因为目前不会算其二阶导
		* 相关：可能的记号混淆，领域中 FPN 有时表示 Feature Pyramid Network
		* 相关：DGM 用 LSTM 每层都输入一次 $x$，最后一层输出预测值；看起来不如 FPN 高效
	* ELM((n2q94e))或 random feature model，NN 仅最后线性层参数可学，训练靠线性最小二乘而非优化
	* OptDNN((_o4tn18))，设计基于一阶优化算法展开，理论 UAP((n7na7l)) 性质只要求有限（常数）宽度；{o4tn1k}
	* KAN((_o5899x))受 Kolmogorov-Arnold 表示定理启发，网络架构图中节点为求和（不像 MLP 带激活函数），边为可学样条函数（而非 MLP 纯线性函数）{o58a0n}
		* ((_o7e91c))用于 PINN 和 DeepONet
* 微分方程启发的架构设计；{n32f3o}
	* 用 neural ODE 构造深度模型，可参考 ((n35f03))invCtrlRL, `[NeuralDE综述-2202.02435]`
	* 超网络调控主网络前传，方式((n35f0h))类似 ODE 外力项
	* `2021-09-29`(dbGrpMeet) jpf学长讲的普通 ODE 分开瑞士卷的可视化，及相关工作
	* `INSP-Net-2210.08772` 对输入的 INR 作用学出的微分算子生成新 INR，证明了微分算子可逼近（连续）卷积算子，并提到传统图像任务 PDE 方法若时间离散则为相似框架
	* `PDE-G-CNN-2001.09046` 含时 PDE 替换 CNN 卷积、池化、ReLU¹，网络参数量远小于传统 CNN；{n3sf7z}
		* ¹传统非线性换为 PDE 非线性项；应指功能替换，形式不完全等价
		* CNN 交替使用卷积、池化、激活层，在含时 PDE 中((n3sf8r))对应算子分裂
		* 相关：((n3sf84))CNN-等价于物理空间 PDE
		* 替换后可不限于均匀网格离散，((n3sf8e))用作 NO 架构时灵活性更高
	* `Diff-ResNet-2105.03155` ODE 视为特征空间上所定义 PDE 的特征线，加扩散项（batch 中数据点前传时交互）；注意思路中的 PDE 空间为特征空间，而非类似图像的物理空间
	* `RKNet-1802.08831` CNN 时间推进用 Runge-Kutta，DenseNet、CliqueNet 分别对应显式、隐式 RK（每 block 而非每层看成一个时间步，中间层只视为一次 RK 推进的一个中间步）
	* 另有图上 GDE `neuralGDE-2106.11581`？
	* `2022-08-19`(lectures) SyQi 用 ODE 时间多尺度结构推导多设备并行训练算法，AuxNet 等
	* 相关：((n37g6a))ResNet 训练过程中不断增加新的中间层，并适当初始化，其合理性从 ODE 可看出（不断缩短时间步长）
	* `2023-01-11`(lectures)张伟伟 提到：ResNet=前向 Euler，PolyNet=后向 Euler，FractalNet=RK，RevNet=（未记录）
	* 相关：((n3vj74))优化算法-理解为数值ODE
* 特殊网络结构，CNN、RNN、Transformer、INR
	* 其他 CNN 相关：{n37m6h}
		* 卷积算子（针对离散特征图）；{n8hj6h}
			* 作为实现技巧，完成不涉及空间交互的操作：((p8he9w))逐点特征升维，((n8hj6b))patchify
			* 旋转等变处理：卷积核输出本 C 通道，额外旋转结果 concat 到输出中，最终共 4C 通道
			* Clifford 卷积((_o9bg1o))，feature map 各通道取值由 $\R$ 换成特定代数结构
			* 谱卷积((p8hf67))，FNO 涉及
		* 关于图像等的讨论见((n3bg7w))CV
		* CNN 提取特征的有效性判断，((_ncab9u))深度特征分解 DFF 可识别不同语义概念的区域并着色；{ncac02}
			* 相关：((ncac08))解释 NN 工作机制-神经元激活值与输入数据如何关联
		* [CNN架构设计“小插件”](https://zhuanlan.zhihu.com/p/343605155)
		* `[CNN发展历史]` 涉及部分，包括先 1×1 卷积降特征数再 3×3 卷积、恢复特征数，以省参数量；CliqueNet 循环结构不排除可用于其他网络
		* [CCNN-2206.03398](https://www.eet-china.com/mp/a147627.html)（continuous CNN，ICML2022，`2022-07-22`(CSImeet2)）不同分辨率的数据都能作为输入
		* CNN 处理图像（尤其分割等任务）可先将定义域提升至 homogeneous space $M^2$，处理遮挡物体更方便 `PDE-G-CNN-2001.09046`
		* 到原有网格的映射：U-Net，FNO，ViT；可联合如 `FourCastNet-2202.11214` 
		* 图像分类、回归等整体性任务，架构可分 pyramid（如 ResNet，层越深特征空间尺寸越小）、isotropic（如 ViT，特征大小、形状不变）两类 `ViG-2206.00272`
		* CNN 或可等价地换为基于（在物理空间的）PDE 的网络架构；{n3sf84}
			* `INSP-Net-2210.08772` 提到卷积算子可用微分算子一致逼近
			* ((n3sf7z))含时 PDE 替换 CNN 卷积、池化、ReLU，网络参数量远小于传统 CNN
		* CNN 架构本身为图像提供了先验，见((n4uj54))DIP
		* 相关框架：((n37m6d))CNN 与 ViT 比较，((n8hj6h))卷积层
		* 注：图像处理相关文献记录放专有位置，许多未整理到这里，CNN,ViT,ViG 等
	* RNN 与一般 belief states、记忆状态 $h$ 的引入；{n2s979}
		* 对最简单的 NN 引入记忆状态，直接输入、输出均预留 $h$ 的位置即可
		* 对于 U-Net，((_n2s96g))每个尺度的编码器均引入自己独立的 $h$
		* 可能性：刻画记忆时用 Transformer+位置编码 而非 RNN
		* 相关：((o1lh1t))引入状态记忆
	* Transformer；{n37n4q}
		* 相关框架：((n2b94u))NLP 大模型，((n3bg7z))NLP
		* 编码器与解码器，单向/双向注意力；{n38n48}
			* 注意这里“解码器”含义不同于通常，这里实际上仅指单向注意力；{nbih2x}
				* 原始“解码”功能应依赖于((nbih2c))交叉注意力
				* 编码解码相关：((n38n4h))AE 与 AD
			* 最早的 Transformer 论文针对机器翻译任务，同时用编码器、解码器
				* 编码器输入整个输入句子，得（与输入句）同长度的词嵌入
				* 解码器（推断）：循环调用、逐个词生成，每生成一个词时输入之前已生成的所有词（作为网络输入）、编码器输出的所有词嵌入（仅在网络中间部分引入）
				* 解码器的训练：极大似然，输入为 开始符-词1-词2…-词N，输出 label 为 词1-词2…-词N-结束符，前传算注意力时要求每个词仅接受它之前的词的信息（否则当然会导致作弊）
			* BERT 只使用编码器；{n38m9d}
				* 训练用完形填空方式，遮住若干词要求模型预测
					* 其图像版本为 MAE，训练时遮住的是 patch，见((n38n02))
					* 相关：mask token 属于((n4t96h))特殊 token
				* 编码器结构擅长自然语言理解任务（相较只有解码器的 GPT1）{n3bg82}
			* GPT 只用解码器；解码器结构擅长自然语言生成任务；{o2rg8d}
				* 另见((n6te4o))生成模型-离散序列生成
				* 序列并行：((_n7bg5v))用户输入（prompt）可并行处理，模型生成结果只能串行；从而 API 输出 token 单价明显高于输入的；{n7bg85}
					* 可通过 batch 并行缓解
					* 生成加速的一种方式：((_n7bg6x))推测解码，用更小更快的草案模型提前生成多 token，整体输入给主模型判断是否同意¹，token 概率高则接受该结果，被拒则回到主模型逐 token 生成；{n7bg7n}
						* 还可参考((_n91g7i))Andrej Karpathy 的介绍
						* ¹严格表述似为重要性采样((_p4ba08))
						* 副本：((n7bg90))dynamic NN-针对数据的动态性，((n7bh21))AISC-时间多尺度动力学
					* 相关：((n7bg8b))并行
				* 一次同时生成多个 token，((o2rg71))视频 Transformer 时间单向、空间双向注意力，同时生成下一帧的所有 patch token
			* UniLM 同一套参数，按编码器、解码器用均可，也可同时混用（一部分编码器、另一部分解码器）
				* 不过据说有人自己实验的结果是 相较 GPT 没什么优势、某些任务更差
				* 混用做法在多模态任务中出现：((_n8mm73))BEiT3 根据图像生成标题，图像部分按编码器，文本部分按解码器；{n8mm7i}
					* 相关：((n32e71))多模态
			* 更复杂场景（如多模态输入）的注意力掩码机制 可能更复杂；{o2rg74}
				* 如((_nbih26))ICON-LM 同时输入文本、数值数据，文本部分为普通单向，其他部分整体单向、局部双向；{nbih3x}
					* 相关框架((nbig72))NO-符号与数值数据同时输入
				* 针对视频的 ST-Transformer((o2rg71))时间注意力单向、空间注意力双向
				* ((o4hn3w))packed sequence，多条短序列拼成长序列作为 batch 中一条数据，子序列间屏蔽注意力
			* ((_n4se6s))有观点认为 encoder 双向注意力存在低秩问题，可能削弱模型表达能力；而纯 decoder 上三角矩阵保证可逆
			* ((_n4se6w))encoder-decoder 架构好于纯 decoder 可能只是因为多一倍参数
			* 架构区别导致训练方式（待预测对象）区别：((n8mn8t))完形填空vsNTP
			* 线性表征能力，((_n8un7x))自回归（解码器）优于 BERT
		* 位置编码：输入仅为集合，用位置编码表示丢失的序列信息；{n3vf6h}
			* 相关：((n3t97n))INR 位置编码是为了引入高频 feature，((n7de8p))图 Transformer 有位置、结构编码
			* 给出方式：预设，纯学，可训练预设，网络输出（如 SWinV2）
			* 作用位置
				* feature-embedding（绝对位置编码）
					* 预设：加性正弦位置编码（Transformer 最初提出时），concat 坐标（FNO）
					* 纯学：ViT 最初提出时
				* attn-bias，各头独立
					* 预设：Alibi((_n8un5s))，惩罚与距离成正比；各头的惩罚权重为独立超参
					* 纯学：SWin
					* 网络生成：SWinV2 LogCPB
				* query/key，如（预设）RoPE
			* 坐标变换预处理，((_q81007))NO 轴向注意力 RoPE 所用位置由原位置过可学网络给出；{q81b7f}
				* 解读方式：((q81c0f))1D 空间坐标变换 共轭作用于现成函数变换（1D 注意力）得新的函数变换
			* 分辨率扩展：朴素位置编码针对给定的序列全长生成，从而最大上下文长度固定；{o2sn4y}
				* 相关框架：((pbpg0g))变长序列-长度外推
				* 博客总结的扩展上下文长度的方法：((_o2sn4t))绝对位置编码，相对位置编码，RoPE 变种
				* 预设：正弦（如 RoPE）有内插、外推方法，低频偏内插、高频偏外推，一般见 SRS((p6eg30))
					* YaRN 还涉及对注意力 softmax 升温
				* 网络输出：SWinV2 LogCPB，相对位置取 log(1+Δd) 输入 MLP
				* 可学编码插值：((_q15k2p))SD3
				* 相关：((n4pl5i))降低长注意力计算量
			* 其他数据结构（非序列）的位置编码；{n98h2z}
				* ViT 中各 patch 的位置编码是可学习向量（而非手动选取）？信源((_n8dm00))；{n8dm0h}
					* SWin 代码似乎也是，直接用 nn.Parameter 给出，形状 [n_patch, embed_dim]，前传时加到 patch_embed 结果上
					* 固定编码 相对vs绝对，2D RoPE 对 x,y 分别旋转，((_paif9v))据说效果不如绝对编码
					* ViT 解 PDE，((_nahe52))周期、非周期 BC 用不同位置编码（表示的 patch 邻接关系不同），从而非周期问题训练结果可直接迁移到解周期问题；{nahe5w}
						* 相关：((nahe5m))PDE 边界条件
					* NaViT 用 2D 位置编码，可处理不同形状、尺寸图像，编码方案为 fractional + aspect-ratio；{o4hn6z}
						* 论文讨论的三种编码方案见 SRS((o4hn6c))
						* 据说 ViT 用 1D 位置编码效果尚可是因为当时只处理固定分辨率
						* 2D RoPE 也支持变长宽比((_pbpg5g))
						* 图片尺寸泛化另一方案：((_oape2y))引入换行符，视频还有换帧符；从而打 patch 无需位置编码，便于自回归生成；{oape3o}
							* 相关框架：((n6te49))图像自回归生成，((n4t96h))特殊 token-表示数据结构
						* 注意尺寸泛化涉及两方面：((pbpg0g))变分辨率，变长宽比；后者通常依赖前者
						* 相关：((o8fe66))NO-网格泛化-gridsize可变
					* 因子化可学习编码，((_oa5g8r))视频时长、宽、高独立编码 再相加，各维度均可处理不同长度；用于 Meta Movie Gen
					* 动态生成（而非预设or可学），((_paig05))美团 MPE 基于各 token 邻域信息动态生成
				* graph Transformer 见((n7de8p))
				* 点云，((n98h47))PointMAE 将全体点划分为 point patch 并进行位置编码
				* NO 可变散点输入，每点的信息作为一个 token，((n9gf5t))用 RoPE（包括 2D 版本）体现散点位置
			* 实现细节：((_n8un4u))混合精度下位置编码可能发生碰撞（相关：((n7sa8l))混合精度）；{n8un50}
		* 注意力机制及变种；{n7pe3p}
			* 相关：((n3gm7a))一般意义上的注意力（非 Transformer）
			* 其他位置：((n38n48))注意力分双向、单向，((n4pl5i))降低注意力的计算量
			* attn-score
				* 内积变种：SWin V2 余弦注意力，((q15j8k))QK RMSNorm
				* 注意力可加 dropout((o31f1s))
				* attn-bias
				* 这里讨论计算方式；计算结果分析见((q5ra8m))attn-map
			* softmax；一般（对象不限于 token）见框架((o31a0d))加权平均
				* 变种：softmax1（分母 +1），允许总和小于 1（一般框架((q6nm78))次归一化）；{n7pe44}
					* 作用：模型训练((q6nn0s))不再产生异常大权重值，从而更适配数值量化
					* 作用：缓解((q5ra8m))attention sink
				* 计算过程，FlashAttention local softmax 避免一次性计算、存储 softmax 涉及的所有元素
				* Galerkin Transformer 去掉 softmax、所得模型可从数值方法角度解读，两种乘法结合方式下分别对应 Galerkin、Fourier
			* 前后处理
				* 注意力层的 LayerNorm 可((o3ll25))引入 modulation
				* Qwen gated attn((_q16e9j))，据说可缓解((n8lg0c))loss spike
			* KV cache
				* 总容量：层数、token数、头数、嵌入维数
				* 降层数，((_q46f08))shared KV cache，一层的 KV 用于连续多层的 Q
				* 降 token 数，如((na1k7z))滑窗注意力，窗口滑出后存的 KV 可删掉
				* 降头数
					* multi-query attention（MQA）((_nbrm55))，不同头仅 query 不同，K,V 共享；用于 PaLM，ChatGLM；{nbrm6p}
					* grouped-query attention（GQA）((_nbrm53))，对头分组，不同头 Q 独立，K,V 在组内共享、组间不同；用于 LLaMA2；{nbrm5y}
						* 相关：((nbrm0z))分组注意力
				* 降嵌入维数：MLA，各头 K,V 共享相同的 latent code
			* 多头注意力；{na1k7v}
				* 若注意力按序列维度分组（只计算组内的注意力），不同注意力头可使用不同分组方式，如((na1k82))LongLoRA
				* 输出层线性投影 $W^O$ 若为方阵，在单头情形是冗余的（可与 $W^V$ 合并），但在多头情形仍有其功能（对 不同来源的多种混合信息 做进一步混合）；见 SRS((nbdh3b))；{nbdh5m}
				* MQA((nbrm6p))、GQA((nbrm5y))、MLA
				* 放大二头注意力大小差别，((_ocve6y))Difference Transformer；针对的问题：原始 Transformer 长上下文中大量无关 token 的注意力总量大，导致结果对上下文顺序敏感；{ocve7c}
					* 相关：((ocve7k))Transformer 长序列挑战
			* 分组注意力：{nbrm0z}
				* 进行分组的维度
					* 序列，或参与自注意力的所有 token
						* SWin Transformer((na1k7z))，目的为((n4pl5i))避免完整注意力的大计算量，分组方式利用了图片 2D 空间结构（patch 邻近关系）
						* 针对图片、视频的((o2rg6j))轴向注意力，依据坐标轴分组
						* LongLoRA((na1k82))，目的为使 LLM 上下文全长大于位置编码的预设全长
					* 注意力头分组，((nbrm5y))grouped-query attention（GQA）组内共享 key, value
				* 同时引入多种分组方式，避免信息传递局限在组内；分组方式划分的维度
					* Transformer 层：（按 token 分组的）((p4b97v))在不同层用不同分组方式
					* 注意力头：（按 token 分组的）LongLoRA((na1k82))，不同注意力头的分组方式不同
					* 相关：((pbfa2u))集合元素分组处理-多标准
			* 交叉注意力（非自注意力），原始 Transformer decoder((nbih2x)) 涉及；{nbih2c}
				* 可用于构建特殊的 hypernet 架构((n9ha7t))
				* 可用于多模态，((n8jm4l))双路 Transformer 靠双向交叉注意力做信息交互
				* 相关框架，作为影响主网络前传行为的方式，算((p4pb1p))网络旁路输入
					* ((p5kg1c))可视为特殊的 modulation 机制
				* 相关，((p5kf7e))grid-based INR 中网格插值操作视为特殊的交叉注意力，可比较直接基于交叉注意力的 INR
			* 非注意力的信息交互机制
				* 利用 2D 网格结构，((n8hm0w))AFNO 将注意力换为 FFT（有全局性质）后对各点特征过 MLP
			* 工作模式解读：各注意力头仅在少数情况真激活((_q5rb27))，其余情况是在找 ((q5ra8m))attn sink；{q5rb2r}
		* 输入长度问题，原始注意力计算量太大；{n4pl5i}
			* 相关：((o2sn4y))修改位置编码以扩大上下文窗口
			* ((_n39f7h))展望：到万量级只需加显卡，十万 linear attention，百万可能需要 recursive encoding 和增加 long-term memory
				* 线性注意力已在 Genie 视频 Transformer 中用到((_o2rg7c))
			* ((_n4pl47))RMT 机制已可扩展到百万 token（原文的背景介绍还有更多相关工作）
			* ((_n7db7g))针对长序列问题，NLP 社区处理方式有 稀疏化注意力、每层加递归/压缩、局部敏感哈希提取有效注意力
			* 针对有序数据（区分先后，语言等，自回归 Transformer）
				* 利用同样 1D 但不强调顺序的做法，见((p8ae4k))
				* 不靠增大注意力上下文窗口，而是((_o28m7o))将历史保存于模型参数（临时 LoRA 模块）{o28m7x}
					* 相关：((n4t94a))微调
				* RetNet，Mambda 等其他架构；{o4hn5c}
					* 似乎有 Transformer+Mamba 混合架构，Mamba 层 : Transformer 层 大概 6:1
					* ((_o9rk80))MCSD（multi-channel slope and decay），线性复杂度
					* 状态空间模型基于 ODE；推广到 1+nD 维 PDE 后用于替代 ViT 完整注意力：
						* PDE-SSM-2603.13663 反应对流扩散，用于 DiT
						* FluidWorld-2603.21315 对流扩散+局部全局记忆，用于视频预测世界模型
				* 长序列可能遇到的其他问题：{ocve7k}
					* ((ocve7c))大量无关 token 注意力总量大，结果对上下文顺序敏感
			* 注意力范围限制，利用数据的特定结构（主要针对 Transformer 编码器）；{o2cg1y}
				* 序列，1D 结构（无序或不依赖顺序）；包括数据 2D 但做法实质上 1D
					* 局部化，按距离：((_p4b96g))LLM Mistral 滑窗注意力
					* 局部化，按分块：CV SWin Transformer 每层仅做局部¹注意力；{na1k7z}
						* 似乎涉及 patch 的组块（打包），不同层组块方式不同
						* 分组方式理解：图像 patch 视为 graph 顶点，((n7a941))局部注意力可理解为 patch graph 全连接改为按空间位置局部连接，且每层连接方式不同
						* ¹又见((nbrm0z))分组注意力
						* 区域（窗口）限制属于((o8h98r))NO-空间非局部信息交互-非全局情形
					* dilation：grid attention，注意力计算带 stride；MaxViT 提出；{o9pa33}
						* 相关：((p8jh36))下采样后再算注意力，整体网络类似 U-Net
					* 结合距离限制、dilation，((_p4b94r))LLM dilated 滑窗、global + sliding window，Longformer 用到；{p8ae4k}
					* 计算实现，注意力矩阵形态（1D 序列情形）：按距离为 n-对角，按分块为分块对角，加 dilation 为 dilated n-对角
				* 针对图像 patch 的 2D 网格结构，也包括视频、PDE 场的 patch 网格
					* ((_nabh2j))轴向注意力，注意力计算仅限于一个轴，水平、竖直方向依次算注意力；{o2rg6j}
						* 一般框架见((o8h98r))NO-空间非局部信息交互-非全局-沿轴，此处仅记录针对注意力的内容
							* 各轴处理顺序（组合方式）、参数共享、计算结果共享 也在其中讨论
						* 注意力矩阵共享变种，剩余轴所有位置用相同注意力矩阵，以减少 softmax 计算量
							* FactFormer((_pamb8h))基于 Fourier 注意力的卷积核分解解读，各方向注意力矩阵依次作用（运算顺序可交换），处于同一个注意力块中（不涉及独立的 W_V,W_O）{pamg6c}
						* 视频时间、空间（二维）注意力分别计算((o2rg71))
						* AlphaFold2 用了类似操作((_p7nf21))，同序列跨残基、同残基跨序列 分别算注意力更新
				* 更多见((o8h98r))NO-空间非局部信息交互-非全局情形
				* 补偿方式，注意力加范围限制后不完整，需补全
					* 深网络信息扩散：固定距离滑窗，经过多层注意力自动扩展到全局((_p4b99k))
					* 多方案混用：每层用不同限制方案，注意力范围在不同层不同；{p4b97v}
						* 分块方式：图像 SWin((na1k7z)) 不同层分块方式不同；AViT X、Y 注意力层交替
						* 感受野大小：语言 ((_p4b97a))Longformer 浅层小滑窗，深层加 dilation 扩大感受野
						* 范围类型：MaxViT 不同层交替使用窗口注意力、grid 注意力、CNN
						* 相关框架，属于((nbrm0z))分组注意力-按层分组
					* 引入少量全注意力：有语言模型混用滑窗局部注意力层、全局注意力层，比例 7:1 交替（信源：2025-10-29 组会yp）
						* 5:1 比例，((_q46e8u))Gemma4、MiMo 采用
				* 相关框架：((p8ag76))集合元素分组处理
			* token 打包降总数（主要 Transformer 编码器）{palf57}
				* 一般框架为((p8ag76))集合元素打包处理，此处仅讨论只适用于注意力计算的内容
				* 无结构纯学习方案 eg. ((o2cg0f))Transolver 对 meta-token 算注意力
				* 利用结构-1D均匀网格-下采样：ViT 中间层对特征图下采样，整体网络结构类似 U-Net；{p8jh36}
					* SWin 即有下采样层，最初用于全局分类任务、与该类 CNN 同理
						* 后续工作重新引入上采样层而成为类 U-Net 架构，如((_p8jh2v))盘古气象、Poseidon
					* 与 U-Net 区别在下采样目的，U-Net 用于远程信息混合（因卷积局部性），ViT 用于降计算量（注意力本就全局）
						* SWin 也有远程信息混合目的
					* 相关：若不是下采样而是分组算，属于((o9pa33))grid attention
				* 利用结构-1D均匀网格-二次 patchify：P3D((pbfa5h))划多窗口，各窗口抽象出单 token 算注意力
				* 模态转换技巧，DeepSeek-OCR 文本渲染后按图片编码
					* 效果：据说 token 数量减 10 倍时性能基本保持，略牺牲性能可再提高压缩比
					* 可变压缩率：利用序列顺序结构，久远内容提压缩率，模拟人类记忆模糊机制；{pam004}
						* 自回归 PDE 基础模型中((_q2jb25))久远 token avg-pool，使系统近似 Markov
						* 相关：((p8ag76))集合元素打包处理-划分类数-可变压缩率，((n7mj1w))动力学-时间分解-输入-多步
					* 原文未测长文本任务，同期智谱 Glyph 则认真测了 LongBench，缩短上下文长度的同时性能还提升了，推测是太大的未压缩 token 量本身会干扰模型（来源：2025-10-29 组会yp）
			* 其他，利用数据特定结构（主要 Transformer 编码器）
				* 1D 结构 换掉注意力机制，((n9291a))用频域 MLP（需长度固定），FFT 复杂度 nlogn 低于平方；AFNO 用到
				* graph Transformer 有声称关于顶点、边数线性复杂度做法((n7de86))
			* Transformer 视为完全图 GNN，从而利用有结构数据的办法降低注意力成本
				* ((_o2cg0z))GNN 社区有划分二部图做法，使 NLP 句子图稀疏化
				* 可能性：改为多尺度图、小世界网络¹等，可考虑动态²连边
					* ¹小世界网络算分布式抽象层？向量数据库里已有“分层可导小世界”技术方案
					* ²可依赖于输入、前传层数等，见一般 dynamic NN
		* FFN 部分；{o3ll7n}
			* 不同 FFN 处理不同模态 token：((n8jm4l))对多模态模型，VLMo 各模态有独立 FFN，而注意力共享
			* 引入外部 modulation：((o3ll25))FFN 层的 LayerNorm 引入外部 modulation
			* 激活函数：((o3198n))GLU
		* 机理解读
			* 理解方式：集合/图/序列数据的翻译；序列通过引入位置编码成为集合，集合视为全连接图；{n4295u}
				* 相关：((n4296a))仿TRIZ原理-顺序结构平行化，包括映射 set2set 化后用 Transformer
				* 深度势能大模型 DPA-1 中的 Transformer 就只考虑邻域原子的注意力，不是全连接图？
				* 相关：((n3qn83))NO-输入形式-集合输入，包括 DeepSet 等方法
			* `Sinkformers-2110.11773` 机制解读与改进算法，考虑无穷深、无穷 patch 平均场
			* `[Transformer为完全图GNN]`，包括二社区处理长序列（大规模图）的方式，多头注意力理解；{n79n3o}
			* ((_n3ud8m))Transformer 作为图灵完备NN
			* 内部多子程序在运行，((_q6k811))文本复制、基础逻辑推理，乃至梯度下降（> 上下文学习？）
			* UAP 定理，((q8dl37))等变网络 可被 无位置编码 Transformer 编码器 一致逼近
		* 变种—条件引入，DiT 讨论的 4 种方式：((_o3lk9b))in-context condition、cross attention、adaLN 及其零初始化版本 adaLN-Zero；{o3lk9o}
			* 相关：((n4te8y))生成模型-条件生成，((nbih2c))cross-attention
			* adaLN-Zero（DiT 原文效果最好）：基于 pre-layernorm，Attn、FFN 层分别进行该操作；{o3ll25}
				* 相当于((n2il89))scale+shift modulation，或 ((nbqg84))LayerNorm 的变种
				* 注意在残差连接前又引入了一次额外的 scale
				* 发现 modulation encoder 输出层权重适合用零初始化（相当于 Transformer 初始化时残差块为 identity 映射）；{o3ll1o}
				* 相关：((_o4ha53))StyleGAN 中已使用 adaLN，视觉自回归模型 VAR 也沿用
				* adaLN 还用于 PDE 通用模型 Unisolver((_o6ch7i))，调制信息按来源分两类，一类的调制信息对各 patch 独立，另一类对所有 patch 共享
				* RMSNorm 版本也保留 shift，(('patm9f))MovieGen 视频生成用到
			* in-context condition（外部条件并入已有序列）可视为((n4t96h))特殊 token；{o3ll9f}
				* 相关：((n4ff89))in-context learning
				* 变种((_q15j34))MM-DiT，条件、图像 token 一起算注意力，但 Transformer 参数独立
			* cross-attention 额外计算量较多（其他的都不多），对更复杂的 condition（如文生图）更适用
			* 多种同时用，((_q15j3b))上下文基础上，条件表征额外用于生成 AdaLN 调制
			* 条件预处理：多种编码器同时用，结果可((_q15j48))按通道or按 seq-len concat；{q15j6j}
				* 可部分缺省：推理要求不严时可去掉高成本编码器，有相应训练配方；各编码器独立 drop
				* 相关：((n3t95j))仿TRIZ原理-输入重复
			* 相关框架，影响主网络前传行为的方式，((p4pb1p))网络旁路输入
		* 变长、变分辨率（序列、图像）训练策略；{pbpg0g}
			* pad 到最大长度：batch 内不同序列需要同长度，若原本长度不同，需补全；见((o4hn3h))pad token
				* 针对 graph 的 Graphormer 涉及补全虚拟节点、设 attn_bias((_n7tm0t))
			* 长度外推（微调等）
				* 位置编码-长度外推((o2sn4y))
				* 按原长分组：注意力原长 L，更长输入做分组注意力、各组长 L
					* 微调的 LongLoRA((_na1k7c))引入分组注意力，将全序列划为多组，只计算组内注意力（组长度为位置编码全长），二个分组方式恰好错开以保证信息的全文流通；{na1k82}
						* 相关：((na1k7v))多头注意力，((nbrm0z))分组注意力，((na1k7z))SWin 滑窗注意力
					* 注：相关手段链分析尝试见(('pbpf7i))
			* 各样本长度差异大情形 提高训练效率
				* 减样本量—pack 到最大长度，((o4hn3w))多个短序列拼接
				* 控序列长—短序列多训，先短序列充分训练，再逐步增大序列长度微调；{pbpg3s}
					* 据说 DeepSeek-V3 预训练始终 4k，之后两阶段 YaRN 依次扩展至 32k、128k（各 1k steps，后一阶段 bsz 减小），扩展成本比预训练小一个量级以上（信源为腾讯元宝 DeepSeek-V3 联网版）
					* VAR 类图像生成((_pbpg1z))多阶段，分辨率 256、512、1024
					* 相关：课程学习((n7d931))，其主要考量是直接训难任务学不到东西，而这里是考虑训练性价比
				* 控序列长—分桶训练：((_pbpg3u))不同长度（包括图像长宽比）数据按桶分类，每次取 batch 先选桶再采数据
				* 控序列长—分卡训练？部分卡处理短序列、用大 batch-size，部分卡小 bsz 长序列
			* 相关：((o4hn6z))NaViT 变分辨率外还变长宽比
		* 实现时的细节考量
			* BERT 训练引入了 dropout((n37m4y))，但 GPT3 没有
			* 为提高泛化能力 ((o31f3k))自监督预训练-基于破坏重建
			* ((_n7hk73))据说 MoE 为提高模型效率、容量的常用技术；{p8c969}
				* 一般 MoE 见框架((p8ai85))多元素对象处理-分类处理、不同元素过不同网络
			* 小型 LLM 深度比宽度重要((_o39l20))；{o39l2g}
				* 相关：((n77m1x))scaling law
			* 网络初始化，BEiT 的做法((n8kd9u))参考价值待考察
			* 训练早期 warmup((_n8kh0j))，先用较小学习率预热；如 ((_n8kh0p))BEiT 用 10 epoch（总 800 epoch）；{n8kh0w}
				* DeepNet 作者认为只治标，调模型架构才治本
				* 另见((n8kh0z))优化-NN训练技巧
			* 输出层 lm-head 在 SFT 阶段对少见词汇可能漂移，((_q5am6c))构造内容抄写类合成数据，以确保生成频率下限；{q5am6j}
		* ViT；{n37m6i}
			* 相关框架：((n37n5d))CV大模型，((n3bg7w))CV，打 patch 做法涉及((n4rm8d))tokenize
			* 与((n37m6h))CNN 的比较；{n37m6d}
				* CNN 更关注纹理，Transformer 更关注形状((_n37m5o))
				* `[图像分类可复现性-2203.08124]` ViT 可复现性不如常见 CNN
				* ((_n37m5v))卷积擅长提取细节，注意力善于把握整体；卷积要求多层堆叠，注意力要求大量数据
				* ViT 与 CNN 的结合尝试，可见((_n37m62))；另外 TransUNet（2021-10-29 CSI讨论）也是例子
				* CNN ResNet 的 loss landscape 与其比较，再考虑预训练、对抗稳定性训练后的影响((n37n18))
				* 有观点认为 CNN 引入的先验结构更多，((n3ag7v))数据较少情形优于 ViT，但数据足够情形反之
					* 等变性先验，((n3ha7e))训练后 ViT 等变性更好，CNN 等变性被 alias 破坏
				* 另：((n8hj6b))ViT patch 的 token embedding 可用特殊卷积实现
			* 预训练的 mask out 策略，MAE 等；{n37m7m}
				* 相关框架：属于((n8mn5a))自监督 范式之一，包括局限性讨论
				* 方法例子：MAE，BEiT
					* 据说二者区别，BEiT 只预测被 mask 的 patch，MAE 预测完整图片的所有像素（包括未 mask 的，似因为有 decoder 结构所以 nontrivial）
					* 据说还有 iBOT，CAE，SimMIM
				* 理解方式
					* `2202.03670` 从 kernel learning, low rank approximation 等角度解释 MAE
				* 仅作用于网络输入时见((q8a88n))输入预处理-maskout
				* 相关框架：((n37n1v))预训练，((n9999m))网络(输入,输出)≠ 数据(样本,标签)
				* 一般的 mask 自监督预训练，不限于 CV；{n8mn72}
					* 用于 NO 预训练：((_o6nf9d))，综述实验性质的((_o73k0p))
					* NLP 领域的((n38m9d))BERT，只用编码器、完形填空方式训练；{n38n02}
					* 图文联合 mask，((n8n89i))BEiT3 涉及
					* 点云局部信息聚合后称为 point patch，有((n98h1e))PointMAE
					* mask 方式区分完形填空、NTP；{n8mn8t}
						* 为训练方式（被预测对象）的区别；相应架构区别((n38n48))单向、双向注意力
						* 完形填空 mask ratio 问题：信息密度；{n8mn9f}
							* BERT 训练遮住了 15% 的词，MAE 则遮住了 75% 的 patch；原文解释说自然语言的信息密度更大，大部分词语都有具体意义，而图像的冗余信息要多很多
							* 多模态模型：((_n8n87u)) BEiT3 单模态文本 15%，图像 40%，图文联合任务 mask 50% 文本；{n8n89i}
						* ((_n8un7p))Ilya 认为 NTP 任务难于完形填空（均只考虑最困难的任务）
							* `2309.06979` NTP 理论分析框架，认为该预测器“可近似图灵机有效计算的任何函数”
						* 完形填空任务转化为 NTP，((_n9na97))CM3Leon 通过引入特殊 token 达到；{n9na9e}
							* 相关：((n4t96h))特殊 token
						* 表示未知、待预测部分的方式不同：完形填空用特殊 mask token，NTP 用序列位置（过去已知、未来未知）{nabe20}
						* NTP 有特殊 token 表示序列开始、结束((nc6l0q))
						* 自回归范式相关：这里视为预训练方法、与完形填空比较，还可((o42g70))视为生成方法、与扩散模型比较
					* mask data modeling 的能力：((n8n88x))BEiT3 预训练中放弃对比学习、图文匹配 loss，一律按 mask 范式自监督训练
			* 相关解释，包括理论、实验；{n7na8n}
				* （旧）`2021-07-02`(MRmeet) 导师推测 Transformer 在 CV 的应用或许有限
					* NLP 中可以定义无穷多的任务，这是它与 CV 的最大区别；当然可能也不绝对
					* `2021-09-29`(CSImeet) 不太清楚 Transformer 用于图像的合理性：相当于打成二维 patch 序列（对应 NLP 单词序列），但是句子里单词顺序不可变，图像 patch 顺序可变
					* 如果导师自己做，可能会用类似 DeepSets 的顺序无关的 Transformer 版本
				* 实验解释：`2022-03-11`(CSImeet2) 预训练、对抗稳定性的作用，loss landscape 与 ResNet、MLPmixer 比较，及如何被预训练、对抗稳定性训练影响；{n37n18}
					* 相关框架：((n37n1v))预训练，((n2bg0w))对抗训练，((n37m6d))CNN ResNet vs ViT，((n37n3t))loss landscape
				* multi-head 注意力机制相当于同时关注图中多个部分((_n37m5t))
				* `2022-06-10`(CSImeet2) 导师觉得 MLP Mixer((o8gg6g)) 动机、可解释性好于 ViT（虽然 ViT 也有解释）{o8gg6h}
				* `Sinkformers-2110.11773` 从理论上解释了 ViT 单样本无穷深前传动力学形如梯度流，进一步无穷 patch 平均场极限为 diffusion；{n7na8o}
					* 相关：((n32b5g))一般 NN 架构解读
				* 关于近似等变性（平移），((n3ha7e))非等变因素主要在开始的 patch embedding；提高等变性程度更适合((n3ha7h))靠训练而非架构
					* 相关：((n7a92o))图像 patch 作为图顶点
			* 相关综述：2012.12556 "A Survey on Vision Transformer"
			* 变种与（针对特定任务的）架构设计
				* 输入层 patch projection 导致训练不稳定；{p6ga15}
					* 换为 CNN 后（保整体计算量不变）性能更好((_p6ga0o))
					* 固定 patchify 部分参数（不参与训练）为早期应对方式((_p6ga0u))，MoCoV3 用到；{p6ub6i}
						* 相关：((p6ub6n))网络参数随机生成、不参与训练
					* 一般框架见((p3va9b))空间分辨率压缩-逐 patch 压缩
				* `FourCastNet-2202.11214` ViT 内部注意力换为 FNO（保留二维排列），用于天气预报；{n8hm10}
					* 架构源于 AFNO²，((n8hm0f))此时各 patch 信息混合不再靠原始注意力，而是 FFT¹后对逐点特征过（单隐层）MLP；{n8hm0w}
						* ¹不同 patch 在中间层对应 feature map 不同像素
						* 替代原始注意力机制的好处：((n9291a))长序列处理能力
						* ²AFNO 当时用于图像分类任务，FourCastNet 作为后续工作才考虑科学计算
						* 另见((n7pe3p))注意力机制变种
				* 引入旋转等群 equivariance 性质，SE(3)-Transformer `2022-09-02`(CSImeet2)；{n3ha70}
				* SWin Transformer((na1k7z)) 注意力计算局限于局部，每层限制方式不同
				* 前传改为 conditional 版本((o3lk9o))
			* 实现时的细节考量
				* `2022-12-14`(dbGrpMeet2) position 和 token 的 embedding 应该分别做 layer normalization，而不是 concat 之后一起做
				* 位置编码可学而非手动指定((n8dm0h))？
				* 代码实现：((_n37m94))各种视觉Transformer的PyTorch实现合集
				* 模型较大时，为缓解((n3891i))过拟合，可((n37m4y))引入 dropout，尤其是((_n38a01))late dropout
				* patch 尺寸，((_n8dm0o))的实现用 9x9 patch，边缘部分对图像用 0 填充
					* ((_o3lm1h))DiT 中减小 patch 尺寸、增大模型参数量 效果类似，均增大计算量、提升生成效果；{o3lm1o}
						* 相关：((o3lm25))scaling law
				* 网络初始化，((_n8kd9c))BEiT 缩放系数与所在层的深度有关（并认为这对大规模训练的稳定性重要）{n8kd9u}
	* GNN 与 graph Transformer；{n79n3g}
		* ((_n79n2w))Graphormer，相比纯 Transformer 额外引入 顶点重要性、顶点两两空间相关性、边信息 编码
			* Graphormer 架构的一般性：((_n79n2q))GCN，GIN，GraphSAGE 均作为其特例
		* GraphGPS 宣称实现计算复杂度 $O(N+E)$((_n7de83))；{n7de86}
			* 相关：((n4pl5i))Transformer 处理长序列
		* 图结构的刻画涉及位置编码 PE、结构编码 SE，((_n7de8h))二者均可分 局部、全局、相对 三类，有表格汇总；{n7de8p}
			* GraphGPS 原文据此设计了新的 graph Transformer 架构
			* 相关：((o9aa14))全局、局部、单点，((n7l966))图的频域，((n98h2z))一般数据结构的位置编码
			* 位置编码
				* Graphormer 依据顶点的 degree $h_i^0=x_i+z_{d(i)}$，各 $z$ 可学；有向图区分出入度((_n7om1j))
					* 可能性：用 page rank 等高级位置编码((_n7om1v))
				* 针对 DAG（有向无环图），((_n7om12))可记录各顶点深度，据此用 sin,cos 生成位置编码（((_n7om4t))用了相同策略）
				* 有做法用 Laplace 位置编码
			* 相关：GNN 消息传递机制同时使用 局部、全局 版本((_o9aa0h))，分别基于图卷积、图 FT
		* Transformer 注意力计算，Graphormer 依赖于顶点距离，另有((_n7om6a))只计算 k-阶邻居的注意力
		* ((n79n3o))普通 Transformer 可解读为完全图的 GNN
		* graph 生成，((_p16e70))不是生成邻接矩阵，而是生成表示图的序列，划分节点集、边集
		* 图映射，应满足置换等变性条件叙述见((q8dl4c))，对 Keriven–Peyré 型高阶等变 GNN 有 UAP 理论
		* 图映射一般形式 $\R^{n\times n}\times\R^{n\times d}\to\R^{n\times c}$
			* 置换等变性((q6o70c)) 要求 $(A,X)\mapsto Y$ 则 $(PAP^\mathrm{T},PX)\mapsto PY$；{q8dl4c}
				* UAP((n7na7l))，Keriven–Peyré 型高阶等变 GNN 满足（中间表示允许高阶节点张量）1905.04943
		* 注：许多 GNN 内容留在独立文件
* 自动架构设计，AutoML 相关：{n4tc1l}
	* AutoGluon 为 Amazon 开发的一个平台，自动生成一个较好的模型 `2022-03-16`(dbGrpMeet2)
	* `2022-03-18`(CSImeet2) 大模型训练的 $\mu$Transfer 有提到相关内容
* 训练稳定性可通过适当设计架构提升((n8kg7r))；{n8kg86}
* 上面这些做法可联合，如 `2021-12-03`(CSImeet2)“DeepONet+FNO”，似在组合 hypernet 和人为设定变换
