* 注：2023-01-12 从 compTree.md 拆分
* 数据同化（data assimilation）{ncmk5t}
	* 传统方法如 `[Kalman滤波]`；HMM（隐 Markov 过程）也属于该框架
	* 这里默认含时问题，不排除不含时问题也有“数据同化”说法（此时可能像反问题）
	* 区分是否已有精确物理模型；若有，为强版本数据同化，未知性仅在于初值；若无，为弱版本，初值与动力学都未知（或说不完全已知）`2023-02-23`(CSImeet3)“n32a40”
	* `2022-09-09`(CSImeet2) DL 做法的 smoothing, filtering 两类，主要关注后一种
		* 演化算子可利用已知的部分动力学，例如用 PDE-Net
		* 仅有部分观测（某变换结果）且有噪声；在真实状态空间表达演化算子，离线预训练（loss：能从预测状态恢复真实观测），并根据动态出现的数据在线 fine-tune
		* 注：之后可专门讨论 loss 设置，包括离线、在线两阶段
	* `2022-09-28`(dbGrpMeet2) 数据同化 overview，包括一般框架（涉及的各量记号）、传统方法、DL 方法
	* 注：只恢复状态初值（而不是当前状态）则算反问题框架 `invDP%`，不过许多方法可互通
		* 共同点包括 ((n32b4n))数据量不足的解决，结合观测与先验
		* 如 `2206.00711`，`DeepM&Mnet-2009.12935` 将自身当作数据同化框架，虽然我只按反问题理解
	* 应用例子包括天气预报；((n77k4o))RL状态空间观测不完全（虽然更多是引入历史记忆的黑箱做法）
	* 不完全同化，用垂域专家模型适配特定场景预测需求：修正模型，输入 全空间历史参考值、散点最新观测值，输出散点/全场 的恢复/预测值；印象中阿里八观气象模型涉及
	* 动力学与观测性质
		* 动力学性质、已知程度同 ((o5sm3k))singleDyn
		* 过程中动态新增观测，新观测质量（同 ((o5sm3k))singleDyn 初值观测质量）
		* 过程中动态新增观测，新观测在何时获得：
			* 持续获得
			* 间隔固定时间获得
			* 随机时间点获得
			* 无新观测
		* 另：可能同时有多种观测途径，每种观测质量、获得时间都不同
	* 时间演化算子设计可能性：同 ((o5sm3k))singleDyn
* 控制问题、RL 记录于((n3bg3a))
* 注：另有动力系统分类问题，做法包括 (1) time-dealy embedding+TDA（见 2020fall 我的组会）, (2) Koopman 算子谱 $\lambda(\mathcal{K})$，类似 graph laplacian（见 ModRedNotes KoopmanReservoir）
* `invDP%` 单个 inverse design/problem 寻找最优参数 $\lambda$，优化目标 $G$
	> 相关记录多了之后，按照 $\lambda$ 形式等分类；
	> 如果 $\lambda=\lambda(x)$ 为系数场，可以使用另一个 NN 参数化，或者用传统的格点、基底表达也行；
	* 狭义 invDP 场景，基于 PDE：{n7ul1z}
		* 典型应用场景
			* 综述文章((_n7ng1u))，分别讨论了 invP,invD
			* invD：`2022-02-15`(ADmeet)“optical computing”设计光学仪器完成 CNN 浅层功能
			* ((_ockf1p))列举了 固体物理、流体、生物力学 中多种由 AI 处理的反问题场景
			* invD，((_paf91y))地下储层压力控制，向一钻井注水、使另一钻井提取石油效率最高、最可持续
		* invD 也称 PDECO，该观点对 invD 问题求解启发见((n43l6g))
		* 反问题视为特殊 invD 问题：取优化目标 $G=l(O(u_\lambda),O^*)$ 为与真实观测的距离¹
			* ¹距离选取，可用 l2 error，尤其是假定噪声为高斯分布时
			* 此时 invP,invD 数学形式差别不大，但问题理解方式仍有不同：$\lambda$ 是待估计的客观对象还是人为设计的结果
		* 可有{额外非设计参数} $\mu$，目标形如 $G=\mathbb{E}_\mu G_\mu$；也可能为 $\min_\mu,\max_\mu$；{n43e25}
			* 注：术语问题，部分文献中((_o2dg0v))“设计”指评估特定设计在多种工况下的性能，区别于“优化”
			* 如机翼设计，$\lambda$ 翼形，$\mu$ 工况（攻角、马赫数、气温等）
				* 工况维度 5–10 个（来流、攻角、速度、高度、侧滑角、舵偏角 等），需对其遍历以确定“飞行包线”，最终要算 1e4–1e5 个状态点（来源：PDElargeModel/scenarios.md NNfoil-C）
			* 可包括 EIT 等反问题情形，一般框架为((n85b0b))边界观测算子推断内部系数场 均可改写为这里的 invD 设定
				* 例：EIT((n7ul5h)) 中 $\lambda$ 为内部电导率场，$\mu$ 为边界电压（方程 BC），$O$ 边界电流；{n85a4g}
				* ODT 的设定待考察((n23a0r))
				* 与原始 invD 问题一处不同，其中 $\mu$ 为客观需求给定；而这里 $\mu$ 可自选（包括分布、采样点），若是观测条件受限（或只能拿到已测好的数据），可能还遇到采样不足¹的问题
					* ¹属于((n85b70))信息不足导致的病态，处理策略包括 引入先验
			* 机翼设计中工况 $\mu$ 为客观需求给定，而 EIT 中 $\mu$ 可自选，类似于 RL 加持 CT 扫描的设定
			* 做了相应实验的有 `HyperFNO-NIPS22` $\lambda$ 为实向量、$\mu$ 为场
			* 相应正问题计算有特殊方法 `paramPDE%`“混合结构参数”
			* 若 $\mu$ 分布可变，属于框架 ((n7vn70))invDPs
			* （非科研）相关：`方法有用性讨论中语言模糊性`“n13b4y”工具、软件制造，非设计参数包括用户需求、能力，要对用户能力取期望、极大还是极小
		* 根据边界观测算子推断内部系数场，为反问题中重要大类((_n7ul2l))；{n7un1l}
			* 数学框架：$F^{-1}:L(G(\partial D),H(\partial D))\to A(D),\Lambda_a\mapsto a$
			* $F^{-1}$ 的存在唯一性等已有理论保证
			* 该文献针对(a)EIT、(b)散射波反演、(c)辐射输运与光学成像、(d)FWI 均给出了(1)PDE 形式，(2)各函数空间 $G,H,A$ 形式，(3) $F^{-1}$ 良态、稳定性理论相关的研究结论
				* (a) EIT，之前的笔记有记录各种算法（MR.md）；{n7ul5h}
					* 我还设想过((n85a4g))转化为 含非设计参数的 invD 来求解该问题
					* 考虑边界观测算子无法完全采样、只能拿到少数几个 $g$ 情形：((n4uk2e))DeepEIT 引入 DIP 先验 + TV 正则化
					* 端到端预测（输入 DtN map eval 的若干数据对，输出预测的阻抗系数场），((_ock96n))基于问题的数学理论结构设计 Transformer 架构
				* (b)散射波反演，正问题为((n7ul5b))Helmholtz；{n7ul5d}
				* (c)辐射输运与光学成像，((_n7ul4r))方程涉及碰撞项 $Q[u]$，出现积分；{n7ul4w}
					* 碰撞项也在 Boltzmann 方程出现((n7ul4f))
				* (d)FWI（full waveform inversion）；{n7ul3d}
					* 如 `Siren-2006.09661`“n2ib0q”
					* 用途包括地震学恢复介质中结构密度 `2206.00711`“n2ia37”
					* 数据集 OpenFWI，在 FourDpONet-2305.17289 中使用；{n93l24}
						* 相关：((n8i96v))科学计算数据集
			* 据该算子形式设计的求解网络架构，((_n85f3x))NIO 包含多个模块，不同部分形式分别成为 DeepONet、FNO 类似的架构；{n86k17}
				* 其他根据该形式设计求解的 NN 架构：((n86k0e))针对 EIT，应乐兴似将边界观测算子先转化为积分 kernel，再建立它到 $a$ 的映射
					* 这里涉及((n4fe6y))NO 输入、输出场在不同定义域，该架构处理办法的解释见((n86k0e))
			* 以下为关于该问题的一般记录（不限于该文献）
			* 相关具体例子还包括 ODT？
			* 考虑将映射 $F^{-1}$ ansatz 表示为((n5bl9s))loss 极小点形式（非直接前传的隐式形式），从而只需用到 $F$ 的显式 ansatz；{n85b0b}
				* 设观测算子 $h^o\in L=(G\to H)$，机理（$F$）给出 $h^m:A\to L$ 即 $A\to G\to H$
				* 隐式 ansatz $\min_a\|h^o(.)-h^m(a)(.)\|_L$
				* 若进一步取 $L$ 上基于期望的范数，问题成为¹ $\min_a\mathbb{E}_g|h^o(g)-h^m(a)(g)|$，则成为((n43e25))带非设计参数的 invD 问题
					* ¹现在的距离在函数空间中求（仍涉及 $\mathbb{E}_{x\in\partial D}$），不过复杂度已低于泛函空间 $L$ 的距离
				* 若对 $g$ 采样不足（客观数据限制等），见((n85b70))信息不足导致的病态，处理策略包括 引入先验 等
		* $\lambda$ 合法取值范围可能小于全空间，并且具体范围未知（否则可 AD 等刻画）
			* `DeepM&Mnet-2009.12935` 考虑了该反问题
			* 所有可能取值 $\{(\lambda,u)\}$，二分量可相互确定对方；常见反问题框架 $\{\lambda\}$ 占满全空间而 $\{u\}$ 不占满，这里可能二者都不占满
			* 可改解读为关于 $(\lambda,u)$ 二分量的不定方程，反问题根据部分分量的部分散点上观测值恢复完整解；形如限制在解流形上的约束优化问题
			* 同上可以有其他参数 $\mu$
		* 观测形式，作为求解信息来源（反问题、控制）；散点观测（涉及散点采样）、间接观测等；{n7un6b}
			* “散点”所在空间：物理空间，还包括其他非设计参数空间等
				* 物理空间散点观测，观测到点取值、局部平均等，对应传感器放置位置
				* 方位角空间，如 CT 照射角度
				* 频域空间采样，如 MRI（见yp用 RL 选 MRI 频域采样哪些列的文章？）
				* 非设计参数空间，如 EIT 选若干个边界电压，相当于作为非设计参数的 $\mu$ 取值
					* 这个例子中 非设计参数空间为有限维空间（若不考虑测量位置选取）
					* 不过这个情况可能采样成本不高，多采样许多次也无妨？
			* 散点位置分布，包括选取（采样）方式：
				* 给定粗糙网格，超分辨率问题涉及
				* 给定散点，无需优化；采样位置可能来自问题的客观约束
				* 散点集合预先（离线）选定，其位置靠离线优化获得
				* 散点逐个动态（在线）生成，涉及((n31m8a))active-learning，((n77k4o))RL-不完全观测；{n1gg8g}
				* 主记录位置：((n81f1z))采样依据-事先决定or动态选取
			* 含时场景，每空间点观测获取的时间分布：
				* 时间位置：所有时间点，均匀间隔时间点，时间散点
					* 时间散点区分 预先给定、非受控随机生成、离线主动选取、在线主动选取
					* 时间观测有限性 可源于成本约束而非客观条件约束，类似 CT 照射角度
				* 时间性质：瞬时值，短时平均，整体平均（单空间位置仅有单个观测）
				* 不同空间位置协同性，观测时间统一与否
				* 时空散点观测 情形，空间位置多，但多数点只有一次观测；是否存在对应的实际场景？
			* 可观测变量：方程涉及的所有变量，仅部分变量，仅间接变量（如示踪粒子）{p4jl27}
				* ((_p4jl07))传统 CFD 不易融合多变量信息，不如 PINN 灵活；压强、速度、示踪粒子轨迹等
			* 相关：观测有限性还见于((n77k4o))MDP-部分可观测情形；信息不足导致病态，可能解决思路见((n85b70))；((n8jb6f))INR 训练涉及的物理空间采样
	* 广义 invDP 场景（或需整理）：
		* 未知机理识别：要求解正问题，但有部分机理不清楚、需结合数据的情形，可能用类似于反问题的框架处理
			* ((_p4jk80))PINN 结合散点观测，推断未知参数、补全方程项，同时得数值解
				* ((_p4jl1t))边界条件也可能未知；传统 CFD 不易处理
			* `DeepM&Mnet-2009.12935` 应是类似的设定
			* 这里求解目标是未知的物理场；若要恢复的是机理（补全方程形式，或仅若干参数取值；也可包括 ((ncmj8o))constiRel）则算另一类反问题
		* 数值 BC 设置：已知动力学在有界区域计算，但不知如何引入数值边界条件，引入方式算反问题；{p8jg5m}
			* 相关：((ncmj8o))降阶建模（限制到子域），((p8jg4b))无穷区域在有穷区域计算，((n3sg21))PDE 内部已知边界条件未知
			* `DyAd-2102.10271`（其内的 link 里我有更多说明）学出数值 padding 方式
			* 或可将引入方式作为元学习任务 numBCasMetaL:
				> 2021-12-29 讨论提到
				* 大区域上的方程，算力有限在小区域算，需确定数值边界条件
				* 满足数值边界条件的函数组成子空间（注意定义域含时空维度），可用 AD 表达该子空间，$Z$ 参数化
				* 在元学习里相当于学泛化能力，元学习目标类似抗噪，可用相同方式元训练
					* 这里真实标签不难生成，直接在大区域上算就行
					* 元学习目标为找出合适的边界条件（隐式刻画即可），常规学习目标为求解含时方程
				* 这个地方好像单任务即可，从而更像超参优化而非一般元学习；此时可考虑使用反问题框架处理 `hyParamAsInvP:`
			* 如果退化为单任务，则成为超参优化问题，可用反问题方式处理 `hyParamAsInvP:`
		* 控制：最优控制问题相当于 $u,\lambda$ 都含时的那一类 inverse design，含时特点导致优化方案有一定不同（与 PDE 求解同理区分含时问题）
			* `QuaSiModO-2102.04722`(AISC2) 动作取值限制为完整动作空间的几个代表点
			* 另有 RL 做法
		* 网格优化：$\lambda$ 为 mesh，优化目标为其上 PDE 求解精度（FEM 等）则为上方讨论的 mesh 调整问题
		* 传感器位置优化：$\lambda$ 为传感器位置（散点），RL 根据其上观测控制流场，优化目标为 RL 控制效果
			* 算双边优化，引用于((n1gg0h))非科研-设计控制耦合；{n1gg0i}
			* 如 `2022-07-18`(lectures)
			* 相关框架：((n81f1z))控制搜集哪些样本（主要框架），((n7un6b))invDP-采样（次要）
		* 重建完整解，$\lambda$ 为完整场而非生成它的 PDE 参数；超分辨率 作为特殊情形；{p4jh6v}
			* 恢复依据 一般见((n7un6b))反问题观测形式，散点位置、缺失变量、仅时间平均观测等
				* 如((_p4jk6i))重建流场，气动场景基于散点速度测量（PIV/PTV），血管血流场景基于载物浓度
			* 根据粗网格观测：超分辨率；{n8i94h}
				* 超分辨率的纯数据驱动方式，2306.14070 似乎提供了一个数据集？
				* 相关：((n8i94k))超分辨率用于图像生成（而非恢复）
				* 纯图像超分辨率：没有（或不使用）物理机理 PDE，只靠先验；{p4jk9u}
					* 网络架构作为先验来源，如((n4uj54))DIP
					* 数据习得作为先验来源
			* 方法：PINN；((_p4jk01))S3GM 训全场生成模型后，观测 loss 引导生成过程以重建物理场
		* （旧）`[PINN适用范围-知乎]` 机理数据融合属于特殊反问题
		* 含时场景学连续时间动力学 `contTimeDynML:` 等可视为 ((nckl0w))phy+MLpersp-特殊的反问题/设计问题，如 HNN、学本构方程；
		* ((n35f03))invCtrlRL 更广的框架，反问题/设计问题与控制问题（确定性与随机）、ResNet 放在一起看待
			* 这里所讨论反向设计主要针对不含时设计变量（系统可含时），否则可参考 `[model-basedRL综述2006.16712]`
		* `Rizutti2019LearnedIS` 认为 ResNet、含时方程时间推进器、反问题优化器、方程迭代求解器 的迭代结构相似
	* 据说((_n8vm7p))反问题加 noise 很重要（相关：((n3c975))noise 的作用）；{n8vm7r}
	* 训 NO 解反问题 $u\mapsto\lambda$((o1hn0d))
	* 基于优化的求解方法，invDP 理解为约束优化问题 PDECO
		* 关于一般 PDECO 观点对 invDP 问题求解的启发；{n43l6g}
			* ((_n44g4j))可用 AtO、OtA 两种策略；{o1ok5x}
				* AtO：约束罚函数化后的((n43l5h))invDP-PINN（包括有用增广 Lagrange 的变种方法）
				* OtA：((n43l6u))约束优化问题写出 Lagrange 乘子（“对偶变量”）后，最优点满足若干等式（PDE），从而约束优化问题转化为方程求解问题
					* OtA 的一般内容记录于其下的((o1hk2r))
			* 若 PDE 等式约束改写为极小化问题，则成为双边优化((n35e9x))；另有((n43l6u))极小化问题改写为 PDE 等式约束，从而统一为单个待解 PDE
			* 相关：((o3da5l))优化-约束问题求解，((n3pm4j))PDE大模型-约束优化作为通用问题形式
		* 约束表达-用正问题 surrogate，对 surrogate model $\lambda\mapsto u/O/G$ 求解优化问题（需精确模型生成数据用于训练 surrogate）{n32a85}
			* eg. `ISMO-2008.05730`，`2206.00711` GNN NO 结合 AD 降维，（事实上 PDE 解算子都行）
				* `MaxwellNet-2107.06164` 优化形状，形状用 AD 表征，NO 反传结果送入 AD，最终优化形状表征；AD 二值输出而非 SDF，从而可直接接收 NO 反传
			* 亦可用于控制：$u,\lambda$ 含时也可 surrogate 加速 `QuaSiModO-2102.04722`(AISC2)
			* surrogate 映射形式
				* 预测目标选取：surrogate 预测 u 比预测 O/G 更灵活((_p8g90q))，因目标函数可能有调整
				* 有额外非设计参数情形可 $(\lambda,\mu)\mapsto u/O/G_\mu$ 或整合的 $\lambda\mapsto G$
				* 隐式 surrogate：RL 相当于用 $\pi/Q$ 函数隐式表达了 surrogate((n77k8h))；{n77k8a}
				* 算法式 surrogate：除用直接推断的形式（普通 surrogate），还可用算法形式（如 PINN 训练）
					* 相关：`unifyMethod1:`“映射形式”；{n1gh25}
					* 元学习加速 PINN 解正问题后，结果可用于这里的加速求解，目前想法在 `invDesign-metaPINN:` 第二点
				* 传统算法而非 surrogate，极小化预测与观测差异，求导用伴随方程 Neptune-2509.00203
			* surrogate 能力需求，通过适当选取架构、训练方式
				* 可解输入分布：
					* 只需在最优 $\lambda$ 附近准确((p8g918))，surrogate 可现场训练
					* ((_p8g910))GRF、均匀采样等不符合实际场景；{p8g99m}
					* 应对 λ OoD((_p8gf0r))，可通过((o72k8l))NO 额外输入参考解
					* 相关：((n4fe9o))NO训练数据生成-数据分布
				* 梯度准确性，考虑到其后求解过程可能使用梯度
					* 架构选取：
						* ((_p8ga26))认为 ViT 梯度拟合能力有限
						* ((_p8gb0c))token 变换式（散点、均匀网格互转）softmax 维度应不同于 Transolver 物理注意力；{q5da50}
							* 相关框架((o2cg0f))Transolver meta-token
					* loss 体现：((_p8ga09))训练 loss 包括梯度误差，label 为传统求解器解伴随方程获得
				* 梯度稳定性，考虑到拟合不精确可能导致 loss landscape 出现局部振荡，梯度不文档
					* ((_p89f8b))先扰动再求梯度，对多扰动结果求平均；{p89f8o}
						* 先求梯度再平均 等价于 先平均再求梯度，因二操作均线性
						* 相关：((n79i86))FD 代替精确梯度，仅适用于低维 INR 等
						* 相关：((p8nf0w))提预测可靠性-网络前传取 ensemble
			* 优化求解过程，相关：((n7cn8a))优化-局部极小
			* 相关：((n79i6t))求导策略-先 surrogate 拟合，((o3da6k))约束优化-约束表达-可行域重参化（这里 $\lambda$ 作为自变量、surrogate 使 $u$ 成为因变量）
		* 约束表达-用 PINN loss：PINN $u(x;\theta)$ 同步优化 $(\lambda,\theta)$，无监督，通常要求 $\lambda$ 形式可用梯度优化；{n2ib43}
			* PINN 设定下正反问题相似：反问题观测 loss 和正问题初边值 loss 形式一致；例如时间可逆方程，已知末值推初值，相当于解反向时间 PDE
			* loss 来自约束优化改写为带惩罚优化 ((n35e9x))PINNinvDesnBiOpt，理论上需使惩罚权重趋于无穷；{n43l5h}
				* 较纯解方程 PINN 有一点限制，例如不适合用 Ritz loss（此时只能用严格双边优化的形式）
				* ((_n43i5y))确实在使惩罚权重不断增大
					* 若将 AtO 求解策略改为 OtA（改写为解方程((n43l6u))），则为纯约束问题求解，此时再用罚方法处理则无需使权重趋于无穷
				* 除罚函数方法，还可增广 Lagrange（类似 `PINN%`“边界处理”）((_n43i5v))
				* 一般 PDECO 观点对 invDP 求解启发的讨论汇总于((n43l6g))
			* 与 ISMO 的比较在 `DeepXDE`(AISC)
			* PINN 解正问题可 metaL 加速，而相应的元训练结果、元训练思路 均可能用于加速 invDP 求解，见 `invDesign-metaPINN:` 后两种设定
			* eg. `HFM-1808.04327`(AISC)（CFD，可作实验 baseline），$\lambda$ 可行值不占满全空间时也可用
			* 考虑优化算法，有的工作((n2ib3t))用了基于 ADMM 的方法，动机不明
				* `ADMM-PINN-2302.08309` 的场景则比较清晰：目标函数中正则化项不便直接 PINN 优化，故引入新变量、新约束后，用 ADMM 求解（不同分量优化分别 PINN、传统的简单算法）{n44g56}
					* 相关：((n41g1e))coordLoss-正则化
			* PINNinvDesnBiOpt PINN 求解 inverse design 问题具有双边优化的形式；{n35e9x}
				* 内层 $u_\lambda=\arg\min L_\lambda[u]$（实现时优化 $\theta$），外层 $\min_\lambda G[u_\lambda]$
					* 与 meta-PINN 的双边优化目的不同，那里是避免过拟合
					> TODO：合并成为三边优化？再按下方拆分约束条件成为四边？
				* 狭义 PINN 可近似求解：通过加权成为单层优化
					* 其实是约束优化问题，此时 $L_\lambda[u]$ 本身表示一个约束 $Lu=f,u|\partial D=g$ 的不满足程度
						* 注意待优化变量是 $(u,\lambda)$，给定 $\lambda$ 下满足约束的 $u$ 唯一；不过 $\theta$ 参数化下只要求近似找到即可
						* `ADnotConstrOpt:`(AD) 则不同，内外 $(z,\theta)$，小样本给定 $\theta$ 也可对应多个 $z$
					* 约束优化可写为双边优化形式，也可用带惩罚项（权重趋于无穷）的无约束优化问题近似
					* 从而，如果一个双边优化问题由约束优化问题转化而来，就可以近似为单层优化
					* 相关：((o3da51))优化-约束表达-惩罚项
				* 如果是 DRM 等方法，不能用惩罚项形式写为无约束优化问题的极限，必须严格按照双边优化来写
					* 它不对应约束优化问题，并且惩罚项优化与原优化不匹配，见((n32e2y))双边优化-单层优化的极限
					* 与双边优化相似的各类 setting 见((n32e3d))
					* 双边优化算法可见 ((n8jk45))OB-diffOrd 下的讨论
				* 作为 PDE constrained optimization（PDECO），约束条件可进一步拆分（类似“三边优化”）
					* 层级：目标函数，PDE 内部满足程度（PINN/DRM loss），PDE 边界满足程度
					* 边界若通过网络设计直接满足，则回到普通的约束优化（PINN）/双边优化（DRM）
					* 内部用 PINN loss 则也是某约束的不满足程度，可与边界约束的惩罚项合并
					* 内部 DRM loss，内层为约束优化（写成双边优化可以但不必要），与外层目标函数构成双边优化
					* 只考虑解方程（这里的内层约束优化），除了 DRM 用罚函数方法转化为无约束优化，还有 DeepNische 用增广 Lagrange 方法转化
						* 但是 PDECO 问题，目前见过的 PINN 方法似乎只用罚函数方法，其他的是没有还是我没看到？
				* 相关：属于约束改写为优化问题，另见((n43l6u))优化问题改写为约束
				* 又见((n43l6g))一般 PDECO 观点对 invDP 问题求解的启发
			* 用于反问题（系数恢复）的理论误差分析((_p9j834))，同时也包括 Galerkin FEM、PINN+FEM 混合方法
		* 约束表达-用 $(u,\lambda)$ 联合概率分布
			* 在优化框架下归结为((o3da85))约束表达-间接惩罚项
			* ((_o3cn4e))使用扩散模型表达联合概率分布 $p=\exp(-E(u,\lambda))$；{o3cn5s}
				* 从而等式约束转化为 $E$ 给出的惩罚项，对 $E+G$ 解无约束优化问题（实现解读为 Langevin 采样）{o3cn54}
					* 实现细节：((_o3de0u))$G$ 不是在当前带噪声点 $(u,\lambda)$ 算，而是预测完全去噪结果、在该预测点算
					* 相关：((n8ql6d))从给定概率分布中采样
				* 相关：((o3dj27))优化惩罚项-来自数据分布估计的似然函数，((n61a0f))扩散模型-非CV应用
			* 进一步希望生成的 $\lambda$ 不在训练集分布内（数据不含最佳 $\lambda$），((_o3de1v))将生成结果推离数据的 $\lambda$ 分布，该分布用另一扩散模型表达；{o3dj4o}
		* 近似约束质量：约束只需在最优 $\lambda$ 附近好用，包括 surrogate、BO、PINN 训练
			* 相关：算 ((n32e7d))domainShift 主动改变分布的设定，有 active-learning 味道((n1gj5a))；{n1ch8h}
				* MBRL 等带模型决策应也有类似考量((n23k6l))
			* ROM 算法形式，“好用”指精度高；((_o2dg1z))传统 ROM 用于优化时会在优化过程中调整/重建 ROM
			* 直接推断形式（确定性输出 surrogate）“好用”指取值准确；{p8g918}
				* eg. `ISMO-2008.05730`
				* 解训交替：求解更新 λ、用新 λ 增量训 surrogate
					* 增量数据（新 λ）类型：λ 完整历史轨迹((_p8g93h))，当前最优 λ（ISMO）
						* 可再加小的随机扰动，以提升 λ 多样性（ISMO 是否用到？）
					* 整合传统求解器：新 λ 位置 surrogate 可能不准，传统算法针对性生成数据用于训练
					* 过拟合风险：((_p8g93o))求解历史轨迹可能相关性高
						* 解决方案：((o72k8l))NO 额外输入参考样本 (λ',u')，缺省时可退化为普通 NO
				* 相关：((n4fe9o))NO训练数据生成-数据分布
			* BO（Bayesian optimization）的直接推断形式（带 UQ 的 surrogate）“好用”指预测方差小
				* BO 体现探索-利用平衡，如 `DEBOSH-2109.13337`
			* 算法形式（如 PINN 训练）“好用”指运行快
				* 目前想法在 `invDesign-metaPINN:` 第 3 点
		* 目标函数拆分：((n44g56))若目标函数两项适用不同算法优化（如 PINN 与传统算法），可引入新变量成为约束优化问题、ADMM 求解
		* 目标函数求梯度-利用对偶：$\lambda$ 允许梯度优化时，解出对偶变量 $p$（adjoint eqn）后可用之直接表达 $\nabla_\lambda G$ 而无需用 surrogate 间接获得；{o1hk1x}
			* `contTimeDynML:` 将连续时间动力学学习（包括学本构方程）视为反问题/设计问题，例如 `2105.08633`（不含时）`2006.08956`（含时）
			* adjoint 方程涉及的 $\partial G/\partial u$ 可能为广义函数（例如 Dirac 函数）
			* `neuralGDE-2106.11581` 图上动力学可求伴随方程，图结构突变处两侧 $p_-,p_+$ 满足相应关系
				* 图结构突变相当于动力学参数的突变，应可推广到一般动力学突变
			* 相关：`ANODE-1902.10298`(x) 先优化后离散的梯度可能不准（原文主要考察 ResNet 训练问题）{o1hk2v}
				* 相关框架：((o1hk2r))OtA vs AtO，((n32e95))梯度计算
			* 相关：对偶变量除用于求梯度（仍为优化问题形式），还用于((n43l6u))直接将原优化问题转为 PDE（不再为优化问题形式）
		* 待优化变量表达-参化：$z\mapsto\lambda$，转为优化 $z$；可用于 $\lambda$ 为函数、形状（难直接沿梯度更新），或有额外约束
			* 对不易直接定义梯度的场景适用（无穷维，曲面、形状等非参化情形）
				* 包括 $\lambda$ 为函数；此时 NN 参数化为一种自然做法，AD 有时也可
					* 另：鉴于函数空间太大，通常还需引入正则化，从而参化同时有体现额外约束的功能
					* 如 `Siren-2006.09661`“n2ib0j”FWI 中待恢复系数场也用 SIREN 表达
				* 曲面转化为 SDF 后也可这么处理 `MeshSDF`
				* 曲面/形状可表达为参考域到它的映射，是转化为映射的另一种方式；更多见 `NO%`
				* `MaxwellNet-2107.06164` AD 参化透镜形状（二值输出，非 SDF），梯度类算法优化 $z$ 比优化形状容易
				* 相关：梯度可定义（有限维）只是不好求的情形可用 surrogate 处理
			* 另一种作用：体现对 $\lambda$ 的额外约束，相当于引入“合法数据流形”先验；{n1ji4a}
				* 如（invD）现有技术手段能造出来的形状，（invP）`MeshSDF`“避免重建出不符合常识的曲面”
				* `2206.00711` 使用 AD 生成参数化形式的系数场，可输入后续 GNN NO
					* 作为病态问题，无此先验时恢复的场高振荡、不符合物理
					* 反问题求解时，除优化 $z$ 还允许微调 AD 参数
				* `2022-12-07`(dbGrpMeet2) EIT 似乎是用 VAE 优化其隐向量
				* （旧，转移至((o1hm0y))）另：体现额外约束原则上也可用 GAN 生成可行的 $\lambda$，零阶优化（从生成样本中挑最好）或继续利用对 $z$ 的梯度；暂未发现这么做的
				* 一般框架((o1hl3s))invDP-解的物理、场景两方面要求-场景要求的引入方式，这里的做法算((o1hl2w))生成器型先验
				* 相关：((o1hl2j))一般 ML-小样本-针对特定场景引入流形先验
			* 即使不表示额外约束，也可通过预训练加快求解
				* 如 `Siren-2006.09661`“n2ib19”使系数场接近预设的常数场
					* 不过我有点怀疑为何要以常数场为目标，这样可能破坏（online 求解阶段）系数场初值的波形先验，可能不如 NN 随机初始化的效果？
		* 待优化变量-多初值；{o1hm25}
			* 相关：((o1hm1c))一般优化算法-多初值
			* invDP 问题有（物理之外的）场景要求时（见((o1hk8c))），初值可根据场景生成
				* 不过需限制优化总步数，以避免过度偏离场景要求（有其他方式引入场景约束的情形除外）
				* 特殊情形：场景生成形如类似 GAN、VAE 的隐向量生成模型，((o1hm0y))此时针对 $\lambda$ 的优化转化为针对 $z$，且无需再限制总优化步数
			* 相关：若去掉“优化”步，只随机生成多解、从中挑最好的，则算((o1hm2x))invDP-propose+筛选 求解方式
		* 相关：((o1hn2s))该方法可能比基于直接映射的方法灵活
	* 优化问题改写为约束：引入对偶变量 $p$，将约束优化问题转化为 PDE 再求解（可用传统方法）{n43l6u}
		* 如 `2111.09880`(x) 化为 PINN 解关于 $u,\lambda,p$ 的 PDE
		* 转化为 PDE 原理可这么理解：视为约束（PDE）优化问题，Lagrange 乘子表明解满足一组等式（新的 PDE）
			* 一般 PDECO 观点对 invDP 求解启发的讨论汇总于((n43l6g))
		* 部分文献将这种做法称为 optimize-then-approximate（OtA），与 PINN 直接求解((n43l6u)) 的 approximate-then-optimize（AtO）相对；{o1hk2r}
			* OtA 梯度可能不准((o1hk2v))
			* 另见((o1ok5x))invDP-PDECO观点-AtO与OtA
		* 相关：对偶变量除用于直接将原优化问题转为 PDE（不再为优化问题形式），还用于((o1hk1x))求梯度（仍为优化问题形式）
		* 相关：((n35e9x))约束改写为优化问题，从而成为双边优化
	* 给出多种可行 $\lambda$（没有 BIP 那样的后验分布含义），用生成模型等；{n32a8i}
		* 注：多解的意义：{n95j4p}
			* 避免局部极小（surrogate 优化法靠多次随机初值可部分解决，但高维 $\lambda$ 效果未必好）；一般见((n8qk2t))优化-ensemble 方法；{n95j1m}
			* （反向设计）有时有 $G$ 之外的设计目标（如制造难度，这不好设计合适的 loss），提供多种可行解可以在结果里((o1hl1i))根据这一额外目标选择；{o1hk8n}
				* 相关：((o1hk8c))invDP 解的物理、场景两方面要求
				* 实际涉及的一些 inverse design 可能未必有形式化版本，主要是目标函数的问题；{n35f0p}
					* $G$ 可能有多目标，每个目标权重未确定，确定不准且需成本
						* 不过这个可能用 Pareto front 等做法解决，至少目标个数不特别多的时候
					* 此外可能有难以形式化的目标，如专业领域人士凭感觉观察流场形态；形式化未必准且有成本
					* 可形式化但计算成本高的目标，例如用复杂的传统算法计算的目标函数
						* 芯片功耗、散热等，飞机机翼受力的材料疲劳问题
						* 建立 surrogate 可部分解决
					* 还可能有动态出现的新目标（类似二阶控制论？）
						* 遇到问题之后才回来设目标，如出现事故后归因发现需要有新设计目标
						* 或者猜到（观察流场，靠专业直觉等）新的可能会出问题的地方
						* 专业领域的技术路线调整后可能有目标调整，如新材料需要注意新事项；若有这样的频繁调整，需要控制网络增量训练的成本
					* 相关讨论：
						* `2021-12-03`(CSImeet2) 机翼设计不只关注升阻比，专业人员还会看压力分布，可能是材料强度问题
					> (freeNotes) 里可能零散地有一些相关记录，之后可以整合进来
				* 或可参考((_q4tb0a))自行车设计数据集，10 目标、40 约束
				* 相关：((p97e9u))ensemble-目的-束搜索，((p97l1n))模型输出质量评估-依据任务需求
			* （反问题）解本来就不唯一；{o1hk8w}
				* 需用其他手段做((o1hl1i))后续筛选找真解，或直接将有部分未知性的结果作为后续决策依据（对反问题解提供 UQ）
				* 有时需要对反问题解的分布有先验估计，不过这种更类似 BIP
				* cloaking 设计问题的可解性说明探测反问题不是唯一可解 `2022-04-22`(CSImeet2)
				* 其他场合出现的 离散多解问题；{n95m1r}
					* 图像分割 有不同尺度的多种可能分割结果（如猫耳朵、猫头、整只猫），((_n95j1p))SAM 让模型同时输出 3 种分割结果，训练时与 label 比较、只反传最小的 loss；{n95m26}
						* 相关：((n95j2d))图像分割
					* 自动驾驶的轨迹预测也有多种可能，也是同时预测 6 种可能轨迹（包括左右转、直行等）；{n95m2a}
					* 预测总数固定时，模型可额外提供各预测的置信度，((_n95m6a))SAM 中所谓 IoU score
					* 又见((n95j35))仿TRIZ原理-部分或超额行动
				* 相关：((o1hk8c))invDP 解的物理、场景两方面要求，((n85b70))反问题-病态性-信息不足导致
			* 无导数优化问题可能本身用随机化算法才好求解，见((n32e4w))
			* 转化((n32a8n))不连续问题与组合优化问题，因((n32a8q))即使某空间离散，其上（所有可能的）概率分布组成的空间仍是连续的
			* 又见((n95j35))仿TRIZ原理-部分或超额行动
		* GAN；可以维护较好 $\lambda$ 组成的“数据集”供 GAN 拟合，eg. `2005.08832`(MR)
			* `1-s2.0-S1270963821006118`(AISC2) GAN 生成的是物理上可行的 $O$，从而可用于一类 $G_\eta$ 问题（生成 $\lambda$ 只针对单个设计问题）
		* score-based SDE 生成模型 `2111.08005`，训练时用 $\{\lambda\}$ 拟合先验分布（非条件生成），使用时为条件生成、SDE 每步利用观测 $O$ 修正；考虑有噪声+部分观测、线性算子的 CT,MRI 任务
			* 建立 $(\lambda,u)$ 联合分布（而非 $\lambda$ 边缘分布），((_o6uf06))扩散模型拟合该分布后，用 $O$ 提供的 loss 恢复完整的 $(\lambda,u)$；{o6uf1s}
				* 副本：((o6uf27))用于解正问题
				* 相关：((o6uf35))扩散模型-条件生成-通过 loss guidance
				* 正反问题用同一网络求解，另一已知做法为((n4km5c))ICON
	* 生成-筛选 方式，希望解 $\lambda$ 满足 1. 数学（物理）上为 invDP 问题解，2. 解满足问题场景需求，则有两种组合方式；{o1hm2x}
		* 1、2 的详细解释（invDP 解的物理、场景两方面要求）：{o1hk8c}
			* 反问题要求 1. 实际观测 $O$ 应能（近似地）由 $\lambda$ 产生，2. $\lambda$ 在先验分布内（如对人体 EIT，恢复出的 $\lambda$ 至少应看起来像人体组织）
				* 相关：1((o1hk8w))反问题解的不唯一性，2((n85b5s))反问题-引入场景先验
			* 反向设计问题要求 1. $\lambda$ 使目标函数取较大值，2. ((o1hk8n))如 $\lambda$ 对应的构型便于制造等
			* 2 场景需求的引入方式（一般）；{o1hl3s}
				* 注：以下讨论的多数方法针对基于优化的 invDP 求解方法
				* （基于隐向量的）生成器型先验，原问题（找 $\lambda$）转化为找 $z$；{o1hl2w}
					* 主要针对情形：$\lambda$ 适合作为 NN 输出，且用基于优化的 invDP 求解方法
					* 低维参化 $z\mapsto\lambda$((n1ji4a))，使用数据流形表达的先验
					* 若除低维外还有额外的 $z$ 分布先验（如 VAE，GAN 的正态分布）：
						* 可将 $|z|^2$ 加入到优化目标函数中
						* 可进一步将单初值优化换为((o1hm25))多初值优化，如正态分布生成初值 $z$；{o1hm0y}
							* 若再取消优化过程，只 propose 多初值、从中筛选最好的，则成为((o1hl0w))依据场景生成多解、依据物理筛选
				* $\lambda$ 为场时，用 INR 表征 $\lambda$、并结合小样本学习¹的一般技巧
					* ¹注：元学习似可理解为特殊生成模型，其生成对象为预测网络（而非图片等传统数据），生成过程为算法
					* [zhao21metaLSI](http://stanford.edu/class/ee367/Winter2021/projects/report_zhao.pdf)（似乎只是 Stanford 课程 project）INR 用于反问题，观测函数有信息损失、观测有噪声，MAML 引入先验
				* $\lambda$ 为图像时，可用 ((n4uj54))DIP，如 `DeepEIT`（还结合了 TV 正则化）；{n4uk2e}
					* 相关：((n7ul5h))EIT
				* （纯分布采样形式的）生成器型先验；{o1hl17}
					* 可考虑零阶优化：((o1hl0w))从先验分布中大量抽样，基于正问题 surrogate 筛选不符合观测的样本
				* 判别器型先验；{o1hl5c}
					* 判别器形式可为 似然函数 $\lambda\mapsto p(\lambda)$
						* 该似然函数模型训练 可利用问题场景的 $\{\lambda_i\}$ 数据，见((n4sl10))一般 ML-数据量不足-问题场景先验
					* 判别器形式也可为 WGAN 判别器等
					* 对基于优化的 invDP 求解方法，判别器可提供额外 loss 项；{o1hl88}
						* 如((_n4sl0u))EIT 简化版问题，对待估计的电导率场引入 似然函数 给出的先验分布
						* 相关：((n4sm0w))优化-正则化-对数据的正则化项
					* 对基于((n32a8i))生成多解的 invDP 求解方法，判别器可用于((o1hl1i))从多解中筛选
				* `DeepM&Mnet-2009.12935` 用双向 NO $\lambda\mapsto u,u\mapsto\lambda$ 刻画原方程可行解约束，最终反问题求解 loss 类似 cycleGAN loss；{o1hn0w}
					* 文中实验结果，NO 处理后输出的解精度高于直接参化、优化的解
					* 二 NO 某种意义上可理解为：分别描述了 物理要求、场景要求
					* 相关框架：((o1hn0d))invDP 用 NO 表达 $u\mapsto\lambda$
				* 注：以上各方法可混用
				* 注：针对反问题，这算((n85b5s))求解困难-信息不足导致的病态-通过引入先验缓解，相关框架((n32b4n))一般 ML-数据量不足的解决
			* 对基础模型，((o1i002))希望表达物理、场景要求的模型独立，分别为大模型、小模型，且可自由组合
			* 另：调研实际需求场景，文献调研可能不够，(('o2th5s))学界业界关心的问题有区别
		* 组合 A：生成满足 1 的多个解((n32a8i))，用 2 的标准筛选；{o1hl1i}
			* 筛选标准可形如((o1hl5c))似然函数 $p(\lambda)$
		* 组合 B：生成满足 2 的多个解，用 1 的标准筛选；{o1hl0w}
			* （旧表述）surrogate 指导 $\lambda$ 筛选¹，可结合生成模型、组合优化算法（遗传等）
				* ¹注：之前可能指的是 1 的筛选过程利用针对正问题的 surrogate
				* eg. `2005.08832`(MR) 扩充数据集用到 surrogate（与生成结果交互）
			* 可用于((o1hl17))反问题-引入场景先验-先验由采样型生成器给出
		* 相关：若目标函数不是用于直接筛选，而是再用它优化几步、更新由生成模型部分 propose 的 $\lambda$，则算((o1hm25))多初值优化；{o1hm1x}
			* 特例：生成器类似 GAN、VAE，则“优化几步”可针对隐向量进行；针对组合 B 的讨论见((o1hm0y))
	* 各解决方案按问题难度来源分类：
		* 含时问题时间长（或者用 RK 等高阶格式）导致 BP 困难：
			* （连续时间形式）用对偶方程的解表达梯度
			* 用 RL eg. `[Dong]-RL-WENO-1905.11079`（学 WENO 数值格式视为最优控制问题），`2010.15761`（不含时方程迭代求解器）, `RL_MR4PDE` 应该也算
				* $Q,V$ 可作为预测最终效果的 surrogate，从而只需用单时间步信息更新，无需长时反传
			* `[NeuralDE综述-2202.02435]` neural O/C/SDE 的 BP 算法可参考：可逆求解器，优化后离散
			* 传统 DL 技巧：FP 时分段存储中间层激活值，BP 时现场重算一小段 FP
				* 比较：可逆求解器的中间激活值可从末层反推，无需存
			* `2021-03-03`(AISCmeet) MALI, asynchronous leapfrog integrator
				* 指出传统 adjoint 做法 BP 反推前向激活的做法不准，如不可逆系统
			* 按无导数优化也能做（见下方）
			* 关于求导计算的一般话题见 ((n32e95))梯度计算
		* 正问题无解析导数（如黑盒模型）：{n32a7n}
			* 更详细的内容记录于相关框架：((n32a7g))处理无导数的目标函数
			* 无导数优化方法，`[BO]`
			* 连续优化，导数客观存在只是难求的情形可 ensemble 法估计，包括 ensemble Kalman inversion 同时估计 Hessian 以近似二阶优化 `2202.05122`
			* surrogate（同时适用于正问题求解耗时场景）
			* 若设计/恢复目标本身不适用梯度类优化（如离散组合问题、拓扑优化）可考虑 GAN 等
		* 若 $\lambda$ 搜索空间过大，可考虑 RL：搜索空间拆分为动作组合，单步动作空间相对较小
			* Google 用 RL 设计芯片的文章，每步决定一个芯片的放置位置
			* 若动作空间还是很大，也许可用生成模型；`[NLP定理证明器]` 用 NLP 模型生成动作（证明中的一条语句）
		* 拓扑优化，设计目标为形状且拓扑可变：
			* 使用 GAN（从而顺便提供多解）：`2005.08832`
			* 每像素位置二值松弛为连续取值，用 surrogate 进行梯度优化 `2204.06684`
			* 遗传算法等无导数优化，可结合 surrogate 快速判断当前解质量 `2204.06684`
			* 注：拓扑不变时为形状优化，容易手动设计参化，也可 AD 学出参化 `MaxwellNet-2107.06164`
		* 部分效用函数不可用（包括未 formalize 如制造难度，或计算成本太高）：生成多种可行解
		* 观测函数未知：`2020-11-04`(AISCmeet)“2009.04142”（待看细节）
		* 反问题观测 multi-fidelity `1910.09098`（待看细节）
		* 优化初始阶段只需低精度，后期才高精度（部分源于 `2022-03-11`(CSImeet2)）
			* 初始离最优解远时，可用 surrogate、PINN 等粗解，后期用传统算法（可 NN 改进）精确改进
				* 传统算法若计算框架可微（未必含时问题），可直接 BP（为先离散后优化）
			* surrogate 可在精确最优解周围增量训练，提高拟合精度
			* 传统算法先优化后离散，可动态改变网格分辨率（同时有可能避开一些局部极小？）
			* 传统算法除分辨率可调，还可能有多精度版本，例如流体的大涡模拟/RANS/LES
		* 病态 需区分：信息不足（数据不足/有噪声），相应正问题映射奇异，等
		* 正问题映射奇异、非凸（局部极小）：见 ((n32e3y))优化算法理论与技巧，若有必要可将科学计算所涉及的单独提取出来
		* 信息不足导致的病态；{n85b70}
			* 场景包括：((n7un1l))边界观测算子推内部系数场情形，无法获得完整边界观测算子
				* 信息不足 可能来自观测((n7un6b))涉及采样（而无法获得完整观测），包括物理空间、参数空间等的采样
			* 通过引入先验缓解，一般框架可见((n32b4n))数据量不足的解决；{n85b5s}
				* 针对反问题的先验引入 已转移至((o1hl3s))invDP-解的物理、场景两方面要求-场景要求的引入方式
			* 相关：((o1hk8w))反问题解的不唯一性
	* invDPs 反问题/反向设计快速求解一类 $G_\eta$ 问题，找到 $\lambda_\eta$；{n7vn70}
		> 相关记录多了之后，按照 $\lambda,\eta$ 形式、加速做法分类；
		* 可能场景
			* 反问题 $\eta=O^*$，即每次观测到的数据不同
			* 反向设计问题 $\eta$ 可以是目标性质（类似 $O^*$）；动力学控制问题中常见，要求接近某目标
			* 各子目标权重（此时结果 $\lambda_\eta$ 组成 Pareto front）
			* invDP-额外非设计参数((n43e25)) 中 $\mu$ 分布可变情形，将 $\mu$ 的分布 $p(\mu)$ 作为 $\eta$
		* 基于原有 invDP 单独求解的做法，引入 meta-learning
			* eg. AD 找 $\{\lambda_\eta\}$ 低维结构用于 surrogate $\lambda\mapsto u/O$ 优化；联合 $u_{\lambda_\eta}$ 找低维结构则属于 `invDesign-metaPINN:`(AD) 第一点
			* 找 $\lambda\mapsto G_\eta[u_\lambda]$ 低维结构以迅速训练出针对 $\eta$ 的 surrogate $G(\lambda)$？
		* 训练 $\eta\mapsto\lambda$ 网络，可以是 NO、有监督训练；{o1hn0d}
			* 训练方式，数据等；{o1lj8k}
				* 若有监督训练，可使用正问题 $\lambda\mapsto\eta$ 生成数据
					* 相关：((oaie9v))正反向数据 同时生成、使用
				* 非传统有监督训练：使用（正问题）surrogate 提供的 loss 训练，"tandem network"
				* PINN loss：PI-DION-2412.03161 仿照 PI-DeepONet，输出 $(\lambda,u)$，训练 loss PINN+data
				* 解不唯一时，((_o1lj5b))consistency loss 似为利用正问题可微数值求解器，希望其解得的 $\eta$ 与反问题 NO 输入接近
				* 引入辅助任务 可提高数据利用率，包括((q3kh4l))同方程正问题、多方程正问题（基础模型）
					* 基模类型：本身支持多时间步输入，如自回归；上下文学习类似乎也可
				* 若数据本身仅有不完整观测，任务为流场补全（输入不完整观测输出完整物理场）：((_q4pa48))人工再限制观测，训练阶段网络输入 场景部分观测×人工部分观测，loss 使用完整的 场景部分观测
				* 相关：((o1lj8r))NO-训练-已知 PDE 情形
			* 网络结构 可依据理论推导的正反问题映射形式¹设计
				* ¹可见((n7un1l))invP-根据边界观测算子推断内部系数场，包括 EIT
				* eg. 应乐兴的系列工作，如((n86k0e))根据 DtN map 预测系数场
			* 注：考察解算子的 `GalerkinTf-2105.14995` 可能属于这类工作，未深入 check
			* 相关：DeepM&Mnet 也构建了该网络，但并非用于直接预测 $\lambda$，而是((o1hn0w))与正问题 $\lambda\mapsto\eta$ 网络联用、用类 cycleGAN loss 优化求解反问题
			* 该方法灵活性可能低于基于优化的 invDP 求解方法：{o1hn2s}
				* 1. 对((n43e25))有额外非设计参数 $\mu$ 情形，正问题为 $(\lambda,\mu)\mapsto\eta_0$，invDP 问题完整参数 $\eta=(p(\mu),\eta_0)$
					* eg. 反问题有多初值 $\mu_i$ 下的观测场 $\eta_i$，且初值个数不定
					* eg. 控制问题希望在某环境参数分布 $\mu\sim p(\mu)$ 下均接近目标 $\eta_0(\lambda)\approx\eta_0$
				* 2. 反问题，观测 $\eta=O^*$ 为散点、有噪声
				* 3. invDP 解除了物理要求，进一步考虑引入 ((o1hk8c))场景要求 的灵活性
					* 若场景要求单一，该 NO 做法可直接通过训练数据体现
					* 若场景要求多变，该做法需将场景要求视为 $\eta$ 一部分
					* 场景要求会动态变化（乃至新增）情形更难直接处理，需重新生成所有训练数据、重训整个网络
					* 而对基于优化的方法，其 引入场景要求的多种方式((o1hl3s))中，物理要求、场景要求常由不同模型表达，可自由组合，更改场景要求时只需重训场景要求部分模型
						* 若考虑基础模型，场景部分模型适合作为灵活、易训练、可完全交由用户负责的小模型，而物理要求部分模型则应为高训练成本、几乎无需微调的基础大模型；{o1i002}
				* 该方法针对 1,2,3 场景的难点：$\eta$ 形式复杂，难作为 NN 输入，或需要用复杂架构的网络
					* 如 反问题多初值情形（$\eta$ 为集合、规模不定），控制问题环境参数分布（$\eta$ 包括 $p(\mu)$ 这个概率分布），反问题部分观测（$\eta$ 为散点而非图片等形式）
				* 该方法需预设 $\eta$ 分布（设错了泛化不好），而基于优化的方法不需要（从而无需担心泛化能力问题）
					* 针对 1,2,3 场景，预设分布难度增加，难以覆盖真实场景需求
				* 相比基于优化方法的可能优势：优化方法常遇到局部极小问题
					* 可与基于优化的方法联用，NO 生成优化方法的初值
			* `1-s2.0-S1270963821006118`(AISC2) 先 GAN 表达物理可行的 $O$ 范围，然后根据 $G$ 找特定的 $O$，再有监督训练反向映射 $O\mapsto\lambda$ 恢复设计参数
				* $G=G_\eta$ 改变时，第 1、3 步的网络无需重训；根据第二步实现方式，可能是一对多版本
				* 比起直接 $\eta\mapsto\lambda$ 的做法，这里引入了求 $O$ 的中间步骤，可能适合 $\eta$ 无法直接作为网络输入、只能以 loss 形式体现的情形（例如区域形状，以及动态增添的新设计目标）
			* 备用：((_o96a9q))综述文章对逆向设计方法的分类，基于 PDE 约束、数据驱动
			* 相关：((o1lg3c))NO-可处理的问题类型
		* 可能性：引入对偶变量转化为参数化 PDE，用相应办法求解（NO 等）
		* 一对多版本
			* CGAN/CVAE 等条件（$\eta$）生成模型，用 $(\lambda,\eta)$-pair 数据训（可 surrogate 生成）, eg. `1908.04851`(MR)
			> 注意数据不需要以 $(\eta_i,\{\lambda_{ij}\})$ 的形式来针对特定 $\eta_i$ 训练
			* `1-s2.0-S1270963821006118`(AISC2) 第二步可实现为一对多
			* score-based SDE 生成模型 `2111.08005`，训练时用 $\{\lambda\}$ 拟合先验分布（非条件生成），使用时为条件生成、SDE 每步利用观测 $O$ 修正；考虑有噪声+部分观测、线性算子的 CT,MRI 任务
		* 反问题，人造数据集生成，可参考：`2022-04-22`(CSImeet2) 煤矿勘探反问题的讨论
		* 注：真解一对多情形，有监督可能网络训练不收敛，tandem 网络可学出一种一对一可能，生成模型恢复一对多；见 `2011.12603`
		* 一个可能相似的例子是((n2ak58))分子结构预测（找势能函数极小值点），当时用了扩散生成模型；{n2ak5q}
		* 相关话题：
			* 反问题，若系统含时、待恢复参数不含时、观测为连续多个时间步，可解读为单动力系统的守恒量求解问题（从而它也可按反问题理解）((n35e96))paramDynConserv
			* 对含参动力学，反问题求解可用于帮助正问题（时间预测）`1TaskHypernet:`(metaL)
* UQ uncertainty quantification；{o1ok77}
	* 场景包括正反问题，((_o2dg14))参数随机性在系统中正向传播 vs BIP
		* ((_p8g89s))美国激光约束聚变所用预测模型需 UQ，便于参数外推探索
	* BIP Bayesian 反问题，反问题的 UQ；{o2dg1b}
		> 目前框架来自 `BIP-ADNN-1911.08926` 汇报时的讨论
		* MCMC（常配合 surrogate，且有 surrogate 都可以做这事）,
			* eg. `BIP-ADNN-1911.08926`，FNO（仅一个相关实验）
			* 未确认 ((n8qk7i))SVGD 是否可行
		* normalizing flow（见 freeNotes 中对生成模型的讨论）
			* `2021-08-09`(lectures) 似讨论了用它解 PDE 系统的 BIP
		* SDE, eg. `GenModSDE-2011.13456`
		* 与生成模型比较；{o2dg1a}
			* BIP 结果为概率分布，这可用生成模型表达（从而部分生成模型的框架可用），也可用散点
				* 生成模型通常要能在分布中动态生成采样结果，一般不允许用散点表达
			* 概率分布的来源，生成模型常来自拟合数据，待表达分布就在观测空间
				* BIP 的分布有反问题背景，待表达分布在参数空间（有预选的先验），与观测数据所在空间不同
		* 相关：`2202.05122`“EKI-1808.03620”将 ML 视为 BIP 并提供无导数优化方法
	* `1812.03511` PINN 结合 GAN 以表征输入噪声带来的解不确定性
	* 原则上 VAE，BNN（Bayese NN），dropout 也能表征不确定性
	* 刻画随机函数的方式汇总 见 ((n32f2t))NN表达函数空间中分布
	* 只刻画单点随机性的：`2204.02488` 单个确定性网络，输出预测值的期望、方差；类似 VAE 编码器
		* DeepONet 新增独立网络输出各点预测方差((_p3ve83))；实际网络与原有均值预测网络共享浅层参数
		* 训练 loss，((_p3ve8p))用 log-likelihood，假定了输出预测为正态分布
	* 含参 PDE 的 forward UQ（uncertainty propagation，给定参数分布，求解的可观测量分布），MC 可用 NN surrogate 加速，如 `MLevelML-1909.09448`
		* UQ 涉及到求解参化 PDE 的 multi-query 需求((n26f0d))；{n26f0c}
	* 正反问题同时可 UQ，((_p2ke6f))DGNO 基于概率图模型，假定 a,u 均根据隐向量 β 生成
	* 可用于 active-learning
		* 深度势能系列，在发现预测不确定度大的数据后用精确模型计算给出准确 label，并用于网络增量训练
	* 其他可能的实际应用场合：{o6uf3s}
		* `GraphCast-2212.12794` discussion 提到现实中中期天气预报需 UQ
		* 以及 ((n32e7u))检测 domain shift 是否发生
		* 输入仅部分已知情形，((o6uf3o))预测输出分布（或完整输入输出联合分布）
	* （备用）综述 "[2011.06225] A Review of Uncertainty Quantification in Deep Learning: Techniques, Applications and Challenges"
		* BNN，MC dropout，MCMC，变分推断 VI，BAL（Bayesian active learning），BBB（Bayes by backprop），VAE
		* 非贝叶斯方法：deep Gaussian process，Laplace approximations
		* ensemble 方法：deep ensemble((n2me2z)), deep ensemble Bayesian, 传统 ML 的 ensemble 方法；{n45m5q}
			* 相关：((n45m5m))用粒子群优化时，顺便也有集成学习功效
			* 相关：((n8qk4m))一般 ensemble 策略
	* 其他备用综述：
		* `2204.02488` 简单比较了 预测方差的单网络、ensemble、BNN 方法，性能递增而复杂度也递增
			* 并推荐如下两篇综述
		* A survey of uncertainty in deep neural networks, 2107.03342
		* Uncertainty quantification in scientific machine learning: Methods, metrics, and comparisons, 2201.07766,
			> 推荐于 `2204.02488`
	* 相关：有时只需在推断时判断当前样本是否超出训练分布，见 ((n32e7u))检测 domain shift 是否发生
	* 备用的（非综述）材料：
		* `BOP-DMD-2107.10878` DMD 用 bagging 改进，可用于时空 UQ
* `whyAISC%` 科学计算问题，AI 的适用场景与潜力
	* AI 方法包括：PINN 类，NO 类，改进传统方法类；目前暂不考虑含时问题
		* 含时问题，用处包括时间多尺度 `2022-08-19`(lectures) 许志钦化学反应从机理计算，靠时间演化算子增大时间步长
	* （正问题）精度：{p1mf8o}
		* 实际问题未必需要很高精度，故有理论收敛性保证、做到机器精度的传统算法不那么重要
			* 如电磁 1e-1，机翼流场 1e-4 `2022-06-22`(AISCmeet2)
		* AI 加持传统算法可有精度保证
			* 可包括 Meta-MgNet；以及 `singlePDE%`“ELM”（视为 FEM 无网格改进）
		* （若需训练）OoD 问题
			* 传统方法及改进、PINN 能得较好解，至少知道当前近似解精度如何
			* NO 不保证解可信度，引入 UQ 可适当缓解但不解决（若能算 PDE loss 倒可以）
		* 备用：`2022-10-05`(AISCmeet2) 导师：许进超（？）finite neural method（？）是说 NN ansatz 可同时实现 spectral accuracy 和 adaptivity，而 FEM 不能同时做到这两点
		* 相关：((pcj876))NO 精度超过其（低保真）训练数据，凭归纳偏置修复数值伪影
		* 相关：((p1mf8n))PINN-精度
	* （正问题）速度：
		* 传统方法网格生成耗时（尤其双曲方程？）；AI 生成网格、PINN 等无网格方法有潜力
			* 针对网格需动态生成的情形（moving interface，裂纹扩展等）；一次生成可复用则代价不大
			* 若考虑流形上定义的 PDE，打网格也比较麻烦 `DINo-2209.14855`“n2ha7x”
				* 流形上定义，或许如布料抖动？但这个可变换到标准域
			* AI 网格生成、区域分解据说暂无理论保证 `2022-06-22`(AISCmeet2)
			* 自适应网格问题：湍流不同区域网格精度要求不同，且湍流还会移动
			* 另：传统的 meshless 方法在 `2022-10-26`(AISCmeet2) 有讨论
			* `2023-01-11`(lectures)“复杂外形”传统 CFD 网格生成需人工干预、质量控制难、耗时多（还提到可能需流场特征提取方法）
		* 需区分算子类型，传统数值格式((_q7b75k))对某些算子快于可学模块
			* 原文推荐做法((q7b75m))算子分裂，仅慢的时间演化算子换为黑盒 NN
		* AI 解法有希望引入先验知识（NO，元学习加持 PINN），NO 最快
		* PINN 若能加速优化算法、改进网络架构（也是方便训练），同样有速度上的潜力
		* NN ansatz 做法是全局表征，区域分解时添加界面约束方便、收敛快 `2022-10-05`(AISCmeet2)
		* 传统算法可被 AI 加速 `paramPDE%`；例如传统迭代法不擅长处理低频分量，Helmholtz 方程等不易
			* `2022-09-07`(AISCmeet2) 提到 Helmholtz 方程中 $k(x)$ 非常数，改为分片常数、不同片取值比例差异大，则更加困难，网格稍微不好误差就会炸
			* `2010.15761` Helmholtz 方程，Krylov 子空间仅做局部更新（由于线性算子来自 $\Delta$，它是局部的），解传播到全域要很久
		* 面向下游任务的正问题，NS 方程只能精细模拟，但工程问题不需这么精细，可 AI 减小正问题时空尺度（来源 `2023-01-11`(lectures)“AI4sci-AI4CFD”）
		* 速度比较公平性问题：((_o9cf0w))需注意传统算法可通过降精度提速，不同传统算法效率可差几个量级
			* FD-Bench 基准数据集上的提速实验，性能提升不如通常报道的那样显著((_p64a0q))
		* 已落地的 ANSYS SimAI，((_o9pa1b))6h 加速到 2min
	* （正问题）可用性：
		* 高维问题
			* `2022-08-18`(lectures) 鄂维南：高维方程不多，除了 Boltzmann（还不算特别高维，不过已超出传统方法能力），更高维的还有 Black-Scholes（业界已经在用 ML），Schrödinger（反对称不好处理），Bellman
		* 解复杂性质，多尺度等，NS 湍流或许有潜力
			* `2021-11-10`(AISCmeet)“FEM处理不好二维复杂边界”，未确认
			* 普通 PDE 解形态较简单，不必像 PINN 用 NN 表达解；解算子则高维
				* 低维复杂度不如图像、3D 场景渲染（NeRF 及后续）；高维 TT 也够
			* 湍流不同区域网格精度要求不同，且湍流还会移动；但目前 AI 方法处理有精度不足问题
		* Steklov-Poincaré 方程，传统方法需要处理大条件数，此时 PINN 或许有优势？但梯度计算也可能慢
			* `2022-06-22`(AISCmeet2)
		* 区域分解预先计算区域界面处解取值（从而无需通信），传统方法干不了（只能算全场），但 NN ansatz 方法可以 `2022-10-05`(AISCmeet2)
		* 机理未知（或部分未知），需结合数据；{p3rb0r}
			* 相关：((o1lg3c))NO-使用场景；同样机理未知，但目标为学机理而非预测的见((n3bh3b))ML2phy
			* 例子：(('p3rb4o))太阳活动预测；某些流体也是（2024-03 MS峰会线下当面讨论的应用专家）
			* 包括同时融合不同种类数据；`[PINN适用范围-知乎]`；不过这常归反问题框架（恢复场而非机理的反问题）
			* `2023-01-11`(lectures)“物理模型”为 CFD 难点之一，包括参数选取、复杂问题机理（如湍流、多相流、化学反应等）
			* 相关：`2023-01-11`(lectures)“建模思路分类”（AI4CFD）包括：全场统一建模、分区建模、基本流动模型组装特定流动
			* 数据受限情形处理（针对 NO 类方法）
				* 通用框架：((n32b4n))ML-数据量不足
				* 利用不完整的已知机理仿真，生成大量低保真数据，((ncbn0o))多保真训练；{p3rb3u}
				* 利用 PDE 基础模型提供的先验；隐式类（不输入 PDE 形式）先验仅在网络参数中，显式输入 PDE 类可额外指定已知部分方程形式
		* 工程方面，`[PINN适用范围-知乎]` 传统 CFD 软件很庞大，更新维护复杂；此外 AI 方法可能数据并行、模型并行都容易
			* `2023-01-11`(lectures)“硬件适配”传统 CFD 程序难针对特定硬件设计、异构平台迁移贵
		* `2023-01-11`(lectures)“鲁棒性”传统 CFD 迭代收敛性、稳定性（尤其高阶格式），且受初边值设定影响
		* `2023-01-11`(lectures)“结果置信度”传统 CFD 难明确 网格设置、模型选取等 对结果影响趋势，缺 UQ 方法
		* 传统仿真局限性讨论((_q5tg3s))，物理机理不完全已知 or 算力不足只能简化方程
	* 反问题、反向设计
		* PINN 可同步求解和优化；据说 George 觉得也能力有限
		* NO 可获得关于参数的导数，类似 surrogate
	* 或可参考((o2dg2r))参化PDE-需求场景
	* 训练数据需求问题，见((n8i96v))NO-数据-科学计算已有数据集
	* 有说服力的实用算例：PINN 暂无（li+1学长的无网格 Boltzmann？）
	* 其他：无网格 ROM 优势描述 `CROM-2206.02607`
	* 注：来源包括 `2022-06-22`(AISCmeet2)（主要 PINN）
		* 术语含义问题，`2023-01-11`(lectures)“AI4sci-AI4CFD”区分了二者，后续又给出 AI4CFD 定义、相较 AI4fluid 特殊性
* AISC 整体框架；{p9tk5n}
	* 组织思路：目的手段链条 DAG `<p9tf99>`；鉴于单手段可服务多目的，以下内容仿照拓扑排序展开
	* 顶层目的：认识，预测，决策
		* 认识，包括 状态，性质，完整方程
			* 状态 也可服务于科学理解，如探测到地下存在速度异常区域，可能作为新科学发现的起点
		* 预测：NN，完整方程 + 数值解算法，整合方式
			* 方程+数值解 可参考 ((n3bh2y))fastPhyDyn
			* 整合方式 所用方程不准 or 数值解分辨率过低，额外引入 NN 预测残差修正
		* 决策，包括 反向设计（不含时），控制（含时，离线预设控制、在线有反馈控制）
			* 手段：预测 + 优化算法，NN
			* 控制 用预测+优化 是 predictive-control，直接用 NN 是 model-free RL
		* MDP 解读：认识状态 s,w，认识性质?，认识方程 p(s'|w,s,a)，预测 s'，决策 a
			* 扩展 MDP 中 w 指背景自在状态；暂未直接体现观测数据 o
	* 状态重建
		* 类型包括 背景自在状态（常不含时）、方程主导状态（常含时）
			* 背景自在：如 FWI，CT，EIT，ODT，材料内孔洞探测，NeRF（据多角度观察结果重建 3D 场景）
			* 方程主导：如 PIV，据示踪物质恢复完整流场，气象数据同化
			* 背景自在 可作为 完整方程 中的系数场
			* 解反问题可能需同时重建二者，如 EIT 恢复电导率（背景自在），算法可能需同时恢复电流（方程主导）
		* 重建手段：底层依据是 观测数据+预测，直接手段待分析
		* （重建所得）状态表征 可能用 INR
	* 机理，包括 性质、数学结构、完整方程
		* 性质，例如 守恒量((o5pg31))、等变性、对称性、单调性（包括耗散、熵増）、无散、BC、奇点/无穷远收敛阶
		* 数学结构，如 应乐兴的 EIT 网络设计，FNO 谱卷积，PDE-Net 卷积核条件（作为微分算子离散）
			* eg+ ((n3pa6x))边界元 INR ansatz
			* 不同于完整方程：方程常用于判别输出合理性，映射框架的数学结构 用于直接生成输出
		* 完整方程：符号形式，区域形状，系数，系数场（背景自在状态），映射依赖关系
			* 次级手段：表示方式←准确性
			* 符号形式 见((nbig7l))ML2phy-符号回归-PDE
			* 结构侧（表示方式），系数场、映射依赖关系 可用 NN，成为 NN in phy
			* 映射依赖关系 eg. HNN、DeepPot、NeuralODE、学 Green 函数；{pcfj5p}
				* 依据 观测数据的见((pcfj4q))ML2phy-方程未知项表达
				* 依据 底层原理模拟数据的见((ncmj8o))约简模型封闭
			* 依据侧（准确保证），非符号形式，恢复方式的手段链分析
				* 记号：本构关系 s ↦ r（状态、响应）；其余对象（系数场等）同理
				* 目标：构建映射 目标系统 ↦ s ↦ r（特定输入输出变换）
				* 常规方案：外层映射基于训练（明确所用原理的大类）
					* 方案：s ↦ r 用 NN 直接表示（明确所用原理的大类）
						* 即：引入辅助映射 θ ↦ s ↦ r，使用其输出表示 s ↦ r
					* ← 只需完成 构建（基于训练的）映射 目标系统 ↦ θ，使所得 NN 反映该材料真实本构关系对应的映射 s ↦ r
						* 要素：loss（下方单独讨论）
						* 要素：优化算法
				* 上下文方案 ((_q6rk3a))ICM：改写为条件输入映射 (目标系统,s) ↦ r；{q6rk68}
					* 要素—目标系统 改为可作网络输入的形式
						* 方案：系统局部满足方程，((q6rk6y))该离散代数方程系数作为系统的表示
					* 要素—映射表示，方案：NN 直接表示
					* 要素—训练← (loss,优化算法)
						* 资源需求，需预先准备一系列目标系统，由于此时目标系统为 NN 而非算法的输入
				* 子要素单独分析：定义 loss func 目标系统 ↦(s ↦ r)↦ l，使其输出能反映 NN 映射关系与真实本构关系的差异
					* 方式解读，相当于为 目标系统 提供一个表示方案
					* 方案：基于真实系统响应机制 a ↦ o（可用资源），对应参化 PDE 参数 a 观测 o；{q6sg4g}
						* 要素：数值模拟程序（以本构关系为输入），(s ↦ r)↦ a ↦ o
							* 即：给定本构关系后，输入外部载荷，可输出应变场
						* 要素：真实系统测量过程 or 底层原理数值模拟程序，目标系统 ↦ a ↦ o'
						* 要素：算子空间距离定义 (a ↦ o) ↦ (a ↦ o') ↦ l
							* 要素：参数 a 的分布
							* 要素：输出状态空间（o）距离定义 o ↦ o' ↦ l
								* 若为物理场 o(x)，则可再拆分 要素：输入点 x 分布（可默认均匀分布），要素：取值空间距离定义 o ↦ o' ↦ l
					* 方案：基于局部满足方程，其可计算实现 涉及方程的数值离散，见((n3bg3i))PDE 残差
						* 解读：可理解为 $(r(s_i))$ 满足特定（代数）方程((_q6rk37))，从而 s ↦ r 由代数方程的解给出；相关：((n3gd6q))映射形式-非简单前传；{q6rk2p}
						* 案例：超弹性涉及应力应变关系，((_q6rk5h))三角网格离散后，每个内点满足受力平衡
					* 相关：该手段的场景一般化，((ncln3c))loss-体现机理-据下游任务
			* 方程可能本质上无法精确描述系统：状态已约简，如连续介质版流体方程不适用于介观（需 Boltzmann）
	* 数值算法：传统格式，PINN
		* 传统格式 内部可涉及 NN 模块，ROM
	* NN 包括 架构、loss，均可引入 数据、机理
		* 架构：数据，性质，数学结构
			* 数据嵌入架构 eg. kNN，((o72k8l))RNO，上下文学习（ICON），AlphaFold2 MSA
				* 辅助数据选择方式：手动，算法
			* 性质嵌入架构((nca99w))
				* 猜性质、设架构、训网络 循环进行，((_p9ua3y))PINN 网络预乘收敛阶因子，猜收敛阶、训练、观察结果、调整收敛阶
		* loss：数据，性质，完整方程
			* 用完整方程类似 PINO、PI-DeepONet
			* 用性质的 eg. 惩罚等变性、对称性、守恒律等的违反程度
	* 数据 区分 部分or间接观测、完整解
		* 完整解：重建，预测，约简（完整系统解转约简系统）
			* 约简 eg. Boltzmann 到 Euler/NS 到 RANS，微观电子波函数 到分子动力学 到粗粒化动力学
			* 预测，NO 训练类数据形如 (f,u) 的特殊情形（一般 (a,u) 不适用，如 a 含区域形状时）
				* 先 f 后 u，两步：采样 f（需((n4fe9o))选 f 分布），解 u（用传统数值算法）
				* 先 u 后 f，两步：采样 u（((o1sa11))需小心分布），求 f（代入完整方程即可）
				* (f,u) 联合采样、修正
	* 待 link 相关框架：
		* 数据不足解决-引入机理
		* MR.md phy+MLpersp
		* 数据-多保真？
		* invDP-广义反问题
		* comp2-whyAISC-正问题-机理不完全已知
