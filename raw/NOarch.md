> 2025-02-24 从 aisc2.md 独立
* 不同 $a$ 形式下的映射架构：{n3qg83}
	* 相关：((n3qi2r))NN架构设计-不同输出形式下的网络架构，((o31f1s))dropout-用于输入
	* 对各种 $a,u$ 形式通用的架构：{n5bl4q}
		* 拆分映射为 $a\mapsto v\mapsto u$，如((n42e6y))（引入中间域）$v$ 为有限维向量
			* 类似 GPT-3¹：先预训练 $a$ 通过一个无监督模型（例如 AE）得到低维表达，将低维表达输入后续网络，从而化归为低维参数情形
				* ¹日后补充：不知道当时写的是什么意思了
			* 据说有这样的情况：形状的设计参数不多，但是直接输入这些参数会丢掉空间信息（多个形状有 overlap 等），不如直接对形状本身用这种方法处理高效
		* 改写为 hypernet；{n42e71}
			* 若 $u$ 用参化表达，主网络形如 $x\mapsto u(x)$
				* 如 DeepONet；
				* 考虑离散图片转 INR（图片或 3D NeRF），((_n42e7f))超网络用 Transformer 架构，输入图片、输出 INR 参数（其中主网络参数用 grouping 手段稍降维，而非 modulation）{n42e9a}
					* 相关：((n42f33))hypernet-主网络参数生成方式，((n4ce5l))NN架构设计-高维输出-分层使用两种高维输出方式，((n4t95z))映射Transformer化-引入init token
					* （备用）音频 INR 2302.04959 似乎用了类似做法
					* 离散图片转 INR 另一方法，((_n54f46))图像超分辨率，Transformer 中 $q$ 待查询坐标，$k$ 低分辨率图像中的坐标，$v$ 低分辨率图像对应位置的像素值/feature；{n54f4n}
						* 副本((n9ha7t))hypernet，解读为主网络参数量可变的 hypernet；((nase8f))假设空间参化方式
						* 可认为 $a,x$ 均（平等地）作为网络输入，不按（通常意义上的）hypernet 理解
						* 类似的((_n9ha74))OFormer，每个 $(x,a(x))$ 信息作为一个 token 过 Transformer 编码器，输出层用于计算 $k,v$ 与待查询坐标信息 $q$ 相互作用
						* 类似的架构还见于((_nase7w))PROSE，((_o29l0g))HAMLET，((_o6tf6z))AROMA
					* 类似的情形：点云转 INR((_n5bg3f))的 Shap-E 编码器¹，直接生成全部主网络权重（除后两层）；{n5bg3k}
						* ¹这里用于构建 INR 参数组成的数据集、供后续扩散模型拟合，可见((n5bk0b))扩散模型-生成INR参数-数据集生成
				* （生成 INR 参数的）超网络用 条件扩散模型¹、其去噪网络用 Transformer；{n5bg9t}
					* 相关：((n5bh10))随机映射=条件生成，((n5bg8b))映射分析-输入转参数 $?\xmapsto{x}y$，
					* 或可理解为 hypernet 套娃：生成 $u(x)$ 的网络由 $w$ 参化，生成 $w$ 的网络由文本参化（若将扩散条件生成模型理解为某种超网络）
					* 如((_n5bg9p))Shap-E 从文本生成 NeRF
					* 非条件生成的有 ((_n5bk05))HyperDiffusion
					* ¹INR 参数组成数据集，可考虑其生成方式((n5bk0b))
			* 若 $u$ 用离散、基底等表达，主网络可形如 $u_0\mapsto u$；{n5bh0h}
				* $u_0$ 可以是常值（零解，基础解；或者有时也可以是另一个 NO 生成的初值）
				* 若允许输出随机，可用随机场，类似((n3q94a))带条件的扩散生成模型（其中使用的是逐像素独立的噪声，这属于特殊的随机场）
			* 广义 hypernet，$a$ 信息在主网络旁路引入，而非主网络输入位置；{p4pb1p}
				* 狭义 hypernet，生成主网络参数权重
				* modulation，((p4pb0v))我的图形化表示不同于 hypernet
				* （主网络用注意力时）交叉注意力((nbih2c))，此时 a 信息长度可变
					* 使用注意力时的另一方式为((p4pb39))a 信息按上下文引入
				* 使用场景：((o3lk9o))DiT 原论文讨论的引入条件信息 4 方式，((p4pb42))三平面 NeRF 生成所用架构前传引入额外信息
		* 非前传式网络，优化 loss $L(u;a)$；{n5bm02}
			* 相关：((n5bl9s))映射形式-函数极小点
			* loss 构建见((p97l1n))网络/算法输出好坏评估，其下也有一些案例属本框架
			* PDE 求解情形如 PINN，及((n5bm09))元学习加速版本；loss 由 PDE 定义
			* NeRF loss 由图像渲染结果与照片比较给出
			* 文本转 NeRF，((_n5bm0d))DreamFields loss 由 CLIP 给出；{n5bm0t}
				* 此 loss 属于((p98k54))算法输出好坏评估-用辅助网络-衡量输入输出匹配度
				* 目的手段链条：构造映射(文本到 NeRF) → 函数极小点类隐式映射 → (目标函数,优化算法)
					* 目标函数 → 辅助网络-基于数据-输入输出匹配 → 度量型 → CLIP
				* loss 定义还涉及((n5cb23))图片作为 INR 替身，((n5cb4s))DreamFusion 也用此实现文转 NeRF
		* Transformer 化 $(a,u')\mapsto(a',u)$，要求 $a,u$ 均已 tokenize；{p4pb39}
			* 更准确的表述：按非移位 Transformer 的上下文引入
			* 相关：属于((o3lk9o))DiT 原论文讨论的引入条件信息 4 方式
	* 有限维向量：
		* hypernet（若 $u$ 参数化表达，如 `NIF-2204.03216`）
		* 解码式 CNN（若 $u$ grid 离散）`DL-ROM-2001.04001`, `DL-ROM-2103.06183`
		* 输入向量二部分性质差异大时代码注意：代码实现的问题，若 MLP 输入向量形如 $(x_1,x_2)$，$x_2$ 重要性低但维数高，则直接输入 MLP 训练可能出问题；{n32f34}
			* 来源：2022 MAD-CAMC Helmholtz 实验，PyTorch 线性层默认初始化方案中，输入分量多则线性变换初始权重小，导致分配给重要的那部分向量的权重不够大
			* 当时的解决方案：代码中网络改写为等价的 $\sigma(W_1x_1+W_2x_2+b)$，二线性层分开初始化
			* 其他可能方案：手动初始化权重矩阵；对 $x_1$ 人为乘上系数，系数根据向量总维数手算
			* 不排除不是所有时候都会有问题；当时是要拟合高频函数，这才要求初始化本身已高频
			* 相关框架：((n37g4c))NN 初始化
	* 函数、场（多数 NO 属于这种情况）{nbig7t}
		* ((_n7fc08))$a$ 输入时可改写为多通道形式 $(x,a)$
			* ((_n93k49))二分量输入时处理所用的网络可独立，如 $f(x)+g(a(x))$
			* concat x 是为体现空间位置信息；据说((_n9gf1z))GalerkinTf 额外引入了隐嵌入来体现
			* OFormer 使用位置编码 RoPE 体现空间位置信息((_n9gf5r))，包括 2D 版本；{n9gf5t}
				* 相关：((n98h2z))Transformer-位置编码-非序列数据，((n3t97n))INR-位置编码 用于引入高频信息
		* 与解 $u$ 同一定义域的函数（可以在不同函数空间）
			* 下方许多 mesh-topology 可变的工作是这一情形（网格泛化更差的方法当然也行）
			* 对函数空间不同的情形，目前没见过针对性的设计方案
			* 一个特例是偏微分算子（PDO）
				* `Neural-ePDO` 考察了依赖于空间位置的 PDO、并给出其满足定义域仿射变换（的子群）下等变性的条件；离散化方案包括 FD（成为卷积）和 GA；{n23g87}
					* 相关框架：((n23h00))算子等变性
				* ((n23g8n))学出一个微分算子，并证明可逼近任意卷积算子；基于 INR 参化而非离散
		* 与 $u$ 不同定义域的函数；{n4fe6y}
			* eg. `[Ying]-EIT-DL` EIT 里预测 DtN map 的 kernel¹，$a(x)\mapsto k(r,s)$, $r,s\in\partial D$，以及反向的算子，NN 设计思路为先化归为两侧函数共用一个维度，在另一个维度用插值离散化后，化为同一定义域情形；{n86k0e}
				* ¹该映射形式见一般的((n7un1l))反问题-据边界观测算子推断内部系数场，包括((n86k17))基于该形式设计的多种 NO 架构
			* ((n86j9u))沈佐伟 IAE-Net 基于积分算子构造的 NO，$a,u$ 定义域不同不影响积分 kernel 定义、计算，故允许二者不同定义域
			* ((n7n96y))NO-基于特定变换构造算子 都可以处理，如 `funcPCA-2005.03180` 和 DeepONet
			* `FNOvsDpONet-2111.05512` dFNO+：若输出维数高 $D_u=D_a\times D_0$，输入升维或 RNN 压缩输出维度；输入为输出子区域 $D_a\subset D_u$ 可延拓（零/近邻）
			* 若定义域为 $u$ 的子集（如 IC,BC），((_n4fe42))有做法仅将未知取值的部分 mask 掉，不确定性能
			* 若 $a$ 为 IC、$u$ 为全时空解、有激波等，((_n4fe63))若纯有监督训，预测解在长时间表现出平均化现象，而额外引入 PDE loss 可缓解；{n4fe6l}
				* 相关：((n4fe4x))用于PDE的NO-额外的PDEloss，((n4fe72))动力学黑盒预测-对解不区分时空坐标
			* a 在边界 u 在内部，((_q2la97))先延拓 a，方案包括：零延拓、调和延拓、可学延拓
		* 定义域形状复杂、不规则情形 处理；{n7na1l}
			* 离散仍尽量用均匀 grid，((n4tc04))其效果通常好于散点离散 + GNN 等方案
				* 否则可用((n7ne27))NO-网格泛化-离散为可变散点，mesh 拓扑可变
			* 变换到规则的 reference domain（限于拓扑简单情形）{n7ne5l}
				* 特殊区域处理
					* 无穷区域：((p4fa30))变换到有限区域
					* 区域随时间变化：((_q4rf3b))材料形变场 $\psi(-,t):\Omega^{ref}\to\Omega_t$ 用粒子（点云）方法离散；{n2gf8b}
						* 相关框架：((n2gf8i))粒子方法汇总
					* 区域可变（作为 a 的一部分），变换本身也不固定，可((n7n97v))将变换本身（及其 Jacobian）作为 NO 输入
				* 变换学出方式
					* 解方程：((_q4re3s))Laplace 方程，Dirichlet BC 保证参考域与目标域边界匹配
					* PINN：((_n7ne61))PhyGeoNet 在规则区域 CNN，变换似乎用 physics-informed 方式给出
					* 人工预设or学出：((_n7n90m))Geo-FNO 变换到规则区域后跑 FNO，变换手动给定、学出均可
					* （学出）所用 NN ansatz((_n7nf0j))可只学位移，作为 coordMLP 可带((n3t97n))位置编码；{n7nf0p}
				* 相关：((q4rc2m))连续介质物理-区域描述-参考域，((n7n92v))变区域 PDE-比较不同区域解-参考域
			* 区域分解，成为多块（有均匀网格的）区域的组合((n7n994))
			* 延拓，选取规则的 master domain；{n7n92y}
				* 延拓方式
					* 传统谱方法有 Fourier continuation 技巧((n7n96e))
					* 效果比较：((_n7na5w))gFNO+ 提到近邻延拓效果好于零延拓
					* 无视延拓部分输出：((_n7n91a))Geo-FNO 直接延拓，计算 loss 时无视延拓后部分即可
				* 网络引入自适应区域分解，自动区分原域与延拓域((_p2qf2m))；架构基于 FNO；{p8ai7w}
					* 属于((p8ai85))集合划分后处理-分类并用不同网络，这里是格点区域分解、用不同变换
				* 延拓相关：((q4rc2m))连续介质物理-区域描述-延拓到超域，((n7n92v))变区域 PDE-延拓以比较不同区域解，((p4fa30))无界区域场-截断（作为延拓的反面）
			* 这里针对固定定义域；可变定义域情形见((n7n97v))
			* 这里针对 不规则区域化规则区域，另见((n4tc04))离散方案-不规则散点转规则 grid
			* 相关：((q4rc2m))连续介质物理-区域描述-复杂区域，((n4tc09))NN架构设计-体现人对任务的理解
		* 包含多个函数 见((p8jh4r))
		* 空间维数可变，多个不同维数同时支持；{q1nb9f}
			* 作用—((q3kj0o))高维 NO 用低维解作辅助数据，协同方式包括 微调or多任务学习
			* 架构—逐轴处理（FFNO、AViT）((o7tb6f))
			* 架构—预设最大维数，架构离散化((o8fe66))支持可变分辨率（且各轴比例可变），多出来的维数按分辨率 1 处理（类似 OpenFOAM 处理 2D 流场）
				* 基于 ViT 完整注意力，如((_q1na6z))Walrus
				* 针对新维数引入额外网络层，如((_q3kg33))SWin 中加入 z-轴向注意力；{q3ki9c}
			* 训练—低维预训练高维微调 ((_q1nc1f))不如预训练时直接各维联训；{q3kk8u}
			* 训练—若数据并行，不同卡负责不同维数数据，((_q1nc16))低维数据用大 batch-size 使负载均衡
			* 相关：((p8jh4r))通用 NO-分量数泛化（因变量数可变，这里维数泛化是自变量数可变）
		* 相关：((nbig7l))系统识别，任务输入也为场、输出不同（PDE 表达式而非场）
	* 曲面、区域形状等；{n3gd80}
		* 一般意义上曲面、形状的可计算表征（未必作为输入）{n8re3h}
			* 相关：((n32f1h))流形学习（高维数据空间为主），((n8re4f))表征方式多样性
			* 点云；作输入时的 NO 架构设计见((n32f77))
				* 用于一般场景渲染时((p9pk71))可能出现孔洞，这方面不如 3DGS
			* 3DGS((p9pk6y))（针对一般物理场景）
			* mesh、单纯复形；{n8re36}
			* voxel grid 体素
			* SDF、level-set；{n8re2y}
				* 多相流问题的传统算法用到((o1ol07))
		* 若有参数化表达，则可按有限维向量处理
			* 翼形表示为((_p8bf6h))在多个固定位置 的厚度、弧度
		* 区域转化为函数、场；{p4ce2r}
			* 表示为示性函数，如((_o6ch8b))Unisolver
				* 均匀网格离散得体素网格，((_papa2b))作为网络输入
			* 表示为 ((n8re2y))SDF/level-set
				* ((_p1aa2y))与点云表征组合使用，曲面点云用 GKN 转 3D 均匀网格隐向量后，再 concat SDF 在该 3D 均匀网格上的取值
				* 若输出用 INR 表示，((p8bf7y))其输入坐标可用 SDF、体积法线 等特征增广
			* 若区域拓扑固定，表示为参考域到它的变换映射
				* （旧表述）若 $a$ 为区域内部形状、拓扑固定，可给出参考域到它的映射，是转化为函数的另一方案
				* 例如机翼形状视为 $S^1\to\R^2$
				* 上面的给出参数化表达的做法可归为这种情形
				* 与下面描述定义域情形不同：这里假设 $u$ 定义域不变，$a$ 转化为映射与 $u$ 的表征方式无关，而下方的转化则顺便给出了 $u$ 表征方式
		* 区域变换到标准域：若 $a$ 描述 $u$ 定义域、拓扑不变，可将可变定义域变换到标准域，考虑标准域上函数到函数的 NO；{n7n97v}
			* eg. 2411.18014，需额外预测区域形变的((q5tg3e))FBNO
			* 单位圆盘用作标准域，((_p8d91w))MMGP（圆盘上有标准网格，将形变后 FEM 网格插值过来）
			* NO 的输入为该变换，它是标准域上的（向量值）函数；可再引入 Jacobian 作额外 feature
				* MMGP 参考域到源域的变换 作为输入场新增分量((_p8d92p))；实现时只需先对原输入场补充坐标分量，再变换到标准域
			* 相关：拓扑复杂时可按 ((n7n994))复杂形状区域 的办法处理，标准域换为组合域
				* 例如机翼用 C 型网格变为长方形域（从而适用 CNN，精度高于 GNN），NO 在该长方形上定义，输入为逆变换（作为函数，映到原问题物理空间）$x,y$ 及其 Jacobian $dx,dy$，输出为该长方形域上看到的流场 `2022-06-17`(CSImeet2)
				* 不排除考虑不同变换方式（如 C 型网格切开的线）作为数据增广 `2022-05-27`(CSImeet2)
			* 相关：((n7ne5l))NO-固定定义域情形（用固定变换）转化到参考域
		* $a=(\Omega,f|\Omega)$ 同时包括可变区域及其上定义的函数，上面做法稍修改也能用
			* 例如若使用参考区域，输入的函数变换为在参考区域上定义的函数即可
			* 定义的函数可延拓、设计 $a=(D_a,f_a)\mapsto u$ 映射结构使其只依赖于 $f_a|D_a$，从而 SDF 等方法可用
		* 区域、曲面表达为点云，架构可用 PointNet++，DeepSets，GNN 等；{n32f77}
			* 相关：((n3qn83))集合作为 NN 输入，((n5be19))点云生成，((n8qk4m))ensemble
			* 能处理 $a$ 包含多个异质性曲面的情形（如电路仿真中不同电子元件表面），曲面性质可作为点特征一起输入网络
			* NO 学习，按照几何深度学习的设定，假设表达的为若干几何形状（例如电子元件的空间排布，而不像 PDE 连续系数场那样的情形），只输入表面的位置
				* 类似 PointNet++（待看；来源 `2021-06-04`(CSImeet)）
			* 可能性：DeepSets((n93k53)) 类似的做法，可以 hierarchical 方式
				* 先每个元件的点组合、再元件之间组合 $f(\sum_ig(\sum_jh(x_{ij})))$
				* 也可以进一步使用 DenseNet 类似做法 $f(\sum_{ij}p(x_{ij}),\sum_ig(\sum_jh(x_{ij})))$
			* PointNet 保证置换不变性为 ((ncnb6e))sum 改用 max；{ncnb5q}
				* 逼近能力定理，不像 sum 那样能严格等于，且个人认为附录中给的证明有漏洞
				* 相较 sum 也许对点云疏密不均的情形更稳定
				* 刻画旋转不变性，对点云坐标预处理使用旋转变换 $A$，$A$ 由 T-net 网络输入点云来给出，有额外惩罚项来希望它正交
				* 用于构造 NO，((_o6mk80))其输入为定义域形状
			* PointNet++ 考虑局部 feature，方式粗看为先最远点采样迭代得到稀疏点表达原点云，然后球内特征聚合；{n98h0m}
				* 处理多尺度有 multi-scale grouping 和 multi-resolution grouping 两种方式
				* PointMAE 似乎是基于这种架构的预训练方式（待确认），最远点采样所得点为球心、每个汇总 kNN 信息，称为 point patch，并进行 mask 操作；{n98h1e}
					* 相关：((n5bf17))patch，((n8mn72))mask 预训练
				* 最远点采样所得点进行((n98h2z))位置编码，PointMAE 似乎涉及；{n98h47}
			* 点云人为连边((n3qn8p)) 后用 GNN，用法类似((n79m1x))区域离散为可变散点情形
				* ((_p1aa0e))GKN 将表示曲面形状的点云 与 3D均匀网格隐向量 互相转换，NO 主干由均匀网格上 FNO 承担；{p1aa1o}
					* 该做法属于((n4tc04))非欧氏域数据先变换到规则域
					* 变换到完整 3D 区域后，原文还进一步整合原曲面的 SDF 信息（在相同网格上的取值）
			* 用 Transformer，如((n5bg3k))Shap-E
			* 转均匀网格((p8fb1n))
			* 点云的预处理
				* 减少点数，如((_n5bg3u))用 PointConv 把 16k 减到 1k
				* ((_n7bh2g))最远点采样，使大规模、非均匀点云变为小规模、均匀点云
					* ((n98h0m))最远点采样在 PointNet++ 中已用到
		* geometric deep learning 里可能有其他处理方案，如输入为点云/graph 的映射，包括 GNN
		* 可能性：隐式编码方案（编码通过解优化问题实现），并采用有限维中介；从而有两个参化的 INR，一个描述曲面形状、用于提取隐向量 $z$，另一个对 $z$ 前传得目标场 $u$
			* 或可考虑合并两个 INR，输出包括 SDF + $u$；训练时二分量均参与 loss 计算，推断时（根据输入的曲面 $a$）只用 SDF loss 优化找到隐向量 $z$，希望此时网络的 $u$ 输出分量能表示相应的 NO 输出
			* 另见((p4ce1k))AD 间接编码，INR 拟合目标场得隐向量
		* 输入增强，((_papa24))有限维几何的统计特征，如质心、边界框维度、PCA轴和特征值
		* NO 优势：如果区域有奇异形态（如机翼尖角），不排除 NO 建立的映射能比传统 PDE 求解更准
		* 架构选择：((_papa2i))有参化情形一般 DeepONet 类好，形状复杂或场有空间突变（样本内）则散点类，尺寸变化范围大（样本间）则网格类
	* {时序输入}（长度不定）
		* 输出可为数（序列分类、回归）或另一序列（序列转换；可再区分等长序列和不等长序列）
			* 输出为另一等长序列时，可额外要求单向依赖性，$u(t)$ 仅依赖于历史输入 $a|[0,t]$
			* 输出为序列时，生成方式或可结合 ((o5sm3k))singleDyn 中的做法
		* 可能场景：预测给定控制序列下的动力系统演化；带记忆动力系统预测下一步状态；其他有长记忆依赖的任务
		* 可能方法：传统 RNN，Transformer，1D CNN 等可考虑
			* Transformer 可认为是转化为集合结构，并通过引入位置编码恢复序列顺序信息
		* 可用 ansatz `HiPPO-LSSL-S4`“SSM”（状态空间模型）引入先验机理，认为对应隐藏的完整状态空间上 ODE
			* 注：自治有记忆动力系统、序列预测任务可用的 ansatz 则是 ((n35e9h))Mori-Zwanzig
			* 导出的算法（长记忆依赖的序列仍有效）`HiPPO-LSSL-S4`
		* 序列（一般意义）的多种可能含义；{n8v96k}
			* ((n8ve1a))permutation 生成，((n8ve1f))数学表达式（符号积分等算 seq2seq，((nbig72))PDE 形式作为网络输入）
			* ((n8vg6a))NN 生成高维输出-输出解读为序列
			* 科学计算动力学，自然语言，((nc6k9m))CV 任务使用“图像序列”统一表达
			* 似乎动力学预测（确定性）任务与序列生成任务有不同，前者关注整体 L2 误差，后者关注分布是否匹配；故前者涉及迭代多步后求 loss，而 Transformer decoder 只用逐词预测 loss
			* 相关：((nas998))非科研-逻辑对象的多种等价表达形式-序列、树、图
	* 集合输入，尤其是集合大小不定时；{n3qn83}
		* 集合的可能含义，见((n8qk4m))ensemble
			* 输入数据打散结果：图像 patch、语言分词、物理场散点；另有数据集（元素为样本）
			* 处理网络特性（看到这些元素一般都对应集合）：Transformer token，INR 输入点
			* 相关：((n4rm8d))tokenize，输入打散为集合
		* 可能涉及的场景：((n32f77))点云输入、((n3qg7s))输入场离散为非固定散点，其中方法也可参考
		* 另见((n4rb6l))超网络输入为数据集时 网络架构，许多方式与这里同理
			* ((n8re18))集合作为网络输出
		* 按时序处理（可能不保证对称性）；视为全连接图，可按 GNN 处理（进一步可设法简化图结构）
		* 规约操作，整合各元素信息，mean/max 等；可能先对各元素提取((n3rn3e))表征 再规约；{n93k53}
			* 用途—((n3qg7s))场输入-可变散点-DeepSet
			* 用途—设计架构满足((q6o70c))置换等变性
			* 求和、平均，如 DeepSet（原版用的求和）
			* 加权求和/平均，如((_n93k40))VIDON 用加权平均；{o31a0d}
				* 收益—如 PDEformer1 func-enc 消融实验中提升明显
				* 术语—加权操作有时称为“gating”
				* 权重取值要求：通常需非负；若还归一化则算“加权平均”
					* 组件—归一化权重，常 softmax 给出；{q6o732}
						* 训练当心：输出权重高度集中于某元素时，反传计算可能出现((q6kh08))softmax collapse，源于 fp32 尾数不足
					* 组件—次归一化权重（求和 ≤ 1）{q6nm78}
						* 如 ((n7pe44))softmax1（分母 +1）
						* 引入冗余元素，如((_q6nl1m))分类模型输出补充额外类
							* 对注意力场景，引入 global token 是否有类似的效果？
				* 对象，区分被加权集合元素类型
					* token，见 ((n7pe3p))注意力-softmax（归一化）
					* 空间散点（NO 输入），见((n7ne27))场输入-可变散点
					* 激活值神经元，见((o3198n))GLU 激活函数（sigmoid 未归一化）
					* 离散概率分布元素（如 分类、词表、VQ），见((n3bg9o))categorical 分布（归一化）
				* “加权”相关：此框架里权重由 NN 给出，其他情形包括((n9hg32))loss 加权
			* max，如((ncnb5q))PointNet（有逼近能力理论分析）、((ncnb69))nf2vec；{ncnb6e}
		* Transformer；可在输入中额外引入一个空位置，其相应位置的输出向量视为对这整个输入集合的编码结果
			* 可见((n4296a))仿TRIZ原理-顺序结构平行化，包括映射 set2set 化后用 Transformer
		* 若集合元素为坐标点，可人为连边（如 kNN）{n3qn8p}
			* 包括((n79m1x))NO-散点离散-用 GNN
			* 同样可参考((n32f77))点云输入、((n3qg7s))输入场离散为非固定散点 中相关工作的做法，有的也涉及连边
			* 编程实现，kNN 计算((n3qn7q))有相应 Python 软件包，复杂度可降到 $O(n\log n)$
			* 相关：((n79m24))graph 类型与来源汇总
		* 双射网络情形，输出也是集合、且与输入元素确定性对应
			* 集合各元素保留某种独立性，不是只要求提取出单个整体信息
		* 集合元素（不限于点集）划分后处理：{p8ag76}
			* 网络处理方式 如何受划分影响：打包（聚合），分组，分类
				* 打包：合并为大元素，再考虑交互（如对 meta-token 算注意力）
					* 目的
						* 降信息混合复杂度，信息混合有多类方式
							* 注意力类：((palf57))降注意力复杂度-token打包
							* FNO 类：打包结果视为抽象空间网格点，用 FNO1D，如((_p8ge26))Opt_RNO；{p8ge27}
						* 转均匀网格((p8fb1n))，可用均匀网格处理的 ansatz；{p8ge31}
							* 元素类型：低维坐标点
							* 划分边界：通常非全局归属
							* 划分类数：固定的较多，但理论上也可用可变分辨率版本
								* 非常规打包：转换后的均匀格点数 未必少于 原元素数
							* 打包结果：持续，编解码可用不同网格
							* 此处仅考虑实际物理空间的均匀网格，另见((p8ge27))抽象空间均匀网格
								* 抽象空间情形差异：元素未必低维，划分常全局归属，打包结果常临时（因信息损失大）
						* 引入新标准做信息混合 eg. NO 中引入新层，((_ocne9w))AMG 的 physics graph，原始散点 graph 信息汇总到该图，节点含义改变（原图：空间网格点，新图：物理变量）
						* 提高信息交互效率，避免模型陷入细节、被干扰((_p8aj1q))
					* 打包结果：临时（保留原元素嵌入），持续（替代原元素），暂时（原元素嵌入搁置、延后使用）
						* 临时：仅算元素间交互时临时打包，用交互结果更新原元素表征
							* eg. Transolver（散点 NO）
						* 持续：打包作为编码操作，对整个打包结果使用处理网络，过完后再设法解码
							* 已清除原元素信息，解码元素含义可与原来不同（如散点 NO 可在另一组散点上预测）
							* eg. MMET（散点 NO），((ocnf1k))LSM（patched NO）
							* 散点 NO 转均匀网格((p8ge31))通常持续
						* 暂时：类似 U-Net 下采样，打包结果作为整体过一段后续网络，后续结合打包前 skip connection 解码得原结果
							* 如((p8jh36))ViT 特征图下采样
							* 交互后的打包结果汇总到原元素方式：更新原元素（类似临时打包，输入处一次性引入），调制原元素处理网络（中间层持续引入），((_pbfc1j))二者可同时使用
				* 分组：交互范围局限于组内；{p8aj0t}
					* 如((o2cg1y))降注意力复杂度-范围限制，包括 grid-attn、axial-attn 等
					* ((o8h98r))NO-空间非局部信息交互-非全局情形
					* eg. （样本）((n9174b))batch-norm
					* 训练—反传仅限部分组：((_pbff3o))为降显存需求，仅若干组的交互参与反传，其余组前传时网络中间层激活值无需保留
				* 分类：独立处理，各类元素用不同的处理网络，MoE，包括 Transformer；{p8ai85}
					* eg. ((p8ai7w))FNO 自适应区域分解 以区分原域与延拓域
					* eg. Transformer MoE((p8c969))提高模型效率、容量
					* 相关：此处 MoE 用于多元素对象；若用于数据集不同样本or任务池不同任务，见((n2pj82))
				* 类比—线性变换：假设所有元素排成一列、形成向量，通信为线性变换 W
					* 分组：分块对角矩阵 W = diag(W₁,..,Wₙ)
					* 打包：低秩矩阵 W = AB；若使用硬边界划分，则 A、B 还都有额外分块结构
					* 分类：逐元素变换不涉及通信，W 为对角矩阵 W = diag(a,..,a,b,..,b,c,..)
			* 区域分解 作为特例：坐标点为元素，划分结果为区域；{p2qf2a}
				* 解读同样适用于((o2cg00))散点 NO 类 Transformer 架构
				* 以下维度类比((n2pe8f))INR区域分解，同样分 区域个数固定vs可变，软vs硬，预设vs可学
			* 划分类数，输入数量改变情形：固定总数 vs 固定压缩率（数量正比于原元素数）vs 其他
				* 固定总数 eg. Transolver、Opt_RNO（散点 NO 打包），((ocnf1k))LSM（patched NO 打包）
					* Transolver((_o2cg04))引入 meta-token，称为物理注意力；{o2cg0f}
						* 一般框架((o2cg00))逐点NO-基于Transformer
					* 原 token 与 meta-token 相互转换：
						* 逐元素线性变换得 logits，沿 meta-token 维度 softmax 得权重：Transolver
							* 反向变换，原版 Transolver 正向变换权重沿原 token 维归一化，((q5da50))logits 改沿 token 维度 softmax
						* 交叉注意力：((ocnf1k))LSM，LRSA-2604.03582
				* 固定压缩率 eg. MMET（散点 NO 打包）
				* 可变压缩率，利用序列顺序结构，((pam004))文本久远部分内容提压缩率
			* 划分边界：硬（属于单类）、软（属于多类）、全（全局归属，仅权重大小差异）
				* 全 eg. Transolver、Opt_RNO（散点 NO 打包）
				* 硬，汇总方式包括 等变网络、简单 concat
					* 简单 concat eg. ((_p8aj2m))MMET（散点 NO 打包）
					* 相关：((p4dc1z))GB-INR 离散部分插值方式，也分 加权和、简单 concat 两种
			* 划分方式：预设 vs 可学
				* 学出 eg. Transolver、Opt_RNO（散点 NO 打包）
				* 预设 eg. MMET（散点 NO 打包）((_p8aj27))对散点连 Hilbert 曲线、按曲线相邻分组
					* ((_p8aj1s))认为预设方案更容易对不同输入条件泛化
					* 利用均匀网格结构，((o2cg1y))1D 网格注意力分组，((p8jh36))ViT 特征图下采样（邻域打包）
						* ((o8h98r))NO-空间非局部信息交互-非全局情形
					* 特殊技巧，模态转换，((pam004))文本改按图像模态 tokenize
				* 预设 eg. SpiderSolver((_q14g1z))蛛网划分 以突出计算域形状影响，参考区域边界点谱聚类、形变得一般区域、按距离向内部扩散得蛛网区域划分
			* 混合功能：划分目的为降低信息交互复杂度情形，需确保划分后信息仍可全局混合
				* 多标准分组（硬划分）{pbfa2u}
					* 各层不同分组，如 SWin 窗口在部分层有滑动，MaxViT 窗口+网格 分组，AViT X+Y 分组；见((p4b97v))注意力-不同层不同分组
					* 注意力各头不同分组，见((nbrm0z))分组注意力
				* 分组+临时/暂时打包（用相同硬划分）
					* P3D((_pbf897))编解码仅算窗口内注意力，隐空间层各窗口抽象出代表 token 算注意力（暂时打包）；{pbfa5h}
					* 打包阶段额外为各组生成异质信息，后续分组交互时使用：((_pbff7h))打包阶段可学初始化，结果用于调制后续分组交互；含义相当于 group-wise [CLS] token
				* 持续打包
				* 临时软打包，如 Transolver
				* 关键类型内完整交互，((_q14g2s))SpiderSolver 全局临时软打包注意力 的基础上，补充 重要点（边界附近）的完整注意力
			* 相关：同样的信息交互目的下，若手段为不划分全量处理，可见((q6o70c))置换等变性架构
	* 方程形式作为网络输入（在 NO 中通常需与数值模态同时输入）；{nbig72}
		* 相关：纯符号输入的任务((n8v95j))ML 用于符号计算；((n8v96k))序列数据的可能含义，((nbig90))ML 与符号表达式，((nasa1c))非科研-逻辑对象等价表达形式-序列、树、图-数学符号表达式
		* 文本输入，按工作汇总
			* ICON-LM((nbih07)) 问题描述为包含 LaTeX 公式的自然语言，自然语言辅助描述其后给出的各数值数据的含义
				* 架构：统一的单向注意力 Transformer，序列依次为 问题的语言描述、数值数据、待求解的量，((nbih3x))单向注意力 mask 对不同输入有一定区别
			* 北航的工作(('ncqn3h))，Transformer encoder 输入 PDE 形式、各分量初值，正问题再输入时间推进长度、输出各分量演化预测值，反问题可恢复被 mask 掉的 PDE 系数
			* PROSE((nbih0h)) 输入为 ODE 前几个时间步数据 + 方程符号表达式（可不完整/为空）
				* 架构：二模态先用独立 Transformer encoder 分别编码，之后 concat 输入统一 Transformer encoder 进行信息混合
				* 后续工作((_oa8e9c))PROSE-PDE 1D PDE，推理 OoD 实验包括 系数、IC 分布（正弦波到 GRF）、PDE 形式（已知项的重新组合）
				* 后续工作((_o9ba2q))LeMON-PROSE 用于 1D PDE，针对新方程微调用到 LoRA
				* 2D 版本((_oam97n))PROSE-FD 网络输入输出由单点改为 patch
				* 陆路跟进工作((_q14j3e))PI-MFM 用 PINN loss 训
			* `PITT-2305.08757`，似可理解为超网络接受方程输入、输出主网络（NO）的 modulation
			* CLIP 文本、数值模态匹配((_o39j0h))，用于提供模型训练 RL reward；{o39j0p}
				* 原文引入该 fine-tune 步骤后 nRMSE 至少减半
				* 此 loss 属于((p98k54))算法输出好坏评估-用辅助网络-衡量输入输出匹配度
				* 相关：((n3bg3i))PDE loss
			* Unisolver((_o6ch5x))PDE LaTeX 输入 LLaMA3 生成主网络 modulation（adaLN）
				* 系数假定全集、输入 MLP
				* 主网络基于 ViT，区域形状、系数场 PatchEmb 后生成主网络各 patch 的 adaLN
			* UPS((p15a2r))
			* NO 利用辅助性自然语言描述信息((_oane8t))，通过 LLaMA3.1 编码后交叉注意力调制已有 NO；{obgf3l}
		* 文本输入，文本信息的形式
			* 纯 LaTeX：Unisolver，PITT
				* ((_p2ka1y))额外使用数值编码器将 IC 信息编码为一个 token 作为网络输入
			* 重整为前缀表达式（Polish notation）：PROSE
			* LaTeX + 自然语言：ICON-LM((nbih07))
			* 纯自然语言：((obgf3l))，Text2PDE((_obgf3o))
			* 方程名称 + 标量系数取值：((_p15a2c))UPS；{p15a2r}
			* 方程名称 + 输入输出场分布统计信息：((_p8hb06))MOFS
			* 简化为有限类型，只输入类标签：((_p85f16))PDE-Transformer
		* 文本信息在网络中引入的方式
			* 长序列上下文：ICON-LM((nbih07))，((_p15a30))UPS
			* 独立编码器、混合编码器、独立解码器：PROSE
			* 主网络 modulation：PITT，Unisolver，PDE-Transformer（用 AdaLN-Zero）
			* 主网络交叉注意力接收文本信息：((_oanf11))
		* 相关，PDE 文本表达式作为网络输入，但不用于构造 NO
			* 对 PDE 形式进行数据增强((_o39j09))，方程改写、形式变换、换符号等；{o39j1k}
				* 且提到原始形式、改写形式以各 50% 概率选取，引入改写形式可增强泛化能力
				* 相关：((n41f93))PDE 形式等价改写（以方便求解等），((n31n1z))数据增强
			* 文本编码器：((_o39j29))albert-math 模型，已在大量 LaTeX 数据上预训练
		* 方程不按文本输入，根据输入方程组合网络模块；需预设微分算符全集，独立学处理块
			* ((_q1qa6y))CompNO 学基础模块库，各块为参化 FNO
			* ((_q3m871))OptSplit 学各项 NeuralODE 形式的基础模块库
				* 类似的 ((_q42b25))LegONet 时间积分的算子分裂；各 BC 类型学独立算子块，非齐次先变换为齐次
				* ((_q7am35))HyCOP 算子分裂方式可学，部分基础模块可传统格式而非 NN
			* ((_q2mf5x))多方程求 POD 降阶形式，多项式插值得新方程降阶形式，插值代码 LLM 在线生成
			* SCNO-2604.11625
		* 相关，不限于单一 PDE 形式的 NO，但不靠直接输入方程来实现；{nbim8p}
			* 示例数据：((n4km5c))ICON，基于 few-shot，输入新方程的少量 $(a_i,u_i)$ pair、待查询的 $a$，输出 $u$（INR 形式）
			* 历史数据：针对含时 PDE 连续输入多时间步((nbim9t))，以此体现方程信息，如 MPP
				* 相关：((naha1f))动力学-必须输入多时间步才可预测
			* 非零通道：`2306.00258` 关注 FNO 架构预训练-微调范式，在 3 方程上同时训练，输入有多通道，靠取非零值的通道位置 表示当前在解的方程
			* 纯多方程预训练：((pbf88t))预训练用多方程，不体现 PDE 信息，靠微调适配下游
			* 逐方程输入层：((_q1ec4k))PDE-FM
			* 逐方程输出层，多任务学习((_o9bg4t))，不同 PDE 共享输入层编码、输出层基底函数独立；不支持 OoD 零样本推理
			* 元学习用于 PINN((n5bm09)) 理论上也可实现，尽管非 NO
			* 对分量个数泛化
				* ((_o44b4y))CoDA-NO 对各分量分别引入位置编码，出现新分量时为其赋予新位置编码即可
				* MPP 的做法是((_o48b3o))ViT 输入通道数为场分量数，引入新场需扩充输入通道数（新参数随机初始化）并微调
					* DPOT 的做法似乎也类似？
					* ((_o49b2s))PROSE 预设最大分量数，不足的补零；((_p2af5p))同组的 BCAT 沿用
					* ((_ocoa0n))VICON 预设分量全集，当前数据没有的分量 mask 掉
					* ((_p15a19))UPS 预设分量全集，没有的分量按 0 处理
				* OmniArch 用 Transformer 解码器，((_o48c0p))各分量直接按固定顺序输入即可
				* 各分量独立 tokenize，添加不同的调制：((_p85f0t))PDE-Transformer
				* 交叉注意力汇总为隐空间单变量，时空信息交互后再解码原变量信息，((_pain6j))MORPH
				* （非基础模型）组合扩散((_pcfb6g))，先用非耦合 PDE 数据训扩散模型，再泛化到耦合 PDE
				* 相关，各物理量独立 tokenize 但非用于基础模型；{p8jh4r}
					* 不同维变量独立：((_p8jh4e))盘古气象模型同时涉及地表变量、空间变量，分别用 2D、3D patch
					* 输入输出函数多分量独立处理 `MIONet-2202.06137`，有相应逼近理论
					* 相关，多对象分别 tokenize 还包括((p8jh4p))对不同投影平面独立 tokenize（triplane INR）
		* 包含多个函数 `MIONet-2202.06137`，有相应逼近理论
			* 相关，((q1nb9f))跨 PDE 维数迁移、共享架构，方程自变量数可变（分量泛化是因变量数可变）
			* BC 刻画：
				* 隐式：通过数据体现（如自回归历史数据）
				* 数据 padding：((_ocoa1g))输入场根据 BC 做 padding
				* 注意力掩码：((_p85f1z))SWin 滑窗注意力在非周期情形屏蔽跨边界交互
					* 仅区分是否周期，对非周期情形的多种情况 无进一步区分
			* 相关框架：((o1le7k))NO-输入形式-间接输入
		* 相关，((_o2fm01))PDE 表达为交换图，程序根据该图自动生成数值求解算法；{o2fm0j}
			* 另一个类似的做法((_o3hc3c))
			* 相关：((o2fm12))PDE-数值方法-传统软件
			* 范畴论还用于形式化 LLM 推理框架，((_o9of5x))DoT
	* 混合形式，同时有上面的一种或多种
		* 同时有有限维向量、函数：可将向量当作常量取值的函数，从而化为只有函数的情形
			* 向量维数较高时需要小心，见((n32f34))输入向量二部分性质差异大时代码注意事项
			* 向量+序列情形也可用常取值序列
		* 有向量和另一种：向量通过 hypernet 引入，主网络只输入剩余部分，如 `HyperFNO-NIPS22`（另一种为函数）
		* 利用 Transformer 灵活性，如((_n7hl7p))GNOT 同时考虑向量、形状、（由散点取值给出的）场、网格连边情况 等
	* 若输入维数高；{ncnc2n}
		* 该场景出现于：((n4dl73))高分辨率场，((ncnb6m))NN 参数作为另一 NN 输入
		* 也可参考((n4dl7e))NN 要产生高维输出时架构设计，包括集合化、序列化、层次输入等处理办法
		* （非特别高维情形）拆多组，作 MLP 不同层 modulation
			* 如((o6g03a))不同 scale 信息不是 concat 统一输入，而是分在不同层输入
	* 若考虑对任务泛化（大模型涉及），需采用统一输入形式；{n4tj9s}
		* 包括两方面：(1) 不同任务输入本来是不同形式，将其先改写为统一形式，(2) 对任务本身的描述 如何作为网络的输入
		* NLP 任务、任务的输入均可统一用语言描述，已用于((n4ff89))上下文学习
		* CV 的相关讨论见((n4tk05))
		* 科学计算的((n4tj7h))，包括((n3pm4j))将约束优化问题作为统一框架
		* 关于“多任务”的一般讨论见((n4sk4r))
		* 相关：((n9999m))网络(输入,输出)≠ 数据(样本,标签)
	* 间接输入，需改写映射输入输出形式；{o1le7k}
		* 一般框架((q7aj9t))网络输入改写，((n9999m))数据预处理-网络(输入,输出)≠ 数据(样本,标签)
		* 表达参化 PDE 求解迭代算子 $(u,a)\mapsto u_+$((n2rm3m))时，不输入 $a$，而是输入 PDE 残差 $e$；{o1le5m}
			* 如((_n2rm17))，认为 $u,a$ 差异大、同时作为网络输入时网络难处理
				* 具体地，原文考虑 Helmholtz 方程求解，$a$ 包括波速场（分片常数）、源项（点源），而 $u$ 为波场结构，方程残差 $e$ 似也是
				* 这种问题在使用 CNN 等形式的架构时或更明显，因涉及跨通道卷积，但这些不同通道结构差异太大
			* 该替换过程造成信息不完全，据此可((_n2rm3a))引入记忆状态，其观点来自不完全观测 MDP；{o1lg1g}
				* 引入记忆状态的网络架构设计方式见((n2s979))
				* 如果是线性方程残差，则((n3fe1c))只输入当前残差即可，无需有记忆
				* 一般见((o1lh4p))将迭代历史作为间接输入方式
			* error 作为网络输入，相关：((n99a18))SAM 图像分割，根据当前预测的错误构造新数据
		* ((nbim8p))同时解多个 PDE 的 NO-间接输入 PDE 形式
		* 针对迭代格式，将迭代历史作为间接输入方式；{o1lh4p}
			* ((o1lg1g))针对 PDE 迭代求解器
			* 针对动力学，((naha1f))可这么输入的包括 多余的状态变量、动力学参数、动力学形式
		* 另：通过元学习，$a$ 通过 loss 间接输入，属迭代形式表达的 NO
		* 另：也可只视为额外的 augmented 输入项，原有输入 $a$ 仍保留
* 网格泛化（对 $a(x)$ 为函数的情形；主要考虑这一函数的表达方式）
	* 注：狭义 NO 要求必须能对分辨率泛化，这样才算逼近了无穷维空间之间的映射
	* （评）可能要修订，区分处理不同形状复杂度的区域、处理区域可变性（变化范围）、处理同区域不同网格
	* 输入、输出均有离散化、参数化、基底表达三种，见((n32e9r))场的数值表征
		* 细分类维度：输入输出函数表达方式（是否相同），网格拓扑灵活性，网格分辨率可变性
		* 注意 ((n32f00))场的数值表征-离散化若指定标准插值格式，则成为基底表达，从而输出可能按无网格对待，如 FNO
		* 以下若无特殊说明，假设 $u(x)$ 和 $a(x)$ 有相同表达方式；$u$ 用参化表征情形可见((n42e71))NO通用架构-hypernet
	* 离散为固定 grid：CNN，U-Net, BCR-Net, `GalerkinTf-2105.14995`
		* CNN 逼近能力理论证明见 `2207.01546`
		* `NO%`“高维或高分辨率场”有特殊处理方式
		* `LSM-2301.12664` 打 patch 后用类 Transformer 架构
		* 基于 SwinV2 的多尺度 patch 结构((_o6cj79))
		* 性能：((p64a1k))ViT 效果好但性价比不如 FNO；CNN 表现不出众（针对 FD-Bench）
		* 多变量交错网格 作为特殊情形，((_p78a4m))可先转换到同一套共享网格的隐空间；{p78a52}
			* 相关框架：((n4tc04))转均匀网格-散点集转换
	* 离散的 grid-size（分辨率）可变：{o8fe66}
		* 架构—一些改进的 U-Net 做法可能是如此；FNO（输出也可理解为基底表达）
		* 架构—NaViT 用 2D 相对位置编码处理不同尺寸图像（可以不是方形）((o4hn6z))
		* 架构—据说 CV 里有 pyramids 方法，可处理不同分辨率并信息融合，可对分辨率泛化
			* 信源：240830 红 IM 私聊，FjXkVs
		* 精度—离散化失配误差：((_p8qg9v))训推分辨率不同时 NO 有效性受损，及相应解决方案（无 alias）
		* 精度—分辨率泛化失败：高频伪影，((_q6jg3q))FNO 在训练未见分辨率产生（仅低训高推情形？）
		* 用途：若各轴分辨率比例可独立变化（不固定长宽比），可((q1nb9f))支持多维度数据联训
		* 训练—多分辨率数据配比，((_q6jg52))7:1:1:1 or 90:5:3:2；{q6jg5c}
			* 资源属性一般化（数据 低分辨率→ 低保真）见框架((ncbn0o))多保真训练
		* 相关：((n2pf83))网格加密、((p8tl0t))离散误差（不是只有加密网格一种方式）
		* 相关：((o8fe4l))NO 带上采样层，((n5da1a))数据的多分辨率
	* 尽量用均匀 grid：非欧氏 grid 域¹数据尽量先变换到规则域，否则用特殊域的专有方法精度不高，球面、GNN 等 `2022-03-11`(CSImeet2)；{n4tc04}
		* ¹这里“域”指函数表征方式，不是指函数定义域；这里讨论不规则散点转均匀 grid，而复杂定义域形状的处理（转规则区域）见((n7na1l))
		* 不过 `GraphCast-2212.12794` 在球面的特定均匀网格跑 GNN（涉及 multi-mesh）效果还行？
		* 一般域转方形，从而可在方形上用 grid；按转方形方式分类
			* 域变换：((_n7nf1i))Geo-FNO，实验结果好于（基于散点表征的）GKN、DeepONet
			* 延拓：((_p2qf1y))
			* 固定插值：((_n7na3w))gFNO+
				* 注意可学插值算散点集转换，见下方
		* 基于散点集转换，目标点集取为 grid；区分 可学插值、编码；{p8fb1n}
			* 二类区别，新旧散点集均有其 feature space，二者相同为插值，不同为编码
				* 理论上可逐点升维编码再插值，整体而言 feature space 也不同？
				* 新旧散点集信息传递方式，（通常）插值 传递方式偏手动设计，编码 为自适应学出
			* 固定规则插值：最近邻（KDTree 数据结构），局部线性插值
				* 散点密度足够情形，((_p8fb2c))利用从均匀网格插值的双线性权重（而非原本的散点到均匀网格），反向汇总到格点位置，汇总时可加权、可按来源方位分类
			* 可学网络插值：
				* ((_obgf49))Text2PDE 使用可学的编解码器，将一般 mesh 物理场插值到均匀网格，从而可用扩散模型
				* 基于交叉注意力((_q4tf5z))，attn score 对正向、反向插值共享，其生成不仅依赖于格点位置 a（Q 涉及）、散点位置 g（QK 涉及），还依赖于散点处物理量 q（K 涉及）
			* 掩码 mask，要求散点为网格点子集，或先主动近似到网格点位置：如((_q3kl5u))
			* 编码：
				* 信息传递范围，原散点信息传递到哪些新格点
					* r-邻域：GINO((p1aa1o))
					* 多尺度 r-邻域：GAOT((_p6af1n))
					* 散点所在方格内 4 顶点：CoPS((_pbgf1m))
					* 理念上全局传递，实现时分块网格降计算量，仅考虑网格邻域范围传递：((_q1q89e))ECHO
				* 信息传递方式
					* GKN：GINO
					* 逐点编码后 GNN：CoPS
					* 注意力：GAOT
					* 连续卷积核：((_q1q888))ECHO
				* 部分格点邻域无输入点、无信息流入，((_pav99y))但仍提高全局性能
			* 若纯点云（各点仅坐标不同、无取值信息）：
				* ((_p8fb18))体素化，计数化，双线性权重汇总（包括四方位分类汇总），可学变换
			* 先转均匀细网格再压分辨率：((_q1q89y))ECHO 认为直接编码到粗网格会损失信息，改先编码到均匀细网格、再逐步压缩
			* 特殊情形，多变量交错网格 转均匀网格 可直接卷积((p78a52))
			* 目标点集不限于普通 grid：((_p6af1l))可考虑 三平面 grid，随机降采样散点 等
			* 相关：可理解为((p8ge31))点集输入划分后处理-打包-转均匀网格
		* 其他先变换到均匀 grid 的工作还有((_p26f2d))CoDA-NO
		* 多物理 若((q4rd9n))不同子域有独立物理场（如流固耦合），((_q4tg14))grid 特征可汇总所有场的信息，而非仅 当前所在子域上定义的场 的信息；{q4tg1m}
		* 相关综述((_p2oe9l))
	* 离散化换用另一组点，插值、编解码等
		* 场景包括输入散点的 ansatz 精度不高，不如 CNN 等规则网格 ansatz，能补偿插值精度损失
		* 非对称编解码：`GraphCast-2212.12794` 球面数据在经纬方形 grid，NO 推断在多面体 mesh，编码输出在 mesh 单顶点，解码输入为 mesh 三角三顶点
	* 多个固定 grid 组合，来自((n32f0k))场的数值表征-多块规则网格拼成复杂区域，或有已知位置边界层等；{n7n994}
		* 以下主要讨论 NO 架构设计；该策略的一般讨论见((n7ne10))
			* 这里设场已取定这种特殊离散表征；NO 处理不规则区域（未取定表征）讨论见((n7na1l))
		* 包括 2D 机翼 C 型网格这类退化
		* $u$ 可在同一个大 grid 上表达
		* 若用 ViT/CNN，重组为大 grid、mask 掉无意义部分、引入边界连续性约束等均与一般框架((p3v96m))相同
		* 网络架构另可用 `ViG-2206.00272`，每块 grid（而非每个 grid point）作为 GNN 一顶点，无需拼接、mask 等复杂处理；纯序列 Transformer（非 ViT）可能也差不多；{p3v96z}
			* 大网格（如 3D）ViT（完全图）存储、计算复杂度过高，而部分连接的 SWin Transformer、ViG 好很多 `2022-09-02`(CSImeet2)
		* 与散点离散都可处理复杂区域，但用更规整网格后 CNN，ViT 精度高于散点 GNN
		* 输入的各格点 feature，除 $a$ 取值外还可有格点坐标、网格变换（矩形均匀网格映到真实区域）的 Jacobian
			* 若有这种扩展 feature，可考虑使用可变（微调）的多块网格划分方式，作为数据增广 `2022-08-12`(CSImeet2)，NO 自动根据当前网格分块方式预测结果
	* 离散为固定散点（若人为将散点连边，则成为 mesh）：
		* DeepONet 原版（输出为参数化，无网格）；后续的一般框架 `HyperDeepONet` 未突破这点设定
		* ((n7kj9d))GraphCast 定义域为球面，在 multi-mesh 上跑 GNN，包括粗分辨率顶点向细分辨率顶点消息传递；{n7l95q}
			* 优势：降低计算代价，使用浅层（单层？）GNN 即可表达非局部依赖
		* NUNO((_n7je83))根据散点构建 分片均匀网格，输入数据插值到该分片网格，在分片均匀网格上训 NO；{n7je8l}
			* 分片均匀网格另一用途是((n32f0k))拼成复杂区域
			* 相关：((n7jf0i))不同离散方案间插值
	* 离散为可变散点：{n7ne27}
		* 散点来源，除了 固定欧氏区域用不固定离散 外，还包括((n32f77))区域本身为曲面，可能还可变
		* 输入散点转均匀网格((p8fb1n))
		* 人为连边((n3qn8p))则是 mesh-topology 可变；{n79m1x}
			* 相关：((n79m24))graph 类型与来源汇总
			* 按 GNN 架构区分
				* GKN，包括((p1aa1o))用于将曲面形状转为均匀网格隐向量
				* `2206.00711` GNN 形式 NO 基于 `MeshGraphNets-2010.03409` 建立，从而可处理可变的复杂区域
				* graph Transformer：((_o29k9q))HAMLET，((_ocna2p))AMG；{o2cf9y}
					* 相关：((o2cg00))基于 Transformer 实现可变散点离散
				* PointConv：funcGAN((_o12g45))，均匀网格上效果接近 CNN；{o12g99}
				* 相关综述((_p2oe9z))
			* multi-scale graph：使用类似多重网格做法设计 GNN 变种，中途下采样到 coarse mesh；{ocna32}
				* 相关：((n5da0x))NO 设计-空间多尺度
				* 可理解为 GNN 版本的 U-Net
				* eg. `MGKN-2006.09535` 引入 multi-level graph，前传我觉得像 U-Net，不过文中解释为传统快速多极算法+V-cycle；{ocna3c}
				* eg+ ((_p2gf0g))RIGNO；GraphCast 架构也是多尺度 graph（尽管实验所用的 mesh 固定）
				* 不对完整网格消息传递，做局部限制：AMG((_ocna10))local/global graph，前者只做局部消息传递，后者由每个 local graph 选一个代表节点组成
			* 相关：上方 多块规则网格拼成复杂区域 情形，可将每块局部网格（而不再是单个格点）作为 GNN 一个顶点
			* 性能不如 ViT、FNO 等架构((p64a1k))（针对 FD-Bench）
		* 离散外微分表示下自带 mesh，((_q17f8p))节点为 k-单纯形，4 类邻居分别做消息传递
		* GEN-1904.09019（涉及对顶点位置 BP 优化；待确认）；
		* 用类似 ((n93k53))DeepSet 的架构输入各坐标点；{n3qg7s}
			* `VIDON-2205.11404`，DeepONet branch-net 换为改进版 DeepSets
			* ((_oajf62))DeepOSets 似乎用的还是原版 DeepSets
			* ((_o12g3l))funcGAN 认为 PointNet、DeepSet 不足以满足需求：需体现各坐标点的相对距离；最后用了((o12g99))基于类 GNN 的 PointConv 做法
			* 单点编码—类INR：考虑到坐标点作为网络输入，或需用与 INR 类似的架构？
				* (x,a(x)) 单点编码网络，((_pbeg5s))用 INR、a 按 shift modulation 输入
		* 生成固定长度隐编码，还可用 Transformer 交叉注意力，((_o6tf5w))固定数量的可学 token 作 Q，输入散点作 KV
			* ((_o6ua3a))用了类似架构，将可学 token 称为 supernode
		* Transformer 中每坐标点点作为一个 token；{o2cg00}
			* ((_n8vn3e))Galerkin Transformer 我认为可以处理均匀分布的散点（但不能非均匀分布），个数也可变
			* ((n54f4n))OFormer 等架构输入散点可变，可认为就是用的这种方式
			* ((_o1df8g))IPOT
			* 使用交叉注意力机制提取输入坐标点的信息((_obea6r))，这些坐标点间未做信息混合（否则关于点数是平方复杂度）
			* 降注意力复杂度：((o2cg0f))集合打包处理，如 Transolver
			* （旧）降注意力复杂度：Transolver((_o2cg04))基于额外引入的 meta-token 算注意力，称为物理注意力
				* 按区域分解解读：注意力不是用所有格点，而是用学出¹的区域分解、算区域间注意力
					* ¹“学出”或应换为“样本自适应”，因 NO 对不同输入场（样本）使用的区域划分方式可能不同，而不仅仅指训完后的划分方式与训前不同
					* 相关：((n2pk9m))INR-自适应区域分解
				* 未降低 FFN 部分复杂度
				* 类似做法，用于在 NO 中引入新层，做全局信息混合：AMG 的 physics graph，原始散点 graph 信息汇总到该图
				* 类似做法，用于构造编解码器：((ocnf1k))LSM 完整输入与预设的 C token 做注意力，得 C 隐向量
				* 相关：((o2cg1y))Transformer-降低注意力成本-利用特定数据结构
			* 用 graph Transformer 的((o2cf9y))
			* 相关综述((_p2of01))
		* `IAE-Net-2203.05142` 利用积分算子构造 NO，不仅允许 $a$ 离散所用散点变化，还允许 $u$ 离散所用散点（独立于 $a$）变化；进而允许 $a,u$ 定义域不同；{n86j9u}
			* 相关：((n4fe6y))NO 中 $a,u$ 定义域不同，((n86k1x))非线性积分算子
			* 同样允许两组散点独立变化，`OFormer-2205.13671` 使用基于 Transformer（每点为一 token）的架构达到
		* AD 间接编码，先用 INR 拟合目标场得隐向量；之后可过 MLP 生成解码 INR 的调制向量；{p4ce1k}
			* ((_p4ce0v))空客 Aero-NeF 用于编码翼形信息（结合了((p4ce2r))形状转为场-SDF）
			* ((_p8e99f))PDEfuncta 输入输出场共享隐向量，从而有双向变换，正反问题前传方式一致
			* 含时版本见((n2h89q))动力学-隐空间-AD作编码器；以下主要关注不含时版本
			* 相关：属于((n42e6y))有限维空间为中介
			* 相关：((n7n974))参化表达整体作为输入，而非用于输入间接编码
		* 扩散模型刻画 $(a,u)$ 联合分布，((o6uf1s))去噪生成过程用 $a$ 的散点值提供 guidance；{o6uf27}
			* 预先假定了固定网格，散点可能出现的位置集合位于网格点上
			* 可处理 $a$ 信息不完整情形，此时输出的 $u$ 相当于 ((o6uf3s))UQ；{o6uf3o}
	* 基底表达（无论基底固定与否）：
		* 固定 grid 样条基底的 `spline-PINN-2109.07143`
		* FEM 基底：暂未发现类似工作；多项式次数、顶点位置可变的可能性应该存在
		* （看不懂当时什么意思了）FNO（能做 FT 即可；要求离散化映射 $\mathcal{A}\to\R^{n\times d_a}$ 为线性映射，故还不算 mesh-free）
		* 相关：`2022-05-13`(CSImeet2) 网络输出结果可为 $u$ 的小波系数
	* 参数化表达（从而可无网格求值）：{n7n974}
		* 先采样（分辨率可调），转化为上方的 可变散点 情形
			* `funcGAN-2102.04776` 的 GAN 判别器属此类，$a$ 采样为点云 $\{(x_i,y_i)\}$ 后用 PointConv
				* 此处 $u$ 只是有限维向量；稍修改应可得到 $u$ 为散点离散表达的情形，参数化表达也可行但看起来训练稍难
			* 可能性：有另一个网络负责生成离散点；可 active-learning、RL，交互多次才给出最终结果
				* 交互多次时，接收该散点的网络可有 UQ 等机制，从而为散点生成网络提供信息、调整下一次生成的散点分布
		* 预设低维参化表征：若输入场网络架构可自行设计，可用预设的低维参化表征方案，从而等效于针对向量的常规任务
			* 如 `functa-2201.12204`；医疗领域例子((_p3ce7d))
			* 一般地 `假设空间参化方式汇总` 都行
			* 若 $a$ INR 架构未知，或可考虑用自行设计架构 NN 先训练逼近该 $a$，再用这里的框架
				* 训练过程可元学习加速、hypernet 先估计初值等，不过预计仍有一定代价
		* $u$ 为同空间定义的 INR 时，`INSP-Net-2210.08772` 将学出的微分算子作用于 $a$ INR 网络、生成 $u$ INR 网络的计算图，可证明该 ansatz 可逼近任意卷积算子；{n23g8n}
			* 不过不排除主要适合逼近局部卷积算子，全局卷积能逼近但很低效
			* 注：学微分算子应该算一般的 PDO 框架，其中也有基于离散而非参化的做法((n23g87))
		* 输入先用 INR 拟合以提取隐向量见((p4ce1k))
		* 完整 NN 参数输入((p2oh7g))meta-NN：((_p2oh70))输入 NeRF 网络，可泛化到训练未见的架构；{p2oh7h}
		* 参化表达不作为直接输入，而用于输入间接编码，见((p4ce1k))AD 解优化问题得隐向量
		* 性能不如 ViT、FNO 等架构((p64a1k))（针对 FD-Bench）
		* 相关：((p2oh7y))仿TRIZ原理-替身-场数据的替身
	* 用含时 PDE 设计网络架构则较灵活，如((n3sf7z))（虽然原文仅试图替代 CNN）{n3sf8e}
	* 引入有限维中间域((n42e6y))，从而不局限于特定的函数表达方式，也不要求输入输出表达方式一致，这里不列出
	* 相关：若 $a$ 包括 $u$ 定义域形状，则大致同 离散为可变散点 情形；定义域拓扑固定时还可考虑 ((n7n97v))可变定义域变换到标准域 化为不可变定义域的算子
		* 此时 $u$ 用参化表达较方便，如 `2204.06684`
	* {高维或高分辨率场}难直接作为输入输出，通常需先压缩，可能的处理方式：{n4dl73}
		* 相关：((n4dl7e))NN 要产生高维输出时架构设计，包括层次输出处理办法
		* 空间分辨率压缩；有时也用于((o8ue5k))tokenize；{p3va9b}
			* “压缩”含义区分，针对现成数据（编码）or 待表征对象（解码）
				* 针对张量数据，要求网络输出低分辨率，用编码器网络；
				* 针对待表征对象，如张量型可训练参数，允许用低分辨网格表达高分辨内容，用解码器网络
					* 如((p59e68))减少 grid-based INR 存储消耗量
					* 超分辨率网络((n8i94k)) 作为特殊解码器，允许先仅表征低分辨率样本
						* 如(('patm59))MovieGen 先生成低分辨率视频、后处理再超分辨率
				* 相关：((n31m5r))数据压缩
			* （旧）空间有重叠压缩：指单个输入点的信息 在压缩后存储于多个点的 feature 中
			* CNN 编码器全局压缩
				* 训练稳定性 好于原 ViT 的 patch 无重叠线性投影压缩((p6ga15))
				* ((_pbf87s))观点认为 3D 及以上 CNN 好于 patch 压缩，因单 patch 格点多信息量大
			* 逐 patch 编码：逐 patch 编码为（无结构）隐向量，全体隐向量组成网格
				* patch 划分方式：最开始无重叠；有重叠版本如((_p6rc1k))AlphaGenome
				* 单 patch 压缩方式：
					* 纯向量拉直 flatten
					* 线性投影
						* 实现用 ((n8hj6h))conv2d，((_n8hj56))此时卷积核尺寸、stride 大小均等于 patch 尺寸，见 AFNO 实现；{n8hj6b}
							* 写代码时需手动初始化权重，见 SRS((parn54))
						* 投影矩阵仅随机生成、不可学，可缓解 ViT 训练不稳定问题((p6ga15))
					* MLP
					* CNN，如((_p6rc1z))AlphaGenome
						* 这里假定 CNN 输出被 flatten；若保留空间分辨率，算逐 patch 压缩（见下方）
					* 带全局隐向量 INR 隐式编码，如 ((n4ta1s))DeepLS
				* 对 ViT，小压缩率（小 patch-size）增强架构的平移等变性先验，提升泛化；{p7bg4c}
					* 来源：PDEformer-2 之后的 ViT 架构消融
					* 相关：((oapa1r))patch-size
			* 逐 patch 压缩，再合并各 patch 结果
				* 若压缩为 1x1 单点，则退化为逐 patch 压缩为无结构隐向量
				* 例：大网格（如 3D）压缩可 patch AE 编码降尺寸后过 CNN `2021-05-21`(CSImeet)
					* 输入维数 $C\times H\times W\times D$ 编码后 $c\times h\times w\times d$，$c\gg C$；AE 由预训练获得
			* 无网格隐式压缩，grid-based INR 训练隐式编码
			* 二次压缩，如 SoRA 先 CNN 再 patch((o33a2v))
			* 空间压缩率（tokenize，不直接针对高分辨率场）：{oapa1r}
				* CNN+离散化用空间 8x，视频再加时间 4x，((_oap98p))Emu3 用到
				* ((p7bg4c))ViT tokenize 小压缩率利于泛化（架构平移等变性先验）
				* 非固定压缩率
					* 多级压缩结果共存：((_ocke7e))多尺度 patching，单图按不同 patch-size 生成多组 token
					* 不同前传不同：
						* 方案—FlexiViT 缩放卷积核；((_q1ka4i))若还涉及解码，卷积核可与编码共用
						* 方案—CSM((_q1ka4l)) 固定卷积核、变 stride，从而各 patch 可有重叠；需结合边界 pad
						* 动力学自回归时间推进，每步交替用不同压缩率，((_q1ka3i))可抑制特定频率的误差累积、提高长时间预测精度；{q1ka3o}
							* 相关：((q1ka40))动力学-抑制误差累积-不通过loss，((n3ri89))仿TRIZ原理-迭代-交替
					* 不同位置不同：((q1ij5t))空间局部自适应加密
			* 压缩结果的处理
				* VQ 后处理((n6sf8q))：压缩所得 feature 向量可追加 codebook 量化操作，如 VQ-VAE
				* 处理所用网络：可在压缩后的空间用其他映射，如 AFNO 相当于 ViT 内注意力换为 FNO
					* （旧表述）压缩后处理的网络
			* 压缩造成混叠 alias，((_q1mc95))压缩前先随机短距平移、输出层平移回来，可提升 NO 自回归长期预测稳定性；{q1mc9j}
				* 相关：((n3h98i))alias，((q1ka40))动力学-长时推出-不靠loss
			* 输出层 unpatchify 边缘可能不连续，出现棋盘伪影
				* ((_q2kf0k))训练时额外引入梯度连续性 loss
		* 降维得三平面等，类似((oc2a2n))grid-based-INR—三平面；{p3vc15}
			* 如((_p3vc07))DynamicCity 4D 场景 VAE 压缩为 HexPlane，编解码网络架构针对体素数据
		* 信息分块，tokenize：`2022-09-30`(CSImeet2) 可能性，参考 NLP 中长序列翻译的做法，文章划分为段落、句子，对应这里划分为大网格划分为小块；包括上面讨论的 多块规则网格拼成复杂区域
		* 换参化表征：以上做法中，高分辨率场均用固定规则 grid 离散化表征；另可用参数化表征 `functa-2201.12204`，通过表达隐向量之间的映射关系来表达 NO
			* 上方讨论 ((n7n974))网格泛化-参数化表征 的也可参考
		* 类似场景：((oa3b7z))grid-based-INR 网格参数量压缩
			* 区别：其网格为人造架构的一部分，改架构设计后可能不存在完整网格；此处网格属于数据的一部分，更接近原生（尽管物理场离散方式选择本身也有人为因素），与网络架构无关
		* 不压缩（或几乎不），直接处理高分辨率数据；常需((p8ag76))划分后处理
			* 逐块处理、补充块间通信：P3D((pbfa5h))划多窗口，编解码仅算窗口内注意力，中间层各窗口抽象出代表 token
* 基于特定变换构造算子，及一般算子内部结构设计方案；{n7n96y}
	* 若能表达为积分算子可将之学出（变换后可表达的也算），((n3fe2j))paramPDE-学Green函数
	* 一般设计思路，((_p8hb4e))将有限维架构改写为 NO
	* 基于 Fourier 变换（谱卷积）：FNO（先升维再 FT，频域上使用逐点线性变换）{n7kj8y}
		* ansatz 能力问题；{p8hf92}
			* 可处理非周期 BC（不同于传统 Fourier 方法）：((_ncqf2l))FNO 原文解释为逐点线性变换保留了绝对位置信息
			* 非线性处理受限？((p8hf8z))由于谱卷积线性性质
			* 间断解预测受限，((n7jm11))由于 Fourier 变换性质、频域截断
			* 自回归精度受限，((n99f7n))受频域截断影响
			* 抗噪能力或不强？((n7nf7k))有噪声时 FNO 表现差于 DeepONet
			* 精度不如 ViT 但性价比更高((p64a1k))（针对 FD-Bench）
		* 与其他架构联用；{n8jh0d}
			* `U-FNet-2209.15616` U-Net 用 FNO 层增强
			* PDE-Net++？
			* FNO-DEQ((n8jh0b))，DEQ 中迭代算子用若干 FNO 层
			* 用超网络生成 FNO modulations((n8ja9w))
	* 小波启发的可学变换：BCR-Net（线性 CNN 起到仿小波变换功能，不升维直接作用，同频率分量使用 locally-connected 变换）
		* 注：旧表述为 传统变换域改写为学出来的版本，用于表达算子
		* 用 CNN 需要 $a(x)$ 在 grid 上给出，离散或基底均可
	* Galerkin 注意力：`GalerkinTf-2105.14995` 基于对 Transformer 的不同解读，有 Fourier 变换版本和 Petrov-Galerkin 投影的版本
		* 需要 $a(x)$ 用 grid 离散给出（这样向量内积才对应矩阵内积的离散化）
	* Lagrange 形变((_q3tf4e))，逐点 MLP 预测位移场；{q4rg7w}
		* 相关：((q4rb00))连续介质物理-机制描述-Lagrange
	* 等变架构：通过调和函数等（待确认）引入旋转等群等变性质，SE(3)-Transformer `2022-09-02`(CSImeet2)
		* 一般框架((n3h983))equivariance 架构设计
	* 混合架构，不同层用不同格式
		* ((_n8vm6l))Galerkin Transformer 文中实验是 4 层 Galerkin、2 层 FNO
	* 若涉及上采样层，建议((o8fe4l))上采样放到 NO 输入层，而非输出层；上采样还可避免 aliasing
	* 性能比较，((_p64a15))ViT 效果好、FNO 性价比高，比较对象包括 有限维中介、CNN、INR、graph；{p64a1k}
	* 相关：((nckf86))不使用纯黑箱架构，如整合低精度求解器、只学与高精度解误差
* 输入层
	* 空间分辨率：不变（如 FNO），降低((p3va9b))，升高((o8fe4l))
		* 相关：((n5bf17))patch，((n4rm8d))tokenize
	* 通道数：不变，升维
		* 升维实现用卷积：若用均匀网格、空间分辨率不变，可用 1x1 卷积完成升维的线性变换操作；{p8he9w}
			* 相关：((n8hj6h))卷积
	* 预训练，((_q3vf8v))基于 AE loss 预训练物理场 tokenizer
* 引入空间全局信息交互 方式
	* 基于空间变换：在变换后的域做通道变换，包括 FNO layer 谱卷积
		* 空间变换，FT 等
			* FT 编程实现
				* 涉及复数，代码中手动分离实虚部计算；{o6r95k}
					* 谱卷积默认输入输出为实数值，仅在频域变换时按复数操作；((_p66a3o))若问题输入输出本就复数值就不需要
					* 其他涉及复数运算的网络：夏志宏老师的 CauchyNet 涉及 Cauchy 公式离散，((o6r95t))LNO 用 Laplace 变换，((o6ua4g))WIRE 所用 Gabor 激活在复数域算三角函数
					* 所谓 CVNN 复值神经网络已有较多相关工作((_o6ua6k))
					* 激活函数可逐分量作用，((_o6ua74))GeLU
				* 混精：半精度下((_o2ga7i))FFT 前先作用 tanh 有精度提升，对全精度没用；{o2ga7u}
					* 称为 pre-activation，相关：((ncqf28))激活函数，((n7sa8l))半精度
				* FFT vs DFT，((_p8hg3q))仅保留数十频率时后者更优
			* FT 限制到单坐标轴：((_o7ta69))FFNO 各坐标轴独立谱卷积，结果求和；{o7ta9d}
				* 坐标轴限制属于((o8h98r))NO-空间非局部信息交互-非全局情形
			* FT 推广到其他定义域
				* 球面上定义的算子，涉及球面的 FT、球谐函数；{n7l952}
					* 19 年组会考虑的 SphericalCNN 等，包括提升到 $SO(3)$ 再 FT
					* ((_n7l946))SFNO 球面版本的 FNO
					* 相关：INR 有 SINR((n9lg8c))，球面版本 MFN
					* 编程实现：((_n7l973))torch-harmonics 软件包 by NVIDIA
					* 球面网格相关：((n7kj9d))球面 multi-mesh GNN
						* 经纬网格 ViT 用特殊位置编码((_p8jh5h))以体现非均匀性，盘古气象涉及
					* 备用参考资料，((_n7mj3e))综述文章涉及 $SO(3)$ 与球谐函数
					* 注：((n4tc04))处理球面数据仍尽量变换到规则均匀网格
				* graph 的频域处理，graph 算子；许多传统 GNN 涉及此类处理；{n7l966}
					* 相关：((n79n3g))GNN 与 graph Transformer
					* FT 定义来自 (normalized) graph Laplacian 的对角化((_o9a99j))，又见 SRS((o9a97g))
						* 据此设计的 graph spectral conv layer 结构同 FNO block
				* 散点离散（非均匀网格），(('o8mh92))导师建议考虑 NUFFT
					* NUDFT，((_p8hg3v))FNO-DSE 使用
			* FT 推广得更一般的数学变换（仍在欧氏区域）
				* 用分数阶 FT（相当于时域频域间插值）构造 NO，((_o6ua7c))认为善于表示 频率特性随时间变化的 非平稳时空信号
				* Clifford Fourier transform((_o9bg0v))，原 FT 涉及的函数为 $\C$ 取值，现在在特定的代数结构中取值；可用于构造 NO
			* Laplace 变换：LNO，((_o6r94s))Fourier 域可视为 Laplace 域的 slice；{o6r95t}
				* 相关：((o6r95k))涉及复数运算的网络架构
			* 变换汇总((_q17g0o))，FT、Laplace 纯 global，卷积、小波 纯 local，Radon（类 CT）及 FT 变种为局部全局的综合
			* 频域其他相关内容：((ncqf18))NO-频率问题，((n8jb3l))均匀网格离散-求导，((n8jb3v))传统数值方法-谱方法（包括非线性 Fourier 变换），((ncqf1p))alias（激活函数等会引入高频信息）
		* 通道变换，如 FNO 频率截断后逐点线性变换
			* 频率截断
				* 激活函数补偿：FNO 原文认为((_ncqf01))截断高频信息影响低于纯 FT 的截断，因激活函数恢复了部分高频成分；{ncqf1j}
					* 相关：激活函数引入的高频信息 ((ncqf1p))加剧 CNN alias 现象、破坏架构等变性
				* 不擅处理间断解：`FNOvsDpONet-2111.05512` LuLu 报告提到由于 Fourier 变换性质，这种做法在激波等间断附近拟合较差；{n7jm11}
					* 感觉原版 DeepONet 也未必擅长处理间断：Kolmogorov n-width
					* ((_n4fb9t))（李宗宜也提到）适合拟合输出连续的算子，因 FT 适合处理连续函数
					* 对 PDE 问题有改进可能：或许((_n4fc0q))通过添加额外 PDE loss（积分形式）可处理不连续解；{n4fe4b}
						* 相关：((n4fe4x))用于PDE的NO-额外的PDEloss
					* 相关：((p8hf92))FNO 能力
				* 影响自回归：部分方程高频成分准确度对长期预测精度重要，((_n99f60))认为此时 FNO 由于截断高频信息而精度不佳，U-Net 表现更好；{n99f7n}
					* 相关：((p8hf92))FNO 能力
				* 细尺度表征不足，((_q2pf5n))系统性评估实验结论
				* 避免频域截断，需解决完整频域权重参数量过大问题
					* 频域分段、权重差低秩：((_p6af8x))FreqMoE 每段用的线性变换 Rᵢ 相差低秩矩阵，先训低频段线性变换，再加低秩成分得其他段权重、接续训练；{p8ng8s}
						* 相关：((p8ng8x))ensemble-各点引入方式-基准对象微调
					* 权重由超网络生成：((ocpa0d))AM-FNO 超网络输入波数 k 输出该点权重 Rₖ
				* 相关：((ncqf18))高频vs低频-NO
			* 逐点：可学变换的参数不再“逐点”独立（“点”指频率向量 k）
				* 假定变换关于 k 光滑（相当于谱卷积在空间域卷积核 local），只记录其低分辨率部分，使用时插值到完整频域((_p66a19))
				* 各点变换矩阵由共享网络生成：((_ocof0c))AM-FNO 将频率（向量）$k$ 输入 MLP 得变换矩阵 $R_k$，从而可保留更多频率；{ocpa0d}
			* 变换：
				* 线性变换，如 FNO；此时整体变换为((p8hf67))谱卷积；{p8hf79}
					* 权重矩阵特殊处理：((_o2ga6x))引入 Canonical-Polyadic 分解，用于 FNO 混精加速
					* 物理域扩大时（改处理细网格大波数），((p8hf70))新域谱卷积的权重矩阵据空间域卷积确定
				* MLP，如 `flowMap-1910.06948`
				* 分块 MLP，((_n8hm02))AFNO 用到；{n9290q}
					* “分块”用于模拟 ViT 多头注意力（副本：((n8hm0w))ViT）；{n8hm0f}
					* 用 FFT 而非传统注意力建模长程依赖，复杂度 nlogn 而非二次，增强了长序列处理能力；{n9291a}
						* 另见((n4pl5i))Transformer-长序列
					* AFNO 源码中频域变换所有点共享 MLP 参数，不同于 FNO 所有点线性变换独立
					* 据说 AFNO 该处 MLP 后有 shrink 操作，且灵感来自小波；有观点认为这部分操作不本质
			* 变换结果群等变增广，((_n7jm21))G-FNO 在频域的 activation 值用其群变换结果增强；{n7jm27}
				* 副本：((n7jm2c))等变性
			* 类 U-Net 结构((_ocka19))，多级操作分别 截断高频、増通道数
				* 好处：能对 Re 等方程尺度因子泛化
				* 相较普通 U-Net：下采样 换为 截断高频，卷积 换为 逐点线性变换
		* 谱卷积 视为整体；{p8hf67}
			* 相关：((n8hj6h))离散卷积
			* 网格分辨率（均匀网格情形）
				* 区域物理尺寸不变，相当于波数不变、等频率：谱卷积的 resolution-invariance 特性
				* 网格物理尺寸不变，相当于波数等比例增大：
					* 根据空间域卷积核改权重：((_p66a27))算空间域卷积核并 pad 零，再 FFT 得新版频域逐点变换系数；{p8hf70}
						* 相关：前提为((p8hf79))频域变换线性，此时才是谱卷积
			* 不擅长非线性方程：`2022-03-16`(dbGrpMeet2) 鄂维南老师觉得 FNO 设计基于 Green 函数，未必适用于非线性方程；{p8hf8z}
				* 相关：((p8hf92))FNO 能力
			* 整体替换：((_n9294e))GalerkinTf 将其解读为积分核，并用修改后的注意力机制构造更一般的积分核
		* 变换后空间 与原空间域信息结合使用，如((_q4pb9r))HPM 单层内融合 FNO+线性注意力
	* 空间多尺度，在下采样后交互；{n5da0x}
		* 固定 grid 下的 U-Net，计算中途引入多层池化、降尺寸的 grid
		* 固定 mesh 下的 `GraphCast-2212.12794`，球面上采用 multi-mesh GNN；{n7kj9d}
			* 副本：((n7l95q))网格泛化-固定散点离散
			* 其实这个是输入数据表达时即已采用多尺度 mesh，不是计算中途才采用
			* 不过鉴于粗尺度格点是细尺度格点子集，也可视为计算中途才引入 multi-mesh
			* 球面算子相关：((n7l952))用球谐函数转化到频域
		* 可变 mesh 的((ocna32))multi-scale graph
		* 针对 PDE，基于多重网格传统算法的也算，Meta-MgNet；可见 `paramPDE%`“n13a5b”
		* 粗尺度成分的产生方式：
			* U-Net 细尺度过网络（卷积、池化）生成
			* `GraphCast-2212.12794` 粗尺度格点作为细尺度格点子集，直接靠限制算子一次性给出所有粗尺度格点值
			* 多重网格取决于格式，如 V-cycle 由细尺度一步磨光后生成
		* ((_n5da0n))LSM 在隐空间求解，编码器使用分块多尺度架构，对不同尺度的不同 patch 分别编码
			* 其一个结果为((n4ta57))隐向量有结构
		* 优势：降低计算代价，使用较浅层网络即可表达非局部依赖
		* 相关：先有网络结构，再用多尺度方式解读((ocna3l))
		* 一般意义的多尺度（不限于 NO 架构-中间处理层）：{nckf50}
			* 空间多尺度：((n5d99m))场（及其表征），((n5da1a))数值PDE-分辨率，((n5da1n))有结构隐向量-多尺度，((n2pe88))INR 架构-多尺度，((nckf4u))coordLoss-多尺度
				* 空间不同位置用不同尺度，((q1ij5t))局部自适应加密
			* 时间多尺度：((n7mj2c))动力学-NO时间推进-时间多尺度
			* 物理多尺度((o92b5b))
	* 隐向量（无结构），以有限维空间为中介描述函数空间间的映射；{n42e6y}
		* 相关：另一种有通用性的架构为((n42e71))hypernet 形式
		* AD 编码，INR 拟合目标场得隐向量：((p4ce1k))有限维向量可通过 AD 解优化问题得到
		* 函数空间 PCA/POD `funcPCA-2005.03180`（$a,u$ 均降维），`FNOvsDpONet-2111.05512` POD-DeepONet 只对 $u$ 降维
		* `funcGAN-2102.04776` 给出了有限维隐空间到函数空间的映射（函数用 hypernet 给出参数化表达）；应可自行结合对输入函数编码映入隐空间的映射
		* `DL-ROM-2001.04001` 含时方程 $(t,a)\mapsto u$ 映射用有限维中介（文中只考虑有限维 $a$、grid 离散的 $u$）, `DL-ROM-2103.06183`
		* `DeepONet-1910.03193` 可理解为：branch-net 生成低维向量，作为 trunk-net 提供的基底的线性组合系数（线性解码器）
			* `FNOvsDpONet-2111.05512` POD-DeepONet 改用 POD 提供基底（此时无法用 PDE loss 训；$u$ 数据表征常需求值离散/基底）
			* 改进为非线性解码器 $f(a,y)$ 的版本见 `NOMAD-2206.03551`；`NIF-2204.03216` 类似 modulation 的非线性解码器
		* 性能不如 ViT、FNO 等架构((p64a1k))（针对 FD-Bench）
	* 注意力：((n37m6i))ViT（输入层 patchify 得低分辨率特征图，可理解为在该特征图上做 pixel-level 注意力）
	* 定分辨率 MLP：MLP mixer，特征图所有点过 MLP，各通道独立做全局交互、用同一个 MLP；{o8gg6g}
		* ((o8gg6h))导师觉得 MLP mixer 动机、可解释性好于 ViT
		* MLP mixer 等变性可好于 CNN((n3ha7e))
	* 交互范围引入空间限制，不再全局；{o8h98r}
		* 问题分解：（参与注意力的）总格点量 = (长度 × 点密度)^空间维数
			* 类似划分还见于((oa3b7z))场表征-网格参化复合-网格总参数量压缩
		* 沿坐标轴（针对维数），各坐标轴分别做信息交互
			* 多种信息交互方式均可使用：((o2rg6j))轴向注意力，((o7ta9d))频域变换-FFNO，((patn00))CNN
				* ((_pbha90))XNN 同时用 卷积、注意力
			* 各轴交互顺序、组合方式：
				* 求和：各轴分别交换、结果求和；如 FFNO，MPP 处理 XY
				* 可交换复合：各轴依次交互但可换序；如((pamg6c))FactFormer 注意力矩阵对剩余轴共享，（可能性）FFNO 变种，利用谱卷积可交换性
				* 不可交换复合：各轴信息交互依次进行，结果与顺序有关
					* 如 MPP 处理时空，先时间注意力再空间注意力
					* CNN 时空依次卷积((patn00))
					* （非空间信息）AlphaFold2 跨残基、跨序列
					* 可能性：FactFormer 注意力变种，不再对剩余轴统一，各轴共享 W_Q/K/V/O，但剩余轴非统一情形 $A_1A_2V\ne A_2A_1V$
				* 独立：网络不同块中分别用；((_pamg5z))据说最早的 AViT 不同块要么只算行注意力，要么只算列
			* 各轴参数共享
				* 作用—维数泛化潜力：((q1nb9f))不同维数可共用同一架构，低维训练高维微调；{o7tb6f}
					* 如((_o7tb69))FFNO 实验中低维训练高维微调，依赖于不同维数方程共享参数
					* ((_pbhb0l))XNN 2D 训 3D 微调，也可各维联训
					* ((_pbhb0x))OmniArch 不同维物理场均编码为相同隐向量，通过逐维约化，卷积orFNO
				* 进阶—计算结果共享
					* MPP 空间注意力部分，XY 计算使用相同的 Q,K,V，二轴 AV 计算结果取均值后再统一乘 W_O
				* 退化—仅部分共享：FactFormer((pamg6c))W_V/O 共享，W_Q/K 独立
			* 维数不充分分解：如((o2rg71))视频按 1+2D 分解
			* 维数仅部分分解：如 1+2D 视频时空独立交互，空间不再分解；{o2rg71}
				* ViViT((_pamh7d))
				* ST-Transformer((_o2rg69))时间维用单向注意力，空间双向
					* 另见((o2rg74))注意力-单双向-混合场景
					* 每次预测一帧（从而应该是每步生成那一帧的所有 patch token）；另见((o2rg8d))Transformer 解码器
				* 视频 VAE 压分辨率(('patm9y))时空卷积核独立；{patn00}
		* 区域/窗口分解（针对长度），块内信息交互：((na1k7z))用于注意力（SWin）
			* 分块方式可各层不同，SWin 中用到
		* 稀疏交互（针对点密度），跨越远距离，但每个局部仅单点参与；((o9pa33))用于注意力（MaxViT 的 grid attention）
		* 补偿方式，引入范围限制后不完整，需补全，方式参考((p4b97v))注意力计算情形
		* 相关：((p8ag76))集合元素分组处理，包括非均匀网格的一般情形
	* 据先验机理：已知算子可表达为特定形式（针对特定任务，常通过理论分析），据此设计架构；{nclm6b}
		* 针对多层复合形式算子 ansatz，引入类 DenseNet 结构，见((_n86k2l))IAE-Net
		* `[Ying]-EIT-DL`, `[EIT/DOT用数学结构设计NO]`
		* `OneShotNO-2104.05512`，PDE-Net，((nclk76))可降低数据量需求
	* 一般意义上的全局、局部、单点 ansatz 相关；{o9aa14}
		* ((n7de8p))图位置编码、结构编码，用于 graph Transformer
	* 多类型层组合交替使用；{q5d901}
		* 2025 年末 PDEformer 新架构探索，谱卷积+窗口注意力 交替
		* DSO-2603.26800 深度可分卷积用于细粒度局部特征提取，MLP-混合器用于长距离全局聚合
		* 相关：((n3ri89))仿TRIZ原理-交替格式迭代
* 局部（单点）通道信息交互：
	* ViT 的 FFN
	* MLPMixer 的 channel-mixing 层；由于全局交互局限于通道内，引入该通道信息交互有必要性
	* 与空间交互分开：((n9me4p))FNO FT 变换与局部特征变换分开进行可提高表现，引入残差、随机深度可再提高
