* 连续介质物理及其数值描述((q4rc2m))
* PDE；{n6tg6l}
	* 强形式、弱形式（Galerkin，Ritz）{n3pa77}
		* 另见((n8o930))FEM
		* Galerkin
			* 在 (N/R) BC 下的弱形式：$\langle Lu,v\rangle$ 分部积分，替换掉 $\partial u/\partial n$ 项即得弱形式
			* 弱形式可以包含边界项，只要等价问题是 $\langle Lu,v\rangle+\lambda\langle\partial u/\partial n,v\rangle$ 即可分别对应原问题的内部方程和边界两项
				* 可行的弱形式并不是唯一的，尽管其中完全消除 $\partial u/\partial n$ 项的那种形式最为简单
			* 对于规则网格，若微分算子平移不变，Galerkin 离散的 $A$ 可写为卷积形式；一般情形比卷积复杂，见 Meta-MgNet 代码笔记
			* Galerkin 近似解用于理论证明 PDE 解存在性：((_o3ok69))Evans PDE 证 $u_t+Lu=f$ 弱解存在，其中 $w_k$ 取为正交基底序列，截断前 $m$ 项作为近似；{o3ok6f}
				* 取正交基底做法也用于((n8jb3v))谱方法
			* 相关：((n3pa64))PDE 中的等式约束
		* Ritz
			* 可用性要求微分算子 $A$ 可以开根号 $A=C^\mathrm{T}C$，这样 $\|Cu\|^2$ 变分能够得到 $C^\mathrm{T}Cu$
			* 因此常用于椭圆问题；注意抛物、双曲问题在时间隐式离散下也化为椭圆问题
		* 有限体积形式
			* 例如((_n4fe7i))用于训有网格 ansatz，其中考虑的有激波问题本身不适合用强形式
	* PDE 形式的等价改写（除强弱形式外）{n41f93}
		* ((n3ne2f))升维降阶改写
		* 方程中有 $\epsilon$-摄动项，可考虑引入新场分量使各分量近似同阶
			* 如 $\Delta_\perp u+\epsilon\Delta_\parallel u=f$ 改为 $\Delta_\perp u+\Delta_\parallel v=f$，$v=\epsilon u$
			* 若用 PINN，`APNN-2111.02541` 认为需要极限方程对应 loss 等于方程 loss 的极限；对 Boltzmann 方程做宏观-微观分解 $f=\rho(t,x)+\epsilon g(t,x,v)$ 可达到该目标（不改写则不满足）{n41f95}
		* 各场分量大小有量级差异，可考虑先 scale 到相近大小；{nafg7e}
			* 或对 PINN 有益：CSI讨论中，各个输出分量 scale 到相近大小发现更加容易学习；可以训练一个额外网络预测各分量 scale；{n41g30}
			* PDE 无量纲化((_n8qn1i))，使输入输出在合理范围内
			* NO 保留输入幅值信息
				* 相关：((n9gg5k))正则化-BN,LN,IN
				* 激活值持续保留，需架构可处理非归一化激活值
					* Galerkin Transformer 去 LayerNorm 可（一定程度上¹）处理非归一化数据；{n9gg54}
						* ¹线性注意力部分缩放无关，但 FFN 部分可能仍依赖于问题尺度
				* 提取幅值信息，输出时重新引入；{nafg7s}
					* ((_nafg6v))输入 ViT 前进行时空归一化，并将提取的均值、方差作用于网络输出层
					* 恢复非对称，输出层 rescale 依据输出预期而非输入，((_q1m87i))Walrus 网络预测时间更新量，rescale 使用历史增量统计量
			* 相关：((n5d99m))“场”相关话题
		* 数值解残差满足新的线性 PDE（忽略高阶小量）((p9u00z))，来自原方程摄动
		* Eulerian 描述改 ((q4rb00))Lagrangian 描述（尤其对流主导问题）
		* 针对特殊解（方程其他解不能这么改写方程），((_p9ua50))时空自相似解变换后满足不含时方程
		* 相关：((ncmk54))近似方程，((o39j1k))PDE 文本形式-数据增强，((nas97x))非科研-逻辑对象的多种等价表达形式
	* 含时与否
		* Koopman
			* VAMP-2 score ((n2bf1x))可理解为某种无穷维的 SVD
			* DMD，EDMD 可用于预测 $x$ 的原因，我当时的想法记录于((n2bf23))，((n2bf26))
			* （备用）2201.12062（作者同 `QuaSiModO-2102.04722`）论证薛方可写为最优控制（HJB eqn）并用 Koopman 方式描述；{n2bf2c}
	* 线性与非线性；{n3g88p}
		* 线性方程可采用((n3fe1c))残差迭代格式
		* 有时非线性方程可通过某些方式转化为线性，如((n3g88l))利用这点给出 Green 函数
	* 典型方程；{n8i979}
		* 椭圆方程（不含时）
			* Darcy flow，分片常数系数；数据集可用 FNO，PDE-bench（2D）
			* 系数分布：可形如 exp(GRF)，DeepONet 实验涉及（1D，源项 $f$ 常数，0 (D) BC）
			* 数据集((_o6ll0h))ConDiff，系数场对比度高
			* 非线性 Poisson 方程 $\Delta u+e^u=f$，((_p1mf95))在微分几何、流体力学、量子场论中有应用
			* Poisson 电场-孔形状((_q82g0z))
		* 弹塑性力学
			* plate 形变，数据集可用 MeshGraphNets（随体 Lagrange 网格，不均匀网格），据说场变量包括位置、von-Mises stress
			* 可变形物体 Lagrange MPM 模拟数据((_q85h10))
			* `Geo-FNO-2207.05209` 实验涉及弹性材料、塑性材料，未确认细节
			* ((o44b37))CoDA-NO 考察不可压 NS 与弹性力学耦合，数据用 TurtleFSI 包生成
			* 3D 工业级工程设计，((_papa12))线弹性、弹塑性、时变塑性
			* ((_q2la6a))Poisson、线性弹性、超弹性数据，不规则区域固定，仅变 BC
			* 极端载荷材料动力学，((_q3uf8s))扰动分层界面（PLI）和动态断裂/破坏演化（FRAC）
			* 超弹性，涉及多种本构方程（真实预设+随机生成）((_q6rm52))，区域为方形内挖多洞；含生成代码
			* 线弹性 von Mises-力角度/孔形状((_q82f9u))，多保真（粗细网格配对数据）
		* 输运、波动等
			* 对流 advection，数据集可用 PDE-bench（1D）
				* Kolmogorov n-width((o1mj39))：{o1mj3b}
					* ((_o1mh6e))L1 范数，另又估计了该范数下的 (inner) entropy number
					* ((_o1mh6k))L2 范数；一般 Lp 范数的可见((_o1mh6n))cohen slides
					* ((_o1mh6t))L2，对初值引入额外光滑性条件
					* ((_o1mh6z))波方程 L2
			* 普通波方程
			* Maxwell
				* PDEArena 提供了((_o39c14))3D Maxwell 数据集与生成程序
				* 方程形式可利用 Clifford 代数表示((_o9bb9w))，且有两种表示方案，可据此设计 NN 架构
			* Helmholtz；{n7ul5b}
				* 作为物理模型未必准确：
					* ((_n31e3n))考虑声波传输问题时，内部突变边界需引入特定的内边界条件等
						* 其原文也提到了((_n31e43))建模需考虑剪切波效应
					* ((_n31e49))非线性效应（如声波吸收等有能量损失的情形）
				* 数据集，当时写 MAD CAMC 版本有用过求解器（先在时域求解到稳态¹，再 FT）{n8jh74}
					* ¹见((n8jh7e))动力学周期轨作为广义“稳态”
					* HA30k((_pcfb57))声学，背景+若干方形障碍物 材料类型均可变
					* ((_q2mg2h))10k 数据对，背景类 GRF 声速下高频解
				* 数据生成
					* 当时 MAD 实验时我用的 MATLAB k-wave 包，先算含时波方程解到稳态，再做时域 FT 得 Helmholtz 解
					* [MUMPS](https://github.com/MIA123MIA321/MUMPS-Install)，from 2024-03-17 导师“清华的uzq老师他们用的是mumps，说2D 512x512的分辨率算一个只需要几秒钟。”
				* 相应反问题((n7ul5d))散射波反演，根据边界观测算子推断内部系数场
			* 弹性力学波方程？
			* Euler-Bernoulli beam equation 涉及空间 4 阶导数；{o6tf0t}
				* 数据集可能来源：2405.20836 涉及该实验
		* 反应项，常用于反应扩散 reaction-diffusion
			* 数据集可用 PDE-bench：1D $u_t=Du_{xx}+ru(1-u)$，2D 为 Fitzhugh-Nagumo 方程（涉及 $u,v$）
				* DeepONet 1D $u_t=Du_{xx}+ku^2+f$
			* 反应项其他形式：((_o9ba18))square Logistic $u^2(1-u)^2$，bistable $u^2(1-u)$
			* 还有二分量的((_oc3b8l))2D $\lambda-\Omega$ 方程、3D GS 方程
		* 扩散及其变种
			* 多孔介质((_o9ba1l))$u_t=(u^m)_{xx}$
			* diffusion-sorption，数据集可用 PDE-bench（1D）：$u_t=(D/R(u))u_{xx}$，可用于建模地下水污染；Freundlich sorption isotherm 取 $R(u)=1+au^{-r}$
			* 芯片散热片热分析，((_papa1f))3D工业级工程设计数据（不规则区域）
		* 匀质流体 CFD, NS, Euler，KdV，Burgers；{n7ll84}
			* Burgers
				* 有解析解（带粘性项，有级数、积分形式两种版本）
					* from clh，PINN 原始论文引用了 1986 的文章，并用它生成真解
						* "Spectral and finite difference solutions of Burgers equation"
					* 另：周蜀林 PDE 习题 3-13 也是求解析解
					* 利用这点进行数据生成，2202.07643 用 Cole-Hopf 变换转为热方程、进而给出数值解
				* 数据集可用 PDE-bench（1D 有粘性）
				* FNO 用的数据集（GRF 初值）有 [MindSpore 自行重生成版本](https://mindspore.cn/mindflow/docs/zh-CN/r0.1/data_driven/burgers_KNO1D.html)（文中搜“下载”），下载无需梯子
					* 文件尺寸相对小很多：存储数据个数 5→2，样本数 2k→1k，空间分辨率 8k→1k，精度 float64→32
					* 另有 [Kaggle 版本](https://www.kaggle.com/datasets/scaomath/pde-dataset)
				* Lie 对称群（仅针对内部方程，不带 BC）可见((n8ve45))
			* Euler；数据集，MeshGraphNets 涉及机翼（2D）
			* NS
				* 数据集，默认速度形式：
					* [dataset-CFD-湍流](http://turbulence.pha.jhu.edu/) （2021-08-13 CSI讨论，SyQi）Johns Hopkins 的 turbulence database 网站
					* `2112.05309` 不可压 NS 数据集（2D、3D）；`2108.07799` 涉及不可压 NS（有障碍物），`CFDBench-2310.05963` 方腔流、管道流、堤坝流、圆柱扰流，`2112.15275` 针对湍流
						* (('o3sk5p))(('q2pm24))CFDBench 数据质量待评估，如 CFL 条件是否满足等
					* 简单数据集，PDE-bench 包括可压（1D-3D）、不可压（2D），MeshGraphNets 涉及圆柱绕流（2D），((_o39c0z))PDEArena 2D 不可压数据与 Φ-flow 生成代码
						* PDEBench 2D INS 数据质量待评估(('ocdg09))，可能通过计算方程残差
					* 用 Lagrange 粒子方法生成的数据集((_o6lg9z))LagrangeBench；((_q85h1a))SPH 流体数据
					* 3D 高精度数据集((_o7dl1z))BlastNet
					* FD-Bench((_p64e8o))，针对 2D 方形区域的多种方程，包括有非规则散点离散
					* 3D 心血管血流数据集((_q14g1k))
					* 多保真（粗细网格配对数据）((_q82g1i))圆柱扰流
					* 使用((q26a8k))随机区域形状
						* FlowBench((_p4tb4h))区域随机挖洞，形状包括球谐、NURBS、动物形状等；{q26a8c}
						* 区域带多个洞的数据集((_q14g1l))；放多个对象((_q1ob1d))基于 OpenFOAM 生成；{q26a9m}
					* 相关：((n8i96v))科学计算数据集
				* 涡度形式数据集
					* FNO 原论文有相应不可压 NS 数据集
					* Kolmogorov 湍流涡度((_q82g1n))，多保真（粗细网格配对数据）
				* 理论导出的统计量（可用于检测数值结果的问题）：(('o3sk5v))各项同性湍流的 Kolgomorov scaling、turbulent kinetic 的 spectrum 分布等
				* 不可压情形，Lie 对称群（仅针对内部方程，不带 BC）可见((n8ve45))
				* 理论推导相关，若考虑((o1ol51))边界层，可变量代换导出 Prandtl 方程；{o1ol5p}
			* RANS（不含时）
				* 数据集—`AirfRANS-2212.07564`，针对亚音速、机翼、不同攻角
				* 数据集—((_q82g17))摩托车骑手真实几何
			* Boltzmann 介观方程，针对 Kn 数范围更大
				* 涉及额外引入速度空间，碰撞项涉及积分；{n7ul4f}
					* ((n7ul4w))辐射输运方程涉及类似（相同？）的项
					* 相关：((n86k1x))非线性积分算子
			* 浅水波 shallow-water
				* 数据集可用 PDE-bench（2D）（原文 secD.7 方程似乎把 $hu\otimes u$ 项写错了，不确定数据集是否按错误方程生成）
				* PDEArena 数据集也可用（球面均匀网格离散），((_n7nf7v))生成使用 Julia 程序
				* 实际场景：(('o5sg0m))风暴潮模拟，驱动风场含时
			* KdV, KS 方程：Lie 对称群（仅针对内部方程，不带 BC）可见((n8ve45))，用于做数据增广
				* PDEArena 提供了((_o39c1b))1D KS 数据集与生成代码
				* KS 导数时间 1 阶空间 4 阶，((o6tf0t))Euler-Bernoulli 方程时间 2 阶空间 4 阶
			* 广义流体，((_q85h1i))沙粒流动的 Lagrange 模拟数据集
			* 流体边界条件（一般“边界条件”见((nahe5m))）；{o99b8m}
				* CFDBench 数据集涉及((_o99b7w))移动/静止边界，速度/压力 入口/出口边界
			* 软件相关，((_o1ok9d))JAX-FLUIDS 以可微分形式实现传统算法，从而“提供 ML 和 CFD 之间无缝衔接”；{o1ok9p}
				* OpenFOAM，Fluent，PhiFlow
					* OpenFOAM 画网格软件，(('o4dk5t))杨老师推荐了一些工具；{o4dk7z}
						* 相关：((o4dk76))网格划分
					* ((_o99b74))CFDBench 数据生成用到 Fluent
				* 2024-03-19 zmy 在 Pf 小群推荐 http://www.dyfluid.com/solvers.html
				* 2024-04-10 lhu 在 Pf 大群里提到可考察 mindflow.cfd，jax sph
					> （JAX-CFD）这个项目之前我们分析过，可以做一些简单的低雷诺数湍流求解（from 2024-05-05 Pf 大群 VhYi）
				* ((o44b37))CoDA-NO 考察不可压 NS 与弹性力学耦合，数据用 TurtleFSI 包生成
				* [CFD中文网](https://cfd-china.com/)
				* 相关：((o1ol2j))一般 PDE 数值软件，((o1ok9o))求导-科学计算-可微分传统求解器
			* 计算场景、具体应用相关
				* 可参考 MindFlow 介绍，[视频](https://www.bilibili.com/video/BV1be411Q7qZ) 08:30
				* 2024-03-19 工院杨老师提到算飞机起落架、汽车后视镜等局部凸出部件的阻力
				* ((_o99b8u))CFDBench 数据集介绍有讨论 方腔流、圆管流、堤坝流、圆柱扰流 各自的复杂性和难点
		* 其他
			* Eikonal 方程；PI-DeepONet 实验考虑了 2D 圆形、机翼
			* ((_o9ba1u))（1D）Cahn-Hilliard, Fokker-Planck
			* stochastic PDE SPDEBench-2505.18511
		* 非匀质，多相耦合，多物理问题等；{n3298n}
			* flag dynamics，数据集可用 MeshGraphNets（随体 Lagrange 网格，不均匀网格）；据说场变量包括位置、von-Mises stress
			* 多相流，如水中大量气泡，据说在气泡数目很多时求解代价高；可能性：类似 DeepPot？
				* ((_o1ok9y))JAX-FLUIDS 软件包实现了基于水平集法的传统求解算法；{o1ol07}
					* 相关：((n8re2y))水平集
				* ((_o99e4d))CFDBench 圆管流为水流入充满空气的管道
				* ((_p2re9v))MPFBench 水滴、气泡数据集，以及 related work BubbleML 沸腾 & 实验数据集
				* 3D Rayleigh-Taylor Boussinesq DNS((_q7pc1d))
			* 机翼结冰
			* 流体+热 等，((_p4tb4x))FlowBench；((_q1ia34))冷热气泡对流碰撞
			* 流固耦合：{q4qm4a}
				* 动态区域描述表征方式：((q4qm3q))ALE 含时形变网格 等
				* 求解算法：((_q4qm4o))分区耦合算法，依次算 固体形变、网格调整、流体运动
				* 求解软件：不可压 NS + 弹性体方程，((_o44b34))CoDA-NO 用 TurtleFSI 包生成数据；{o44b37}
			* 燃烧，((_p64f11))Open-CK 模拟工业园区火灾发展
			* PDE-ODE 耦合，流体+化学反应，时间尺度差异大((_q1sa7d)) from REALM 套件
			* DeepM&MNet 考虑的问题
			* 天气预报，数据集包括((_o6lg6o))SuperBench, ClimSim, DynaBench, OceanBench, ChaosBench
			* 相关处理办法：((n3298l))INR 区域分解
			* 微观宏观不同物理耦合 导致多尺度；{o92b5b}
				* 如((_o92b58))流体宏观粘度、湍流受微观分子动力学影响
				* 相关框架((nckf50))多尺度（一般意义）
			* （相变？）数据集与 benchmark BubbleML-2307.14623（另见((n8i96v))科学计算数据集）；{n8i96u}
			* Multiphysics-Bench 涉及的耦合方程((_p6de6h))Electro-Thermal, Thermo-Fluid, Electro-Fluid, Magneto-Hydrodynamic, Acoustic–Structure, Mass Transport–Fluid
			* 磁流体经典算例（用于测传统算法可靠性）((_pcfj2j))
				* 磁流体方腔流((_q1ia3c))
		* 随机表达式的生成；{n9hm6g}
			* ML 用于符号计算（不定积分等）((n8v95j)) 涉及
			* 数据中识别 PDE((n9hm6a))用遗传算法对表达式二叉树采样
			* 相关：((nbig90))ML 与符号表达式
			* 相关：((nasa1c))非科研-逻辑对象等价表达形式-序列、树、图-数学符号表达式
		* 相关：((n8i96v))科学计算数据集
	* 求解区域
		* 随机区域形状生成；{q26a8k}
			* 单连通区域（直接作为求解区域，作为洞，取环状边界邻域）
				* 极坐标表达式 按调和函数生成((_q26a85))
				* 球谐、NURBS、动物形状((q26a8c))
				* 撒点取 envelope((_q26a91))，允许非凸区域、有参数控制
			* 多洞区域((q26a9m))
			* 可能性（暂未看到先例）：物理场仅保留取值在某区间内的点
				* 物理场来源：已有数据集的解；自然图片；AI 生成图片（GAN 可能就够了）
				* 区间可随机生成
				* 若担心区域形状过于复杂，可考虑后处理光滑化操作
		* 自由边界（区域形变方式待求解）可用数据集((_q5tg2h))
	* 网格生成
		* 另见((n6tg3i))变区域 PDE
* PDE 正问题数值方法；{nclm4c}
	* 离散化方式
		* FDM 有限差分；{n8o92z}
		* FEM；{n8o930}
			* 另见((n3pa77))PDE 强形式vs弱形式，包括 Galerkin、Ritz
			* 有限元主要擅长处理光滑解，小波则可以处理不连续的
			* 基函数保证连续即可，梯度在 element 边缘无定义并不影响，只要基函数没有间断就不会引入 Dirac 函数
			* Maxwell 方程离散可以使用专有的 Nedelec element，见 `Maxwell-MG-FEM`（内有一些理论推导，如 curl 弱形式推导）
				* 按照自由度处理时不要与有限差分混淆（尽管网格长得像），如边界面的 edge 自由度表示的基函数会延续到内部，因此对内部积分项有贡献，不像有限差分那样可以计入或不计入内部积分
				* 注意对于两个边界面的自由度，它们在内部积分与边界积分下的内积不同
			* 相关：Galerkin 近似解用于理论证明 PDE 解存在性
			* 用于生成 PDE 数据集，((_o1ol26))可调用 ARCSim, FEniCS and SU2 等软件包的 Python 项目；{o1ol2r}
				* 相关：((o1ol2j))PDE 数据集生成可用软件包
		* FVM；{n8o931}
			* Python 包 PyClaw 为传统 FVM 求解器，PDEBench 用它解浅水波方程
		* 谱方法；{n8jb3v}
			* 定义域拓扑不规则情形，谱方法有 Fourier continuation 技巧((_n7n912))，延拓涉及多项式拟合；{n7n96e}
			* 需要 3/2-dealiasing((_p34e4g))，结合 NO 的混合方法不需要（相关：((n3h98i))alias）；{p34e4l}
			* 相关：Fourier frame 用于求解不规则区域的方程（传统谱方法做不到）`2021-10-27`(组会)
			* 频域一般内容见((n7kj8y))
			* 非周期边界可用多项式（而非 Fourier）基底，((o1ol4p))为避免龙格现象，对应空间离散点非均匀选取
			* 相关：正交基底也用于((o3ok6f))理论证明 PDE 解存在性-构造 Galerkin 近似解
			* 相关：xxy组会曾介绍非线性 Fourier 变换
		* （传统）无网格方法，（按 wiki 词条）包括粒子方法
			* 粒子方法；{n2gf8g}
				* 框架—属于((q4rb00))连续介质物理-Lagrange描述-离散方式-随体散点
				* 精度—`2023-02-16`(CSImeet3)“n2gg02”认为相较 FEM，FVM 等方法有精度损失
				* 相关：((n8qk4m))一般 ensemble 策略（不限于数值分析），((n8qk2t))粒子群类优化算法
				* 方法—针对 ML 用于动力学预测场景，其中的粒子方法汇总见((n2gf8i))
				* 方法—((_o6u964))常见 Lagrange 数值格式有三种：离散元，材料点，SPH
			* `2021-10-27`(AISCmeet) 无网格，只要求方程在给定采样点成立
				* 具体地，采用了两层优化：内层给定采样点取值，在某个 RKHS 中极小化 $\|u\|$（有解析解）；外层优化采样点取值（约束为方程在这些点成立），优化目标和内层一样；方法推导来自 GP（高斯过程）
			* WoS walk-on-sphere，解满足 SDE；{q7ia3q}
				* 目的（直接求数值解）换为衡量解好坏（从而可用于 PINN loss）见((q7ia3j))
				* 后处理，可用 NN 对有噪声预测解去噪，见((q7ak67))AI增强传统算法-后处理
		* 按 wiki 词条 numerical PDE，还有 特征线法 method of lines（除了一个维度外，其他维度均离散化）
		* 相关：((o4dk76))场的数值表征（离散、参化、基底）-网格划分
	* 其他策略
		* 多重网格 multigrid (MG): {n2pf9o}
			* 旧记录位置 papers/MultiGridNotes.md
			* `2022-04-06`(AISCmeet2) 区域分解块数多时，分解方式可类似 MG 处理，以加快真边界条件传播到内部子区域的速度；单块内部也可结合 MG
				* 一般话题见 ((n32d8v))PDE区域分解
			* 涉及磨光、限制、提升算子，Meta-MgNet 用 NN 表达这些算子；见((ncmk3t))NN 增强传统数值方法
				* `2022-07-28`(CSImeet2) Meta-MgNet 似乎可用于一般有限元网格，应该是一般 MG 方法的性质
			* 相关：((n2pf83))场在散点离散下，网格可进行局部或全局加密
				* 其中((n2pg2o))覆盖式做法相当于这里用粗网格表示完整解，非覆盖式做法相当于这里粗网格存细网格残差
				* FEM 等基底离散也有类似做法，暂未放入
		* 多保真 multi-fidelity；{n8o93k}
			* 粗网格解作为细网格迭代初值，可多层；如 `2022-07-22`(CSImeet2) RANS 三维机翼流场这么算
			* 导师觉得读 multi-fidelity 相关文章可以读十年前的，比近年的更值得关注，近年很多都是把原有做法挪到 NN 上
			* 相关：((ncbn0o))NO-多保真数据训练，((n31m8z))数据的多保真
		* PDE 区域分解；{n32d8v}
			* 求解目标：((q8fb7o))区域分解-多区域输出协同
				* 从而预期全局解应为 各块局部解算子 作用映射的((q8gg4x))映射不动点
			* 全局（外层循环）求解方式同样可参考((q8gg4x))映射不动点
			* `2022-04-06`(AISCmeet2) 区域分解块数多时，分解方式可类似 MG 处理，以加快真边界条件传播到内部子区域的速度；单块内部也可结合 MG
			* 传统区域分解讨论见 `2022-04-06`(AISCmeet2), eg. Dirichlet-Neumann 算法, Robin-Robin 算法
				* 剖分块数多时可类似多重网格，动态调整块数；单块内也可结合多重网格
				* 后续用于改进 PINN `2022-06-22`(AISCmeet2)
				* `2022-10-05`(AISCmeet2) 提到 NN ansatz 下使不同区域边界吻合只需加 penalty，比传统算法简单，收敛速度快于传统 FEM 等
				* `PFNN2-2205.00593` 含时方程空间区域分解、有重叠，按传统迭代法各区域分别解 (D) BC 问题，讨论了多处理器通信等；{p8jg52}
					* 相关：多区域求解耦合也见于((p8jg4b))无穷区域在有穷区域计算
			* 各子域算子匹配 作为解算子 ansatz，((_q8g70x))OneShotNO；{q8g71l}
				* ← 子域算子表示：NO
				* ← 子域算子训练：原始单样本限制到各子域
				* 方法所属大类：暂时归为((nclm6b))据机理设计架构，尽管语义上可能更接近((ncmk3t))NN in numeric
		* 复杂区域划分为多个简单区域((n32d90))
		* 算子分裂（含时为主）{n3sf6k}
			* NS 方程传统算法分裂为 3 步：对流、BC（涡产生）、扩散；SRS 中记录于((n5fe9b))
			* 各子算子用不同实现方式，依据算子属性
				* 据线性与否，线性隐式、非线性显式：数值 ODE 格式中有 IMEX，算子分裂后显式、隐式交替
					* ((_n3sf9e))proximal gradient 优化算法可与这种格式对应；{n3sg0e}
				* 据线性与否，用不同 NN 架构
					* 线性项简单卷积、非线性复杂架构：`DOSnet-2212.05571`
					* 线性项谱卷积（类 FNO；频域变换用矩阵指数），非线性项 KAN：((_q84g26))Origo
				* 据速度，传统格式慢的换 NN，快的保留((_q7a91n))；{q7b75m}
				* 据是否已知，已知项传统格式、未知项 NN 交替推进，见((q7an1i))动力学部分项已知-算子分裂学未知项
				* 据类型差异，方程各项用独立网络，用于((q84g1s))PDE基模-组装多项网络-算子分裂
			* 各子算子组合方式，顺序、作用强度等
				* 默认固定等比例组合，N 项每项强化 N 倍、演化 Δt/N 时间
				* 策略网络生成，((_q7al9b))输出一系列 (所用算子,演化时间) pair
			* 相关：交替迭代还出现在((n3fe73))不含时PDE迭代求解
				* 仿TRIZ原理，((n3sf5f))分割-微分算子求和分解、时间推进算子复合分解，((n3ri89))迭代-交替迭代
			* 不限于科学计算：((_n3sf6t))CNN 架构等价（某种意义上的）改写为含时 PDE，原 CNN 交替用卷积、池化、激活层，含时 PDE 架构中对应算子分裂；{n3sf8r}
		* 传统方法用 AI 加速见((ncmk3t))，细分版本((ncmk3t))包括简化物理、简化网格、ROM、改进数值格式等
	* 数值边界条件((n2ib4h))
	* 传统软件；{o2fm12}
		* （2021-08-13 CSI讨论，SyQi）COMSOL 等商业软件算得快，方式是加了许多正则化 trick（不能自己取消），这导致有时算的结果不可信
		* PDE 外微分形式表达为交换图，据此自动生成求解程序((o2fm0j))
		* 生成 PDE 数据集可调用的软件包；{o1ol2j}
			* 相关：((o1ol2l))针对 PDE 正问题的 NO 训练数据生成
			* 规则区域（包括圆盘、球面等）Dedalus 基于谱方法
			* ((o1ol2r))基于多种 FEM 软件包的数据生成，支持非规则区域
			* CFD 相关软件见((o1ok9p))
			* 可微分的传统软件包见((o9ie94))
	* 与真解的误差：((_naha2h))分辨率不足时，频域可观察到数值耗散，导致用该数据训练的模型也有相应耗散
* 反问题等具体场景见((n7ul1z))
* 集成 ensemble 策略：{n8qk4m}
	* 可能的同义词：((n3qn83))集合
	* 目的（据此确定各元素整合方式）
		* 表征数学对象
			* 刻画分布((n8ql5a))；{n8ql7o}
				* 用途—((n45m5q))UQ-ensemble，分布求方差即得 UQ 指标
					* 次级用途—((n32e7u))OoD 检测（算特殊的集成学习）
				* 对象—映射，包括((n2me2z))用于刻画随机场
				* 对象—网络预测结果，可((n2me2z))通过 bagging 实现
				* 变分推断中待优化变量为分布，此时 SVGD 使用点集表示待优化的分布；{n8qk7i}
					* 作为散点型变分推断算法，可用于((n8ql6d))分布刻画-概率密度转样本点（采样问题），SVGD 有时比 MCMC 等高效
					* 大意：给定总点数、随机初始化，用特定算法更新
					* 算法直观解读：两项梯度，第一项让每个粒子接近目标分布，第二项防止粒子之间靠得太近
					* 算法导出：在 RKHS 中做某种梯度下降；SMCM 课程有介绍，另外知乎有解读文章（未细看）
					* 各元素联合更新，涉及((q8bi0a))ensemble-各元素动态交互
			* 曲面((n8re3h))：((n32f77))点云，ensemble 对象为低维空间点集
			* 物理系统状态，数值分析的((n2gf8g))粒子方法
			* 场：((q8g80j))RBF，((p9pk6y))3DGS，((oc9a3v))GB-INR 自适应格点变种
		* 优化跳出局部极小，((n8qk2t))粒子群、蚁群等
		* 提网络表达力，ensemble 对象为 NN
			* 输出层求和（所有网络都激活），((n7d90o))boosting 减小偏差
			* 输入类型切换（一次仅激活单网络），((n2pk98))INR 区域分解，((n2pj82))MoE
		* 提输出质量、可靠性，ensemble 对象为 网络/算法输出；多输出整合有不同方式
			* （分类）求众数、多数投票，eg. LLM 输出进行多轮独立尝试，选最频繁的输出
			* （回归）求平均，可提预测稳定性；{p8nf0w}
				* “稳定性”包括：降方差，((p89f8o))避免局部振荡
				* 提稳定性—去除最大最小再平均（每次去除固定数量/比例）
				* 提稳定性—引入异常检测，去除异常样本后再平均（有多少去多少）；需构造检测方案
			* （改进）额外判别机制筛选：束搜索，生成多候选样本，整合结果提高质量；{p97e9u}
				* 整合结果 $y=\sum_iw(r(x_i))p(x_i)$，具体使用需选取各要素 $x,r,w,p$
				* 生成机制 即父级框架讨论的 ensemble，不在此处展开
				* 评估函数 r
					* 打分，((p97l1n))网络/算法输出好坏评估
					* 人工筛选：((o1hk8n))反向设计生成多候选，可由专家筛出容易制造的设计
				* 多样本整合方式 w
					* 只留最好，例如((n8qk2t))集成型优化算法
					* 阈值筛选，预设阈值，只保留打分超过阈值的元素
						* 理论分析—概率测度变化((_q8ba80))，用于 约束生成-多方向整合
					* 组内择优，组内按比例选出打分最优的一批元素，如 RL-GRPO
					* 据打分加权，如((_q8ba7n))约束生成-多方向整合
					* 各元素提取优势局部组件、分别整合；{q8bb8d}
						* 前提—元素有组合结构，各元素均可从中取出部分组件
						* 前提—各组件可独立判定好坏
							* 否则只能靠随机重组后试错，如((n8qk6v))遗传算法
				* 用例
					* 优化算法-集成型((n8qk2t))，要求各元素产生自 随机采样 or 基础优化算法输出，后者进一步要求 基础优化算法的目标函数 与 当前筛选所用评估函数r 相同
					* 网络输出：生成多预测，从中选最优作为最终预测，((p9779w))用于 NO 单步时间推进（抑制长期误差累积）
					* ((n7d91k))网络训练-bagging-多次训练择优
					* 约束生成，对过程((q4oh3d))推理方向调制-多方向整合，对结果((q51g0s))多产出择优
					* 广义用例—((q89a4w))非科研笔记-方案设计-提多版本
		* 过程中各元素动态交互，区分交互目的；{q8bi0a}
			* 全局最优通信：((n8qk2t))集成型优化-粒子群PSO
			* 刻画整体分布：((n8qk7i))SVGD，((q8bi21))ensemble Kalman filter
			* 维持相互差异：((q8bi25))bagging-联合 loss 拉大差异
	* 对象：空间坐标点，数据样本，NN
	* 各点引入方式，如何生成 ensemble 中元素；{q8be8j}
		* 基础来源两类：分布采样（可动态生成），预设全集
		* 基础来源—分布采样，可动态生成
			* 独立生成：空间采样，NN 不同参数、不同训练集划分从头训，((p8ng9d))RNO 额外参考样本
			* 基准对象破坏：((n31n1z))数据增强（包括加噪声），NN dropout/随机参数
			* 基准对象微调：MCMC，网络引入 LoRA 变种（类似((p8ng8s))FreqMoE）{p8ng8x}
			* 输出随机值的网络、映射((n3bg65))，包括生成模型
		* 基础来源—预设全集
			* 预设的异质性设计，如 NN-bagging-((q8bi6l))用不同架构|-((q82b3x))输入输出形式差异
			* 特定算法单次长程运行，取多中间结果
				* 例如((n7d91q))优化-cycleLR，取出各学习率低谷时的解 形成 ensemble
			* 动态变种—((n8qk6v))遗传算法，元素除变异外还会消失、重组
		* 衍生来源—同映射、不同输入
			* NN 输入全集：((p8ng9d))RNO 额外参考样本选取
			* NN 输入采样：((q8ag8a))网络输入-破坏
			* 特定算法 多随机初值 独立运行；{q8bi50}
				* 例如((n8qk2t))优化-集成型-multistart，((n8qk3w))NN-训练-bagging-靠随机种子
				* 注：初值本可按采样生成，但鉴于算法运行昂贵，只能取少量用于实际运行，故成为全集
		* 衍生来源—同输入、不同映射
			* NN 全集：((n8qk3w))bagging
			* NN 采样：((n37n39))网络前传引入随机性
* invCtrlRL 已有 control、ResNet、RL 的统一框架，进一步把 inverse design/problem 也放进来；{n35f03}
	> TODO：回顾 compTree 内容，给出此处的可能框架与算法
	* 场景的编号：
		1. inverse design
		2. inverse problem
		3. control（确定性）
		4. ResNet 的连续时间版本，用于 SL（有监督学习）
			* 输入用补零升维，输出端前几个分量直接作为 $y$（而不显式降维）
			* 每个样本在网络内的 FP 过程视为 ODE，层数对应 ODE 时间
		5. RL 或随机 control
		* 导师（与lzc学长？）已经说明了 3,4,5 如何放在统一框架下
			* 一个角度：3,4,5 必然含时，前两个未必
		* `[NeuralDE综述-2202.02435]` 可参考，讨论了 neural ODE/CDE/SDE，包括 BP 方式汇总
			* 其中提到 neuralCDE 为 RNN 连续时间极限，待考察
	* 以下为便于描述，记号统一用 $a\mapsto u\mapsto O\mapsto L$
		* 控制变量 $a$，用其生成物理过程 $u$、观测 $O$ 及其好坏度量 $L$
		* 问题需给出最优的 $a$，即求解 $\min_aL$
			* 有时需要给出多个候选 $a$
		* 有时涉及可变初边值 $u_0$，物理依赖为 $(a,u_0)\mapsto u$
			* 此时 $L=\mathbb{E}L(u_0)$ 为期望形式
			* 不含时问题可变边值例子：EIT DtN map 相当于 $u_0\mapsto O$ 映射（在给定 $a$ 时）
		> 我在 compTree 里对 inverse design/problem 记号对应：$a/\lambda,L/G,b/\eta$
		* 1 考虑 PDE，其参数为 $a$ 解为 $u$，$a$ 为设计、恢复的对象（可能为形状、参数、函数等）
			* 可含时，有时也可为 ODE
			* 学连续时间动力学（包括 HNN、本构方程）的可放在这个框架下解读 `contTimeDynML:`
			* 其实 4 也可以按这个框架理解
		* 2 与 1 基本一致，$L=\|O-O^*\|^2$ 度量模型结果与观测的区别
		* 3 通常考虑 ODE 或含时 PDE，外部干预 $a(t)$ 可含时
			* 不应有随机的 $u_0$；不同初值只能在 $b$ 体现，不对应同一个优化问题
		* 4 为 ODE，$a(t)=f(\cdot,t)$ 为待定函数，必然有非确定性的 $u_0$（对应样本点 $x$）
			* 不同初值给出的 ODE 共享一套控制变量 $a$（或含时的 $a(t)$）
			* 给出观测 $O=u(T)=y$ 即终值（预测标签），最后 loss 具形式 $L=\mathbb{E}_x\|y-y^*\|^2$
			* 由于不对应真实物理过程，ODE 求解无需使用精确数值格式，直接前向 Euler 即可（对应 NN 前传）
			* 1,2 的 PDE 求解更复杂，可能用 PINN 或者其他 surrogate（见 compTree）
			* 相关：`ANODE-1902.10298`(x) 利用 adjoint 方程先优化后离散提供的梯度可能不准，低存储代价的梯度计算需要设 checkpoint，BP 时现场算一段 FP
		* 5 为 SDE，控制变量需根据随机演化情况调整，需要给出控制的生成方式，形如 $a(x,t)$
			> 对 ODE 一般有 $u(t)=x(t)$，有记号混用
			* $a\mapsto u$ 映射有随机性，依赖于 $\omega\in\Omega$
			* SDE 初值可以取定，演化（对轨道采样）来自与环境交互
			* loss 形式 $L=\mathbb{E}_\omega$
		* TODO 求解等相关讨论（关于 1,2 的在 compTree）
	* 若考察一系列问题的求解（类似元学习），问题角标 $b$
		* 问题场景与 $b$ 来源：
			* 1,3,5 场景包括：$a,b$ 均为方程参数，$b$ 由可变客观因素给出，仅 $a$ 能控制
				* 此时 $b$ 影响 $a\mapsto u$ 映射
			* 1,3,5 也可以 $b$ 描述目标的变化，此时 $b$ 影响 $O\mapsto L$ 映射形式
			* 2. $b=O^*$，从而仅 $O\mapsto L$ 依赖于 $b$
			* 4. 为元学习，$b=\tau\in\mathbb{P}(X\times Y)$ 描述样本标签联合分布
				* 初值（样本）分布改变后影响 $a\mapsto u$ 映射
				* 标签分布改变影响 loss $O\mapsto L$ 形式
		* 可能涉及三层优化（纯优化问题，其实不要求系统含时；一般框架见 `L2O加速双边优化`）{n35f06}
			1. 用 PINN 解 $u_{a,b}=\arg\min_uL^\text{PINN}_{a,b}[u]$
				* 仅针对用 PDE 求解不直接的情形，如 1,2,3；像 4 FP 直接得 $u$ 当然不需要
			2. 单问题的求解 $a_b=\arg\min_aL(a,b)$
			3. 用元学习类方法优化 $b\mapsto a_b$ 求解流程，参数 $\theta$
				* 用双边优化类元学习时，上一层成为 $a_{b,\theta}$，如 AD 找候选控制集合 $A_\theta$
				* 与元学习记号对应：$a_{b,\theta}/\phi_{\tau,\theta}$
				* 用 hypernet $g_\theta:a\mapsto a_b$ 等做法时其实跳过了一层优化
		* 元学习加速的多种可能方式见 `invDesign-metaPINN:`(AD)，也许多样性是三层优化形式带来的
	* 在动力学一般框架描述（反问题、设计也只考察问题含时的框架）
		* $u(t)$ 涉及元素：初值 $u_0$，动力学中含时成分 $f_t$、不含时成分 $\lambda$
			* 每个元素都有多种情况：固定，被随机给定，可主动选择（优化），有对抗攻击；以及这些的组合
			* 有随机部分时，目标函数 $L$ 可能同时被随机给定
			* $L$ 可能性：只依赖终值、依赖整体 $u(t)$、更复杂依赖（如反向设计中或依赖时间导数）
				* eg. 时序分析，序列转换（翻译等）依赖整体、序列总结分类等可只依赖终值
			* $f_t$ 中可主动选择部分记为 $a_t$，随机给定部分常为布朗运动项 $W_t$
			* $\lambda$ 在无穷维系统（含时 PDE）中更常见
		* 各常见场景对应元素：
			* 反问题、反向设计：可优化 $\lambda,u_0$
				* 拟合动力学、预测同为此框架 `contTimeDynML:`，动力学形式相同，只是含义不同
			* control、RL：优化 $a_t$；$u_0$ 可固定或随机给定
				* 随机情形 $a_t$ 的产生可依赖于 $u(t)$，即 $a_t=a_\theta(t,u)$
			* ResNet：优化 $a_t$，且它表达了几乎全部的 $f_t$；$u_0=g_\theta(x)$，$x$ 随机给定
			* 时序分析（类似连续时间 RNN）：$f_t$ 有（较复杂的）随机给定部分；优化 $a_t$
			* 动态博弈问题，$f_t$ 中有对抗攻击项（依据 $L'$ 优化的 $a_t'$）
			* 反向 SDE 生成模型 `GenModSDE-2011.13456`，$u_0,W_t$ 随机，$a_t=a_\theta(t,u(t))$ 有更精细结构
		* 耦合另一动力学 $u'(t)$，常用于设计 hypernet；{n35f0h}
			* `[NeuralDE综述-2202.02435]`“hypernet”控制生成方式为 $f_t=a_t=u'(t)$，其动力学 $f'_t=a'_t$ 完全可学，初值 $u_0$ 不可学、$u'_0$ 可学
			* `tileAD+sinArch-2104.03960` 特殊设计的 hypernet 架构
				* $u_0$ 根据主网络输入获得，$u'_0$ 根据超网络输入（那里是 $z$）获得
				* $f_t=a_t=a_\theta(t,u(t),u'(t))$，$f'_t=a'_t$ 也完全可学
				* 主、超网络的不同性质可由 $a_t,a'_t$ 的不同设计体现；这里主网络表达高频函数
			* ((n8vg4p))clh自己设计的效果较好的 hypernet 也用 CDE 形式，生成 INR（MFN 架构）各层 bias modulation
			* 另可考虑((n4ub8w))超网络调控主网络前传行为 其他方式，可理解为 生成动力学本身、((n4ub8t))生成脉冲形式外力项
			* 多动力学耦合的另一场景：((n61965))CoDi 多模态扩散生成模型，各模态有独立的反扩散动力学，为保证联合生成时结果匹配，额外引入这些动力学间的信息交互
			* 相关：((n32f3o))NN架构设计-微分方程启发，((n8vg6b))NN 产生高维输出-输出视为序列-ODE 各步状态作为输出序列
	* 部分相关工作（除了在 `invDP%` 已有的）
		* `2022-07-18`(lectures) 流体控制，RL 根据散点观测施加控制，PSO（粒子群）方法优化观测所用散点的位置
		* ((n2bf2c))薛方可写为最优控制问题（HJB 方程）
	* 注：控制问题、RL 主要内容记录于((n3bg3a))
