> 2025-08-23 从 ML.md 独立
* 算法设计领域 TRIZ 版本的可能性？{n2ln3z}
* 非科研相关：属于((n3bb7n))一种解搜索问题
* 40 原理((n2ge0h)) 是否可给出相应版本？{n2ek9k}
	* 随机性
		* 输入加噪声
		* 确定性输出改随机性输出
		* 随机场，函数空间中随机性
		* 优化计算中引入随机性
	* 合并
		* 场的离散表征改参化（有时也可基底），时间、空间 可分别连续化
			* 另外流程的时间连续化也按分割原理解读
		* 序列、非独立的集合改函数，$x_i$ 改 $x(i)$（可再用替身原理成为 $x(z_i)$，类似 MAD）
			* 暂不完全肯定“合并”的含义，似乎和“分割”并非相反操作，先分割（尤其按分量拆分）再合并也可得新的结果
			* 例如：EIT 中加多次不同的电流，相应得多个场 $u_i$；RL 传统 Q-learning 为表格 $Q(s,a)$ 形式，现在由 NN 参化 $Q_\theta(s,a)$
			* 另若个数固定，还可考虑改向量（整体作为输入/输出），如 EIT 多个场 $u_i$ 合并成一个多分量的场
			* 除用替身原理，有时可用其他方式编码，如 hash（类似 NeRF_5s）
				* 不过或许需避免某种意义上的 mode collapse，改写为 $x_i=x^1(h^1(i))+x^2(h^2(i))$，$h^1,h^2$ 不同的 hash 算法
		* 输入、输出的打包，如 ViT 不是对像素的集合用 Transformer，而是打包一些像素形成 patch，对 patch 用
			* 打包后可再结合替身原理，如 ViT 对打包后的像素编码
		* 映射的 end-to-end？
	* 分割、局部质量
		* 输入、输出
			* （分量/concat）拆分，$x$ 改成 $[x_1,x_2]$；{n3t97e}
				* Transformer 解码器按迭代方式逐词产生输出，ViT 按 patch 输入图像
				* 彻底分割，打散为集合 $(x_i,i)$，所谓的 tokenize；{n4rm8d}
					* 这或许可以((n3t97h))让预训练 INR 可对输入维数泛化
					* 如 NLP 中 word embedding((n4rm8c))
					* NLP 中((_n5im3j))MegaByte 不再像往常按（英文）单词划分 token，而是用固定长度字符 patch
					* 图像 tokenize 方法被分为二类((_nc6n38))：简单 patch 序列、预训练的离散化 tokenizer；{o8ue5k}
						* 新框架见((p3va9b))输入场降低分辨率，区分更细致
						* （推测）二类区别 1. 连续vs离散，前者各 token 可能取值空间连续，后者可能取值离散
							* VQ 可视为对隐向量分布的一种正则化((n6sf8q))，包括 VQVAE,VQGAN
							* 若有相应生成模型，前者为回归模型、后者为分类模型；见((oapf91))离散分布建模适合自回归、连续分布适合扩散
							* 离散词表大小：((_oap992))Emu3 32k
						* （推测）二类区别 2. 空间尺寸压缩方式，patch（单个输入像素影响单 token）、CNN（影响多相邻 token）
							* 相关框架((p3va9b))空间尺寸压缩
							* 混合方式：先编码为 ((n5bk3r))latent feature，再对此打 patch；Sora 用到；{o33a2v}
								* 进行了两次((p3va9b))空间分辨率压缩，第一次 CNN，第二次 patch
								* Meta MovieGen 类似，(('patm43))压缩率 CNN 时空均 8，patch 时间 1 空间 2
								* 理论上也可 先 patch 再 CNN
								* 第一次 grid-based INR，第二次逐 patch 用 CNN 解码器（不再是张量 reshape），压缩针对待表征对象而非现成数据：((p59e68))ACORN
							* 空间压缩率见((oapa1r))
							* 打 patch 如 ViT((n37m6i))，编程时((n8hj6b))可通过卷积层实现线性变换
								* patch 独立编码可由线性层换为 MLP
								* 相关：打 patch 后可考虑 ((n5bf17))patch manifold
							* 网络架构，用于视频时((_oap995))MoVQGAN 编解码器中加入时间残差层，其卷积核三维
						* 相关框架：((oaqb09))场表示为有空间结构的隐向量，情况更多样，也包括 连续vs离散、空间尺寸压缩方式
						* 类似场景：((p8he9w))NO 输入层
					* 处理集合输入的网络架构：((n3qn83))NO，((n4rb6l))hypernet
					* 相关：((n31m78))数据预处理，((p8ag76))网络输入-集合-划分后处理
				* 可有重叠，如 ViT patch 拆分可允许不同 patch 有公共部分
			* （向量/加法）拆分，$x=x_1+x_2$，两项可有性质差异
		* 集合、定义域的（并集）拆分，INR、PDE 区域分解
		* 映射
			* （复合）拆分，引入中介：意识 GWT（全局工作空间理论），引入有限维中介
				* 引入多重中介，成为迭代格式
				* 令中介数趋于无穷，成为 ODE、CDE、PDE 等，时间连续化
			* 循序渐进，同时（复合）拆映射、拆输入（或 condition）
				* 例如((_n5cd7m))文转 NeRF 用 refine 方式，逐步添加想要的元素、引入想要的修改
				* 映射分析下可能为：$y_0\xmapsto{x}y$ 改 $y_0\xmapsto{x_1}y_1\xmapsto{x_2}y$
			* 按输入、输出拆分
			* 定义域（并集）拆分，1taskHypernet
		* 场（当然也可用一般映射可做的拆分，这里不讨论），包括自变量为时间的场
			* （求和）拆分（也是按输出拆分）：求 Green 函数时表达为全空间基本解（奇异）+ 处理边值的项（光滑）
			* 离散化，包括时间、空间离散化
		* 并行存储，并行计算，大模型三种并行方式
		* 数据拆分为 训练、验证、测试集；((n31m8z))多保真数据
		* 微分算子
			* （复合）拆分，((n3ne2f))PDE升维降阶改写（从 $u$ 角度看算 引入中介物？）
			* （求和）拆分，((n3sf6k))含时方程算子分裂；{n3sf5f}
				* 从短时时间推进算子来看算（复合）拆分，可见((n3ri89))迭代原理-单一格式迭代改交替格式迭代
		* 动态、自适应拆分，而非用预先设定的拆分方式；{n4rn02}
			* ((n4rm8c))NLP 中有单个字母 embedding，常连续出现的多字母（如单词）则统一 embedding
			* INR 动态区域分解
	* 迭代
		* ((n3rj2c))单步流程做多次，如线性方程残差再次作为右端项（主要放重复原理）
		* 固定流程改迭代流程（在分割原理也有讨论）
			* 映射形式-有限步迭代，不定长迭代
		* 固定步数迭代改为不定步数迭代
		* FPN 等？
		* Transformer 解码器，逐个词输出；还涉及((n5hg3n))学移位算子
		* 固定格式迭代改成可变格式迭代
			* 形式上 $x_t\xmapsto{w}x_{t+1}$ 改成 $x_t\xmapsto{w_t}x_{t+1}$
			* 优化器 lr scheduler
			* loss 权重（如 MAML++ 权重分配）
			* 多重网格？在不同的网格上执行迭代算法
			* 注：若在迭代过程中，迭代格式是突变而非渐变，则算（下面的）二阶段迭代
		* 单一格式迭代改成交替格式迭代；{n3ri89}
			* 优化器—交替迭代格式：ADMM，proximal gradient（可((n3sg0e))与动力学算子分裂对应）
			* 优化器—((o53h2m))Adam 与 L-BFGS 交替使用
			* 数值格式—（不含时）PDE 求解用混合迭代算子((n3fe73))
			* 数值格式—含时 PDE 涉及((n3sf5f))算子分裂；这也可理解为 分割原理
			* 数值格式—NPDE 的蛙跳格式？
			* 架构-加新功能—((n3ri6n))stable diffusion 在生成模型中引入 condition，通过在逐层后加注意力后处理层引入
			* 架构—动力学自回归预测，((q1ka3o))ViT 每步用不同 patch-size
			* 架构—FNO 原为每层同步 MLP + FT 积分算子，((_n9me4a))改为（更像 Transformer 的）先 FT 积分再 MLP 的做法有 significant improvement；{n9me4p}
				* ((_o7ta7k))FFNO 用到类似策略，((_ocof0s))在 AM-FNO 中沿用
			* 架构—((q5d901))NO 交替使用多类型的层
			* 非科研相关：((n1fa3u))引入异质性以克服思维定势，((n1vk82))异质性的作用
		* 单一迭代格式改成二阶段不同格式迭代；{n4280s}
			* 优化器—((n42806))PINN 训练中换用优化器，先 Adam 后 L-BFGS
			* 优化器—((_p8dk7j))DFT 等场景的优化问题，需 无梯度、有梯度 算法在不同阶段结合
			* dropout 策略包括((n3892d))early dropout 缓解欠拟合，((n3891k))late dropout 缓解过拟合
			* 注：若在迭代过程中，迭代格式是渐变而非突变，则算（上面的）可变格式迭代
		* 在外层迭代中引入内层迭代，如 Runge-Kutta，((_n3vh2f))
	* 顺序结构平行化，包括映射的 set2set 改写；{n4296a}
		* Transformer（相比于 RNN；引入位置编码来恢复顺序信息）
			* 相关：((n4295u))Transformer 架构理解为集合/序列/图数据的翻译
		* 映射的 Transformer 化（set2set，不改变集合大小），$x\mapsto y$ 改成 $(x,y')\mapsto(x',y)$
			* 或许更合适的表述：引入 init token；{n4t95z}
				* 相关：((n4t96h))特殊token
			* 可再对 $x,y$ 运用分割原理，按分量分割
			* 若 $x,y$ 来自不同域，可用替身原理（输入其表示），该替身有时可通过比较原理（类似 CLIP）学出
			* 例如 ViT 用于图像分类，输入人为引入 class label；以及 ((n42e9a))超网络
		* DenseNet 顺序前传的隐层激活视为同时用于生成后层，$(x,h_i)\mapsto h_+$（也可 Transformer 化）
		* 算法并行化
	* 周期
		* 前传 activation 占用内存太多，不存全部 activation，而是隔几层存一个，BP 时现场计算
			* 是否可能算 时空置换 原理（如果有的话？不过 TRIZ 可能更倾向于将它作为时空二参数的技术矛盾）
	* 重复
		* 流程的重复：单步流程做多次，如线性方程残差再次作为右端项；{n3rj2c}
			* 另作为迭代原理
		* 模型的重复：ensemble((n2me2z))，包括传统 ML 做法（bagging, boosting），学已有模型残差以减小 bias（欠拟合），引入多个平行模型投票以减小 variance（过拟合）
		* 输入的重复：{n3t95j}
			* DeepSDF 网络架构，从网络中间位置再引入一次输入
			* ((n3t95e))DGM 网络架构用 LSTM，每层都输入一次 $x$
			* ((n3vf61))INR 通过每层都输入一次 $x$ 来表达高频信号
			* 非等价重复：((q15j6j))图像生成的文本条件用 3 种不同编码器分别处理，结果整合后同时作用于主网络前传
		* 参数共享，从而可用有限参数表达高维输入、输出的映射？如 CNN,RNN,GNN,Transformer
	* 替代、替身、变换；{n4sj3v}
		* 不是处理对象本身，而是处理对象的替代；如动力学在隐空间计算演化
		* 一般数据的替身：
			* 隐向量（无损）((n31g86))，如 AE；注意隐向量本身可有结构
			* 表示（有损）((n3rn3e))，可通过对比原理获得
				* 两类数据的共有表示 使其可作为对方替身，如 CLIP 对齐图文后，Stable Diffusion 去噪模型的 condition 在训练时用图像、推断时用文本
				* 特征表示的使用((oafe86))
			* 简单复制：ADMM 优化算法 $f(x)+g(x)$ 优化改写为约束优化 $f(x)+g(y),x=y$
			* 有 BP 计算图时，复制出无计算图版本，stop-gradient 算子
				* 如 loss $|f(w)-g(w)|$ 希望由 $f$ 提供的梯度幅度大于 $g$，可写为 $|sg(f)-g|+r|f-sg(g)|$；在 VQ-GAN 用到((_n4rm14))
			* 编码：见预处理原理
		* 场数据的替身：Fourier，小波，functa（无论是用参数还是 modulation）{p2oh7y}
			* 2D 图片（渲染结果）作为 3D NeRF 的替身；{n5cb23}
				* 用于文字转 NeRF：((n5bm0t))DreamFields 用 CLIP 定义图文距离，从而可优化 NeRF 参数
				* ((_n5cb3x))DreamFusion 对渲染图用文本信息编辑（条件扩散模型）以提供 NeRF 梯度；{n5cb4s}
				* 实现时可随机选取光照条件、视角等((_n5cd4s))
			* 理论上 2D 图片可作为 2D INR 替身
			* 参化表达的场作为网络输入((n7n974))
			* INR 梯度需频繁调用时，((p6nf5u))用额外 INR 作替身；需惩罚新 INR 输出等于梯度，但调用频率相对低
		* 指标 $i$ 的替身：位置编码（Transformer 用到），隐向量 $z_i$（可自适应优化）{n3t97t}
			* 相似地，动力学中 $t$ 可用 $z_t$ 作为替身
		* 一般的 surrogate，包括 NO 替代数值求解器，小模型替代大模型以加快推理速度（pruning？）
		* PDE 的替身，ROM（来自函数空间的替身：低维表征）
		* 物理系统（如 PDE 本构关系）的替身：系统局部满足方程，((q6rk6y))该离散代数方程系数作为系统的表示
		* 数据产生机制¹的替身：((n2bg0v))actor-critic，MBRL²
			* ¹如 RL 中（产生 reward 的那部分）环境，包括((n3ti7c))RLHF 中训排序模型代替负责排序的人
			* ²这里指先学环境模型（reward，状态转移）再用它训 RL，不是指 RL 中 policy 生成时动态调用环境模型、现场优化计算（而非简单 NN 前传）的那类 MBRL
		* loss 的替身：((n4sj3s))由另一辅助网络来提供 loss
		* 非科研有考虑((n3ln2s))分量、局部、多保真模型 及相应高阶模型，该思想可用于设计时间多尺度 NO 等
	* 引入中介
		* ((n3ne2f))PDE升维降阶改写（从微分算子角度看，算对微分算子用分割原理）
		* ADMM 优化算法 $f(x)+g(x)$ 优化引入 $y$，改写为约束优化 $f(x)+g(y),x=y$（也算对 $x$ 用替身原理）
	* 引入记忆；{n41958}
		* 网络架构，对激活值的记忆：ResNet，DenseNet，U-Net（跨尺度连接）
		* 状态的记忆：{o1lh1t}
			* 注：目前主要为 从原理上有必要引入状态记忆 的情形，针对动力学预测问题的必要性讨论见((naha1f))
			* Mori-Zwanzig 及一般动力学模型，((n35e9h))无记忆完整观测动力学 转化为 有记忆部分观测动力学
			* ((o1lh1z))含参无记忆动力学 转化为 不含参有记忆动力学
			* MDP 只有部分观测时引入记忆项 ((n77k4o))RL状态空间观测不完全，belief states（处理信息不完整情形）
			* 优化器的记忆，BFGS 与截断记忆长度的 L-BFGS；（部分观测 RL 也可视为某种优化器）
			* PDE 迭代求解器中((o1lg1g))设 PDE 参数不可观测、引入迭代历史记忆
			* 相关：((n2s979))引入记忆状态的网络架构设计方式
		* NLP 大模型 上下文学习，对话能力
		* 考虑记忆衰减速率，单一衰减率记忆改为同时有多种衰减率记忆：LSTM，((_n4bn4w))优化算法中动量、二阶矩衰减率
		* 动力学建模，((o9ac0l))无记忆系统由于离散化失真，仍建议按有记忆系统建模
		* 相关：memory 的另一含义是((n3gm7j))编程实现上的存储、内存问题
	* 角色转化：((n5bg83))参数转化为输入，((n5bg8b))输入转化为参数
	* 自适应
		* 对任务自适应：固定映射改可学映射，形式上 $x\mapsto y$ 改 $x\xmapsto{w}y$
		* 优化器 Adam，引入动量等
		* 对输入自适应：
			* 1taskHypernet，形式上 $x\xmapsto{w}y$ 改成 $x\xmapsto{w(x)}y$（即引入 $x\mapsto w$）
			* 自适应卷积核，((n2pk00))dynamic NN
			* 注意力机制？
	* 预处理
		* NN 预训练，元学习
		* NN 引入先验结构？（架构设计上的先验，而预训练、元学习是靠数据引入的先验）
		* 数据预处理((n31m78))，数据清洗、数据增强
		* 输入预处理，如 INR 位置编码，((_n4bn4m))迭代步数的编码；若预计输出依赖于 $a(x)$ 则输入写为 $(x,a(x))$
	* 对比，用对比 loss 替代直接 loss 训练；{n4sj63}
		* 注：似乎不应仅视为“训练策略”，因会影响网络输入输出形式
		* 大小/排序型比较：目前见((p98k8g))算法输出质量评估-用辅助网络-多候选输出比较
		* 匹配/度量型比较：如 CLIP、Siamese NN，用于((n3rn3e))产生表示；{n61a97}
			* 据说对比学习通常需较大 batch-size，如((n61a9r))CLIP 用 32k；{n61b30}
				* 可能因为负样本对数量需要比正样本对多许多？
				* 减小 batch-size ((o9pk04))可通过改变负样本对构造方式（甚至不再用负样本对）达到
				* 相关：((n3890b))batch
			* 正样本对来源，作为对比学习依据
				* 跨模态配对，如 CLIP，((n8n88x))对比学习作为多模态预训练范式
				* 同模态多样本 数据产生机理
					* 物理信息对比学习((_o6of13))，数据相似度度量利用了 PDE 系数、物理相似度（如二轨迹单步更新幅度相似度等）
				* 单样本衍生，通过((n31n1z))数据增广 产生不同样本；{p8nb65}
					* NO 输入场 crop、对称群变换，要求同源数据编码结果相似((_n7fb9r))；{n8mn6x}
						* 好于直接有监督：原文实验结果，编码结果用于下游有监督任务好于直接从头有监督训练
					* 物理场取时空子区域，((p8ma7y))用于训 NO 的 1TaskHypernet
					* 由于增广成对，选取方式可耦合（非独立随机），耦合方式可体现场景特点，如((p8nj9k))物理场 crop 两时空子块的方式联合选取
					* 据说还有 feature-decoration-based 变种，包括 Barlow Twins, VICReg；细节未确认
				* 前传随机性，((_o31f3w))同一输入带不同 dropout 前传两次；{o31f46}
					* 相关：((n37g82))dropout
				* 性能比较：((_o73k12))认为 数据增广、物理信息对比学习 两种做法在实验中提升有限
			* 防退化，避免编码器成为常值映射，通过引入负样本对等；{q5cn4f}
				* 构造负样本对；普通 end-to-end 做法使用一个 batch 内非对角线样本
					* 需较大 batch-size((n61b30))
					* MoCo 额外引入新 batch b'（size K），原 batch b 的每数据样本和自己形成 1 个正样本对，又和 b' 中样本共形成 K 个负样本对；{o9pk04}
						* 该做法 batch-size 可减小
						* b' 来自历史样本过编码器结果：当时生成所用的编码器参数相比最新的落后一些（称为 momentum encoder），且不参与 BP
					* 据说 SimCLR 也属该类（未了解细节）
				* 额外 loss，((_p8n97o))要求编码器非退化
				* 基于自蒸馏（self-distillation）的对比学习，不用负样本对
					* SwAV 构造聚类中心？未确认细节，见 2024-09-25 组会
					* SimSiam 通过正样本对之一 stop-gradient 就直接可以避免模型退化？{o9pk2r}
						* stop-grad 相关：之前的 a+(b-a).detach()
					* BYOL 通过特定方式避免编码器退化到常值输出，stop-gradient 加 predictor
					* JEPA 同网络的历史权重（EMA）作为 teacher
				* 相关：防 mode collapse 还用于((ncnf7r))MoE
			* 具体场景，((o72l51))NO-预训练-对比学习（算同模态比较）
			* 相关：((n9999m))网络(输入,输出)≠ 数据(样本,标签)，((n8mn5a))自监督预训练
		* 与参考样本比较、提高预测精度((o72k8l))
		* 推断中的对比：((n9me8w))推断时引入已有网络作为参考，试图远离该结果
		* cycle loss：((_ocgf11))CycleGAN，((o1hn0w))DeepM&MNet 引入方程形式约束，((_ocgf14))INR 训练散点集合的 cycle consistency
	* 部分或超额行动；{n95j35}
		* MTL 多任务学习，为增强一个任务的效果，人为引入更多任务辅助训练
		* 考虑((n95m1r))离散多解问题（如图像分割、自动驾驶路线规划），模型同时生成多个解、loss 只反传最小那个
		* ((_o31e9c))多分类任务引入假类别可提高性能；{o31e9g}
			* 相关：((o31e9h))分类-视为极大似然
		* ((n8qk2t))优化-ensemble 方法，((n95j4p))invDP-多解
	* 引入调控（待定）
		* ODE 改 CDE
		* 生成模型改条件生成（包括扩散生成模型），如((n3ri6n))stable diffusion 用迭代-异质迭代原理
	* 关于如何总结出新的原理，可能性
		* 试图提炼更多可能的原理，尝试分析 AI 领域影响力大的工作/思想？
			* 注：该思考过程也可在笔记中保留（而非只留下从中分析出的原理），本身有价值
				* 或许应优先分析教材中（如 NPDE，凸优化，数据结构与算法；统计计算？）中的思想，都是长时间后被保留的想法，整体质量更高、筛选成本更低？
			* ResNet、DenseNet 已理解为 引入记忆
				* 不过若理解为 网络深度的物理矛盾，或可理解为 突变过程改为循序渐进？（GAN 到扩散生成模型也可认为是类似的原理）
			* RLHF 中((n2bg13))让人排序¹、再训排序模型²，数据质量vs人工成本
				* ¹对比原理：人排序模型生成的答案，而非自己写参考答案
				* ²替身原理（这里需与前一点结合，若人负责写答案，替身也负责生成答案，则不太可能比被训练的模型好，替身没什么用）{n3ti7c}
			* Transformer（相比于 RNN？）序列结构改平行结构；另需考虑 输入为集合、输出为单个向量（序列分类、回归任务，通过在输入集合中引入特殊空输入实现），算什么原理？
			* modulation，DeepSDF，DeepSet，MTL（多任务），MAE、BERT 的自监督训练
			* DEQ=FPN，用于解决内存不足问题？
			* ensemble（包括传统 ML 的 bagging, boosting），重复原理？
			* 引入辅助网络联合训练((n2ek9h))？
			* HRL 是否可对应某原理？
* 矛盾矩阵
	* 涉及的技术参数？数据量算原材料/信息需求问题？机理算啥？
		* 不排除最开始先只整理单个技术参数改进方式，后期再考虑整理参数对（矛盾）的处理方式
		* 再开始可能连参数也没整理好，使用时只是直接查原理
	* ResNet（记忆原理）：逼近误差（NN 表达能力，靠深度体现）、优化误差（训练难度）
	* 不存所有 activation，而是隔几层存（周期原理）：NN 表达能力（靠深度体现），内存占用
	* double descent（虽然只是现象）：逼近误差，泛化误差；似乎还涉及 SGD 导致的 flat minima，或者初值选取问题？
	* pruning 模型修剪场景，模型复杂度vs精度、准确性（ChatGPT 说的）
	* ensemble（重复原理），bagging、boosting，分别减偏差（欠拟合）、减方差（过拟合）
	* dropout？Transformer？
* 映射分析，仿照 物场分析？
	* 先对一些已有例子进行分析
		* ResNet，DenseNet
			* 问题模型：$h\mapsto h_+$ 网络单层前传，问题：难训练，因 BP 梯度消失
				* 问题可改表述为 正向映射不足，输入变化较大时输出变化小
			* 解模型：$h\mapsto h_+,h\mapsto h$（后一个为 id），$(h_+,h)$ 作为下一层输入
				* 可直接 concat 输入下一层（DenseNet），也可先加和（ResNet）
				* id 映射也可跨几层，使后面的 $h$ 与 $h_{++}$ 组合
		* RNN
		* Transformer
		* NN 处理高维输出时的 合并原理
		* 生成模型，许多是引入辅助网络联合训练
			* 问题模型 $z\mapsto x$ 随机向量到数据，问题：难训练，因无梯度
			* GAN 解模型 $z\mapsto x,x\mapsto[0,1]$，训练后一个网络，利用它为前一个网络提供梯度
			* VAE 解模型 $x\mapsto z,z\mapsto x$，联合训练
			* 正则化流？
		* actor-critic
		* stable diffusion，直接映射训练难（因缺少直接的文生图数据对），通过引入 CLIP 的语义中介实现了间接训练
			* 问题模型 $x\mapsto y$ 文字到图像，问题：难训练，因缺（有监督）数据
			* 解模型 $x\mapsto z,y\mapsto z,z\mapsto y$
				* 训练：先 $x\mapsto z,y\mapsto z$ 联合训（CLIP，用图文配对，对匹配程度要求低于文生图，故数据较多）
				* 再训 $z\mapsto y$、用到并固定 $y\mapsto z$（用大量纯图片；其实是条件扩散模型，这里略去架构）
		* ControlNet-2302.05543((_n4tj05))，通用架构部分；{n4tj11}
			* 设已有预训练模型 $x\xmapsto{w}y$，希望微调得 $(x,c)\mapsto y_n$
				* 相关框架：((n4te7l))基于已训练网络构建新网络
			* ansatz：$(x,c)\mapsto x_c\xmapsto{w'}y_c$，$(y,y_c)\mapsto y_n$（原有的 $x\xmapsto{w}y$ 仍要用到、不参与微调）
				* 其中 $w'$ 由 $w$ 微调而得（a trainable copy）{n4tj0u}
				* $(x,c)\mapsto x_c$ ansatz：$x_c=x+Conv(c)$，Conv 1x1 卷积
				* $(y,y_c)\mapsto y_n$ ansatz 类似：$y_n=y+Conv(y_c)$，Conv 1x1 卷积
			* 注意原来的 $x\xmapsto{w}y$ 可以是单个 block、若干 block、整个网络；{n4ub8o}
				* 解读见 `<n4th0a>`；原论文针对 Stable Diffusion 的 U-Net，依据训练资源（数据、算力）多少有相应调整
				* 若主网络解读为 ODE，这种做法或可解读为((n4ub8t))引入 pulse 外力项
			* 细化解读，拆分为两步修改：`<n4tg41>` 先引入 $x\xmapsto{w'}y'$，再修改这个新加入的映射、引入额外输入 $c$
			* 属框架((n4ub8w))hypernet-调控主网络行为
	* 想到的一些改写方式
		* 似乎这对网络架构设计比较重要，而不考虑数据、训练优化算法等？
		* $x\mapsto y$（v1）
			* 引入参化 $x\xmapsto{w}y$；再转化为 $(x,w)\mapsto y$；{n5bg83}
				* 等价表述：$W\to(X\to Y)$ 改成 $X\times W\to Y$，$f_\theta(x)$ 改成 $f(x,\theta)$
				* eg. MAD
			* $?\xmapsto{x}y$ 或 $x_1\xmapsto{x_2}y$，可进一步改写为 hypernet、条件生成模型；{n5bg8b}
				* 另一种解读方式：输出 $y$ 改成输出 $\theta$（$f_\theta:y_0\mapsto y$），或称：输出改为元输出；不直接产生 $y$，而产生产生 $y$ 的机制
				* 此时普通网络变成超网络：$x$ 是外挂网络输入，主网络输入待定（可取常数输入，或也可用另一个网络的输入）
				* modulation 算啥？
				* 这种做法下 ((n5bh10))随机映射 改写为 条件生成
					* 条件生成模型例子，目前主要扩散模型：((n5bg9t))Shap-E，$y$ 为 INR 参数；((n5bh0h))纯想法，$y$ 为图像等
			* 分割原理 $x\mapsto w\mapsto y$；后一个映射角色转化成为 $?\xmapsto{w}y$，再次得到 hypernet
			* 迭代预备的几种方式，迭代要求同空间 $z_0\mapsto z_1$（允许 $z=x,z=y$）
				* 平行化 $(x,y')\mapsto(x',y)$，从而可用 Transformer；可进一步对 $x,y$ tokenize
				* $y_0\xmapsto{x}y$，作为((n5bg8b))的特例
				* 基于 $x$ 变种的映射分割 $x\mapsto x'\mapsto y$
				* 基于 $y$ 变种的映射分割 $x\mapsto y'\mapsto y$
				* 基于公共隐空间的映射分割 $x\mapsto z\mapsto z'\mapsto y$
		* 有记忆动力学推进，Transformer 解码器 $(?,x_1,\dots,x_t)\mapsto(x_1,\dots,x_{t+1})$ 表达为移位算子；{n5hg3n}
		* 含时方程 $u(x,t)$ 与 $u(x;z_t)$？暂未搞清楚其双向转化如何进行的
			* 2023-03-29 Xournal 有部分尝试；另外还可考虑时间维度的参化、离散表征
			* 或许分别对应 替身（$t$ 换 $z(t)$）、合并（$u(x,z(t))$ 换 $u(x,t)$）？
			* 相关：((n5bf2t))time-slice 组成流形，$z$ 为其参化
	* 新版，试解读为 DAG 而非序列结构
		* INR 及 grid-based 变种版本尝试解读现有工作 `<p41m2k>`
			* p2 广义“区域分解”((n2pe8f))含义，存在 switcher 且其 Q 输入为 x，或依赖于 x；{p44b73}
			* p2 INR 每层输入 x((n3vf61)) 通用含义；{p44b7d}
				* 影响主网络前传的不同方式：激活值 modulation（图中箭头），前传权重（仅线段连接）；{p4pb0v}
					* modulation 见((n2il89))，前传权重见((n42e71))hypernet
					* 不限于 MLP，影响主网络前传行为的多种方式见((p4pb1p))旁路输入
	* 总结出映射模型、标准解时或可考虑的东西
		* 与物场分析不同，这里一个数据可多次复用，在多个不同地方出现
			* 一种方案：所有复用均写为 id 映射，视为新增对象（尽管编程时不需要真的新增）
				* 一个好处：容易看出可改写为其他映射，如线性变换、slice 等；而写为同时输入多个位置的话，可能不那么容易看出来
		* 物场分析包含 抽象问题（有缺陷物场模型）+抽象解（标准解），我目前只列了一些抽象解（映射改写方式），缺少相应的抽象问题
* ARIZ 与 IFR
	* 描述系统组成部件时，或许需区分研究对象：考虑网络架构时，部件为输入、网络、输出；考虑训练时，部件为数据、网络、优化器
* 问题意识，哪些问题最值得追求？
	* AISC 大模型((n2ni9r))
	* 复杂系统相关，可包括 AI 自动建模，无论是提取机理数据还是其他
