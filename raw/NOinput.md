> 2026-08-04 从 NOarch.md 独立
* 不同 $a$ 形式下的映射架构：{n3qg83}
* 相关：((n3qi2r))NN架构设计-不同输出形式下的网络架构，((o31f1s))dropout-用于输入
* 对各种 $a,u$ 形式通用的架构：{n5bl4q}
	* 拆分映射为 $a\mapsto v\mapsto u$，如((n42e6y))（引入中间域）$v$ 为有限维向量
		* 类似 GPT-3¹：先预训练 $a$ 通过一个无监督模型（例如 AE）得到低维表达，将低维表达输入后续网络，从而化归为低维参数情形
			* ¹日后补充：不知道当时写的是什么意思了
		* 据说有这样的情况：形状的设计参数不多，但是直接输入这些参数会丢掉空间信息（多个形状有 overlap 等），不如直接对形状本身用这种方法处理高效
	* 改写为 hypernet；{n42e71}
		* 若 $u$ 用参化表达，主网络形如 $x\mapsto u(x)$
			* 如 DeepONet；
			* 考虑离散图片转 INR（图片或 3D NeRF），((_n42e7f))超网络用 Transformer 架构，输入图片、输出 INR 参数（其中主网络参数用 grouping 手段稍降维，而非 modulation）{n42e9a}
				* 相关：((n42f33))hypernet-主网络参数生成方式，((n4ce5l))NN架构设计-高维输出-分层使用两种高维输出方式，((n4t95z))映射Transformer化-引入init token
				* （备用）音频 INR 2302.04959 似乎用了类似做法
				* 离散图片转 INR 另一方法，((_n54f46))图像超分辨率，Transformer 中 $q$ 待查询坐标，$k$ 低分辨率图像中的坐标，$v$ 低分辨率图像对应位置的像素值/feature；{n54f4n}
					* 副本((n9ha7t))hypernet，解读为主网络参数量可变的 hypernet；((nase8f))假设空间参化方式
					* 可认为 $a,x$ 均（平等地）作为网络输入，不按（通常意义上的）hypernet 理解
					* 类似的((_n9ha74))OFormer，每个 $(x,a(x))$ 信息作为一个 token 过 Transformer 编码器，输出层用于计算 $k,v$ 与待查询坐标信息 $q$ 相互作用
					* 类似的架构还见于((_nase7w))PROSE，((_o29l0g))HAMLET，((_o6tf6z))AROMA
				* 类似的情形：点云转 INR((_n5bg3f))的 Shap-E 编码器¹，直接生成全部主网络权重（除后两层）；{n5bg3k}
					* ¹这里用于构建 INR 参数组成的数据集、供后续扩散模型拟合，可见((n5bk0b))扩散模型-生成INR参数-数据集生成
			* （生成 INR 参数的）超网络用 条件扩散模型¹、其去噪网络用 Transformer；{n5bg9t}
				* 相关：((n5bh10))随机映射=条件生成，((n5bg8b))映射分析-输入转参数 $?\xmapsto{x}y$，
				* 或可理解为 hypernet 套娃：生成 $u(x)$ 的网络由 $w$ 参化，生成 $w$ 的网络由文本参化（若将扩散条件生成模型理解为某种超网络）
				* 如((_n5bg9p))Shap-E 从文本生成 NeRF
				* 非条件生成的有 ((_n5bk05))HyperDiffusion
				* ¹INR 参数组成数据集，可考虑其生成方式((n5bk0b))
		* 若 $u$ 用离散、基底等表达，主网络可形如 $u_0\mapsto u$；{n5bh0h}
			* $u_0$ 可以是常值（零解，基础解；或者有时也可以是另一个 NO 生成的初值）
			* 若允许输出随机，可用随机场，类似((n3q94a))带条件的扩散生成模型（其中使用的是逐像素独立的噪声，这属于特殊的随机场）
		* 广义 hypernet，$a$ 信息在主网络旁路引入，而非主网络输入位置；{p4pb1p}
			* 狭义 hypernet，生成主网络参数权重
			* modulation，((p4pb0v))我的图形化表示不同于 hypernet
			* （主网络用注意力时）交叉注意力((nbih2c))，此时 a 信息长度可变
				* 使用注意力时的另一方式为((p4pb39))a 信息按上下文引入
			* 使用场景：((o3lk9o))DiT 原论文讨论的引入条件信息 4 方式，((p4pb42))三平面 NeRF 生成所用架构前传引入额外信息
	* 非前传式网络，优化 loss $L(u;a)$；{n5bm02}
		* 相关：((n5bl9s))映射形式-函数极小点
		* loss 构建见((p97l1n))网络/算法输出好坏评估，其下也有一些案例属本框架
		* PDE 求解情形如 PINN，及((n5bm09))元学习加速版本；loss 由 PDE 定义
		* NeRF loss 由图像渲染结果与照片比较给出
		* 文本转 NeRF，((_n5bm0d))DreamFields loss 由 CLIP 给出；{n5bm0t}
			* 此 loss 属于((p98k54))算法输出好坏评估-用辅助网络-衡量输入输出匹配度
			* 目的手段链条：构造映射(文本到 NeRF) → 函数极小点类隐式映射 → (目标函数,优化算法)
				* 目标函数 → 辅助网络-基于数据-输入输出匹配 → 度量型 → CLIP
			* loss 定义还涉及((n5cb23))图片作为 INR 替身，((n5cb4s))DreamFusion 也用此实现文转 NeRF
	* Transformer 化 $(a,u')\mapsto(a',u)$，要求 $a,u$ 均已 tokenize；{p4pb39}
		* 更准确的表述：按非移位 Transformer 的上下文引入
		* 相关：属于((o3lk9o))DiT 原论文讨论的引入条件信息 4 方式
	* 构造条件生成模型 $p(u|a)$，即使输出分布退化到确定性单点
		* AlphaFold3 涉及？
		* 适用范围，对 NO 类((_q69b64))数据稀疏的病态反问题相对适用，对数据密集的正问题 不如 有监督+对抗 loss
* 输入预处理，任务输入特殊处理后用作网络输入（一般策略）
	* 相关框架：((q7aj9t))网络 IO 改写，((n9999m))数据预处理-网络(输入,输出)≠ 数据(样本,标签)
	* 增强，加辅助信息，任务输入提取特征 $\phi(a)$ 作网络额外输入；{q8af8l}
		* 物理场→ 数值微分结果；{q7kf9z}
			* 物理场空间导数计算方式((n2o879))
			* 案例：((_q75g12))预设微分 stencil
			* 作用：基于 FD，((_q7kf9d))FNO/ResNet/ScOT 均提升（即使 FNO 理论上可表示频域导数）
			* 前提：物理场无噪声，((_q7kg04))否则降鲁棒性
			* 对象特殊化为 区域间变换，见((q7kg5p))Jacobian 增强
		* 物理场→ scale-free features((_q7al9t))，无量纲量、粗网格状态统计量等（尽管原文是改输入而非仅增强，由于网络预期功能简单）
		* 坐标→ 高频特征，见((n3t97n))INR 位置编码
		* 区域形状→ 统计特征((q8af8j))
		* 相关：数据集引入新样本 而非数据点引入新通道，见((n31n1z))数据增强
	* 换用等价表示，间接输入；{o1le7k}
		* 相关框架：对象（网络输入）一般化见((n4sj3v))仿TRIZ原理-替身
		* PDE 参数→ 当前残差：表达参化 PDE 求解迭代算子 $(u,a)\mapsto u_+$((n2rm3m))时，不输入 $a$，而是输入 PDE 残差 $e$；{o1le5m}
			* 如((_n2rm17))，认为 $u,a$ 差异大、同时作为网络输入时网络难处理
				* 具体地，原文考虑 Helmholtz 方程求解，$a$ 包括波速场（分片常数）、源项（点源），而 $u$ 为波场结构，方程残差 $e$ 似也是
				* 这种问题在使用 CNN 等形式的架构时或更明显，因涉及跨通道卷积，但这些不同通道结构差异太大
			* 该替换过程造成信息不完全，据此可((_n2rm3a))引入记忆状态，其观点来自不完全观测 MDP；{o1lg1g}
				* 引入记忆状态的网络架构设计方式见((n2s979))
				* 如果是线性方程残差，则((n3fe1c))只输入当前残差即可，无需有记忆
				* 一般见((o1lh4p))将迭代历史作为间接输入方式
			* error 作为网络输入，相关：((n99a18))SAM 图像分割，根据当前预测的错误构造新数据
		* PDE 形式→ …：((nbim8p))同时解多个 PDE 的 NO-间接输入 PDE 形式
		* 区域形状→ ((n7n97v))到标准域的变换、((p4ce2r))SDF
		* 针对迭代格式，将迭代历史作为间接输入方式；{o1lh4p}
			* ((o1lg1g))针对 PDE 迭代求解器
			* 针对动力学，((naha1f))可这么输入的包括 多余的状态变量、动力学参数、动力学形式
		* 额外网络提供等价表示，((n3rn3e))表征
		* 另：通过元学习，$a$ 通过 loss 间接输入，属迭代形式表达的 NO
		* 另：也可只视为额外的 augmented 输入项，原有输入 $a$ 仍保留，见((q7aj9t))网络改写-输入增强
	* 破坏、污染，故意引入信息损失；{q8ag8a}
		* 方式—基于单元素取值
			* 加噪声 noise
				* 变种—同时缩减原输入的幅值，例如流匹配正向过程
				* 对象（任务输入样本）一般化见((n37g9c))训练-引入随机性，噪声可加在 网络激活值、loss、更新方向 等
			* 黑白化，多通道信息忽略方向、仅保留幅值
			* 去通道，多通道信息屏蔽部分通道
		* 方式—基于元素间（各位置）取值差异
			* 模糊化 blur，相当于压制高频信号，依赖于空间结构（距离度量）
				* 电场线外推((_q8ah09))，基于电动力学 Poisson flow，原文针对物理场形态的输入
			* 降分辨率、下采样 downsample
			* 图片旋转、镜像，恢复时要求翻/转回正确位置
		* 方式—破坏组合结构
			* 掩码 mask-out（空间随机），丢弃或屏蔽部分元素，依赖于((n3qn83))多元素结构；{q8a88n}
				* mask 表示—丢弃、删，不输入网络
					* 解读：子集采样代表全体
					* 目的功能：降复杂度（前传降本）
						* 例：((_o4tl74))FLIP 在 CLIP 中输入的图文模态均引入 mask
						* 只留单元素情形通常算 crop 而非 mask-out
				* mask 表示—保留元素，屏蔽原始内容信息，换为预设信息
					* 剩余信息形态：零、随机预设（全程用同一个）、可学、动态随机（每次使用重新生成）
						* eg. ((_n4sn70))VQ-GAN，未确认是 随机预设or动态随机；{n4sn74}
					* 屏蔽状态告知：通过新通道（二值分别表示 原信息、屏蔽后信息），不告知（要求网络根据特征取值自行推断）
				* mask 比例
				* mask 范围，元素分布
					* 全局随机：BERT、MAE
					* 连续多块：网络可能需做 inpainting
					* 连续块的补集：网络可能需做 outpainting
					* 概率据元素内容自适应调整，如图片 patch 内容简单（方差小）的增大 mask 概率
						* 训推一致，((_q8ah5c))推理换自适应 mask 不如保持与训练一致的均匀随机
			* 裁切 crop，依赖于空间结构（邻域存在），包括文本拆小段
				* 案例：图像（BEiT3 用到），针对物理场的见((n8ve45))
				* 变种—补集 crop，如图像添加随机遮盖方框（普通 crop 是只保留方框内部）
				* 目的功能：降复杂度（前传降本），如((q68m4l))图像-patch-单patch-作输入-为降本，案例如((n4sj7z))GAN判别器降计算量
			* 混洗 shuffle
				* 案例：NLP 词元重排（T3 实验设定之一）
				* 案例：图片 patchify 后不提供原位置标注（重建过程为拼图）
				* 案例：视频各帧时间重排((q8bf94))
		* 方式—变换到新空间后破坏，例如 频域、隐空间；{q8ba1m}
			* ((q4ni2m))隐空间/频域去噪生成
		* 用途—((n37n39))NN 前传-引入随机性
			* 次级用途—((n37g9c))训练-引入随机性，再次级((n4sk0m))噪声相当于正则化
			* 次级用途—((q8be8j))使 NN 输出构成 ensemble
				* 噪声类，((p89f8o))数据加噪声后前传，结果取平均
				* crop 类，((p8ni4g))物理场取多个时空块，前传结果 concat、由后续网络整合
				* mask 类，(('q8be9g))推理时多次随机 mask out 取 ensemble；{n8mn6i}
				* 正则化：`2022-01-28`(CSImeet2) 导师认为 MAE 做法不应只理解为无监督学习，它可视为加某种噪声（mask 视为特殊信息污染方案）引入的正则化；{n37m6z}
		* 用途—((n31n1z))数据增强-有损增强
			* mask 类，((_n37m82))有工作将 MAE 作为数据增强策略；{n37m8g}
		* 输出形式—预测修复重建结果；{n99a2u}
			* 用途—((n8mn5a))预训练-自监督；{o31f3k}
				* 功能—((_o31f0z))认为可使 feature((n3rn3e)) robust
				* mask 类 ((n8mn72))MAE
				* 去模糊、超分辨率((_o6mb1c))
				* shuffle 类，视频((_o73j9m))恢复各帧时间顺序，效果好于恢复空间排列；{q8bf94}
				* 弱化版本—预测破坏是否发生，((_q8bf63))针对视频各帧时间顺序 shuffle
				* 支撑—临时引入重建网络，其规模应小于（作为预训练对象的）编码器，如((_o6mb1i))MAE
			* 用途—((q8am5h))多步生成-状态更新-逐步修复
				* 修复幅度连续性
					* 连续修复、幅度可调（如去噪、去模糊），可自然引入多步修复迭代过程
					* 单次修复（如 unmask）通常需配合重破坏过程
				* 重破坏：每步修复后重新引入小幅破坏，使净修复幅度小于网络直接修复幅度；{q8ah60}
					* 基于 unmask（默认只能全量），((_q8ah4t))GRN re-mask 比例逐步降低；{q8ac0z}
						* 各步 re-mask 位置—集合单调收缩，成为((q85k5k))多步生成-信息补全-空间域
						* 各步 re-mask 位置—每次独立采样，((_q8b744))GRN 元素变化存在“擦除”情形
					* 基于全量去噪，((q4oc1s))IR 采样（尽管其采样分布不准，因全量去噪不是后验采样，只是后验期望）
					* 基于逐步去噪，((q8ah6i))生成所用 SDE 中 drift 对应小幅去噪、noise 对应小幅加噪
					* 理论—生成分布收敛：((_q8an77))交替破坏/重建 采样生成过程 有条件可保证收敛到数据分布
			* 用途—denoise，去噪模型也用于((n99f58))动力学-提高长时间预测准确性
			* 预测对象—denoise，输出去噪后干净结果 or 提取出的噪声 均可
				* 性能比较，((_n5bg24))Shap-E 提到前者（预测去噪结果）的实际表现更好
			* 预测对象—unmask 有多种可能
				* 输出范围—全部被 mask 元素（默认情形）
				* 输出范围—重建任务原始输入（对未 mask 部分只需重建网络输入），如 MAE
				* 输出范围—恢复单个已 mask 元素，其位置可与 mask 范围联合设计
					* 特例：序列 NTP((n8mn8t))，未 mask 元素连续排列，只要求预测下一个
					* 目的功能—((q85k5k))多步生成-信息补全-逐元素生成，相当于每次去除一个 token 的 mask
				* 其他同时适用于其他输入破坏方式的内容见下方专门讨论
			* 相关：输入破坏后样本而输出干净样本，涉及((n9999m))网络(输入,输出)≠ 数据(样本,标签)
* 以下对任务输入 逐形态讨论，可能内容包括：改写为网络输入方案，直接处理该输入的网络结构，绑定输出形态时架构设计方案，多候选架构选型（实验中哪种效果更好），架构能力
* 有限维向量：
	* hypernet（若 $u$ 参数化表达，如 `NIF-2204.03216`）
	* 解码式 CNN（若 $u$ grid 离散）`DL-ROM-2001.04001`, `DL-ROM-2103.06183`
	* 输入向量二部分性质差异大时代码注意：代码实现的问题，若 MLP 输入向量形如 $(x_1,x_2)$，$x_2$ 重要性低但维数高，则直接输入 MLP 训练可能出问题；{n32f34}
		* 来源：2022 MAD-CAMC Helmholtz 实验，PyTorch 线性层默认初始化方案中，输入分量多则线性变换初始权重小，导致分配给重要的那部分向量的权重不够大
		* 当时的解决方案：代码中网络改写为等价的 $\sigma(W_1x_1+W_2x_2+b)$，二线性层分开初始化
		* 其他可能方案：手动初始化权重矩阵；对 $x_1$ 人为乘上系数，系数根据向量总维数手算
		* 不排除不是所有时候都会有问题；当时是要拟合高频函数，这才要求初始化本身已高频
		* 相关框架：((n37g4c))NN 初始化
* 函数、场（多数 NO 属于这种情况）{nbig7t}
	* ((_n7fc08))$a$ 输入时可改写为多通道形式 $(x,a)$
		* ((_n93k49))二分量输入时处理所用的网络可独立，如 $f(x)+g(a(x))$
		* concat x 是为体现空间位置信息；据说((_n9gf1z))GalerkinTf 额外引入了隐嵌入来体现
		* OFormer 使用位置编码 RoPE 体现空间位置信息((_n9gf5r))，包括 2D 版本；{n9gf5t}
			* 相关：((n98h2z))Transformer-位置编码-非序列数据，((n3t97n))INR-位置编码 用于引入高频信息
	* 与解 $u$ 同一定义域的函数（可以在不同函数空间）
		* 下方许多 mesh-topology 可变的工作是这一情形（网格泛化更差的方法当然也行）
		* 对函数空间不同的情形，目前没见过针对性的设计方案
		* 一个特例是偏微分算子（PDO）
			* `Neural-ePDO` 考察了依赖于空间位置的 PDO、并给出其满足定义域仿射变换（的子群）下等变性的条件；离散化方案包括 FD（成为卷积）和 GA；{n23g87}
				* 相关框架：((n23h00))算子等变性
			* ((n23g8n))学出一个微分算子，并证明可逼近任意卷积算子；基于 INR 参化而非离散
	* 与 $u$ 不同定义域的函数；{n4fe6y}
		* eg. `[Ying]-EIT-DL` EIT 里预测 DtN map 的 kernel¹，$a(x)\mapsto k(r,s)$, $r,s\in\partial D$，以及反向的算子，NN 设计思路为先化归为两侧函数共用一个维度，在另一个维度用插值离散化后，化为同一定义域情形；{n86k0e}
			* ¹该映射形式见一般的((n7un1l))反问题-据边界观测算子推断内部系数场，包括((n86k17))基于该形式设计的多种 NO 架构
		* ((n86j9u))沈佐伟 IAE-Net 基于积分算子构造的 NO，$a,u$ 定义域不同不影响积分 kernel 定义、计算，故允许二者不同定义域
		* ((n7n96y))NO-基于特定变换构造算子 都可以处理，如 `funcPCA-2005.03180` 和 DeepONet
		* `FNOvsDpONet-2111.05512` dFNO+：若输出维数高 $D_u=D_a\times D_0$，输入升维或 RNN 压缩输出维度；输入为输出子区域 $D_a\subset D_u$ 可延拓（零/近邻）
		* 若定义域为 $u$ 的子集（如 IC,BC），((_n4fe42))有做法仅将未知取值的部分 mask 掉，不确定性能
		* 若 $a$ 为 IC、$u$ 为全时空解、有激波等，((_n4fe63))若纯有监督训，预测解在长时间表现出平均化现象，而额外引入 PDE loss 可缓解；{n4fe6l}
			* 相关：((n4fe4x))用于PDE的NO-额外的PDEloss，((n4fe72))动力学黑盒预测-对解不区分时空坐标
		* a 在边界 u 在内部，((_q2la97))先延拓 a，方案包括：零延拓、调和延拓、可学延拓
	* 定义域形状复杂、不规则情形 处理；{n7na1l}
		* 离散仍尽量用均匀 grid，((n4tc04))其效果通常好于散点离散 + GNN 等方案
			* 否则可用((n7ne27))NO-网格泛化-离散为可变散点，mesh 拓扑可变
		* 变换到规则的 reference domain（限于拓扑简单情形），利用((q8169o))坐标变换；{n7ne5l}
			* 使用场景、目的，如处理特殊区域
				* 非矩形区域、想用均匀 grid 离散，((q8173w))域转矩形-域变换
				* 无穷区域：((p4fa30))变换到有限区域
				* 区域可变，该变换本身也不固定；{q81a31}
					* 区域为自变量，作为 a 的一部分需输入 NO：((n7n97v))参考域变换作为区域形状的替身
					* 比较不同区域上定义的场((n7n92v))，刻画 width 等
					* 区域连续变化，域变换也连续变
						* 场景—((n1sb7w))考察区域趋于极限时的行为
						* 域变换刻画—可改用间接的((n1sb7o))变化速度场
					* 时变区域：区域变化依据为时间，域变换也随时间变
						* 场数值离散—参考域用固定散点离散，则在原域上看为移动散点，成为((n2gf8i))粒子方法
							* 粒子移动一定程度上受限，原来近邻的未来难以远离，新粒子难以穿越二相邻粒子连线等；{q81800}
						* eg.((_q4rf3b))材料形变场 $\psi(-,t):\Omega^{ref}\to\Omega_t$ 用粒子（点云）方法离散；{n2gf8b}
				* 简化动力学：固定域上动力学复杂，但配合上动态域变换后简化，见((q4rb00))连续介质物理-Lagrange描述（其域变换据物理意义直接设定，非学出）
					* 域变换目的为 简化场的动力学，而非简化区域；认为区域不复杂，或不视为主要复杂度来源
				* 简化解流形：固定域上解流形 width 大，((_q81b46))人工设计区域变换 以加速 Kolmogorov n-width 衰减
			* 变换学出方式
				* 解方程：((_q4re3s))Laplace 方程，Dirichlet BC 保证参考域与目标域边界匹配
				* PINN：((_n7ne61))PhyGeoNet 在规则区域 CNN，变换似乎用 physics-informed 方式给出
				* 人工预设or学出：((_n7n90m))Geo-FNO 变换到规则区域后跑 FNO，变换手动给定、学出均可
				* （学出）所用 NN ansatz((_n7nf0j))可只学位移，作为 coordMLP 可带((n3t97n))位置编码；{n7nf0p}
			* 相关：同手段的目的（复杂区域规则化）一般化见 ((q8169o))场-坐标变换
				* 同手段其他目的 ((q4rc2m))连续介质物理-区域描述-参考域，((n7n92v))变区域 PDE-比较不同区域解-参考域
		* 区域分解，成为多块（有均匀网格的）区域的组合((n7n994))
		* 延拓，选取规则的 master domain；{n7n92y}
			* 延拓方式
				* 传统谱方法有 Fourier continuation 技巧((n7n96e))
				* 效果比较：((_n7na5w))gFNO+ 提到近邻延拓效果好于零延拓
				* 无视延拓部分输出：((_n7n91a))Geo-FNO 直接延拓，计算 loss 时无视延拓后部分即可
			* 网络引入自适应区域分解，自动区分原域与延拓域((_p2qf2m))；架构基于 FNO；{p8ai7w}
				* 属于((p8ai85))集合划分后处理-分类并用不同网络，这里是格点区域分解、用不同变换
			* 延拓相关：((q4rc2m))连续介质物理-区域描述-延拓到超域，((n7n92v))变区域 PDE-延拓以比较不同区域解，((p4fa30))无界区域场-截断（作为延拓的反面）
		* 这里针对固定定义域；可变定义域情形见((n7n97v))
		* 这里针对 不规则区域化规则区域，另见((n4tc04))离散方案-不规则散点转规则 grid
		* 相关：((q4rc2m))连续介质物理-区域描述-复杂区域，((n4tc09))NN架构设计-体现人对任务的理解
	* 包含多个函数 见((p8jh4r))
	* 空间维数可变，多个不同维数同时支持；{q1nb9f}
		* 作用—((q3kj0o))高维 NO 用低维解作辅助数据，协同方式包括 微调or多任务学习
		* 架构—逐轴处理（FFNO、AViT）((o7tb6f))
		* 架构—预设最大维数，架构离散化((o8fe66))支持可变分辨率（且各轴比例可变），多出来的维数按分辨率 1 处理（类似 OpenFOAM 处理 2D 流场）
			* 基于 ViT 完整注意力，如((_q1na6z))Walrus
			* 针对新维数引入额外网络层，如((_q3kg33))SWin 中加入 z-轴向注意力；{q3ki9c}
		* 训练—低维预训练高维微调 ((_q1nc1f))不如预训练时直接各维联训；{q3kk8u}
		* 训练—若数据并行，不同卡负责不同维数数据，((_q1nc16))低维数据用大 batch-size 使负载均衡
		* 相关：((p8jh4r))通用 NO-分量数泛化（因变量数可变，这里维数泛化是自变量数可变）
	* 相关：((nbig7l))系统识别，任务输入也为场、输出不同（PDE 表达式而非场）
* 曲面、((q8fa35))区域形状 等；{n3gd80}
	* 一般意义上曲面、形状的可计算表征（未必作为输入）{n8re3h}
		* 相关：((n32f1h))流形学习（高维数据空间为主），((n8re4f))表征方式多样性
		* 点云；作输入时的 NO 架构设计见((n32f77))
			* 用于一般场景渲染时((p9pk71))可能出现孔洞，这方面不如 3DGS
		* 3DGS((p9pk6y))（针对一般物理场景）
		* mesh、单纯复形；{n8re36}
		* voxel grid 体素
		* SDF、level-set；{n8re2y}
			* 多相流问题的传统算法用到((o1ol07))
	* 若有参数化表达，则可按有限维向量处理
		* 翼形表示为((_p8bf6h))在多个固定位置 的厚度、弧度
	* 区域转化为函数、场；{p4ce2r}
		* 表示为示性函数，如((_o6ch8b))Unisolver
			* 均匀网格离散得体素网格，((_papa2b))作为网络输入
		* 表示为 ((n8re2y))SDF/level-set
			* ((_p1aa2y))与点云表征组合使用，曲面点云用 GKN 转 3D 均匀网格隐向量后，再 concat SDF 在该 3D 均匀网格上的取值
			* 若输出用 INR 表示，((p8bf7y))其输入坐标可用 SDF、体积法线 等特征增广
		* 表示为参考域到它的变换映射((n7n97v))，要求区域拓扑固定
		* 相关：该手段的对象（区域）一般化见((n4sj3v))仿TRIZ原理-替身
	* 到标准域的变换((q81a31)) 作替身，对区域要求拓扑固定；{n7n97v}
		* 退化情形：（上方讨论的）有限维参数化表达
		* 下游影响：若区域为 $u$ 定义域，则顺带给出了 $u$ 在参考域上的表征方式
			* 例：机翼形状自身表征只需 $S^1\to\R^2$，但若要给出 $u$ 定义域，则需延拓至环状区域 $D(r,R)\to\R^2$（允许 $R=\infty$）
			* 旧表述备份
				* （不作定义域）若 $a$ 为区域内部形状、拓扑固定，可给出参考域到它的映射，是转化为函数的另一方案
				* （二者区别）与下面描述定义域情形不同：这里假设 $u$ 定义域不变，$a$ 转化为映射与 $u$ 的表征方式无关，而下方的转化则顺便给出了 $u$ 表征方式
				* （作定义域）区域变换到标准域：若 $a$ 描述 $u$ 定义域、拓扑不变，可将可变定义域 变换到标准域，考虑标准域上函数到函数的 NO
		* 所属框架：间接输入((o1le7k))，将任务的等价表示（包含所需的所有信息）作网络输入
		* eg. 2411.18014，需额外预测区域形变的((q5tg3e))FBNO
		* 单位圆盘用作标准域，((_p8d91w))MMGP（圆盘上有标准网格，将形变后 FEM 网格插值过来）
		* 该变换作为 NO 输入，它是标准域上的（向量值）函数
			* MMGP 参考域到源域的变换 作为输入场新增分量((_p8d92p))；实现时只需先对原输入场补充坐标分量，再变换到标准域
		* 变换信息增强：可再引入 Jacobian 作额外 feature；{q7kg5p}
			* 相关框架：对象（区域变换）一般化为物理场，见((q7kf9z))输入增强-物理场-数值微分
		* 相关：拓扑复杂时可按 ((n7n994))复杂形状区域 的办法处理，标准域换为组合域
			* 例如机翼用 C 型网格变为长方形域（从而适用 CNN，精度高于 GNN），NO 在该长方形上定义，输入为逆变换（作为函数，映到原问题物理空间）$x,y$ 及其 Jacobian $dx,dy$，输出为该长方形域上看到的流场 `2022-06-17`(CSImeet2)
			* 不排除考虑不同变换方式（如 C 型网格切开的线）作为数据增广 `2022-05-27`(CSImeet2)
		* 相关：((n7ne5l))定义域复杂但固定，其上的场（用固定变换）转化到参考域
	* $a=(\Omega,f|\Omega)$ 同时包括可变区域及其上定义的函数，上面做法稍修改也能用
		* 例如若使用参考区域，输入的函数变换为在参考区域上定义的函数即可
		* 定义的函数可延拓、设计 $a=(D_a,f_a)\mapsto u$ 映射结构使其只依赖于 $f_a|D_a$，从而 SDF 等方法可用
	* 区域、曲面表达为点云，架构可用 PointNet++，DeepSets，GNN 等；{n32f77}
		* 相关：((n3qn83))集合作为 NN 输入，((n5be19))点云生成，((n8qk4m))ensemble
		* 能处理 $a$ 包含多个异质性曲面的情形（如电路仿真中不同电子元件表面），曲面性质可作为点特征一起输入网络
		* NO 学习，按照几何深度学习的设定，假设表达的为若干几何形状（例如电子元件的空间排布，而不像 PDE 连续系数场那样的情形），只输入表面的位置
			* 类似 PointNet++（待看；来源 `2021-06-04`(CSImeet)）
		* 可能性：DeepSets((n93k53)) 类似的做法，可以 hierarchical 方式
			* 先每个元件的点组合、再元件之间组合 $f(\sum_ig(\sum_jh(x_{ij})))$
			* 也可以进一步使用 DenseNet 类似做法 $f(\sum_{ij}p(x_{ij}),\sum_ig(\sum_jh(x_{ij})))$
		* PointNet 保证置换不变性为 ((ncnb6e))sum 改用 max；{ncnb5q}
			* 逼近能力定理，不像 sum 那样能严格等于，且个人认为附录中给的证明有漏洞
			* 相较 sum 也许对点云疏密不均的情形更稳定
			* 刻画旋转不变性，对点云坐标预处理使用旋转变换 $A$，$A$ 由 T-net 网络输入点云来给出，有额外惩罚项来希望它正交
			* 用于构造 NO，((_o6mk80))其输入为定义域形状
		* PointNet++ 考虑局部 feature，方式粗看为先最远点采样迭代得到稀疏点表达原点云，然后球内特征聚合；{n98h0m}
			* 处理多尺度有 multi-scale grouping 和 multi-resolution grouping 两种方式
			* PointMAE 似乎是基于这种架构的预训练方式（待确认），最远点采样所得点为球心、每个汇总 kNN 信息，称为 point patch，并进行 mask 操作；{n98h1e}
				* 相关：((n5bf17))patch，((n8mn72))mask 预训练
			* 最远点采样所得点进行((n98h2z))位置编码，PointMAE 似乎涉及；{n98h47}
		* 点云人为连边((n3qn8p)) 后用 GNN，用法类似((n79m1x))区域离散为可变散点情形
			* ((_p1aa0e))GKN 将表示曲面形状的点云 与 3D均匀网格隐向量 互相转换，NO 主干由均匀网格上 FNO 承担；{p1aa1o}
				* 该做法属于((n4tc04))非欧氏域数据先变换到规则域
				* 变换到完整 3D 区域后，原文还进一步整合原曲面的 SDF 信息（在相同网格上的取值）
		* 用 Transformer，如((n5bg3k))Shap-E
		* 转均匀网格((p8fb1n))
		* 点云的预处理
			* 减少点数，如((_n5bg3u))用 PointConv 把 16k 减到 1k
			* ((_n7bh2g))最远点采样，使大规模、非均匀点云变为小规模、均匀点云
				* ((n98h0m))最远点采样在 PointNet++ 中已用到
	* geometric deep learning 里可能有其他处理方案，如输入为点云/graph 的映射，包括 GNN
	* 可能性：隐式编码方案（编码通过解优化问题实现），并采用有限维中介；从而有两个参化的 INR，一个描述曲面形状、用于提取隐向量 $z$，另一个对 $z$ 前传得目标场 $u$
		* 或可考虑合并两个 INR，输出包括 SDF + $u$；训练时二分量均参与 loss 计算，推断时（根据输入的曲面 $a$）只用 SDF loss 优化找到隐向量 $z$，希望此时网络的 $u$ 输出分量能表示相应的 NO 输出
		* 另见((p4ce1k))AD 间接编码，INR 拟合目标场得隐向量
	* 输入增强，((_papa24))有限维几何的统计特征，如质心、边界框维度、PCA轴和特征值；{q8af8j}
		* 对象一般化见((q8af8l))输入增强
	* NO 优势：如果区域有奇异形态（如机翼尖角），不排除 NO 建立的映射能比传统 PDE 求解更准
	* 架构选择：((_papa2i))有参化情形一般 DeepONet 类好，形状复杂或场有空间突变（样本内）则散点类，尺寸变化范围大（样本间）则网格类
* {时序输入}（长度不定）
	* 输出可为数（序列分类、回归）或另一序列（序列转换；可再区分等长序列和不等长序列）
		* 输出为另一等长序列时，可额外要求单向依赖性，$u(t)$ 仅依赖于历史输入 $a|[0,t]$
		* 输出为序列时，生成方式或可结合 ((o5sm3k))singleDyn 中的做法
	* 可能场景：预测给定控制序列下的动力系统演化；带记忆动力系统预测下一步状态；其他有长记忆依赖的任务
	* 可能方法：传统 RNN，Transformer，1D CNN 等可考虑
		* Transformer 可认为是转化为集合结构，并通过引入位置编码恢复序列顺序信息
	* 可用 ansatz `HiPPO-LSSL-S4`“SSM”（状态空间模型）引入先验机理，认为对应隐藏的完整状态空间上 ODE
		* 注：自治有记忆动力系统、序列预测任务可用的 ansatz 则是 ((n35e9h))Mori-Zwanzig
		* 导出的算法（长记忆依赖的序列仍有效）`HiPPO-LSSL-S4`
	* 序列（一般意义）的多种可能含义；{n8v96k}
		* ((n8ve1a))permutation 生成，((n8ve1f))数学表达式（符号积分等算 seq2seq，((nbig72))PDE 形式作为网络输入）
		* ((n8vg6a))NN 生成高维输出-输出解读为序列
		* 科学计算动力学，自然语言，((nc6k9m))CV 任务使用“图像序列”统一表达
		* 似乎动力学预测（确定性）任务与序列生成任务有不同，前者关注整体 L2 误差，后者关注分布是否匹配；故前者涉及迭代多步后求 loss，而 Transformer decoder 只用逐词预测 loss
		* 相关：((nas998))非科研-逻辑对象的多种等价表达形式-序列、树、图
* 集合输入，尤其是集合大小不定时；{n3qn83}
	* 集合的语义，元素来源
		* 原生集合—数据集（元素为样本 或 样本-标签对）
			* 用例包括((n4rb6l))超网络-数据集作输入-网络架构，部分架构设计思路可参考
		* 原生集合—((n2gf92))动力学-多体系统ODE
		* 来自离散：((n32f77))曲面/形状-点云 $\{x_i\}$，((n3qg7s))场-离散-非固定散点 $\{(x_i,f(x_i))\}$
		* 来自表征—场-基底表达
		* 来自表征—3DGS 表示 3D 物体
		* 来自表征—((n4rm8d))tokenize，输入打散为集合，包括图像 patch、语言分词
		* 来自((n8qk4m))ensemble
		* （旧）处理网络特性（看到这些元素一般都对应集合）：Transformer token，INR 输入点
	* 相关：((n8re18))集合作为网络输出
	* 形式转换，元素关系重解释，集合转化为 时序、graph 等
		* 视为时序（不再保证置换对称性），可用 RNN 处理
		* 视为全连接图，可按 GNN 处理（进一步可设法简化图结构）
		* 人为连边（如 kNN）得图，元素为坐标点情形常用；{n3qn8p}
			* 包括((n79m1x))NO-散点离散-用 GNN
			* 同样可参考((n32f77))点云输入、((n3qg7s))输入场离散为非固定散点 中相关工作的做法，有的也涉及连边
			* 编程实现，kNN 计算((n3qn7q))有相应 Python 软件包，复杂度可降到 $O(n\log n)$
			* 相关：((n79m24))graph 类型与来源汇总
	* 输出形式—单对象整合所有元素信息，网络需满足置换不变性
		* 常用架构—((n3qg7s))DeepSet
		* 常用架构—Transformer 在输出层((n93k53))规约
		* 常用架构—((n4t95z))Transformer 输入补 init token 以汇总信息
			* （旧表述）输入中额外引入一个空位置，其相应位置的输出向量视为对这整个输入集合的编码结果
	* 预处理—先对各元素提取((n3rn3e))表征
	* 预处理—((q8a88n))mask-out，丢弃或屏蔽部分元素
	* 中间层—网络满足置换等变性((q6o70c))，用于更新元素表示
	* 中间层—规约，整合各元素信息，mean/max 等；{n93k53}
		* 用途—((n3qg7s))场输入-可变散点-DeepSet
		* 用途—设计架构满足((q6o70c))置换等变性
		* 求和、平均，如 DeepSet（原版用的求和）
		* 加权求和/平均，如((_n93k40))VIDON 用加权平均；{o31a0d}
			* 收益—如 PDEformer1 func-enc 消融实验中提升明显
			* 术语—加权操作有时称为“gating”
			* 权重取值要求：通常需非负；若还归一化则算“加权平均”
				* 组件—归一化权重，常 softmax 给出；{q6o732}
					* 训练当心：输出权重高度集中于某元素时，反传计算可能出现((q6kh08))softmax collapse，源于 fp32 尾数不足
					* 稀疏化变种：Sparsemax（有不可微风险）；((_q84e7k))α=1.5 Entmax 效果更好
						* loss 促进稀疏化：((_q84k32))惩罚输出熵
					* 更多变种可参考 [苏剑林科学空间](https://kexue.fm/archives/10145)
				* 组件—次归一化权重（求和 ≤ 1）{q6nm78}
					* 如 ((n7pe44))softmax1（分母 +1）
					* 引入冗余元素，如((_q6nl1m))分类模型输出补充额外类
						* 对注意力场景，引入 global token 是否有类似的效果？
			* 对象，区分被加权集合元素类型
				* token，见 ((n7pe3p))注意力-softmax（归一化）
				* 空间散点（NO 输入），见((n7ne27))场输入-可变散点
				* 激活值神经元，见((o3198n))GLU 激活函数（sigmoid 未归一化）
				* 专家网络，见((n2pj8d))MoE-软路由
				* 离散概率分布元素（如 分类、词表、VQ），见((n3bg9o))categorical 分布（归一化）
			* “加权”相关：此框架里权重由 NN 给出，其他情形包括((n9hg32))loss 加权
		* max，如((ncnb5q))PointNet（有逼近能力理论分析）、((ncnb69))nf2vec；{ncnb6e}
	* 中间层—集合元素（不限于点集）划分后处理：{p8ag76}
		* 网络处理方式 如何受划分影响：打包（聚合），分组，分类
			* 打包：合并为大元素，再考虑交互（如对 meta-token 算注意力）
				* 目的
					* 降信息混合复杂度，信息混合有多类方式
						* 注意力类：((palf57))降注意力复杂度-token打包
						* FNO 类：打包结果视为抽象空间网格点，用 FNO1D，如((_p8ge26))Opt_RNO；{p8ge27}
					* 转均匀网格((p8fb1n))，可用均匀网格处理的 ansatz；{p8ge31}
						* 元素类型：低维坐标点
						* 划分边界：通常非全局归属
						* 划分类数：固定的较多，但理论上也可用可变分辨率版本
							* 非常规打包：转换后的均匀格点数 未必少于 原元素数
						* 打包结果：持续，编解码可用不同网格
						* 此处仅考虑实际物理空间的均匀网格，另见((p8ge27))抽象空间均匀网格
							* 抽象空间情形差异：元素未必低维，划分常全局归属，打包结果常临时（因信息损失大）
					* 引入新标准做信息混合 eg. NO 中引入新层，((_ocne9w))AMG 的 physics graph，原始散点 graph 信息汇总到该图，节点含义改变（原图：空间网格点，新图：物理变量）
					* 提高信息交互效率，避免模型陷入细节、被干扰((_p8aj1q))
				* 打包结果：临时（保留原元素嵌入），持续（替代原元素），暂时（原元素嵌入搁置、延后使用）
					* 临时：仅算元素间交互时临时打包，用交互结果更新原元素表征
						* eg. Transolver（散点 NO）
					* 持续：打包作为编码操作，对整个打包结果使用处理网络，过完后再设法解码
						* 已清除原元素信息，解码元素含义可与原来不同（如散点 NO 可在另一组散点上预测）
						* eg. MMET（散点 NO），((ocnf1k))LSM（patched NO）
						* 散点 NO 转均匀网格((p8ge31))通常持续
					* 暂时：类似 U-Net 下采样，打包结果作为整体过一段后续网络，后续结合打包前 skip connection 解码得原结果
						* 如((p8jh36))ViT 特征图下采样
						* 交互后的打包结果汇总到原元素方式：更新原元素（类似临时打包，输入处一次性引入），调制原元素处理网络（中间层持续引入），((_pbfc1j))二者可同时使用
			* 分组：交互范围局限于组内；{p8aj0t}
				* 如((o2cg1y))降注意力复杂度-范围限制，包括 grid-attn、axial-attn 等
				* ((o8h98r))NO-空间非局部信息交互-非全局情形
				* eg. （样本）((n9174b))batch-norm
				* 训练—反传仅限部分组：((_pbff3o))为降显存需求，仅若干组的交互参与反传，其余组前传时网络中间层激活值无需保留
			* 分类：独立处理，各类元素用不同的处理网络，MoE，包括 Transformer；{p8ai85}
				* eg. ((p8ai7w))FNO 自适应区域分解 以区分原域与延拓域
				* eg. Transformer MoE((p8c969))提高模型效率、容量
				* 相关：此处 MoE 用于多元素对象；若用于数据集不同样本or任务池不同任务，见((n2pj82))
			* 类比—线性变换：假设所有元素排成一列、形成向量，通信为线性变换 W
				* 分组：分块对角矩阵 W = diag(W₁,..,Wₙ)
				* 打包：低秩矩阵 W = AB；若使用硬边界划分，则 A、B 还都有额外分块结构
				* 分类：逐元素变换不涉及通信，W 为对角矩阵 W = diag(a,..,a,b,..,b,c,..)
		* 区域分解 作为特例：坐标点为元素，划分结果为区域；{p2qf2a}
			* 解读同样适用于((o2cg00))散点 NO 类 Transformer 架构
			* 以下维度类比((n2pe8f))INR区域分解，同样分 区域个数固定vs可变，软vs硬，预设vs可学
		* 划分类数，输入数量改变情形：固定总数 vs 固定压缩率（数量正比于原元素数）vs 其他
			* 固定总数 eg. Transolver、Opt_RNO（散点 NO 打包），((ocnf1k))LSM（patched NO 打包）
				* Transolver((_o2cg04))引入 meta-token，称为物理注意力；{o2cg0f}
					* 一般框架((o2cg00))逐点NO-基于Transformer
				* 原 token 与 meta-token 相互转换：
					* 逐元素线性变换得 logits，沿 meta-token 维度 softmax 得权重：Transolver
						* 反向变换，原版 Transolver 正向变换权重沿原 token 维归一化，((q5da50))logits 改沿 token 维度 softmax
					* 交叉注意力：((ocnf1k))LSM，LRSA-2604.03582
			* 固定压缩率 eg. MMET（散点 NO 打包）
			* 可变压缩率，利用序列顺序结构，((pam004))文本久远部分内容提压缩率
		* 划分边界：硬（属于单类）、软（属于多类）、全（全局归属，仅权重大小差异）
			* 全 eg. Transolver、Opt_RNO（散点 NO 打包）
			* 硬，汇总方式包括 等变网络、简单 concat
				* 简单 concat eg. ((_p8aj2m))MMET（散点 NO 打包）
				* 相关：((p4dc1z))GB-INR 离散部分插值方式，也分 加权和、简单 concat 两种
		* 划分方式：预设 vs 可学
			* 学出 eg. Transolver、Opt_RNO（散点 NO 打包）
			* 预设 eg. MMET（散点 NO 打包）((_p8aj27))对散点连 Hilbert 曲线、按曲线相邻分组
				* ((_p8aj1s))认为预设方案更容易对不同输入条件泛化
				* 利用均匀网格结构，((o2cg1y))1D 网格注意力分组，((p8jh36))ViT 特征图下采样（邻域打包）
					* ((o8h98r))NO-空间非局部信息交互-非全局情形
				* 特殊技巧，模态转换，((pam004))文本改按图像模态 tokenize
			* 预设 eg. SpiderSolver((_q14g1z))蛛网划分 以突出计算域形状影响，参考区域边界点谱聚类、形变得一般区域、按距离向内部扩散得蛛网区域划分
		* 混合功能：划分目的为降低信息交互复杂度情形，需确保划分后信息仍可全局混合
			* 多标准分组（硬划分）{pbfa2u}
				* 各层不同分组，如 SWin 窗口在部分层有滑动，MaxViT 窗口+网格 分组，AViT X+Y 分组；见((p4b97v))注意力-不同层不同分组
				* 注意力各头不同分组，见((nbrm0z))分组注意力
			* 分组+临时/暂时打包（用相同硬划分）
				* P3D((_pbf897))编解码仅算窗口内注意力，隐空间层各窗口抽象出代表 token 算注意力（暂时打包）；{pbfa5h}
				* 打包阶段额外为各组生成异质信息，后续分组交互时使用：((_pbff7h))打包阶段可学初始化，结果用于调制后续分组交互；含义相当于 group-wise [CLS] token
			* 持续打包
			* 临时软打包，如 Transolver
			* 关键类型内完整交互，((_q14g2s))SpiderSolver 全局临时软打包注意力 的基础上，补充 重要点（边界附近）的完整注意力
		* 相关：同样的信息交互目的下，若手段为不划分全量处理，可见((q6o70c))置换等变性架构
* 方程形式作为网络输入（在 NO 中通常需与数值模态同时输入），PDE 基础模型；{nbig72}
	* 相关：纯符号输入的任务((n8v95j))ML 用于符号计算；((n8v96k))序列数据的可能含义，((nbig90))ML 与符号表达式，((nasa1c))非科研-逻辑对象等价表达形式-序列、树、图-数学符号表达式
	* 输入—符号—显式—计算图
	* 输入—符号—显式—文本
		* 按工作汇总
			* ICON-LM((nbih07)) 问题描述为包含 LaTeX 公式的自然语言，自然语言辅助描述其后给出的各数值数据的含义
				* 架构：统一的单向注意力 Transformer，序列依次为 问题的语言描述、数值数据、待求解的量，((nbih3x))单向注意力 mask 对不同输入有一定区别
			* 北航的工作(('ncqn3h))，Transformer encoder 输入 PDE 形式、各分量初值，正问题再输入时间推进长度、输出各分量演化预测值，反问题可恢复被 mask 掉的 PDE 系数
			* PROSE((nbih0h)) 输入为 ODE 前几个时间步数据 + 方程符号表达式（可不完整/为空）
				* 架构：二模态先用独立 Transformer encoder 分别编码，之后 concat 输入统一 Transformer encoder 进行信息混合
				* 后续工作((_oa8e9c))PROSE-PDE 1D PDE，推理 OoD 实验包括 系数、IC 分布（正弦波到 GRF）、PDE 形式（已知项的重新组合）
				* 后续工作((_o9ba2q))LeMON-PROSE 用于 1D PDE，针对新方程微调用到 LoRA
				* 2D 版本((_oam97n))PROSE-FD 网络输入输出由单点改为 patch
				* 陆路跟进工作((_q14j3e))PI-MFM 用 PINN loss 训
			* `PITT-2305.08757`，似可理解为超网络接受方程输入、输出主网络（NO）的 modulation
			* CLIP 文本、数值模态匹配((_o39j0h))，用于提供模型训练 RL reward；{o39j0p}
				* 原文引入该 fine-tune 步骤后 nRMSE 至少减半
				* 此 loss 属于((p98k54))算法输出好坏评估-用辅助网络-衡量输入输出匹配度
				* 相关：((n3bg3i))PDE loss
			* Unisolver((_o6ch5x))PDE LaTeX 输入 LLaMA3 生成主网络 modulation（adaLN）
				* 系数假定全集、输入 MLP
				* 主网络基于 ViT，区域形状、系数场 PatchEmb 后生成主网络各 patch 的 adaLN
			* UPS((p15a2r))
			* NO 利用辅助性自然语言描述信息((_oane8t))，通过 LLaMA3.1 编码后交叉注意力调制已有 NO；{obgf3l}
		* 文本信息的形式
			* 纯 LaTeX：Unisolver，PITT
				* ((_p2ka1y))额外使用数值编码器将 IC 信息编码为一个 token 作为网络输入
			* 重整为前缀表达式（Polish notation）：PROSE
			* LaTeX + 自然语言：ICON-LM((nbih07))
			* 纯自然语言：((obgf3l))，Text2PDE((_obgf3o))
			* 方程名称 + 标量系数取值：((_p15a2c))UPS；{p15a2r}
			* 方程名称 + 输入输出场分布统计信息：((_p8hb06))MOFS
			* 简化为有限类型，只输入类标签：((_p85f16))PDE-Transformer
		* 文本信息在网络中引入的方式
			* 长序列上下文：ICON-LM((nbih07))，((_p15a30))UPS
			* 独立编码器、混合编码器、独立解码器：PROSE
			* 主网络 modulation：PITT，Unisolver，PDE-Transformer（用 AdaLN-Zero）
			* 主网络交叉注意力接收文本信息：((_oanf11))
		* 相关，PDE 文本表达式作为网络输入，但不用于构造 NO
			* 对 PDE 形式进行数据增强((_o39j09))，方程改写、形式变换、换符号等；{o39j1k}
				* 且提到原始形式、改写形式以各 50% 概率选取，引入改写形式可增强泛化能力
				* 相关：((n41f93))PDE 形式等价改写（以方便求解等），((n31n1z))数据增强
			* 文本编码器：((_o39j29))albert-math 模型，已在大量 LaTeX 数据上预训练
	* 输入—符号—显式—人工选块，据输入方程组合多个已有网络模块；需预设微分算符全集，独立学处理块
		* ((_q1qa6y))CompNO 学基础模块库，各块为参化 FNO；CompNO2D-2605.11691
		* 组合方式—含时方程 ((n3sf6k))算子分裂，各算符组合只需各块网络输出直接求和；{q84g1s}
			* ((_q3m871))OptSplit 学各项 NeuralODE 形式的基础模块库
			* ((_q42b25))LegONet 时间积分的算子分裂；各 BC 类型学独立算子块，非齐次先变换为齐次
			* ((_q7am35))HyCOP 算子分裂方式可学，部分基础模块可传统格式而非 NN
			* ((_q84g2l))Origo 非线性算子预设候选库全集
		* ((_q2mf5x))多方程求 POD 降阶形式，多项式插值得新方程降阶形式，插值代码 LLM 在线生成
		* SCNO-2604.11625
		* 组合系数—不据方程形式手工设定，而是据定解信息网络自动生成；{q84g3q}
			* 属于 同手段（组合使用分别对应多方程项的网络块）用于新目的（仍是 PDE 基模，但不再人工选块）
			* 系数生成网络：((q84g3d))MoE分类路由-预设专家功能
			* 网络输入—符号：同一般 PDE 基模，如 隐式—历史轨迹 的((q84i2a))Origo
	* 输入—符号—隐式，跨方程通用类，方程符号形式未知情形仍可用；{nbim8p}
		* 示例数据：((n4km5c))ICON，基于 few-shot，输入新方程的少量 $(a_i,u_i)$ pair、待查询的 $a$，输出 $u$（INR 形式）
		* 历史轨迹：针对含时 PDE 连续输入多时间步((nbim9t))，以此体现方程信息，如 MPP
		* 微调过程：((pbf88t))预训练用多方程、不体现 PDE 信息，靠微调适配下游
			* 所用手段((n5bl92))映射形式-由算法给出-迭代算法
		* 元学习用于 PINN((n5bm09)) 理论上也可实现，尽管非 NO
		* 输入输出—数值—适配不同分量个数，对其泛化
			* ((_o44b4y))CoDA-NO 对各分量分别引入位置编码，出现新分量时为其赋予新位置编码即可
			* MPP 的做法是((_o48b3o))ViT 输入通道数为场分量数，引入新场需扩充输入通道数（新参数随机初始化）并微调
				* DPOT 的做法似乎也类似？
				* ((_o49b2s))PROSE 预设最大分量数，不足的补零；((_p2af5p))同组的 BCAT 沿用
				* ((_ocoa0n))VICON 预设分量全集，当前数据没有的分量 mask 掉
				* ((_p15a19))UPS 预设分量全集，没有的分量按 0 处理
			* OmniArch 用 Transformer 解码器，((_o48c0p))各分量直接按固定顺序输入即可
			* 各分量独立 tokenize，添加不同的调制：((_p85f0t))PDE-Transformer
			* 交叉注意力汇总为隐空间单变量，时空信息交互后再解码原变量信息，((_pain6j))MORPH
			* （非基础模型）组合扩散((_pcfb6g))，先用非耦合 PDE 数据训扩散模型，再泛化到耦合 PDE
			* 相关，各物理量独立 tokenize 但非用于基础模型；{p8jh4r}
				* 不同维变量独立：((_p8jh4e))盘古气象模型同时涉及地表变量、空间变量，分别用 2D、3D patch
				* 输入输出函数多分量独立处理 `MIONet-2202.06137`，有相应逼近理论
				* 相关，多对象分别 tokenize 还包括((p8jh4p))对不同投影平面独立 tokenize（triplane INR）
	* 输入—符号—隐式，预设可解方程全集
		* 非零通道：`2306.00258` 关注 FNO 架构预训练-微调范式，在 3 方程上同时训练，输入有多通道，靠取非零值的通道位置 表示当前在解的方程
		* 逐方程输入层：((_q1ec4k))PDE-FM
		* 逐方程输出层，多任务学习((_o9bg4t))，不同 PDE 共享输入层编码、输出层基底函数独立；不支持 OoD 零样本推理
	* PDE 基模性能评估；{q7kh6b}
		* PDE 类型差异：同一模型在不同 PDE 上误差差 1-2 数量级((_q7kh66))
			* pretrain 和 scaling 改变量级但不消除偏好模式
		* 预训练不总有益((q7kj7k))，37.5% 架构-PDE pair 预训练负迁移
		* 网络规模扩大不总有益：25% 更大模型不如小模型((_q7kl88))
		* 现有架构失效模式分析：((_q7kh67))DPOT OOD 最敏感，MORPH rollout+缩放最不稳，MPP pretrain 负迁移最高；不能用单一指标排名
		* OoD 迁移能力见((q7oh0e))NO-OoD
	* 包含多个函数 `MIONet-2202.06137`，有相应逼近理论
		* 相关，((q1nb9f))跨 PDE 维数迁移、共享架构，方程自变量数可变（分量泛化是因变量数可变）
		* BC 刻画：
			* 隐式：通过数据体现（如自回归历史数据）
			* 数据 padding：((_ocoa1g))输入场根据 BC 做 padding
			* 注意力掩码：((_p85f1z))SWin 滑窗注意力在非周期情形屏蔽跨边界交互
				* 仅区分是否周期，对非周期情形的多种情况 无进一步区分
		* 相关框架：((o1le7k))NO-输入形式-间接输入
	* 相关，((_o2fm01))PDE 表达为交换图，程序根据该图自动生成数值求解算法；{o2fm0j}
		* 另一个类似的做法((_o3hc3c))
		* 相关：((o2fm12))PDE-数值方法-传统软件
		* 范畴论还用于形式化 LLM 推理框架，((_o9of5x))DoT
* 混合形式，同时有上面的一种或多种
	* 同时有有限维向量、函数：可将向量当作常量取值的函数，从而化为只有函数的情形
		* 向量维数较高时需要小心，见((n32f34))输入向量二部分性质差异大时代码注意事项
		* 向量+序列情形也可用常取值序列
	* 有向量和另一种：向量通过 hypernet 引入，主网络只输入剩余部分，如 `HyperFNO-NIPS22`（另一种为函数）
	* 利用 Transformer 灵活性，如((_n7hl7p))GNOT 同时考虑向量、形状、（由散点取值给出的）场、网格连边情况 等
* 若输入维数高；{ncnc2n}
	* 该场景出现于：((n4dl73))高分辨率场，((ncnb6m))NN 参数作为另一 NN 输入
	* 也可参考((n4dl7e))NN 要产生高维输出时架构设计，包括集合化、序列化、层次输入等处理办法
	* （非特别高维情形）拆多组，作 MLP 不同层 modulation
		* 如((o6g03a))不同 scale 信息不是 concat 统一输入，而是分在不同层输入
* 多类输入统一形式，以便基础模型对任务泛化；{n4tj9s}
	* 包括两方面：(1) 不同任务输入本来是不同形式，将其先改写为统一形式，(2) 对任务本身的描述 如何作为网络的输入
	* NLP 任务、任务的输入均可统一用语言描述，已用于((n4ff89))上下文学习
	* CV 的相关讨论见((n4tk05))
	* 科学计算的((n4tj7h))，包括((n3pm4j))将约束优化问题作为统一框架
	* 关于“多任务”的一般讨论见((n4sk4r))
	* 相关：((n9999m))网络(输入,输出)≠ 数据(样本,标签)
