> 2025-09-30 从 compTree.md 独立
* `coordMLP%` 输入为坐标的 NN；{o58a0h}
* （评）可把 HNN 表达为梯度形式的内容放进来
* {区域分解}，不同网络处理不同区域（可用于不同性质区域等）{n2pe8f}
	* 一般含义，((p44b73))INR 广义区域分解
	* 一般框架((p8ag76))集合划分后处理（这里是区域点集划分），同样分 软硬、自适应与否、划分数固定与否
	* 相关，一般区域分解：((n32d8v))PDE区域分解，包括目的的讨论（简化函数表征，体现平移不变性）
		* PDE 求解时还有迭代格式等方面考量，见((n7nd86))PINN-区域分解
	* 作为 ansatz 的区域分解，目的：{n2pj5z}
		* 不限于 PDE 的场景，区域分解主要 ansatz 表达能力更强
		* 若不区域分解，获得充分表达力需大网络，导致单点求值代价高((_n2pe6m))
			* ((_p4ck49))KiloNeRF 子区域数上千，网络减小使加速倍数也上千
		* 是否还有 优化问题求解难度？
		* 若考虑固定的 INR 用于下游任务，可能还有并行计算（不过不分解也能并行？似乎还是模型大小问题）
		* 训练时也有利于并行，不过不同于 PDE 求解的区域分解有迭代算法含义
		* 相关：非 INR、离散表征情形还有((n32d90))处理复杂区域，使每个子区域可用均匀网格
	* 可认为属于 ((n2pj82))单任务 hypernet 使用分类网络情形
	* 相关：多尺度((n2pe88))相当于频域区域分解，包括((n2pf3y))在细尺度同时引入空间区域分解的情形
	* 含时方程可有时空统一 ansatz，区域分解可包括时间维度，即时空区域分解
		* 对自适应分解情形，可认为相当于随时间动态变化的空间区域分解
	* 软分解 vs 硬分解；{n2pj84}
		* 相关：((n2pj8d))单任务 hypernet 使用分类网络时也区分软硬
		* 硬分解，不同区域交界处 连续性要求靠 loss 引入，可包括导数 loss
			* 根据 `PFNN2-2205.00593`“n18h4h”，cPINN 在区域交界要求 $u,\partial_nu$ 一致，`XPINN-285_2002` 要求 $u,\Delta u$ 一致
			* 若是 PINN，还可考虑((_n41f6n))引入 PDE 残差的连续性要求（未确认用处）
			* `COIN++-2201.12904` INR 用于图像压缩，大图拆 patch 分别压缩
			* `PPINN-1909.10145` 时域区域分解，预测-校正格式默认硬分解，不过也可软化
			* 考虑到连续性惩罚项有多种引入方式，((_n41f6u))用 RL 学出如何根据 PDE 参数、生成引入方式
			* 可能性：((n1eb1g))区域指标作为网络输入，各区域共用一个 NN
		* 软分解，ansatz 自动保证区域边界连续过渡
			* `[PINN适用范围-知乎]` FBPINN-2107.07871 空间软分解
			* `POUnet-2101.11256` 利用单位分解，构造用 RBF-Net、ResNet（尽管其单区域内为多项式基底 ansatz 而非 NN）
			* `tileAD+sinArch-2104.03960` 图像 INR（AD）表达，拆 patch，为保连续性用单位分解
			* `[block-NeRF-2202.05263]` 大尺度有细节函数（城市场景）表征，按街道分块，块间 50% 重叠，以便于渲染时外观对齐
			* 可能性：((n1eb1g))区域指标的连续过渡版本 作为网络输入，各区域共用一个 NN
		* 插值 作为软分解极端情形，不存在单网络主导区域；见于((n31e8h))grid-based INR
			* 此时 x 无需显式输入网络，因插值系数能完全体现 x 的所有信息
			* 在网络参数层面区域分解：((_ocge52))MLP 前传参数根据当前坐标位置双线性插值获得；{ocge5x}
				* 方法层面类似于((n3vf61))INR 坐标在每层反复输入
				* 区域分解机制属于((p44d8l))switcher位置在网络参数层
		* （自适应分解的）MOE 有介于软硬之间的做法((_n2pk5f))，截断到前 $k$ 个再加权和；{n2pk5p}
			* ((_n2pk6c))不过许多工作中认为只保留 1 个（硬分解）即可
			* （在输入位置软分解）concat（而非加权平均）输入后续网络，k 权重参与 concat；会受顺序影响，如((_p4dc1a))ARC；{p4dc21}
				* 相关：((p4dc1z))grid-based INR 插值方式
			* 注意((n2pl48))自适应硬分解不适用于 PINN
		* 相关：`coordMLP%`“ELM”确定参数靠解方程而非优化，硬分解也可达较高精度
		* `POUnet-2101.11256` 的后续工作，软区域分解用随机网络（类似 BNN）表达
		* 软分解的多块加权平均进行多次（而非仅在输出处平均）：((_n7hl62))前传每层残差连接都用一次 MoE（原文网络架构为 Transformer）{n7hl6k}
		* 相关：视为每区域有一隐向量，软硬分解相当于((oaqb1j))场表示为有空间结构的隐向量-空间压缩有重叠vs无重叠
	* 若为 自适应区域分解（包括区域个数可变），而非手动划分区域；{n2pk9m}
		* 若手动划分为网格区域，算微观参化、宏观离散，见 ((n76e2e))场表征-混合表征
			* 多尺度分解中有特例为八岔树分解，如((n76f5d))TINC
		* 手动划分可根据问题物理性质，如已知的界面等
			* 机翼结冰等多物理场景可考虑类似的分解((_n3293i))；{n3298l}
				* 相关框架：((n3298n))多物理问题
			* PDE 系数场已知分两块/多块，则解的 ansatz 也可据此划分两块
			* 已知的激波
			* 不过似乎这种（根据已知界面进行的）分解偏向于硬分解，需引入恰当的界面连续性条件，如 CFD 中  RH 间断条件
		* 仅调整区域边界，分解总数固定
			* 软分解情形，连续优化即可处理
				* ((_ocae9z))区域单位分解可学，与各区域（多项式基底 ansatz）系数 交替优化
				* `MoE-PINNs` MoE 为不同子网络分配（依赖于空间位置的）权重
			* 分解的 router 网络本身也靠学；若考虑不同场的多任务表征，router 网络和主网络可考虑同样的处理方式，如引入 AD 隐向量输入等
				* 注意这里可能涉及父子两个多任务，父任务层面不同场的拟合是不同任务，子任务层面一个场不同区域的拟合是不同任务；用 AD 时，相应的隐向量等也要区分这两类
					* 关于隐向量含义的一般讨论可见((n31g86))
			* router 网络可与各区域网络共享浅层((ncnf6d))；{ncnf6w}
			* 训练技巧可参考 ((n31g6h))单任务超网络的训练，例如引入额外 loss 避免 collapse 到一个或几个区域特别大的情形
			* 对分解结果进行解释，((_n31g8j))NeRF 中不同算法观察到的划分依据不同，有的基于空间局部性，有的则基于 pattern（建筑，草地）等；{n31h12}
				* 之后可确认一下原因，是否来自 loss、网络结构等方面的不同
			* 相关：((o2cg0f))NO 对输入散点区域分解、Transformer 算区域间注意力
		* 分解块数可动态新增，不考虑块合并、块数减小；类似传统算法根据误差局部加密网格
			* 新增条件：局部误差
			* 新增方式，分裂得新区域后，网络参数如何（依据原被分裂区域）初始化：{ocaf68}
				* 保留原区域参数，新区域从零初始化；最终 INR 形如((n2pf3y))多尺度求和形式
					* 原区域参数可就此固定，也可继续参与优化
				* 均初始化为原区域参数，后续独立优化
				* 相关：((n37g5t))网络动态新增参数，新参数初始化
			* 相关：之前关于动态添加区域的（不成熟）讨论((n4rg7x))，包括新增区域初始化策略等
		* 块数动态增减，包括合并
			* 增减条件：((_ocaf6h))ACORN 求解一个离散优化问题，决定当前是否 split/merge 一些块
			* 增减方式，参数初始化
				* ACORN 本来各区域就不存在自己维护的独立参数((ocaf6y))，区域增减时无需对参数做特殊处理
			* 相关：MeshingNet 对 mesh 动态增减边
		* 硬分解不适用 PINN loss：分解边界未知，无法手动引入连续性约束，故通常不用于 PINN（只用软分解），而仍可用于 NeRF、SDF 等；{n2pl48}
			* 这类情形有监督，或间接有监督（如 NeRF 将若干角度渲染所得图像作为 label）
				* 相关框架：((n31m50))有监督 loss 与 PDE loss
			* 此种情形的分解方式还可介于软硬之间((n2pk5p))，不过许多情况硬分解已足够，如 `Switch-NeRF`
		* 相关：((p2qf2a))NO-自适应区域分解
	* 不同区域 ansatz 的独立（自有，非共享）部分：{n2pk98}
		* 新区分标准，按区域 switcher 位置：{p44d8l}
			* 输出层（狭义区域分解）
			* 中间层((n2pk8u))
			* 输入层（包括 grid-based），((n4ta1s))
			* 网络参数（而非激活值）((ocge5x))
			* 网络 modulation((p48e5p))
		* 完全独立：各区域用 ((n32e9r))场的数值表征 参化表征，若为原始 NN 表征则相当于整个网络独立学习
			* 属于((n8qk4m))ensemble，目的为提网络表达力，对象为 NN
		* NN 深层共享、浅层独立：((n2pk8v))表现好于整个网络独立；{n2pk8u}
			* 有更复杂的可能性，如仅中间若干层独立，甚至网络的奇数层共享、偶数层独立（此时 router 或比较复杂）
			* 自适应区域分解情形，((ncnf6d))各区域网络与（负责划分区域的）路由网络浅层共享
			* 不同层共享程度不同，浅层共享为主（区域少），深层独立为主（区域多）{p5ja23}
				* TINC((n76f5d)) 浅层共享，且越靠近的区域共享层数越多（区域由八岔树层次分解得到）
				* 每层共享程度相同（均两套参数）但共享（区域分解、参数交替使用）方式不同，((_p5ja1h))LoE
					* 原文实验结果表明浅层低频、深层高频更合适，好于频率随机排列
				* 浅层低频、深层高频还用于((o6fn5a))NFFB（对 modulation 而非 MLP 参数用区域分解）
		* 仅独立隐向量，相当于 NN 输入层独立：若按 `假设空间参化方式汇总` 低维参化，每区域仅独自维护其低维表征，而低维参化的参数共享；{n4ta1s}
			* 想法见 ((o6fn12))domDecmp，主要（而不限于）用 AD
			* 隐向量输入方式：INR 图像分块表征，按架构引入隐向量的方式区分
				* 输入层与坐标 concat（类似 DeepSDF）：((_oc297w))DeepLS
				* modulation：((_oc2987))ModSine，COIN++；包括((p44f3l))插值型软区域分解情形；{p48e5p}
			* 可能性：该低维表征可有不可训练版本，表征由区域指标（可能是 one-hot 形式）给出；{n1eb1g}
				* one-hot 形式也便于采用软区域分解版本
			* 对于区域预先取定为网格（而非自适应）划分情形，则相当于((n31e8h))场使用离散、参化复合的方式来表征；{n31e95}
				* 其原版实现方式其实还要求不显式将 $x$ 输入网络，不过这似乎不是必要的做法
					* 虽然主网络可以不输入 $x$，但负责判断当前点所在区域的 router 还是需要的
			* 可考虑使用自适应网格划分，从而输入特定坐标点后，router 从大量低维表征中选出一个与它对应的，输入后续网络
			* 相关：若拼接所有隐向量，认为大隐向量表征了整个场，则算((n4ta57))结构化隐向量
			* 可视为 patch manifold 用 $Z$ 参化，见((n5bf17))patch/slice manifold
			* grid-based INR 可视为特例（插值型而非硬区域分解）
			* 相关：若区域分解为均匀网格，其训练过程将原始高分辨率数据 转为低分辨率 feature 网格，算((p3va9b))张量空间分辨率压缩-逐 patch 编码
		* 基底表征的线性组合系数：若各区域用基底表征，各区域可仅独自维护其线性组合系数
			* `POUnet-2101.11256` 手动取定全局多项式基底，各区域共享（输入不像 FEM 那样先标准化）
			* `coordMLP%`“ELM”不可训练的基底表征
				* `2021-11-10`(AISCmeet) 2110.14121, `locELM-2012.02895` ELM 前面层随机生成的参数可能共享，最后一层各区域单独训练
				* 不过这里的基底其实还可以不共享，此时类似于整个网络都独立学习的情形
		* 各区域用 grid-based INR 情形((ocae93))
		* 自有部分可有异质性，如为复杂/重要区域分配更多参数量；{n76f8h}
			* ((n76f8e))TINC 就采取了类似策略
	* 不同区域坐标输入方式，直接输入，或标准化后输入（从而不同区域输入的取值范围一致）
		* 相关：FEM 各单元内基底 可认为是先将输入变换到标准单元
		* `[PINN适用范围-知乎]` FBPINN-2107.07871 坐标标准化后输入 NN
		* `tileAD+sinArch-2104.03960` INR（AD）图像分块表达，每块 rescale 到单位方形
		* `COIN++-2201.12904` INR modulation 图像分块表达，各块共享部分训练靠抽取随机 patch
	* 各区域用 grid-based INR 情形((ocae93))
	* 有时可与传统 FEM 比较
		* `POUnet-2101.11256` 每区域多项式基底，自适应区域软分解，原文讨论与传统 FEM hp-逼近关系
		* ((o6fn12))domDecmp 想法提到与 FEM 相似性，相当于每单元内用非线性的参化表征、而非基底表征，克服线性 ansatz Kolmogorov n-width 问题
	* 区域分解相关理论，为何可提高 ansatz 表达能力
		* 如果被逼近函数为分形，则局部逼近的容易程度和整体逼近差不多？（不过鉴于局部逼近时函数取值按比例缩小，误差也按比例缩小，仍有可能提高？）
* 高频（局部性质或源于方程），间断，多尺度：{n7jm07}
	* `2022-04-13`(ADmeet) 认为高频、间断已解决：输入层不可学的 Fourier feature+后续 sin 激活；真正挑战是多尺度，如 NS 方程解尺度跨度可认为是无穷
		* `Song2021AVersatileFS` 用了相同架构，有给出说明其合理性的参考文献
		* 额外 Fourier feature 有用性，我某次经验为((n8ih0n))它可使 loss 下降过半；另外((n8ie5b))残差连接也有用
		* debug 相关：初始化时的预测场应为无规律高频；若为明显网格状高频，则去掉位置编码后很可能为低频，说明可能代码实现有问题
		* 另：clh后来认为更高效的架构与此不同((n8vg4p))，(('nbff5l))改进后的 MFN 精度有不小提升
	* 激活函数；{n8eh18}
		* sine：`Siren-2006.09661` 使用 sin 激活函数、特定初始化；{n37g4n}
			* 初始化重要性：据说之前用 sine 激活的人不少，但都没成功，因为没改成正确的初始化
			* 学习率：
				* 与 MFN 相比((nbff8s))需用较小学习率
				* 随学习率增大，((n8an2l))我观察到所表达场的高频成分变多
			* 残差连接：(('n8bj94))我观察到引入残差连接后拟合能力有提升；{n8ie5b}
			* 考虑((n79i86))求导用 FD 而非 BP，((_o1sa2w))梯度估计准确度 sine+FD > ELU+BP > sine+BP；{o1sa7n}
			* 相关框架：((n37g4c))NN 初始化
		* hat：`2208.04924` 许进超从((n2pg3v))NN spectral bias 角度提出 hat 激活好于 ReLU，tanh；未比较 sine；{n3ff7v}
		* 理论分析：激活函数会影响 NN 训练 F-principle（spectral bias）的具体表现((n3ff9c))，可从 NTK 等角度分析，某些激活函数下可高频先收敛
		* Gauss：`2111.15135` 自称提出比 sine 更好的非周期激活函数，Gauss 等
		* RePU：非高频问题，`2022-12-12`(lectures) 认为对微分方程 RePU 好于 tanh,sigmoid，不易过拟合，且正向计算效率更高
		* Gabor：`WIRE-2301.05187` 用 Gabor 小波，其中 sin 项保证频域局部、Gauss 项保证空间局部；{o6ua4g}
			* 实验比较 sin、Gauss、((n3ng51))MFN、ReLU+位置编码 等，在边缘等细节表达方面有优势；{n3ng4u}
			* 相关：((o6r95k))网络前传涉及复数运算
		* PSWF((_p4ee7g))，形状与 Gabor 有点像
		* 一般激活函数，不限于 INR；{ncqf28}
			* GELU 激活，据说在 GPT2，BERT，ViT，MLP-Mixer 等工作中采用
				* 有随机版本，此时相当于随机输出 $0,x$，概率与 $x$ 取值有关；相当于 dropout((n37g82)) 与激活函数的融合；{n9nn4c}
			* GLU（gated linear unit），常用于 ((o3ll7n))Transformer-FFN；{o3198n}
				* ((_o3198m))据说 self-gating 加上都涨点，基本形式 $\sigmoid(Wx)x$；变体有 context gating 和 SE 模块等
				* LLaMA SwiGLU，Grok-1 据说 GeGLU
				* 相关：gating 在 activation 层面执行，softmax((o31a0d)) 在 instance 层面执行
			* 激活函数引入了高频信息((ncqf1p))
			* 针对半精度的 pre-activation 将数值限制到有限区间，((o2ga7u))用于 FNO
	* 位置编码引入高频信息，可能使输入升维；{n3t97n}
		* 局限性：有观点认为((_n3vf5v))不能升维太高，从而引入的 feature 有限、不易设计，不如((n3vf61))坐标反复输入 的方式灵活
		* concat 输入 $[x,4x,16x]$，甚至可以加到十几个；{n2pg39}
			* 整合不同尺度信息，这里在输出层，另有((n2pg38))MscaleDNN 在输出层，以及((n2pg46))在中间层的可能性
		* Fourier 位置编码 $[a_kh(b_k^\mathrm{T}x)]$，$h=[\sin,\cos]$
			* 系数 $a_k,b_k$ 取定即可，((_o87f06))随训练更新的帮助不大
			* 频率 $b_k$ 选取
				* 单频率编码 $h(x)$，用于体现单变量周期性
				* Fourier feature，$[h(kx)]_k$
					* ((_o87e9p))幅值可考虑幂函数衰减 $a_k=1/k^p$
				* 指数标量频率 $[h(2^kx)]_k$
					* 我的实验结果，(('n8ih0d))在已用 sine 激活前提下，引入该位置编码明显提高表现；{n8ih0n}
					* ((_n3ng0n))幅值引入指数衰减 $a_k=\exp(-2^{2k-1})$
						* （旧表述）Mip-NeRF 的位置编码涉及 $\mu,\Sigma$，频率取为 $2^m$（$m=0,\dots,M-1$），编码形式同时出现 sin,exp（不排除类似 Gabor filter，exp 为衰减项）
					* 底数 2 可换其他值，((_o87f1f))$b_k=\sigma^{k/K}$，$\sigma$ 待调参数
				* Gaussian $h(Bx)$，$B\sim N(0,\sigma^2)$ 矩阵各元素独立选取、不参与训练；{p6ub6q}
					* (('o87e7l))实验结果好于 $h(x)$、$[h(kx)]_k$
					* ((_o6rj50))输入层形如 $1/\sqrt{m}W[\cos(Bx);\sin(Bx)]$
					* 使用案例还包括((_p9ub4d))DeepMind PINN 找流体爆破解，二阶段网络
					* 相关：((p6ub6n))网络参数随机生成、不参与训练
				* multi-Gaussian，频率从多个不同标准差的分布中采样
					* MARIO((_p8bf5y))，成为 NeurIPS 2024 ML4CFD 竞赛获奖模型之一
				* 标量频率可能有局限性，((_o87f19))只刻画走向沿坐标轴的频率先验，而实际可能有斜方向的
			* 相关理论分析：((_o87e8q))NTK，该编码使特征值衰减变慢；{n9a880}
				* 相关：((n3ff9q))NTK
		* 可学 grid 编码 $\sum c_iI_i(x)$（i 格点角标，$I_i$ 双线性插值基底），((n31e8h))grid-based INR
		* 混合方式，((p4l95u))Fourier、可学 grid concat
			* 相较纯坐标好处，((_p4lf6y))减轻位置编码拟合负担，使其可专注于局部精细细节
		* 混合方式，((p5ja2j))可学 grid 插值结果后再接 Fourier embedding
		* 旧内容
			* 如引入 $\sin\omega x$（[Rahaman]ICML2018）
			* `2022-08-19`(lectures) 提到输入的 sine embedding 也可用 MscaleDNN 方式给定
			* 也可以随机生成参数，如随机矩阵，或多个不同方差的随机矩阵（Fourier feature 似乎同理）
			* `2022-12-14`(dbGrpMeet2) 对位置编码的较细致讨论，Fourier feature 用随机生成、不训练的高斯矩阵效果好于用整数频率；并提到其有效性有 NTK 分析，该编码使特征值衰减变慢
		* 相关：((n3vf6h))Transformer 位置编码的目的是恢复位置信息，不同于这里引入高频 feature；((n7de8p))图 Transformer 涉及位置、结构编码
			* NO 输入为可变散点时，((n9gf5t))也用位置编码（包括 2D 版本）体现空间位置信息
		* 向 reference domain 的变换也有用位置编码的((n7nf0p))
	* 坐标在每层反复输入，而非仅在首层；{n3vf61}
		* 广义含义，((p44b7d))INR 坐标逐层输入 示意图解读
		* ((_n3vf74))poly-INR 用 $h\odot(Ax)$ 替换 $h$ 输入下一层 NN，$A$ 可学仿射变换；{n3vg23}
			* 与 MFN 比较：有激活函数¹（leaky ReLU），坐标输入用仿射变换而非高频 feature
				* ¹指作用于隐层激活 $h$ 的非线性函数；MFN 中非线性仅作用于输入 $x$
			* 这里激活函数（leaky ReLU）与位置编码（仿射变换）都未引入高频信息，故可认为通过反复输入坐标能有效引入高频信息（按文中说法是 高阶多项式）
			* 我的实验发现该架构比同宽度深度的 Siren（都带超网络）慢，内存需求也更大(('n8mb6m))
		* `2001.04536` 对输入过一层 NN 后，结果反复输入主网络各隐层；{n4km8g}
			* 后续工作 `PirateNets-2402.00326` 随训练进行逐渐在残差网络后追加新层；原文针对 PINN；{o2gh56}
				* 相关：((n37g5t))NN-训练中动态新增参数，((o2gh5s))PINN-训练技巧
			* 后续工作用于 DeepONet((_p2bf9d))，branch/trunk net 输入额外过 encoder，结果作为 branch/trunk net 中间层的 ((n92h7w))scale modulation；{p2bg0f}
		* 坐标编码后再输入
			* MFN((n3ng51)) 用三角函数编码，每层的编码方式独立可学
			* 各层编码使用不同 scale 以构造多尺度网络((o6g03a))
		* 网络深度或比宽度重要：该架构中 x 反复输入次数越多，feature 越复杂
		* 理解为 modulation：每层反复输入相当于基于 x（而非 z）生成((n2il89))modulation
			* 网络参数 而非 modulation 逐层由坐标给出：((ocge5x))MLP 参数根据输入坐标位置插值获得，算（插值型）软区域分解而非高频表征
		* 另：((n3t95e))DGM 网络架构用 LSTM，每层都输入一次 $x$（不确定用处）
		* 相关：((n3t95j))仿TRIZ原理-重复-输入的重复
	* 反复利用之前的隐层
		* DM-PINN((_o2ag7f)) 每隐层乘上之前所有隐层的输出
	* MFN：用固定基底（可解释），基底表达为基本 feature 的多项乘积（靠深层复合实现），从而使基底数指数增长；2022 年末有人认为是 INR SOTA；{n3ng51}
		* DINo((_n2h99t))用了 MFN，每层线性变换后并不过（sin 等）非线性激活，而是逐元素乘输入 $x$ 的 Fourier feature
			* 各层的 Fourier feature 频率向量 $\omega$ 独立、不共享
			* $\omega$ 随机取定（均匀分布）即可，使其参与训练的收益不大 `DINo-2209.14855`“n2ha0x”
		* `Fathony2021MFN` 可用基底包括全局 Fourier 基底、局部 Gabor 基底
			* 利用积化和差可证明最终所表达的场为指数多个这样基底的线性组合
				* 一个推论：若进一步引入 hypernet 或者((n2il89))modulation（记输入 $z$），则相当于进行了某种变量分离，最终表达出的场形如 $\sum c_i(z)g_i(x)$；{n2il9j}
					* `DINo-2209.14855`“n2il96”考虑动力系统，$z$ 满足隐空间动力学、依赖于 $t$，从而相当于进行了时空变量分离；这((n2im2k))能增强时间外推能力；{n2im20}
					* 关于引入 modulation 的 hypernet 架构，((n8vg4p))clh用了 CDE 而非 MLP 形式
				* 另外积化和差还意味着：所得基底频率（向量）为各层频率之（带符号）和
			* 原文有说明参数初始化方式((_n2ie5h))，((_n2ie5i))；{n37g4f}
				* 相关框架：((n37g4c))NN 初始化
		* 原文实验结果表明 Gabor 基底用于 PINN Helmholtz 好于 SIREN `Fathony2021MFN`“n2ib7f”
		* 有工作实验结果认为((n3ng4u))用 MLP、Gabor 激活函数效果好于 MFN
		* clh在 `<n8vg40>` 引入的改进：对上一层输出先 layernorm 再仿射变换；同时用 sin,cos；{n8vg4p}
			* 超网络类似 CDE（又见((n35f0h))），用于生成 MFN 各层 modulation
				* 相关：((n2im20))之前 DINo 已用超网络生成 MFN modulation，不过可能不是用的 CDE 架构
				* 超网络也对上一层输出用 layernorm（据说是有文章推荐的，且做了 ablation），ELU 激活（试了多个后选的）+残差连接
				* 超网络每层输出过线性层后，作为 MFN bias modulation
		* 推广到球面定义域，lvy学长的 SINR（2309.09574 的一部分）利用球谐函数构造；{n9lg8c}
			* 细节 1：球面为特殊周期性 BC，球谐次数 m 只能为整数
			* 细节 2：二球谐函数相乘未必仍球谐
			* 方式：第 l 层只引入次数 l 的球谐函数 $Y_l^m$；每层有额外输出直接连到 整体网络的输出层
			* 逼近能力，有相关理论估计
			* 相关：((n7l952))球谐函数
		* 相关：((n3vf61))坐标在每层反复输入
	* 权重矩阵不同参化方式：((_n92e0r))RWF 参化，允许权重（训练后）跨越多个量级；{n92h7j}
		* 若其中的 $s$ 由超网络生成，则架构类似于((n92h7w))scale modulation；不过其初始化方案有区别，是按普通方式初始化 $V$、$s$ 初始化由超网络初始化决定，而非按标准方式初始化 $W$
	* （上面“不同性质区域”讨论的区域分解也可用，若规模够大）
	* （旧）很细致的（软）区域分解((n2pf3y))，多尺度求和形式
		* `NeRF_5s`（包括多尺度形式，网格很细而 $u$ 间断部分稀疏时用 hash 压缩参数量）
		* 这两种做法也许可在高维问题中建立网格法，并避免维数灾难？
	* 多尺度，相当于频域区域分解，不同网络负责表征不同频率的成分；{n2pe88}
		* 相关：((n2pe8f))空间区域分解，((n2pf83))散点离散靠网格加密实现多尺度表征，((n5da0x))NO-多尺度（并作为“多尺度”汇总）
			* ((n2pf3f))场写为散点离散复合参化表征时 在散点离散部分体现多尺度，而参化部分用一个网络（而非不同频率的网络独立）；((_nb1n5y))该架构已被用于 PINN
		* 多尺度 `HiDeNN`（待确认；网格像 NeRF？）
		* 对输入坐标引入多种 scale 后，各 scale 输入网络的方式
			* 输入不同网络：MscaleDNN，多个网络分别输入 $2^kx$；在输出层整合各 scale；{n2pg38}
				* 原文的讨论基于 F-principle((n2pg3v))
				* 据说（某些方程？）只输入 $x,2x$ 效果就足够了，再多意义不大
			* concat 输入同一网络：((n2pg39))在第一层整合，属于输入升维做法
			* （可能性）输入不同浅层网络、共享深层网络，或称在中间层整合；{n2pg46}
				* 具体地：多个网络分别输入 $2^kx$，输出求和或 concat 后输入后续网络
				* 不过估计意义不大，不如在输入层整合
			* 在同一网络的不同层依次输入，如 ((o6fn5a))NFFB；{o6g03a}
				* 相关：((n3vf61))INR-坐标反复输入
				* 高维向量不 concat 而是分组在不同层输入，可用于处理((ncnc2n))NO-输入维数高
		* 相关理论：F-principle，NN 优先拟合低频部分，而传统迭代求解器优先处理高频；{n2pg3v}
			* 与梯度下降形式无关，传统算法也可写为梯度下降形式 `2023-03-15`(AISCmeet2)“n3ff7b”
			* 事实上并非所有 NN 都优先拟合低频，这取决于网络架构等方面，尤其是激活函数
				* F-principle 原文似乎是用的 ReLU 激活？
				* 例如((n3ff7v))激活函数改用 hat function 后表现不那么一致
			* 同样用梯度下降类迭代，NN 在频域收敛情况有多样性，这可用((n3ff9q))NTK 分析：相较传统算法，NN 更新速度多了 $K$ 项，该项形式与架构、激活函数有关 `2023-03-15`(AISCmeet2)“n3ff8a”；{n3ff9c}
			* 可通过换二阶优化器缓解((_q4pg64))
			* 相关：((n3fe73))混合迭代算子，两种迭代器交替使用
			* NO 也涉及频率问题：{ncqf18}
				* 后果—((n99f5h))对某些含时 PDE，高频预测精确度对长时预测精度重要，而 MSE loss 未对高频成分给予足够关注
					* 机制：((_q64m2h))高频误差传播到低频，非线性项导致（如 KS 方程）
					* 现象描述+((_q6jg4q))混叠伪影随自回归加剧
				* 归因—架构—谱卷积频率截断：((n99f7n))FNO 截断高频信息导致精度不佳
				* 归因—架构—alias：NO 平移等变性会 ((n3h98j))被 alias 破坏，高频成分在有限网格分辨率下被扭曲
				* 归因—loss：湍流等高频分量幅值较低，((_q64f41))L2 loss 下被轻视；{q64m2a}
					* 应对—谱感知 loss((_q4pg69))
					* 应对—（可能性）用 $H^1$ 乃至一般的 $H^s$ loss((o2ga6g))，高频分量增加惩罚权重
					* 反向缺点：L2 loss 过度惩罚高频误差，导致((nckf3f))预测结果过度平滑
				* 应对—boosting 拟合高频残差，((_p3ja6v))单个 FNO 更擅长低频，引入第二个 FNO 学其残差；{p3ja79}
				* 应对—架构，((n99f58))用迭代修正（实现为去噪）模型降低高频误差，原文主要针对长时间外推稳定性
				* 变种—频率泛化，低频数据训练、高频数据推理，((_q6jg4a))FNO 出现高频伪影在高频
				* 相关：((n7kj8y))在频域表达 NO，((n7na92))NO-逼近理论
		* 各频率网络依次训练，boosting：((_n2pk1w))许多工作采取 progressive 训练方式，先训最粗的网络，再让后续网络逐个拟合残差；{o5ph09}
			* 例子：((_n2pk35))Bungeenerf（ECCV2022），MINER（ECCV2022），Pins（ICML2022）
			* 用于 PINN：PINN 用 boosting 处理多尺度问题((o5pg9h))
			* 用于 FNO((p3ja79))拟合前网络的高频残差
			* 网络规模，多个小网络 boosting 好于单个大网络((_o6ea5d))
			* 依据当前残差初始化后续新网络，((_o6ea3j))MSNN 据残差幅值设置新网络在求和中权重、据残差频率设置新网络首层初始化因子；{o6ea5w}
				* 新网络训练问题，((_o6ea45))空间采样点需增多；相关：((n3h98i))alias；{o6ea4b}
				* 用于 PINN，不同于回归问题，((o6ea64))需设法从 PDE 残差中估计方程解的残差
			* loss 差异性：(('o8uh9n))可考虑换 loss，老网络 L2 loss 拟合整体，新网络 H1 loss 拟合激波；{o8uh9u}
				* 相关：coordLoss-((o8ui03))间断位置拟合，((n29g64))Sobolev loss
		* 各频率整合方式，未必形如多个独立网络求和，可换为单网络内多层复合、多层中间输出求和形式，见((n76f5d))
		* 类似小波的做法，空间域、频域同时区域分解，高频部分用更细的空间域分解((n2pf3y))
		* 注：为一个任务使用多个网络的 ensemble 做法，这里类似 boosting、提高网络表达能力，尽管各子网络通常同时而非依次训练；可见((n7d90o))boosting vs bagging
	* 相关：((n7jm0c))关于“间断”的一般框架
* 架构设计引入先验，包括无界区域适配
	* 区域形状复杂（包括无界区域）：{p9ua7b}
		* 结合区域分解
		* PhyGeoNet 映射至规则形状区域
		* SimNet 据说使用椭圆、矩形交并补构造
		* level-set（待探索）
		* 无界区域可先变换到有界区域((p4fa30))，只需表达有界区域上的场
	* 满足约束：多输入的对称、反对称，一般的群等变，凸，$\nabla\cdot u=0$：{n3pa7n}
		* 一般框架（不限于 INR）((nca99w))NN-引入先验知识
		* 低维函数奇偶性，((_p9ua4v))PINN ansatz
		* 对称：DeepSets；一般地可参考 hypernet 输入训练数据集的方式
		* 反对称（如薛方电子波函数）：FermiNet，PauliNet；DeepWF 可改为输入个数可变的版本
		* ((n23g9c))各种群（包括置换旋转耦合等）等变函数的一般形式
		* 凸函数：使用凸激活函数和非负权重，或惩罚非凸性 `2106.08973`(x)
			* 关于部分分量凸（用于表达哈密顿量等）的可能性：主网络以凸分量为输入，hypernet 输入其余分量（若只用于生成主网络的 bias 则可解读为双路网络，未必理解为 hypernet）
		* divergence-free $\nabla\cdot u=0$：{p9hf3q}
			* `2210.01741`
			* 备用：`2023-01-11`(lectures)庞龙刚 提到的 linearly constrained NN 可算更一般框架，线性算子 kernel 试写为另一算子 image
			* 不限于 INR：2507.09146 扩散生成模型给出初始流场，之后 Helmholtz-Hodge 分解得无散、无旋、调和分量，重新编辑、组合得最终流场
		* $Lu=0$，若 $L$ 对应的全空间 Green 函数已知可用边界元表达 $u$ ansatz `BINet-2110.00352`，从而训练只需考虑 PDE 边界 loss；{n3pa6x}
			* 流体守恒律 $\rho_t+\nabla\cdot(\rho u)=0$ 可写为无散条件 `2210.01741`
		* 满足特定边界条件的见 `PINN%`“边界处理”
		* 取值大小范围：要求取正值可用 $\exp(NN)$ 形式，要求取值在某区间内可((_n43j5o))用 sigmoid
			* 区间取值另一场景：`2303.09280` 拓扑检测反问题，待恢复密度场取值范围 $[0,1]$
			* 也可用于约束 $Lu$ 的取值范围等：如 $Lu=\exp(NN)$；{n43j7d}
				* 为场添加连续性约束时也用到了类似的做法((n43j79))
			* 需小心陷入平坦区域（尤其训练初期）后梯度消失、不易离开该区域的问题
				* 不排除写为 $\exp(rNN)$，对 $r$ 退火，或周期性升降、根据当前解与梯度方向自适应升降？
					* 引入 $r$ 或许更适合处理真实目标取值范围 $\{a,b\}$ 的情形，$r$ 增大后边界更清晰（计算所得边界可用 level-set 给出）
				* `2303.09280` 额外对 NN 施加 Eikonal 方程违反程度的正则化项
				* 或许 softplus 好于 exp
		* 相关：((n3pa64))PDE 与科学计算问题中的等式约束
	* 输入增广：根据物理背景，输入除原始坐标以外的信息；{p8bf7k}
		* 只考虑额外输入也随空间变化；否则见((n4dl2q))假设空间参化-隐向量concat
		* ((_p8bf7m))MARIO 包括 SDF，体积法线，边界层 mask；{p8bf7y}
			* 对训练数据少情形尤其重要；数据足够则重要性较低
			* 相关：((p4ce2r))NO输入形状表示为场
	* 输出值跨量级（不同空间位置 or 不同输出分量）见((p9ua2i))
	* 对维数泛化；{n3t96d}
		* 用((n3t97n))位置编码 转化为同维度输入，再输入后续 NN
			* 若输入高维，位置编码中可用的频率数量减少；不过若认为高维函数通常光滑，可能最终影响不大
		* 还可考虑((n3t97e))分割原理？各维度 $x_i$ 按序列/集合理解，可用 Transformer 等；{n3t97h}
			* 若作为集合，$i$ 可用((n3t97t))替身原理，如位置编码
				* 若用隐向量形式的替身，输入形式成为 $(x_i,z_i)$，其中 $z_i$ 为特定于 INR 的参数，不随 $x_i$ 取值而改变
		* ((_n5bk6e))HyperDiffusion 直接生成隐向量参数，已可同时支持 3D、4D（动画）的 INR 生成
		* 相关：((n2ni9r))PDE大模型
* 其他架构相关：特殊设置，逼近能力，非传统 NN 架构
	* 网络架构
		* 针对高频表达的架构见上方
		* DeepSDF 原文附录 $x$ 不仅作为网络输入，还在中间层 concat 进来；与 DenseNet 有相似之处
			* DGM 用 LSTM 每层都输入一次 $x$，仅最后一层有输出并作为 $u(x)$；不确定优势；{n3t95e}
				* 迭代格式有点像 ((n32f5j))DEQ=FPN，但内存复杂度会比它高，不过至少能算二阶导？
				* 相关：((n3t95j))仿TRIZ原理-重复-输入的重复
		* `2022-08-26`(CSImeet2) 提到可在网络架构中引入频域、空间域、特征域编码，提高网络表达能力
			* 当时主要考虑 INR，三者分别对应 纹理、区域与形状、颜色
		* 训练时根据当前解性态，自适应新增神经元，并恰当初始化其参数 ANE ((n37g53))
		* 一般的 ((n32f33))NN架构设计 也可参考
		* （不确认用处）`[PINN适用范围-知乎]` PIELM-1907.03507 为方便训练，结合 ELM、仅用单隐层
		* 边界条件先验（如周期边界）处理方式同 `PINN%`“边界处理”
	* {逼近能力}，以及利用传统 FEM 理论等解读网络架构
		* ((n37g53)) ANE，ReLU 网络视为 FEM moving mesh，并考虑动态新增神经元、其初始化
		* 备用：`2021-11-10`(AISCmeet)“n1db3e”许进超 FiniteNeuralElement 认为 NN 能够拟合复杂边界、同时有 spectral 精度
			* 不知道是不是这篇：FNM-2010.01458
		* 部分任务参数量千到万的 MLP 足够，((_p9ua2l))DeepMind PINN 找流体爆破解
		* 相关：((n32b5g))NN架构解读
	* 非直接 NN 的其他 ansatz：
		* {ELM}（extreme learning machine）{n2q94e}
			* 注：其实算一般 ML ansatz，最早用于单隐层 NN、解分类或回归问题；近期相关工作主要面向 PDE 故仍放这里；另见((n4tb9n))MLP 的变种
			* 架构为 NN 但仅最后一层可学；{o31f0c}
				* 属框架 `假设空间参化方式汇总`“基底表达”，已不同于一般 NN 的参化
				* 相关：((o31f0d))微调-仅最后一层参与微调，其网络主干参数为事先学出，而不是像这里随机取定
			* 前层参数随机生成、不训练
				* `2021-11-10`(AISCmeet) 提到用均匀分布，发现方差太小太大效果都不好
				* 随机生成、不参与训练的网络参数，不限于 ELM；{p6ub6n}
					* reservoir computing
					* ViT patch 投影层参数((p6ub6i))直接冻结可增强训练稳定性
					* INR 位置编码的调频矩阵((p6ub6q))
					* 相关：((o31f0d))网络参数预训练后冻结、不参与微调
			* 常浅层（通常不超过 3），最后一层较大 以加强表达能力
				* random feature model 可能指单隐层的 ELM？
				* `2021-11-10`(AISCmeet) 提到层数变多效果不怎么提升
			* 基底 ansatz 表达能力
				* 线性假设空间的有效性 依赖于解集 Kolmogorov n-width 低
				* 或需结合区域分解才有足够表达力
					* `2021-11-10`(AISCmeet) 文章介绍提到分解 4 区域已有明显提升
				* 相较 FEM 基底：阶数高、非局部基底，或使表达能力更强
					* `2021-11-10`(AISCmeet) 讨论提到相较于 FEM 算高阶基底；局部性类比 Fourier 变换及其 windowed 版本
				* `singlePDE%`“n1dh4d”区域撒点够多后精度不再提升，或意味着表达力有限
			* 例：`locELM-2012.02895`，`DPGM-2201.12995`
			* 训练常解最小二乘（线性为主，具体问题也有非线性），而非优化；可见 `singlePDE%`“ELM”
		* TT 方法（`o-2102.11830`）处理高维问题的传统方法（目前版本无法处理复杂边界）
			* 导师认为 PDE 解结构相对简单，用 NN 杀鸡用牛刀，TT 结构通常足以对付；类似 PDE 解算子、图像等问题 NN 的强大拟合能力才能够充分发挥
			* `2021-07-23`(CSImeet) NIPS2015 用于图像识别效果不太好，但 PDE 解相对简单；TT 有理论保证
				* 减少参数的方式，CNN 好于全连接是假设了参数的平移性质；此外还可以低秩逼近，$k(x,y)\ne k(x-y)$，此时可以分解为 $m\times r,r\times n$ 矩阵乘积；对于高维张量，可以使用 TT
			* `2021-10-27`(dbGrpMeet) 认为逐 tensor 的优化方式太 local 了，初值不准时可靠性可能不如 NN
		* 边界元方法，只参化边界上定义的函数，用微分算子基本解作积分算子来获得全区域有定义的目标函数 ansatz((n3pa6x))，解自动满足 PDE 内部方程
		* 另：FNO 结构 `PINO-2111.03794`“ansatz”，为离散或基底型函数表达
		* KAN((o58a0n))作为 MLP 替代选项
* 训推成本
	* 训练内存开销大：CV 任务网络架构可用隐式层 `(Implicit)2`
		* 由于隐式层暂无二阶导求法，仍不适用于 PINN
	* 推理慢的解决方式
		* 降 MLP 规模：区域分解以允许用较小的 MLP
		* 减少空间采样点个数：((_p5j97v))MINER 多尺度分解时，粗尺度为粗网格求值再上采样，而非细网格求值
* 其他：
	* 相关术语 `functa-2201.12204` functa，INR（主要针对 CV 任务）
		* `INSP-Net-2210.08772` INR 任务表达为 找满足若干约束的函数，包括信号存储、解 PDE、据图像重建 3D 场景等
	* 训练问题：
		* loss 技巧见 ((n3pj3j))coordLoss
		* 学习率：(('n8an26))我观察到学习率变大，训练后 Siren pattern 变高频；{n8an2l}
			* 相关：((n37g4n))Siren，((n7db46))学习率
			* 注意(('nbff5w))Siren 架构需要较低学习率（我怀疑是因为后面层取的 $\omega_0=30$ 偏大了），一些情形下需为 MFN 的 1/10；{nbff8s}
		* PINN 边界层、多尺度等问题，((_o5pg9c))用集成学习，Gradient Boosting；{o5pg9h}
			* INR 处理多尺度问题时((o5ph09))多网络联合是常用做法，包括同时训练和分阶段训练的做法
			* 考虑((o6ea5w))新网络初始化需依据残差幅值、频率，需((_o6ea5s))从 PDE 残差中估计解残差的性态；{o6ea64}
				* ((_p9ub3u))DeepMind 找流体爆破解，直接用 PDE 残差主频，用于设定新网络 Fourier 位置编码幅值
				* 相关：((o6eb08))PDE 残差、预测解残差 之间的关系
			* 相关：((n7d90o))boosting，((o1ol51))边界层
	* INR 作为网络输入，算 `NO%` 网格泛化-参数化表达 框架
	* 有用 INR 表达时序数据的做法 `HyperTime-2208.05836`
	* 相关：((n5cb23))仿TRIZ原理-替身-图片作为NeRF替身
