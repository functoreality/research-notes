# 元素分类处理笔记重构：任务说明

## 任务目标

现有笔记在三个框架中分别讨论了同一类子要素：
先用映射 $h$ 将网络输入或输入中的元素分到类别 $i$，
再按类别组织后续交互、聚合或网络处理。

本次重构要抽取这些讨论的共享部分，建立一份独立的
“元素分类处理”笔记框架。新框架集中讨论分类对象、分类映射、
输出形式、约束、训练及下游使用等可跨场景迁移的内容。
原框架只保留各自如何使用该子要素来实现整体目的的场景特有内容。

本次不是简单合并三段旧笔记，也不是从头枚举分类网络方案。
需要先从旧内容还原共同问题，再按未来查阅需求和目的手段关系重新组织。

## 参考范围

直接材料：

- `refactor-plan.md` 中已有但不完整的新框架草案
- `raw/INRarch.md` {n2pe8f} 子树
- `raw/NNarch.md` {n2pj82} 子树
- `raw/NOinput.md` {p8ag76} 子树

以上行号仅作初始定位。正式分析时应结合大纲层级、UID 链接和连续原文
确认实际内容边界，不能把给定行号机械地当作迁移边界。

## 恢复任务前的必读材料

本文件是跨轮次和跨模型的主任务记忆。上下文压缩、任务交接或间隔较长后，
先重读本文件，再完整阅读以下 skill，之后才继续正式笔记迁移：

- `.agents/skills/literature-note-writing/SKILL.md`：借用读者回查、信息压缩、
  自解释节点和第三方盲审方法。当前产物是主笔记，不机械套用文献笔记的
  TLDR、文件骨架、引文、`（AI 评）` 或专用格式检查脚本。
- `.agents/skills/main-note-integration/SKILL.md`：借用写入前来源映射、目标语境审视、
  跨语境翻译和 UID 链接原则。该 skill 原本只增补单篇文献、不负责大范围重构；
  本任务的结构改写权限和迁移边界仍以本文件及用户逐轮确认为准。
- `/home/yzh/.codex/skills/purpose-means-analysis/SKILL.md`：借用直接目的手段关系、
  同级展开轴和抽象层级检查。正式笔记要兼顾折叠查阅，不机械写入该 skill 的
  `or`、`末端`、`暂缓` 等分析标记，也不为补全每个中间目的而无限加深层级。

需要浏览深层大纲、跟随 UID 或生成 UID 时，继续使用 `outline-read` skill。
以上 skill 是分析和复核依据；它们与本任务的专门规则冲突时，以本文件和
用户最新反馈为准。

## 内容归属原则

### 迁入新框架

迁移能脱离单一应用场景、描述“分类并分流处理”这一共享子要素的内容，
例如：

- 被分类对象及其结构
- 分类映射的输入、输出和依赖
- 预设分类、可学习分类及混合分类
- 硬分类、软分类、多分类与稀疏选择
- 分类结果应满足的性质及防退化约束
- 分类网络的训练信号和训练方式
- 类别怎样控制交互、聚合、路由或多个处理网络
- 分类粒度、层间分类关系及其他可跨场景比较的设计维度

这份列表是分析入口，不是预先锁定的新大纲。
第一阶段仍需从三处原文补齐宽度和深度，并判断各项之间的从属关系。

### 留在原框架

保留只有放回原问题才能成立的内容，尤其包括：

- 原框架的整体目的
- 分类子要素在原方法完整目的手段链中的角色
- 原方法怎样使用 $h:X\to I$ 的输出完成区域分解、集合处理、
  超网络控制等特定任务
- 不能自然抽象到共享分类框架的场景约束、接口和后续计算

迁移后，原框架可以保留对新框架的简短引用或接口说明，
但不再重复共享设计知识。

## 工作阶段

### 阶段 0：确认任务理解

产物是本任务说明文件。用户确认前，不开始新框架设计和正文迁移。

### 阶段 1：设计独立顶层大纲

1. 阅读三处旧框架的完整局部结构和相关链接。
2. 识别已有内容覆盖的讨论维度、方案分支和更深层子问题。
3. 参考 `refactor-plan.md`，补齐当前草案缺失的宽度与深度。
4. 以目的手段关系为主要组织依据，同时按未来查阅效率调整层级：
   - 避免从总体目的直接跳到具体网络或论文实例。
   - 同级节点尽量采用一致的展开轴。
   - 不为形式完整而制造过深层级。
   - 读者更常直接查找的维度可以适当压平。
5. 创建一份独立 Markdown 文件，只写新框架的顶层大纲和必要的
   占位说明，不迁移旧正文。
6. 写完后停止，等待用户确认大纲。

阶段 1 当前状态：completed，用户已调整并确认独立大纲。

### 阶段 2：循环迁移具体内容

阶段 2 当前状态：blocked。本轮已迁移 `NNarch.md` 中“利用多个子结构形成路由依据”
这一连续语义子树，双评审及相应修订已完成，等待用户审核后继续。
`route-h-refactor-design-v2.md` 作为已完成设计过程的历史文件保留；
后续结构调整只写入正式笔记，不再同步维护该文件。
迁移基线为 `e14321c4a956a47bc9dc4e3e3ddb133069d3f5db`。
本次“路由 $h$”局部重构的起点为
`96dfe02172febc5ea8d3473013ebd26201e0dadd`。

用户确认新大纲后，分批执行迁移。三个连续块只是单轮上限，不是目标。
默认每轮只处理一个连续语义子树，当前目标约八个源信息点，且尽量只触及
新框架的一至两个分支。只有相邻小块很短、链接关系简单时才合并处理。
源信息点按本轮子树包含的全部 bullet 计数，包括各级子孙节点。
前几轮逐批等待用户审核；连续几轮只需轻微调整后，再考虑放大粒度。

每轮执行：

1. 明确本轮源文件、连续块边界及其父子层级。
2. 判断每个信息点应迁入新框架还是留在原框架。
3. 从原框架移除确定属于共享子要素的内容。
4. 将这些内容放入新框架中的准确位置。
   同一源块迁入后可以分散到多个不连续的新位置。
5. 写入前逐项标记“原文保留”或“必须改写”；后一种须记录新语境中的具体原因。
   按新位置的父节点和邻近内容重建语境时，只做原因所要求的最小改动。
6. 必要时在原框架补充简短接口说明，使其整体目的手段链仍可读。
7. 检查内容没有丢失、重复、错挂或改变技术含义。
8. 完成本轮正文后并行启动两个只读子 agent：
   - 全新读者审视：不预先加载本任务规则，先读
     `.agents/skills/literature-note-writing/references/blind-review-guide.md`，再以实际问题渐进查阅
     新增部分，报告难找、难懂、歧义、跳步和父节点不能提示子树内容之处。
   - 任务准则审视：先读本文件，再结合本轮源块、目标局部和 Git diff，检查
     目的手段关系、跨语境翻译、信息完整性、链接角色及任务专门格式；对每处
     正文改写要求给出必要理由，尤其检查是否把成熟抽象表述退回论文具体语境。
9. 两个子 agent 均只报告、不修改文件。主执行者逐条判断是否为实质问题，
   修正后再做结构、UID 和 diff 检查；不能把评审建议不加判断地直接采纳。
10. 汇报本轮迁入、保留、改写及评审处理结果，然后等待下一轮指示。
11. 把本轮新发现且可复用的规则及其动机回写本文件；一次性的局部取舍不记录。

本文件兼作跨轮次和跨模型的任务记忆。不能只依赖当前对话保存用户反馈；
发现已有规则缺失、过时或表述不足时，应在完成当前修改的同一轮更新本文件。

## 迁移时的改写规则

本任务首先是结构重构，不是重写正文。成熟主笔记中的信息点默认连同其抽象层次、
判断力度、公式、相关链接和紧凑表达一起迁移。改变缩进和排列不自动授权改写内容。

### 0. 默认保留正文

- 来源优先级为：用户最新修改、已有成熟主笔记、文献笔记、执行者现场重述。
  文献笔记用于核对证据和链接，不应用来覆盖已经完成跨语境翻译的主笔记表达。
- 默认只修改缩进、排列、必要的链接格式，以及因新父节点改变而必须调整的最少指代。
- 只有原文明显不成熟、不准确、有歧义，或新语境使原句无法独立成立时才改写。
  改写前须能明确指出原因，并逐项确认信息量、抽象程度和判断力度没有减弱。
- 原文已经用一般变量和功能语言抽象出方法时，不把论文名称、数据形态、作者术语或
  实现细节重新写回正文。链接后的文献内容不能反向降低主笔记的抽象层次。
- 若无法确定新表述是否优于原文，保留原文；结构安放与正文润色分开决策。
- 评审发现可读性问题不自动授权重写。先判断问题是否来自本轮结构、是否确实存在于
  原成熟正文，以及修改会不会损失原有抽象和信息密度；无充分理由时不动正文。

此前失误的直接原因是：把“需要跨语境翻译”误解成“每次迁移都重新翻译”，
又按文献链接目标而非当前正文角色判断节点性质。后续必须先判断原主笔记是否已经
完成抽象，再决定是否有改写必要。

### 1. 补充可检索行首

优先采用 `维度—属性值：具体解释`，使未来读者只看行首即可判断：

- 这一行回答哪个维度的问题
- 该维度采用哪种方案或取值
- 是否值得继续阅读冒号后的细节

若维度已由父节点给出，子节点可缩写为 `属性值：具体解释`。
行首的单个 `—` 是专门设计的检索格式，可以使用。
冒号后的正文一般不使用破折号。

行首的根本作用不是概括正文，而是提示读者“带着什么问题时值得继续看冒号后内容”。
优先用讨论对象、待实现目的、待解决问题或需要判断的设计维度作为回查入口。
只描述方法自身属性、实现条件或论文内部角色的词，如“同一硬划分”“类别代表”或
“关键设计”，若不能帮助带着问题的读者判断相关性，就不应独占行首。
这类信息应放在手段正文、适用条件或更准确的父节点下面。

行首首先要短，避免读者为了判断正文是否相关，反而先阅读一段长标签。
父节点已经给出的动作可直接省略。例如父节点已说明正在构造分类模式时，
子节点写 `类别集`、`分类结果`、`路由 h` 即可，不重复写 `定义` 或 `实现`。

短标签若压缩过度，可按内容选择以下形式：

- `短标签：具体解释`
- `短标签，简短释义：具体解释`
- 保持 `短标签：`，在冒号后先解释标签，再展开具体内容

逗号后的释义只用于补回辨认当前节点所必需的信息，不把完整正文搬进行首。
这些做法服务于缩短扫视时间。只有当检索行首确实改善定位和比较时才使用，
不为满足固定模板而添加空泛标签。

### 2. 微调表述和组织

- 以组织更成熟、可读性更高的现有笔记为写法蓝本。
- 仅在“默认保留正文”列出的条件成立时压缩、重述或拆分；否则保留原表达。
- 每个 bullet 表达一个完整信息点，子节点不续写父节点未完的句子。
- 比较多个方案时，先拆成正交维度，再逐维度比较。
- 主笔记先写待解决的问题，再展开可用手段，最后把论文或算法实例挂在其采用的手段下面。
  不把具体实例放在抽象手段之前，也不把实例与手段作为同级方案并列。
- 节点是否为例子由正文承担的角色决定，不由它是否带文献 UID 决定。临时移除链接后，
  若正文仍给出可复用的一般操作、判断或公式，它就是手段或知识点，文献 UID 只是来源。
- 例子默认是叶节点。实现细节留在文献笔记，不在主笔记的例子下重新展开。
  只有例子同时关联另一个主笔记主题时，才可添加指向该主题的相关性子节点。
- 从文献例子中发现的可复用策略，应提升为相应一般方法的独立子节点，例子另作为叶节点，
  不能把通用策略作为对某篇论文的解释挂在例子下面。
- 逐条检查父子边的直接性：子节点应是实现父目的的直接手段、直接子目的，
  或在父节点明确采用的展开轴下给出一个同级方案。方法的性质或使用条件不能冒充手段；
  例如“采用同一硬划分”可以限制某种组合手段，却不能仅凭该属性成为父目的的子方案。
- 同组兄弟节点采用一致的关系轴和接近的抽象层级。一般手段、手段组件、
  实施条件和具体论文例子不能直接平铺为同级方案。
- 区分共享接口与特定实现的中间量。先明确一个量属于问题定义、方法输入还是某种
  实现路径，再决定其层级；不能因若干方法都引用该量，就把它写成所有方法共有的步骤。
- 同一父节点下若是在划分方案，兄弟节点须采用同一划分标准，并检查重叠与遗漏。
  不把“直接利用已有结构”和“经预处理构造新结构”等不同层次的概念并列混用。
- 层级服务于折叠和查找，不机械保留完整分析链。若一个区别能在父节点末尾、
  行首标签或一行正文中讲清，不为它单设只有分类作用的中间节点。
- 同时控制直接子节点的宽度。经验上尽量不超过约 10 项；明显超过时，
  优先把其中能形成稳定维度的一组内容收进中间节点，不要求其余内容一并加层。
- 宽度和深度共同决定查找成本。加层能减少一次扫视的候选数，但也增加展开次数；
  只在减少扫视负担的收益大于新增展开成本时引入中间节点。
- 保留旧内容中已经准确、成熟的表达，不为统一风格而无意义重写。
- 原来的一行可拆成多个独立节点，并按各自归属放到不同位置。
- 少数信息点可重构成一棵小子树，由自解释的父节点统领多个短子节点。

### 3. 按新位置重建语境

- 原表述依赖旧框架提供的背景，而新位置没有时，补足理解所需的最少背景。
- 原表述重复了旧位置缺失、新位置已有的背景时，删除重复信息。
- 按新父节点已经建立的概念和符号调整指代，避免迁入内容仍像旧段落的续写。
- 折叠父节点后，每个迁入节点仍应表达完整判断。
- 展开子节点时，信息顺序应符合新框架的阅读路径，不保留无意义的原顺序。

#### 文献内容进入主笔记时重新定性

先判断来源主笔记是否已经完成跨语境翻译。若已经用一般变量、功能语言或目的手段关系
表达，就保留该表述，只重新安放结构；不要因重新打开文献笔记而恢复论文语境。
只有来源正文仍依赖旧父节点或论文术语时，才回答：该内容在目标父节点下提供的是哪一种
可复用策略、解决哪个子问题、承担什么角色；然后做必要的最小翻译。
作者术语若在新语境中可能产生不同解读，应换成当前框架的功能性描述。
例如论文所谓“细粒度注意力”若实际是不打包的逐点完整交互，主笔记应直接写后者，
避免读者误以为它只是另一种打包粒度。

文献笔记的父子层级只表示论文内部的叙述关系，不决定主笔记层级。一个文献子节点若能
独立启发技术设计，应在主笔记的相应一般策略下独立安放；若只是实现某篇论文的局部细节，
通常留在文献笔记，通过例子链接进入即可。主笔记的目标是提供可扩展的策略集合，
论文例子只用于证明或示范某项策略，并让需要实现细节的读者跳回文献笔记，不在主笔记中
复述整篇方法结构。

压缩或重命名技术动作时检查歧义。“代表元”通常表示从已有组员中选择一个元素；
把多个元素的信息汇总成新表示应写“打包”“聚合结果”或“类别元素”，不能笼统称为
“代表元素”。若一个术语在当前框架中已有专门含义，优先沿用该含义。

### 4. 提供多个检索入口

同一信息可以同时放在多个位置，只要各位置回答的是不同问题。例如某个
稀疏交互方案既可作为“为什么分类”的分组场景，也可作为“依据什么分类”的
路由实例。此时两个入口分别服务目的检索与手段检索，并不构成无意义重复。

- 每个位置按当地父节点重新表述该信息的角色，不能原样复制后让读者猜用途。
- 详细解释保留在主要位置，其他位置可只放简短指针和准确链接。
- 判断是否重复时看读者的检索路径和问题是否不同，不只看链接 UID 是否相同。
- 为未来可能追加的同类内容保留清楚的父子层级。

#### 为多个入口建立反向链接

若新框架的 A1、A2 两个位置都引用原框架 B，B 也应提供回到新框架的入口，
使读者从任一方向都能发现重构后的知识位置。反向链接有两种常用形态：

- B 分别链接 A1、A2：适合两个子位置已有稳定 UID，而且读者通常只需要
  进入其中一个具体问题的情形。
- B 链接共同父框架 A，再简述它分别对应 A1、A2 的角色：适合两个入口
  同属一个紧凑框架，或并列多个链接会增加选择负担的情形。

选择标准是读者从 B 出发时能否直接判断下一步该点哪里，而不是机械追求
最细粒度链接。若使用共同父框架，B 处的说明须明确各入口分别回答什么问题。

### 5. 记录规则及其动机

任务约束不只记录具体做法。对不显然、存在例外或可迁移到其他场景的规则，
同时简要记录它要改善的读者体验、结构性质或核验目标。

记录动机本身是为了让后续执行者能判断规则的适用边界。当字面规则在特殊
场景中妨碍原目的时，可围绕该目的调整做法；当现有规则没有覆盖新情况时，
也可由动机推出新的处理方式。动机应足以支持判断，不扩写成重复的长说明。

### 6. 更新并设计链接

- `((UID))`：用 `outline-read` 跟随到定义位置，理解链接目标及上下文。
- 旧式链接目标的行首严格无空格或 Tab，正则为：

```regex
^\* `
```

- 在这些顶层条目中搜索对应的行内代码标记，确认旧链接的原始指向。
- 若旧链接只指向整篇或整块文献笔记，优先改为引用支撑当前判断的
  具体行级 UID。
- 需要新 UID 时，使用 `outline-read` skill 提供的 `gen-uid` 脚本，
  并按主笔记与文献笔记的不同约定写入。

若需把旧式文献链接改成行级 UID，按需阅读 `main-note-integration`，
尤其关注主笔记落位、语境翻译和行级引用。

#### 先判断链接承担的角色

放置链接前，先判断它在当前句中的作用：

- 作为句子成分，直接补足当前表达所引用的对象。
- 标注某个词或短语，说明该局部概念的主笔记位置。
- 支撑整句判断，指出该判断的证据或出处。
- 给抽象方法提供一个具体文献例子。
- 指向相关主题，提示当前内容换到另一场景后的专门框架。

链接位置和说明文字必须让读者不点开链接，也能判断它指代什么、
为何值得点开。不能仅凭“附近有链接”让读者自行试探。

#### 用位置与层级消除歧义

- 链接只标注某个词时，贴近被标注词；与不属于指代范围的文字用空格隔开。
  例如 `((UID))ensemble` 或 `bagging((UID)) 区别`。
- 链接支撑冒号后整句判断时，优先紧接冒号放置，再写具体内容。
  这样既表明链接覆盖整句，也便于扫视行首时发现可跳转位置或漏链。
  当前行没有合适冒号或前置后影响阅读时，才放在句末。
- 文献链接只提供一个例子时，明确写成 `例—方法名` 或 `例如`，
  不把它放在抽象判断末尾伪装成整个判断的唯一出处。
- 例子较长或未来可能继续增加时，单独建立子节点，不与抽象方法挤在同一行。
- 不连续堆放多个链接。多个链接角色不同时，拆成不同节点并写明各自作用。
- 若一行只是枚举同一父框架中的多个入口，且每个入口只需短标签即可辨认，
  可把各链接贴近对应标签后合并在一行；只有需要分别解释或单行过长时才拆成子节点。
- 文献例子另有主笔记中的目的或场景框架时，主框架链接放在例子子节点下，
  表明它解释的是这个例子的使用目的，而不是抽象手段的另一个并列例子。
- 若主笔记链接表示抽象手段在某个场景下的特化，而不是解释某个文献例子，
  则把它与文献例子并列放在抽象手段下。链接层级取决于语义从属关系，
  不取决于链接指向文献笔记还是主笔记。
- 两个候选链接是父子节点时，通常只保留一个。若一个子节点特别相关，
  直接链接子节点；若多个子节点都相关，则链接父节点。
- 指向主笔记时，可在链接后写压缩后的目标路径，如
  `见((UID))INR-区域分解-区域网络共享`。只保留足以定位主题的祖先层级。

#### UID 定义标点

- 行尾定义 `{UID}` 前通常加中文分号，如 `具体判断；{UID}`。
- 若 `{UID}` 前已经有句号、分号、右括号等标点，不重复添加分号。
- 不让 `{UID}` 仅隔一个空格直接跟在无标点正文之后。

以上规则服务两个目的：让读者知道什么时候该点哪个链接，并为未来追加
同类例子或场景特化预留清晰层级。特殊情况下若固定格式妨碍这两个目的，
应根据链接的真实语义角色现场调整。

## 迁移完整性核验策略

采用“每轮自检与双局部评审、中期累计完整性审查、最终全盘审查”的分层策略。
局部表达问题每轮及时发现；跨多轮的信息完整性和整体结构另在中期、最终检查。

### 每轮自检与双局部评审

主执行者为每个源信息点记录去向：迁入位置、留在原处、合并到其他判断，
或因重复而删除。完成本轮后对照源块逐项核对，并检查新旧语境是否自洽。
随后按“阶段 2：循环迁移具体内容”所述，启动全新读者和任务准则两个
只读子 agent。两者聚焦本轮新写局部；这不替代下方跨多轮累计 diff 的审查。

### 中期独立审查

连续完成若干轮、形成一个完整分支，或准备由用户提交 Git commit 前，
另启动一次面向累计改动的全新上下文子 agent。审查者只读，不修改文件：

1. 查看本阶段基线版本到当前工作树的 Git diff。
2. 枚举旧文件中删除或改写的信息点。
3. 在新文件和保留后的原框架中寻找每个信息点的语义对应位置。
4. 报告可能丢失、含义变窄、错挂、无意重复或语境断裂的内容。
5. 将不能靠关键词匹配确认的项目列为人工语义复核项。
6. 检查每个链接的作用域、目标粒度、放置位置和点击提示是否清楚。

由主执行者判断审查结果是实质遗漏、合理改写还是误报。
用户负责 Git commit。若期间已经提交，以约定的历史 commit 为基线；
若尚未提交，记录阶段开始时的 commit 哈希并显式与它比较，
不依赖默认工作树 diff 的范围。

### 最终全盘审查

全部迁移完成后，再启动一次全新上下文的子 agent，
比较整个重构基线与最终状态。核查范围包括：

- 旧内容是否均有明确去向，且技术含义没有无意丢失
- 新框架是否存在重复、错挂、孤立节点或不完整父子关系
- 原框架移除共享内容后，整体目的和场景特有链条是否仍完整
- `((UID))` 是否能解析到预期定义，新增 UID 是否唯一
- 链接是否能看出指代词、整句证据、具体例子或相关框架等不同角色
- 多链接是否已拆清角色，父子目标是否只保留最有用的一个
- 旧式链接是否均已按计划处理，链接目标是否足够具体
- Tab 层级是否有效，是否存在缩进跳跃和空节点

中期审查的触发频率根据实际迁移规模调整，初步以完成一个主要分支
或累计约三至五轮迁移为参考。最终审查不可省略。

## 每轮迁移的边界与安全要求

- 删除只发生在内容已确认写入新位置之后。
- 不使用行号作为长期身份，定位完成后以内容边界、层级和 UID 为准。
- 不顺手重构本轮范围外的相邻内容。
- 如果文件内容与此前读取结果不一致，视为用户可能已修改，
  不自动恢复旧版本。
- 遇到归属有实质歧义的内容，先保留原处并说明，不擅自迁移。
- 不因新大纲存在占位节点，就主动补写三处旧材料均未支持的新知识。

## 阶段验收标准

### 阶段 1 验收

- 新文件覆盖三处旧框架已经出现的主要讨论维度。
- 相比 `refactor-plan.md`，横向方案和纵向子问题均有实质补全。
- 层级大体反映目的手段关系，但没有为了避免跳步而过度加深。
- 大纲能容纳三处待迁内容，不需要把明显不同的概念强塞进同一节点。
- 只完成大纲设计，没有提前移动正文。

### 阶段 2 每轮验收

- 单轮最多迁移三个源端连续小块，每个小块内部在原文件中连续。
- 共享知识进入新框架，场景特有目的和用法留在原框架。
- 新位置按概念归属组织，不受源内容连续顺序约束。
- 原文件与新文件合计没有信息丢失和无意重复。
- 新增行首能帮助检索，正文表述准确且不增加格式噪音。
- 迁入内容已按新父节点重建语境，不依赖旧位置才能理解。
- 链接可以解析到真正支撑当前判断的具体内容。
- 不点开链接也能判断链接指向什么，以及什么时候值得继续查看。

## 最终完成条件

- 三处旧框架中的共享“元素分类处理”知识已全部迁入独立框架。
- 原框架只保留各自整体目的、场景特有设计和对共享框架的必要接口。
- 新框架形成可独立查阅、可继续扩充的统一知识结构。
- 所有受影响链接和 UID 均有效，相关大纲结构检查通过。
- 用户逐轮确认的范围均已完成，没有遗留未说明的待迁块。
