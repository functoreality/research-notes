# 元素分类处理重构：迁移记录

迁移基线：`e14321c4a956a47bc9dc4e3e3ddb133069d3f5db`

## 第 1 批：NNarch 离散单任务 hypernet / MoE

源端按基线版本定位，共三个相邻块：

1. `raw/NNarch.md` 原第 272 至 274 行：MoE 术语与专家总数。
2. `raw/NNarch.md` 原第 275 至 301 行：专家功能、路由输出、
   防退化与预设路由。
3. `raw/NNarch.md` 原第 302 至 309 行：专家参数共享及分类单位边界。

迁入 `raw/elementRouting.md` 的去向：

- 框架范围：MoE、ensemble、mixup 及样本级和元素级 MoE 的边界。
- 目标用途：处理器功能、输出融合及处理器参数共享。
- 分类模式：固定类别数、多激活、常驻类别、软分类与预设映射。
- 训练策略：类别 collapse、目标占比、容量限制及适用边界。

留在 `raw/NNarch.md` 的内容：

- 单任务 hypernet 为什么把一个任务拆成多个子任务。
- 推理时先用 $l:X\to\Lambda$ 选子任务，再由对应网络处理。
- 连续 $\Lambda$、离散连续联用及动力系统等场景特有内容。
- 连续路由防退化时可采用的目标分布。

源端接口改为引用 `((q8na8l))`。原块内定义的 UID 已随对应判断迁入，
没有删除 UID 定义。

### 用户复核后的调整

- 将抽象判断、文献例子和例子对应的主笔记场景拆成不同层级。
- 修正 ensemble、bagging 等局部概念链接的作用域与空格。
- 连续链接改为分行说明角色；父子目标只保留更利于定位的一个。
- 在链接后补充压缩后的主笔记路径，提示读者何时值得跳转。
- `NNarch.md` 连续路由防退化保留原处，并回指离散类别 collapse。

后续批次默认缩小为一个连续语义子树，约五至十个源信息点，
尽量只触及新框架的一至两个分支。连续几批反馈稳定后再考虑放大。

## 第 2 批：NOinput 集合元素分类处理

本批局部基线：`0f9ae0f07cd9c18e677e35d92fd3788f0fc55efc`

源端为 `raw/NOinput.md` 中“分类：独立处理”及其三个子节点，
共四个连续 bullet。本批刻意小于默认规模，用于继续校准迁移写法。

迁入 `raw/elementRouting.md` 的内容：

- 样本内元素逐元素按类别选择处理网络；处理器内部交互另行决定。
- FNO 自适应区域分解区分原域与延拓域的场景例。
- Transformer token 级 MoE 提高模型效率与容量的场景例。

原“样本内元素 MoE 与样本级或任务级 MoE 的区别”已由新框架的
“框架范围”和“分类单位”节点表达，不再重复新增。UID `{p8ai85}` 随通用判断
迁入新框架；`raw/NOinput.md` 原位置保留集合划分用途所需的简短接口。
迁移后同步更新 `raw/NOinput.md` 与 `raw/Transformer.md` 中指向该 UID 的
目标路径说明，避免链接仍显示旧框架位置。

## 第 3 批：NOinput 预设与可学划分

本批局部基线：`905918fe77447d7cd75e44e9a2e1f1fde030688f`。
开始迁移前，工作树另有用户对 FNO 场景例的微调，本批原样保留。

源端为 `raw/NOinput.md` 中“划分方式：预设 vs 可学”完整子树，
共八个连续 bullet。迁入 `raw/elementRouting.md` 的内容：

- 可学习划分：Transolver、Opt_RNO 在集合打包中学出元素划分。
- 顺序先验：MMET 用 Hilbert 曲线序列化散点，再按相邻区段打包。
- 预设理由：MMET 为跨输入条件泛化而避免可学习 patch。
- 网格先验：利用均匀网格结构预设分组或打包。
- 分类单位重构：把文本转成图像后，对渲染所得新元素复用图像 patch 结构。
- 几何先验：SpiderSolver 从边界几何与到边界距离构造蛛网划分。
- 相关用途：NO 用预设空间范围限制非全局信息交互。

源端原位置保留指向新框架“实现路由”分支的接口。新增 UID `{q8ph8w}`
作为该分支入口；另为 Transformer 的文本图像化节点补充 `{q8ph8x}`，
替换原先借用下级“可变压缩率”UID 的粗粒度链接。
