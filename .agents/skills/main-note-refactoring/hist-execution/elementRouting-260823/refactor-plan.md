## 新框架：元素分类处理
* 分类对象—预设集合元素，分类映射 $h:X\to I$
	* $X$ 简单，如区域分解/时间分段 所涉及 $X$ 为有限维空间子集
		* 划分方案可预设
		* 下游网络 额外连续性要求
	* $X$ 复杂，如图像所有可能取值；分类映射通常需学出
	* $X$ 混合，如气象自回归模型输入 $(t,u_t)$
		* 分类映射可混合，如 STCast 对 $t$ 预设、对 $u_t$ 可学
* 分类对象—动态元素集合，角标集 $J$ 可有结构，分类映射 $h:(J\to X)\to(J\to I)$
	* 预设情形为常值输出 $J\to I$，不依赖于输入内容
* 映射依赖，可学的有额外参数 $w\in W$，如预设集合元素情形 $W\to X\to I$
* 输出变种
	* 分类软硬，软分类输出改为 $\mathbb{P}(I)$ 概率分布；可进一步加稀疏性约束 $\|-\|_0\le K$
	* 多分类，输入改为 $I$ 的 K-元子集
* 预期性质—防退化，避免 collapse 到特定 $i$ 值
	* 即 push-forward $h_#:\mathbb{P}(X)\to\mathbb{P}(I)$ 后所得 $I$ 上概率分布相对均匀
* 下游使用，预期用途
	* 多元素交互（仅限动态元素集合），输出 $J\to Y$ 来自 $y_j=msg-pass(x_{j'}|i(j')=i(j))_j$
	* 多元素打包（仅限动态元素集合），输出 $I\to Y$ 来自 $y_i=reduce(x_j|i(j)=i)$
	* 路由不同处理网络，MoE，路由规则 $I\to(X\to Y)$
		* 对动态元素集合，输出 $J\to I$ 来自简单复合映射 $J\to X\to I$，不涉及元素间交互关系
		* 各层路由可独立；包括各层 $\#I$ 不同情形，如 INR 浅层多子域深层少子域

## 旧框架
* raw/INRarch:4-142
* raw/NNarch:270-309
* raw/NOinput:394-470
