# 项目背景与设计意图

本文供参与维护本仓库的 AI 编码助手阅读。
它只记录稳定的项目意图和领域语义，不罗列容易从代码中发现的功能与实现细节。

## 项目是什么

本仓库用于公开一套 AI for PDEs 科研笔记，并提供适合在线阅读这些笔记的网页。
它不是一个普通的 Markdown 博客，而是一个面向大纲式语义笔记的专用阅读器。
网页的主要价值，是让笔记原有的层级结构、知识链接和检索方式在浏览器中仍然可用。

`raw/` 中的 Markdown 文件是原始笔记。
笔记主体由不同缩进层级的条目组成，缩进表达内容之间的从属关系，
而不只是视觉排版。

## 核心设计意图

### 保留大纲的层级阅读方式

笔记默认按层级折叠，读者可以逐层展开或重新折叠内容。
任何涉及笔记展示的修改，都应保留缩进所表达的树状结构，
避免把笔记退化为一篇从头到尾平铺的长文。

### 把行级链接当作知识网络

笔记使用 `((pattern))` 表示链接，使用行末的 `{pattern}` 标记目标行。
目标 pattern 在全部笔记中具有唯一性，链接可能指向当前文件或其他文件中的某一行。

读者打开链接后，应直接到达并看见目标行，而不只是打开目标文件。
为此，界面需要展开目标行的祖先层级，展开目标行直属的下一层内容，
并高亮目标行。链接适合在独立的页面标签或网页内子标签中打开，
使读者能够保留原来的阅读位置。

### 让全局搜索返回具体内容行

搜索面向全部笔记，但结果的基本单位是行，而不是文件。
读者需要看到关键词出现在哪些具体内容行中，并能从结果进入相应位置。
这是针对数十个较长笔记文件的知识检索，而不是普通博客的文章标题搜索。

## 修改代码时的判断原则

- 优先维护大纲层级、行级定位和跨文件知识链接的语义。
- 将原始笔记视为内容源，将解析后的数据视为可重新生成的派生数据。
- 新功能应服务于密集科研笔记的定位、阅读和来回跳转，避免用普通博客的假设替代上述语义。
- 本文描述的是设计意图。组件划分、数据结构和交互细节应以当前代码为准，并在修改前阅读相关实现。
